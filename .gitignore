# ===== Python 生成工具（一次性素材脚本，不入库）=====
__pycache__/
*.pyc

# ===== 素材生成脚本（meowa 素材生成工具，需环境变量，不入库）=====
新demo/assets/pets/*.py
新demo/assets/pets/__pycache__/

# ===== 中转站 / 自检脚本（含中转配置，不入库）=====
relay-selfcheck.*
*.log

# ===== 内部开发配置 / 设计文档（本地留档，不入库）=====
新demo/agents/
新demo/多Agent协作架构方案.md
新demo/游戏设计理念，所有Agent必须阅读.md
新demo/游戏数据地图 v1.md
新demo/大陆地图规划 v1.md
新demo/大陆世界观设计.md
新demo/第1大陆完整设计 v1.md
新demo/第1大陆设计-素材分层 v1.md
新demo/战斗界面Redesign说明.md
新demo/战斗界面-怪物悬浮提示需求.md
新demo/Mvp/原型代码/装备打造多样性方案*.md

# ===== CodeBuddy / WorkBuddy 内部配置（不入库）=====
# ⚠️ 2026-09-13 改动：把 .codebuddy/memory/ 单独放行入库。
#    原因：那里存着 19 天 735KB 的排障记录与决策依据，是全项目唯一的「纠错账本」，
#    原先整目录被忽略 → 零备份零版本历史，一次误删/重装全没。
#    其余子目录（skills/agents/teams，含本地会话残留）继续忽略，保持最小作用域。
.codebuddy/*
!.codebuddy/memory/
!.codebuddy/memory/**
.workbuddy/

# ===== Obsidian vault 本地配置（编辑器状态 / 工作区，不入库）=====
.obsidian/workspace*
.obsidian/workspace.json

# ===== 本地密钥（Meowa API key，绝不入库）=====
.env
.env.*

# ===== 系统 / 编辑器 =====
.DS_Store
Thumbs.db
*.swp
.zcode/

# ===== 临时测试产物（playwright 日志 / 探针页面 / 试算脚本）=====
docs/.playwright-cli/
docs/__probe16x9.html
docs/tests/_tmp_*.js
docs/tests/tmp_*.js

# ===== 根目录 js Junction（mklink /J → docs/js，不入库，避免重复内容）=====
# 必须带前导斜杠：不带斜杠的 js/ 会匹配任意层级的 js 目录（含 docs/js/），
# 导致 docs/js 下新增的 js 文件无法 git add（历史 tracked 文件不受影响，所以长期没暴露）。
/js/

# ===== 本地临时文件 / 归档 / demo（2026-09-03 误传后清理，不入库）=====
archive/
_html
_game_screenshot*.jpeg
新建文本文档.txt
系统整合_full_tmp.md
docs/_dl_skill.tmp
docs/_preview_*.png
docs/onboarding-demo.html
docs/battle-demo.html
docs/css/onboarding-demo.css
# ⚠️ 2026-09-17 从这里删掉了一行 `docs/css/ui-onboarding.css`：
#   它是【正式引导引擎的样式表】（游戏.html:30 引用），却被误归进"demo 临时文件"这组忽略掉了
#   ⇒ 该文件从来没进过仓库 ⇒ **线上 GitHub Pages 一直 404**，引导聚光灯（压屏挖洞）的样式全丢。
#   同组的 onboarding-demo.* 才是 demo（那两个文件 09-10 已删）。别再把它加回来。
# ⚠️ 2026-09-17 从这里删掉了一行 `docs/AI假人经济系统策划_v1.md`：
#   该文件 09-16 已移进 `docs/档案/` 并入库，这条规则只剩副作用 ——
#   哪天有人把它移回根目录，会被**静默忽略**，重演 ui-onboarding.css 那类「本地有、线上 404」。
#   教训：忽略规则指向的文件一旦入库/搬走，规则就要一起删，不能留。
docs/游戏UI体检报告.md

# ===== 本地垃圾（2026-09-10 整理：以前没盖住，`git add -A` 会误入库）=====
# 事故背景：2026-09-04 `git add -A` 把误删文件一起提交过（commit 26f8f6c），
# 所以凡是「本地产物 / 快照 / CLI 状态」一律在这里挡掉，别指望下次记得手挑。
.tmp-cli/
_backup_rollback*/
supabase/.temp/
$null
docs/tests/_*.txt
docs/tests/_current.json
docs/tests/out_*.txt
docs/tests/result.txt
/screenshot*.png

# ===== Remotion 视频工程（2026-09-15）：源码入库，依赖与渲染产物不入库 =====
# 产物 mp4 要发布给玩家，所以渲染完统一拷到 docs/assets/video/（那一份入库）。
video/node_modules/
video/out/

# ===== 浏览器测试产出（2026-09-16）=====
# ⚠️ 2026-09-17：`tools/` 的入库内容已全部移除（48 个 `patch-*.py`/`bump-v-*.py`/`revert-*.py`
#   一次性脚本 + `frames-*`/`meowa-atk`/`atk-*` 帧图 —— 逐帧动画实测不可用，
#   `pet-sprites.js` 的 `ANIM_ENABLED` 已关停；素材需要回溯时从 git 历史取）。
#   本目录只剩这里被忽略的浏览器缓存，而它当时**被浏览器进程占用、没删干净**，
#   所以这条规则先留着（否则 `?? tools/` 会一直污染 git status）。
#   👉 关掉浏览器后手动删掉整个 `tools/` 目录，删完这条规则就可以一起删。
tools/


