# 00 · 项目导航（从这里开始）

> 打开 Obsidian 先看这一页。**找不到东西、或者想拍板一件事之前，先来这儿。**

## 📌 新开一个 AI 对话时，把这段复制给它

> 先读 `AGENTS.md` 和 `PROJECT_CONSTITUTION.md`，再看 `.codebuddy/memory/` 里最近两天的记录，
> 然后**告诉我你明白了什么**，我们再开工。

最后那句是关键 —— **让它复述一遍**，你一眼就能看出它读没读对。
读错了当场纠正，比干了两小时才发现强得多。

⚠️ **AI 不认识 Obsidian。** 它读的是磁盘上的文件，跟你在哪个软件里看无关。
所以：**重要决定一定让它写进宪法**，别只留在对话里 —— 对话会关掉，文件不会。

---

## 一句话：这是什么

**暗黑风「挂机养宠 + 宠物装备真实交易」的网页游戏。**
**游戏名：永夜灵市，英文 Evernight Bazaar**（2026-09-13 定名）。
⛔ 旧名 `Forge of Souls`／`Soulforge` 是**上一个项目继承来的**，已废弃 —— 任何地方不许再用。

核心循环：

```
挂机打怪 → 掉材料 → 合成 / 进化 / 涅槃 → 打造装备 → 玩家之间摆摊交易
```

**三爽点，按优先级排（这个功能服务哪个？一个都不服务的就别做）：**

1. **掉宝 / 鉴定**（最重要）
2. **市场捡漏**
3. **成长跳变**

---

## 🚨 拍过的板（不许翻）

这些是已经定的事。**不要再重新讨论，除非你明确决定推翻它** ——
推翻时在 `[[PROJECT_CONSTITUTION]]` 原文标注「已推翻（日期）」，不要删原文。

| 拍板 | 内容 | 出处 |
|---|---|---|
| **不卖数值** | 魔石只卖便利（挂单额度之类），**永远不卖材料、不卖战力、不卖进度** | 2026-09-12 |
| **不学氪金那套** | 不做月卡、开包保底、神秘商店、品级分层、五行种族值 | 2026-08-31 |
| **等级上限 60** | 图 1-10 是教学，60 之后才是真玩法 | 2026-09-03 |
| **不做可交易经验包** | 满级溢出经验转经验池可以，但**不能做成能交易的道具** | 2026-08-30 |
| **AI 假人不标注** | 20 个 persona 全部当作真实玩家呈现，不标「AI」 | 2026-09-03 |
| **不引动态难度** | 数值写死在固定表里，不做"跟着玩家变强"的难度 | 长期 |

### ⚠️ 曾经自己推翻过自己（左脑攻击右脑高发区）

**下面这些以前反复横跳过，提起来之前先确认自己不是在重蹈覆辙：**

- **技能系统**：先说「纯被动 16 个」→ 推翻 → 改成「主动自动技 16 个」（2026-08-31）。**被动方案已废。**
- **商店卖什么**：一开始写满了卖材料、卖进化素材 → 推翻 → 只留便利类。（2026-09-12）
- **涅槃/经验包**：多次改方案（经验包 → 溢出转存 + 不可交易）。**别再提做口袋精灵那种可交易经验包。**
- **品级系统**：调研完决定不做。
- **世界地图**：改过好几轮，现在的定调是「选图导航层」，**不是**一个独立玩法系统。

---

## 🧭 拍板四问（拿不准的事，先过一遍）

1. **这事以前定过没有？** → 查 `[[PROJECT_CONSTITUTION]]`
2. **它服务三个爽点里的哪一个？** → 一个都不服务的，先放放
3. **会不会让玩家觉得"花钱就能买进度"？** → 会的，不做
4. **万一失败了，玩家会丢东西吗？** → 会丢的，顺序必须重排

---

## 🚀 干活流程（vibecoding 七步）

> 规矩建好了，但**规矩不自动生效** —— 靠这套动作让它生效。
> 每一步都对应一份已有文件，**没有新建第五份文档**。

| # | 动作 | 查 / 用什么 | 卡住就停的信号 |
|---|---|---|---|
| 0 | **有想法，先查是不是已经拍过板** | 本页「拍过的板」+「曾经推翻过自己」+ `[[PROJECT_CONSTITUTION]]` | 命中已拍板的 → 别重开讨论；要推翻就在宪法原文标「已推翻（日期）」，不删原文 |
| 1 | **过一遍拍板四问** | 上面那四条 | 「服务哪个爽点」答不出 → 先放放；「花钱买进度」= 会 → 不做 |
| 2 | **决定自己干还是派出去** | `[[docs/任务单/README]]` 的**派活四问** | 列不出「允许碰哪些文件」→ 说明活没拆清，**先拆再派**；会动数据库 → **串行，先问人** |
| 3 | **开工前重读** | `[[AGENTS]]` 六条红线 + 任务单的**边界栏**；共享文件（`config.js`/`游戏.html`/`battle.js`/`ui-common.js`/`game.css`）**改前重读**（防并发会话覆盖） | 撞共享文件 → 串行 |
| 4 | **写代码** | 按领域调 skill：`fos-balance`（数值）/ `fos-cloud`（数据库）/ `fos-verify`（验证）/ `forge-of-souls-dev`（总纲） | 想用 `!important` / 负 margin / 硬编码怼 → **停手找根因** |
| 5 | **验收（两件事，缺一不可）** | ① `cd docs && npm run check`（自动对比基线，看有没有**新搞红的**）② `http://localhost:8030/游戏.html` **浏览器里真看一眼**（测试绿 ≠ 界面存在） | 存量红 5 个不是你的锅；`enemy_balance`/`botbuy` 报红先连跑 3~4 次 |
| 6 | **提交** | 只 add 自己路径；提交前 `git status --short` 看有没有意外的 `D` | 改动横跨几十文件（并行会话）→ 才考虑整批提交，并写下原因 |
| 7 | **收尾回填** | 任务单「六、结果」写清改了什么 + **什么没做完**；新拍板写回宪法；坑记进 `.codebuddy/memory/YYYY-MM-DD.md` | 这一步不做 = 下次还会踩同一个坑 |

**一句话版**：先查板 → 过四问 → 划边界 → 守红线写码 → `npm run check` + 浏览器看一眼 → 只提自己的 → 回填。

### 🛑 什么情况下必须停下来问人

- 要**改数据库 / 线上配置**（唯一生产库，改坏没法撤销）
- 要**推翻一条已经拍过的板**
- 改动会**碰到钱**（魔石、市场、发奖）
- 想**推翻某个"别建第二个"的既有实现**

---

## 📁 地图：东西都在哪

### 给人看的

| 想查什么 | 去哪 |
|---|---|
| **AI 干活的规矩**（改代码前必读） | `[[AGENTS]]` |
| 已拍板的决策（权威账本） | `[[PROJECT_CONSTITUTION]]` |
| 系统怎么实现的（全览） | `[[docs/玩法系统全览]]` |
| 数据流 / 权限有没有洞 | `[[docs/代码审计_2026-09-11]]` |
| 哪些代码是死的能删 | `[[docs/墓碑代码审计_2026-09-12]]` |
| 资源从哪产出 | `[[docs/资源归属矩阵现状]]` |
| 任务系统 / 副本设计 | `[[docs/Forge_of_Souls_任务系统重设计_v1]]`、`[[docs/Forge_of_Souls_副本与资源循环重设计_边界基线_v1]]` |
| AI 假人经济 | `[[docs/AI假人经济系统策划_v1]]` |

### 给 AI 看的（在隐藏目录里，Obsidian 默认看不见）

- **`.codebuddy/memory/`** —— 19 天排障记录，共 735 KB。
  `[[.codebuddy/memory/MEMORY]]` 是提炼版（每会话会更新），其余是按天流水账。
  🔎 **想找"某个坑上次怎么填的"——先在这里全文搜。**
- **`.codebuddy/skills/`** —— 4 个 `fos-*` skill，是 AI 的行为手册。

### 代码

| 领域 | 唯一权威 |
|---|---|
| 运行代码 | `docs/` |
| 数值 | `docs/js/core/config.js` |
| 测试 | `docs/tests/`（84 个脚本） |
| 数据库表 / 迁移 | `supabase/`（33 个 SQL） |

---

## ✅ 当前状态速览（2026-09-13）

**已经能玩的**：挂机战斗、5 阶进化、16 个技能、12 部位装备 + 词缀、合成、涅槃、打造、
任务系统（182 条）、交易行（含 20 个 AI 假人）、副本（三路线×20 层）、通天塔（30 层）、
背包三连屏、魔石商店（只卖便利）。

**还没做的**（按重要性）：

1. 服务端权威 —— **要收真钱之前必须做完**。
   ✅ **野图挂机已搬完**（`idle-bridge.js` 直连 `battle-settle` EF，预结算入账、客户端只回放）；
   🔴 **打造 / 进化 / 定价 / 副本 / 塔仍在前端**，改 DevTools 还能改结果 —— 这才是推广没底气的原因
2. 魔石收款通道 —— 现在只有卡密和管理员发放
3. 部署配置重配 —— `netlify.toml` / `wrangler.toml` 还指着旧目录，打好包推不上去
4. 塔重置卡获取断链（做不出来 → 拿不到 → 卡进度）
5. `pets` / `equip_items` / `pet_egg` 表的权限没收，登录玩家能改自己数据。
   🔴 **2026-09-13 已拍板：先不动**，因为现在收权限会当场挂掉大半游戏功能（打造/进化/涅槃/穿脱装备/掉蛋全靠客户端直写），
   必须先补 8 个服务端函数。**施工图已备好**：`[[docs/资产表收口调研_2026-09-13]]`
   ⚠️ 别再提议"先加个 CHECK 约束顶一下" —— 那只能拦住把等级改到 99999，拦不住改到合法上限 60，是假的安全感

**改完代码怎么验**：`cd docs && npm run check`
它会自动跟 `docs/tests/baseline.json` 对比，**直接告诉你有没有新搞红的**（以前得靠人记数字）。

✅ **2026-09-13 实测基线：70 通过 / 5 失败（75 个 vtest）**，约 2.6 分钟。
失败的是这 5 个存量红（不是你搞的，别去修）：`action_freeze` / `boss`(D4) / `bugfix`(emoji 断言过期) / `equip_score` / `pet_skill`。
flaky（时红时绿）：`enemy_balance`、`botbuy`。

📋 **要派活给别人（或 AI）**：先看 `[[docs/任务单/README]]` 的「派活四问」，用 `_模板.md` 开工 —— 那张模板的**边界栏**是必填项。

> 📌 `[[PROJECT_CONSTITUTION]]` 第五节有张「待补录」表，列的都是**已经拍板但还没写进宪法**的决策。路过时可以顺手补。

---

## 🔧 Obsidian 使用提示

- 排除了 `docs/assets`（290 张图）等重目录，所以打开不卡。**看不到图片是正常的，无所谓。**
- 装 **Dataview** 插件后，可以把散在各处的待办自动聚成一页。其余插件不用装。
- 做关联用 `#标签`，少用 `[[]]`（AI 读 Markdown 时双链是噪音）。
- `.obsidian/workspace*` 已加进 `.gitignore**，你自己的窗口布局不会上传。
