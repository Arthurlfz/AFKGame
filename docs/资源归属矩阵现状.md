# 资源归属矩阵 · 现状表（2026-09-09 落地）

> 配套《副本与资源循环重设计_边界基线_v1.md》第 3 节。本文记录「每种资源实际从哪里来」的代码现状，
> 由 `docs/tests/vtest_resource_matrix.js` 静态守门（改 `config.js` 后必须跑它）。
> 设计铁律：**每种受控资源只有一个主来源；普通地图只产区域材料 + 对应图档的专属材料 + 装备/蛋。**
>
> 📊 **2026-09-17 分工（别在这儿再手抄一份现状，两份一定会对不上）**：
> **「谁在哪几张图能掉、权重多少、是不是全域」的现状清单已经改成 Excel** →
> `docs/产出归属总表.xlsx`（7 个页签：物品总表 / 地图掉落矩阵 / 通天塔产出 / 副本产出 / 其他来源 / 异常与待定）。
> 它由 `cd docs && node tests/export_matrix.js` **从代码现场生成**（一条命令，跑完覆盖），
> 所以改完数值重跑一次就是最新的，不用手改。
> 本文只保留**设计铁律 / 归属规矩 / 历史决策**这类需要人写的东西。
>
> ⚠️ **2026-09-10 更新：通天塔已上线**（`docs/js/tower/`）。本表里所有标「未落地 / 塔接线前绝版」的行已改为落地后的真实来源，
> 并新增「腐印」「通天塔重置卡」两行；`Config.towerDrops.enabled` 已由 `false`（纯登记）改为 `true`。
> 塔的**服务端权威（EF）仍未落地**，见 `PROJECT_CONSTITUTION.md` 上线前必做清单 #6。
>
> ⚠️ **2026-09-17 更新：经验包加了第二来源（通天塔层掉落）** —— 起因是用户要求「塔里面还需要增加一些掉落经验包才行」；
> 核实后发现塔层战斗**根本不发经验**（`battle.js` 的 trial/tower 分支绕过 `main.js handleFightEnd`），
> 塔因此是唯一「花时间但等级零回报」的玩法，而涅槃后主宠要重练回 Lv60。见下表「经验包」行。

## 🔴 2026-09-17「全域与区域掉落重设计 v1」已落地 —— **本节的规矩变了，先读它**

> 起因（用户原话）：「**我发现有的时候被简单物资卡脚了，我很烦；这就是为什么我现在想要把掉落的东西设置成 poe 那种全域掉落和区域掉落**」。
> 完整施工图与测算：`docs/待办/全域与区域掉落重设计_v1.md`。

### 新总原则（判"该不该绑图"照这句）

> **基础物资按「深浅」分，区域特产按「图」分，顶级专属按「内容」分。**

| 类别 | 谁 | 归属 |
|---|---|---|
| 基础物资 | 打造通货（重铸/增缀/剥离/鉴定/强化丹A）、合成之石、进化素材三档、腐印 | **全域** —— 每张图都出，深浅只改权重 |
| 区域特产 | 10 种区域材料 | **绑图** —— 每图只出自己那一种（区域感的唯一载体） |
| 顶级专属 | 塔的 6 件 + T1/T2 词缀装备、副本的锁定石 + 涅槃丹 | **绑内容**，不回地图 |

⚠️ 这是对原「唯一主来源」原则的**第二次有意放开**（第一次是"兑换"）。放开的前提写在下面对应行里。

### 实际改了什么

| # | 改动 | 数字 |
|---|---|---|
| 1 | **进化素材三档全域化**（治「卡脚」的主刀） | 旧表靠 `areaEvolutionTiers` 把一阶/二阶素材锁在图 1~5 / 图 3~8，**毕业图 9~10 一件都不掉**。现在三档是 `materialWeightsByTier` 里的**独立键**（`areaEvolutionTiers` + `evoMaterialWeights` 机制**已删**），每图三档都有权重。图 9~10 从 **0** 变成 **0.58~0.65（普通）/ 0.92~1.08（精粹）个/小时**。<br>⚠️ **当晚二次修（只动普通档）**：用户指出「低了点点」→ 权重 `2.5~3` 抬到 `12~14`，**实测 2.51~2.66 个/小时**（图 6~8 = 2.80 / 2.51 / 2.16，原 0.42~0.58）。判据 = 同期**宠物蛋掉 4.17 个/小时**，一阶素材不该比它低一个数量级。<br>**精粹档原样未动**（图 9~10 仍 0.89~1.04，与图 8 的 6.37 是同一形状的断层 —— 用户本轮只点了普通档，等发话） |
| 2 | **合成之石补图 1~3** | 权重 3 / 5 / 8（图4~10 原值不动）。合成 Lv40 才解锁，浅图给一点不构成浪费 |
| 3 | **腐印图 8~10 拉平** | 旧：图8 只 3 种 / 图9 只 6 种 / 图10 全 12 种。现：**图 8~10 都出全部 12 种**，权重按辣度（轻辣常见、重辣稀有）。⛔ 仍**不进图 1~7**（腐印服务于塔，别让中期背包堆废料） |
| 4 | **区域材料降权 30%** | 权重 100~125 → 70~84，**占该图材料池 41% → 21.5%~32.5%（均值 ≈28%）**。⛔ 专属属性保留（每图仍只出自己那一种）、列表总量不变（子权重只决定"掉到哪一种"） |
| 5 | **觉醒之路门槛 888 → 100** | 888 个 = 每种 **47~70 小时**、十种合计 **≈570 小时** ⇒ 门形同虚设（这就是"区域材料像废料"的真因）。100 个 = 每种 **5.3~7.9 小时**、十种合计 **≈60 小时**。"必须跑满 10 张图"**保留** |
| 6 | **通天塔三段全域化 + 前期加厚** | 旧表按层段"开/关"（天仙玉露/锁魂玉/琼浆玉露 只在 21~30 层）。现：**三段都含全部物品**（27 种 → **26 种**，2026-09-17 晚鉴定石移出，见下方补充节），权重随深度爬。且 **band1（1~10 层）原来一件塔专属物都没有**（2026-09-10 那次重排漏改）→ 压低地图货（鉴定石 30→22 等）、把权重给塔专属 6 件 + 腐印。打到 10 层：**塔专属 0 → 4.8 件/局**。同时下调 `layerDrop.none`（84→82 / 42→34）吸收新增权重，保证原有物品不减产 |
| 7 | **通天塔档位奖励加厚** | 5 层档 + 神圣石1/强化丹A2；10 层档 + 越龙之石1；15 层档 + 天仙玉露1；保底（不足 5 层）重铸石2 → +鉴定石3 + 微光经验屑2。20/25/30 层档不动 |
| 8 | **至尊神石：守关 Boss → 通天塔** | 旧：图1~10 任何 Boss 5% × 领主**每小时现身一次** = **一天约 1.2 颗** ≈ 一天一只神级宠（"很容易给好东西"的最大口子）。现：Boss **完全不出**；塔材料池 **0.5 / 1 / 3**（随深度，打到 10 层约 5.4%/局）；**第 30 层通关档保底 ×1**；**每周兑换「神石重铸」保留**（够得着的那条路，≡20 小时，每周限 1） |
| 9 | **百变魔石一个数不动** | 守关 Boss 全局 10%（用户只要求挪走至尊神石）。⚠️ 也**不该**改：它变多会顺着"两颗百变换一颗至尊"的兑换链把至尊神石也变容易，与第 8 条正面对冲 |

### 顺带修掉的老 bug

- **`mat-wiki.js` 区域材料词条图号错 5 处**（`sources()` 用 `areaMaterials` 的**对象键序号**当图号，与 `battle.areas` 顺序不一致：`blight-heart` 排键第 6、实际是图 10）→ 已改成按真实图序号查。受害的 5 种材料：腐变之心 / 回响之羽 / 腐沼黏液 / 余烬残灰 / 魂渊之尘。

### 守值测试的改动（旧断言被**有意推翻**，各留了替换说明）

| 测试 | 旧断言 | 新断言 |
|---|---|---|
| `vtest_resource_matrix.js` | 图 9~10 只出最高档 + 权重 ≤ 图 8 的 1/4 | 10 张图三档齐全 + 图 9~10 传说仍是该图最高 + 深处传说远高于浅处 |
| `vtest_drop_tier.js` | 合成之石图 1~3 不出现；图 9~10 只产传说 | 合成之石图 1~3 出现但权重 ≤8；三档 10 图都有 |
| `vtest_tower.js` | 浅层只出基础打造石、不出塔专属高级材料 | 浅层**有**塔专属货，但权重远低于深层 |
| `vtest_awaken_flow.js` | 需求数量 = 888 | 需求数量 = 100 |
| `vtest_worldmap.js` / `vtest_pet_balance.js` | 与 `areaEvolutionTiers` 对齐 | 与「权重表里出现的那几档」对齐 |
| `vtest_worldmap.js` 第 7 条 | 占比和 = 100 **±2（硬编码）** | **±(N/2+1)**：`pct` 是逐行 `Math.round` 的展示值，N 行最多累计 ±N/2，硬编码 ±2 是运气而非契约（图 9 有 22 行，光舍入就能到 ±3 —— 2026-09-17 晚二次修时真被撞红，占比和 = 103）。新容差仍能抓真错：误除以 `maxW` 而非总重时图 9 得 **≈306** |

验证：`cd docs && npm run check` = **85 通过 / 3 失败**（存量红，无新红）。`docs/产出归属总表.xlsx` 重跑后「异常与待定」页签的**文案图号不符从 5 项降到 0 项**。

## 🔴 2026-09-17 晚·补充：**塔的稀有度**与**鉴定石移出塔**

> 起因：用户看完塔的产出体检表，回了两句 ——「**不是**」（"一局 1% = 三个月见一次"不是他要的稀有度）
> 和「**鉴定给我移除啊**」。前一句修稀有度、后一句撤地图货，**两条互为账**：抬稀有档用的权重就是撤鉴定石腾出来的。

| # | 改动 | 数字 |
|---|---|---|
| 1 | **鉴定石移出塔的三段材料池** | 撤 `22 / 22 / 16`。它是**地图货**（地图 8.7~10 件/小时、地图表第一大项），却占了 band1 的 **22.2% = 4.8 件/段（前 10 层最大的一份）**。⚠️ 塔每局掉约 9 件**未鉴定**装备，鉴定石得回地图刷；但**保底 ×3 与第 5 层档位 ×5 仍给**（确定性、不是抽），所以"打完塔开不了装备"不会发生。⛔ 这两处本轮没动 |
| 2 | **抬"摸不到的那批"** | 判据换成 **「一局碰到的概率」**。band1 走完 = 10.8 次材料命中；旧值下 魂晶经验匣 **1.1%/局**、腐印·天罚 2.2%、禁疗 3.2%、一批 5% —— 塔一天只有 1 局 ⇒ **1% = 三个月**。改后同口径：**主力 50~62%**、天仙玉露/荆棘/蚀甲/破阵 **31%**、琼浆玉露 **25%**、原 5% 档 **17%**、禁疗 **13%**、天罚 **11%**、魂晶经验匣 **11%** |
| 3 | **至尊神石刻意不动** | 只保持**占本段比例**不变（0.51% / 0.81% / 1.92%）⇒ 一局 **5.4% / 8.2% / 18.6%**，**与改动前完全一致**。它是「必定出神级宠」，上一轮刚从守关 Boss 挪进塔（那边一天 1.2 颗）就是为了收紧它，**不许被"顺手抬稀有档"带走** |

### 追加：**通天塔重置卡加回兑换**（用户拍板「A」）

用户先追问「**你又考虑过重置卡这个东西嘛**」，再问「**？？？任务里现在没有了？**」——
第三问才让他拍板。完整沿革（**别只看最后一行**）：

| 时间 | 发生了什么 |
|---|---|
| 更早 | 重置卡唯一来源 = 魔石商店（60 魔石/张、每周限 3 张） |
| 09-10 | **商店关闭**（个人收款码作废，`shop.enabled=false`）⇒ 卡断了来源 |
| 09-10 | 为救断链，加了每周兑换「**塔券铸成**」（当时是第 1 次**有意放开**） |
| **09-16** | 用户「**任务给的重置卡太多了**」⇒ 整条删除（成本先提到 24 个腐印他仍否，点明问题**不在价格**、在"任务能产重置卡"本身）⇒ 卡变**无任何来源** |
| 09-17 下午 | 定论"**维持无来源**"（多打一局 = 更多产出，符合"不能很容易给好东西"） |
| **09-17 晚** | 用户在三选一里选 **A**：**加回兑换**，并定「**只每周 1 张、成本拉高**」 |

**实现与护栏**（写在 `config.js` 的 `ex_week_reset_card` 上）：`reset:'weekly'` 额度限死 1 张；
成本 **36 个腐印**（腐印·暴怒 / 疾影 / 狂乱 各 12）> 被删前的 24，命中兑换铁律 ③「消耗物必须是真富余的」；
⛔ 不许改成 `repeatable`、别把成本降回 8/种。
**它不是"任务能农重置卡"**：付出是塔自己的掉落（腐印），换来"再打一局" = **把多余副产烧成时间**。

**连带复核（已做）**：塔的产出上限 7 局/周 → **8 局/周（×1.14）**⇒
每个「一局 X%」不变、但"多少天见一次"整体快 14%（魂晶经验匣 11 → 9.6 天、至尊神石 19 → 16.7 天）。
**结论：稀有档不动** —— 花 36 个腐印换 +14%，为它去压低所有人的产出不划算（结论登记在 `tower-config` 的稀有度段）。

✅ `产出归属总表.xlsx` 的「**名义来源为空**」由 1 项 → **0 项**（改完重跑自动验证）。

### 判据（以后调塔的掉落照这条）

> **「表里有这个键」不等于「玩家拿得到」—— 用「一局碰到的概率」验收，不用权重数字，也不用"平均几局出 1 个"。**
> 塔有【每天 1 局免费】的硬闸门 ⇒ 一局 **10% ≈ 十天一遇**；一局 **1% ≈ 三个月**（比"根本没有"更糟 —— 玩家会当成 bug）。
> 读数命令：`cd docs/tests && node probe_tower_drops.js`（② 表给「一局碰到的概率」，③ 表列所有低于 10% 的）。

⚠️ 三段池合计由 99.1 / 123.3 / 156.5 → **233.7 / 226.3 / 195.7**：**别拿新旧权重横向比大小**（那样会得出"什么都涨了 2 倍"的错结论）。
验证：`npm run check` = **85 通过 / 3 失败**（无新红）。

## 受控资源归属

| 资源 | 主来源（唯一） | 允许的少量补充 | 明确禁止的来源 | 代码落点 |
|---|---|---|---|---|
| 区域材料 | 普通地图掉落（按图档） | — | 资源试炼、任务、分解 | `Config.drop.materialWeightsByTier` |
| 进化素材（普通） | **地图全 10 张**（图 1~2 主产地 10.8~11.4 个/小时；图 3~5 为 2.5~5.2；**图 6~10 为 2.2~2.8**，2026-09-17 晚二次修）+ 试炼·蜕变（Lv1 档） | 主线当前阶段一次性 | 循环任务 | `materialWeightsByTier` 的 `'进化素材'` 键 / `resourceTrials.routes[metamorph].tiers` |
| 精粹进化素材 | **地图全 10 张**（图 3~8 主产地 3.5~7.3 个/小时；**图 9~10 仅 0.89~1.04**，与图 8 的 6.37 之间存在同一形状的断层，**未修、等用户发话**）+ 试炼·蜕变（Lv25 档） | 主线当前阶段一次性 | 循环任务 | `materialWeightsByTier` 的 `'精粹进化素材'` 键 / `resourceTrials.routes[metamorph].tiers` |
| 传说进化素材 | **地图全 10 张**（图 6~8 主产地 9.3~13.7 个/小时；图 9~10 为 4.4~5.2；图 1~4 仅 0.08~0.22 —— 浅图"能出但等于不出"）+ 试炼·蜕变（Lv40 档） | 终阶相关首通一次（≤2/条）；成就档 ≤1/条；**每日兑换「传说补遗」×2**（2026-09-10，用户点名大头） | 循环任务 | `materialWeightsByTier` 第 6~10 档 + `ex_day_legend` |
| 涅槃丹 | 试炼·涅槃（Lv25+） | **每日兑换「涅槃资粮」×2**（2026-09-10） | 地图常规掉落、新手引导 | `resourceTrials.routes[nirvana]`（已道具化，非涅磐兽） |
| 重铸石 / 增缀石 / 剥离石 | 试炼·淬炼（Lv1 / Lv25 档）+ 地图低权重 | 循环任务 | — | `resourceTrials.routes[temper].tiers` |
| 神圣石 | **试炼·淬炼 Lv43+（2026-09-09 起为唯一活来源）** | **通天塔档位奖励（10/15/20/25/30 档）** + **地图委托图 8~10（2026-09-16 由每轮 5/6/8 收到 2/2/3）** | ~~地图 7~10 掉落~~（已从 `materialWeightsByTier` 全删，防「挂机白给毕业必需品」） | `temper.tiers` + `tower-config` `floorTiers` + `loop_ember_hollow` / `loop_soul_abyss` / `loop_blight_heart` |
| 锁定石 | 试炼·淬炼高阶（Lv43+） | **每周兑换「锁纹重铸」×2**（2026-09-10） | 地图任何掉落（通天塔也不发） | `temper.tiers` + 地图权重表无该键 |
| 合成之石 | 地图（低权重，已砍半）+ 试炼·涅槃失败补偿 | 商店；每日兑换「涅槃资粮」会消耗它 | — | `materialWeightsByTier` |
| 越龙之石（合成 +10%/神级30%） | **通天塔**：11~30 层材料池 + 20/25/30 档 | **每周兑换「龙石回炉」×3**（2026-09-10） | ~~地图 4~10~~（已删） | `tower-config` `materialBands` / `floorTiers` |
| 天仙玉露（进化成长 +30%） | **通天塔**：21~30 层材料池 + 25/30 档 | **每周兑换「灵液提纯」×3**（2026-09-10） | ~~地图 8~10~~（已删） | 同上 |
| 强化丹B（进化成长 +20%） | **通天塔**：11~30 层材料池 + 10/15/30 档 | **每周兑换「玉液凝丹」×3**（2026-09-10） | ~~地图 4~10~~（已删） | 同上 |
| 腐印（进塔词缀，消耗品） | 地图 8~10 图掉落 + 试炼·淬炼 15/20 档 | **通天塔层掉落（塔自给）** | 任务、分解 | `materialWeightsByTier`（图 8~10）+ `trial-config` 淬炼档位 + `tower-config` `materialBands` |
| 通天塔重置卡（塔的额外次数） | **每周兑换「塔券铸成」限 1 张**（2026-09-17 晚加回，用户拍板「A」；成本 36 个腐印 = 3 种常见腐印各 12）。沿革：09-10 建 → **09-16 删** → **09-17 晚加回**；商店**从未上架**（09-12 拍板「魔石只卖便利、不卖数值」，而重置卡 = 多打一局 = 产装备/材料 ⇒ **属数值 ⇒ 不该上架**）。玩家可见文案**已如实**（百科/塔详情页写"额外次数：每周兑换「塔券铸成」限 1 张"，名字从 config 派生） | — | 掉落 / **市场交易**（⛔ 任务不再禁止：09-17 晚用户有意放开，每周限 1 张） | `Config.tower.resetCard`（**预留死配置**，全仓只有 `vtest_tower.js` 断言它 >0）；**刻意不进** `Config.trade.materials` |
| 资源试炼门票（副本的额外次数） | **地图委托 `collect_loop` 每轮 1 张**（每轮需该图材料 **200** 个 ≈ 8.3 小时挂机；2026-09-16 由 50 提到 200，`trial-config` 统一注入） | 引导 `n6` / 毕业礼包（一次性，各 1~2 张） | 掉落 / 分解 / 市场交易 | `trial-config.js` 门票注入；~~`ex_day_ticket`「门票熔铸」~~（09-16 删）、~~`wk5` 周常 ×2~~（09-16 改发通货）；**不在** `Config.trade.materials`（账号绑定） |
| 副本每日免费次数 | **每条路线各 1 次**（2026-09-16 由 3 收紧；3 条路线 = 每天 3 局免费） | — | — | `Config.resourceTrials.freeEntriesPerDay` |
| 强化丹A（进化成长 +10%） | 地图低权重（属「燃料」级） | **每日兑换「淬火凝丹」×4**（2026-09-10） | — | `materialWeightsByTier` |
| 涅磐兽 | **图 10 极低概率掉落**（`materialWeightsByTier[10]`，权重 0.05 ≈ 每 6.5 万场一个） | —（**2026-09-11 起从 AI 出货表与交易白名单摘除**：假卖家不再上架、玩家不能再挂，市场不再产出这件无消耗口的收藏物；老库存留作纪念，持有量统计保留） | 作为涅槃消耗品（已退役）、低图掉落 | ~~`Config.trade.materials` / `Config.marketBot` 五处~~（已移出） |
| 鉴定石 | 地图掉落 + 循环任务（`d11` 每日孵化）+ 通天塔**保底/第 5 层档位**（⛔ **2026-09-17 晚已移出塔的随机材料池**：它是地图货，却占了塔前 10 层最大的一份） | **每日兑换「废石辨真」×20**（2026-09-10） | — | `Config.trade.materials` |
| ~~凝魂晶石~~ **2026-09-16 整套退役** | ~~满级经验池凝出（账号绑定）~~ | — | — | ~~`Config.pet.expPool`~~ **已删**；满级溢出经验直接丢弃；**魂铸的消耗品改为「合成之石」×10**（`Config.soulCast.material`）；老库存留在库表里、不再显示也没有用途 |
| 宠物蛋 | 地图掉落（`egg` 权重，4.2 个/小时）+ 主线 `m15`/`m27` | —（**兑换「卵石相易」已删**：36 个剥离石（8.4h 产出）换 2 个蛋（0.5h 产出）= 净亏 17 倍，死内容） | — | `Config.drop.poolByStage` |
| 经验包（5 档：屑 **100** / 囊 **1,000** / 囊 **15,000** / 匣 **50,000** / 髓 **120,000**） | **① 任务奖励**（交任务按该任务经验值折算，绑定）—— 只用 100/1000 两档<br>**② 🆕 地图掉落（2026-09-18 加，全域）**：见下行 | 🆕 **地图掉落（2026-09-18 加）**：**图 1~10 全掉**，按图分层 —— 图1~3「微光经验屑/残魂经验囊」、图4~6「残魂/聚魂」、图7~8「残魂/聚魂」、图9~10「聚魂/魂晶」；**统一 3.5 小时掉一个**（图10 ≈6,342 经验/小时 ≈练级速度的 43%）。<br>**塔（2026-09-17 加）**：一局 ≈**10,508 经验**；**第 30 层通关档另给「幽冥经验髓」×1**（10 万档刻意不进随机池） | **市场交易一律禁止**：经验包能买 = 花钱跳过练级，而「练级成本」是涅槃成长爬升的唯一刹车。<br>🔴 2026-09-18 起**地图也掉** ⇒ 绑定改由 **`Materials.gain` 自己判定**（`isExpPack` 唯一判定处）+ 调用处显式传，两层兜底，守值 `vtest_quest_material.js` | `Config.expPacks` + `Config.drop.materialWeightsByTier` + `tower-config` 的 `materialBands` / `floorTiers` + `Materials.isExpPack`；守值 `vtest_tower.js` ⑫ + `vtest_quest_material.js` |

## 任务奖励规则（写在 `Config.drop.quests` 上方注释，测试守）

1. 循环任务（`repeat`/`repeatable`）只发打造通货 + 经验 + 门票 → 禁止进化和涅槃材料。
2. 传说进化素材只出现在 `unlockLevel` 31~48（= 图 6~8）的任务里；图 9~10（`≥49`）一律改发该阶段打造通货。
3. 单条任务给传说 ≤ 2（一只宠终阶总共只要 2，2026-09-11 取消终阶额外×3 后；任务只补当前缺口）。

### 装备生成规则重做（2026-09-11 用户拍板，按 PoE 词缀模式，资源矩阵的装备口径随之变更）

一切由**装备等级(ilvl)**决定，颜色只是结果 —— 旧的两张按图 roll 的权重表（`materialTierWeights` / `rarityWeightsByTier`）已作废不再被读取：

| ilvl | 词缀条数（含基础 1 条） | 颜色 | 词缀 T 阶池（按权重抽） | 底材 T 阶池 |
|---|---|---|---|---|
| ≥70 | 4~5 | 金 | T1 5% / T2 15% / T3 30% / T4 25% / T5 25% | 同左 |
| 60~69 | 3~4 | 金 | T2 15% / T3 30% / T4 25% / T5 25%（**T1 不入池**） | 同左 |
| 25~59 | 2~3 | 蓝~金 | T3 30% / T4 25% / T5 25% + T4/T5 加权 | 同左 |
| 1~24 | 1~2 | 白/蓝 | T4 / T5 | 同左 |

- **PoE 模式**：每个 tier 有自己的门槛（`affixIlvlGates`：T1=70/T2=60/T3=25/T4=1/T5=1）+ 自己的权重
  （`affixTierWeights`：5/15/30/25/25）；掉落时「门槛 ≤ ilvl」的 tier 全部进池按权重抽 —— **T1 可以出现但稀有**。
- ilvl = 掉落它的怪等级：野图最高图 10（怪 55）→ 最高 T3；**通天塔怪 Lv60→120，10 层后 T1 入池** —— 塔产出全面压过野图。
- 每条词缀独立 roll（同一件金装可以 T1/T3 混排）；数值随机保留（每条 min~max）；底材 T 与词缀 T 同池同机制。
- 重铸/增缀沿用装备出生 ilvl —— 低图装备洗不出高档词缀。
- 「掉落稀有度」词缀的加成落点 = **多 1 条词缀**（颜色自然升档）。
- 守护测试：`vtest_tier_rarity.js` 按 PoE 模式重写（门槛池 / T1 ≈5% / 条数→颜色 / 底材同池 / 统一入口）。
4. **（2026-09-10 补）任务只发现阶段有消耗口的资源** —— 已退役/无消耗口的道具不得进奖励表（涅磐兽即因此出局，见下）。
5. **（2026-09-10 补）收集类日常的解锁等级 = 该材料所在图的等级下限**：原先 12 条日常都没有 `unlockLevel`，Lv1 就会看到「腐变之心 ×20」（图 10 材料），列表里躺着永远做不完的条目。现已按图对齐（`d3`→1 / `d4`→7 / `d5`→13 / `d6`→19 / `d7`→25 / `d8`→55）。

### 兑换任务规则（2026-09-10 新增一级分类 `exchange`，`vtest_quest.js` 守）

背景：用户报「很多东西不好拿到了」。参考口袋精灵2 的【物资兑换】（提交富余物 → 换需要的东西）落地本作。
它同时解掉了 **通天塔重置卡断链**（商店关闭后唯一非付费来源）。

三条自制规矩（比铁律 1 更严，改之前先读）：

1. **必须有硬上限**：`kind:'exchange'` + `reset:'daily'|'weekly'`，**绝不允许 `repeatable`**（测试直接断言）。
2. **每日只换低/中价值物**（鉴定石 / 强化丹A / 门票 / 凝魂晶石 / 涅槃丹 / **传说进化素材**）；
   **受控高价值物一律每周 1 次**（强化丹B / 天仙玉露 / 锁定石 / 越龙之石 / 重置卡 / 至尊神石），
   产出量压到远低于正常来源 —— 免得把「打淬炼 / 打塔 / 打守关 Boss」架空。
   **唯一例外**：`ex_day_legend`「传说补遗」每日 ×2（2026-09-10 用户点名传说是大头）。
   敢放每日的理由：传说已经有**四条**来源（图 6~8 / 图 9~10 低权重 / 试炼·蜕变 / 一次性任务），
   兑换只是第五条保底，2 个/天 远低于挂机 4.3~13.3 个/小时，不可能变成最优解。
3. **不能拿稀缺物换稀缺物**：消耗物必须是真富余的（基础打造通货、腐印、百变魔石）。

> ⚠️ 这是对原「唯一主来源」原则的**有意放开**，放开的前提就是上面三条。
> 任何一条被破坏（把某条改成 `repeatable`、或把每周的挪到每日），
> 那条资源立刻变成新的最优解，地图与副本的循环会被打断。
>
> 换出的 **通天塔重置卡 / 凝魂晶石** 刻意不在 `Config.trade.materials` 里 → 天然账号绑定，不给 RMT 开门。
>
> ⚠️ **批判者回合补的一条红线（2026-09-10）**：兑换消耗的必须是**真富余物**（基础打造通货），且比值不许亏到 10 倍以上 ——
> 「卵石相易」就是亏 17 倍才删掉的。另：**兑换会和交易行抢生意**（官方固定价 vs 市场发现价），
> 所以它的定位只能是"最后兜底"，凡是能直接挂机刷到的资源，比值必须明显差于挂机（涅槃丹 / 传说现在亏 3~6 倍），
> 刷不到的（神圣石 / 锁定石 / 重置卡 / 强化丹B / 天仙玉露）才允许接近持平。

### 🔴 2026-09-16 收紧（用户：「副本重置以及通天塔重置怎么能够这么简单就获取」）

两处「再打一次」的资格本来都有硬上限，但**获取太便宜 ⇒ 上限形同虚设**。现值：

| 条目 | 原值 | 现值 | 理由 |
|---|---|---|---|
| `Config.resourceTrials.freeEntriesPerDay` | 3（**按路线各算** ⇒ 3 条路线 = 每天 9 局白打） | **1**（每天 3 局） | 塔才 1 次/天，副本却白送 9 局 |
| 每日兑换 `ex_day_ticket`「门票熔铸」 | 门票 ×3 | **×1** | 门票的主来源该是「交地图委托」（挂机→交委托→换门票），兑换只兜底 |
| 每周兑换 `ex_week_reset_card`「塔券铸成」 | 腐印 4/种（共 12 个） | **8/种（共 24 个）** | 一局塔到 10 层就掉 ~3 个腐印 ⇒ 12 个几乎白拿；24 个 ≈ 8~16 小时挂机 |

- ⚠️ **没动**：地图委托「每轮 1 张门票」（核心循环，`vtest_guide_chain` 守着）、周常 `wk5`（打 3 次送 2 张 = 循环激励）。
- ⚠️ 改 `freeEntriesPerDay` 牵动 `vtest_trial_node_ui.js` 三处免费次数断言（原先硬编码 3）—— 已改成**从 `Config` 读**，以后调这个数不会再红。
- 🔴 **顺带查出**：`通天塔重置卡` 在魔石商店**从未上架**（`products` 表只有充值档 + 3 个便利权益）。
  也就是说塔详情页/百科页写的「魔石商店购买，每周限购 3 张」是**空的**，玩家照着找不到。
  → ✅ **2026-09-17 已定并修完**（用户「那就修复啊」）：**当时定的是维持无来源**——
    商店不上架（09-12 拍板"只卖便利不卖数值"）、任务不产（09-16）。同时修掉三处与事实不符的说法（**注：09-17 晚又改判了 —— 见上方「晚·补充」节的重置卡小节**）：
    百科文案早已改成"额外次数的获取途径暂未开放"；`tower-config.js` 文件头原写「（魔石商店，硬限购）」、
    以及 `resetCard` 注释里原写「**玩家拿到它的唯一路 = 每周兑换「塔券铸成」**」——那条路 09-16 就删了、**注释没跟上**，一并改对。
    ⚠️ 教训：**改数值/删来源时，必须回头搜"提到它的注释"**，否则注释会替下一个会话编出一个不存在的来源。

### 📊 2026-09-16 任务比例审计（用户：「检查一下所有的任务比例，我是觉得真的很多东西太简单了」）

**方法（可复现）**：探针 `docs/tests/probe_quest_audit.js` 把每条任务的【需求】与【奖励】都折算成**挂机小时**——
- 场/小时 = 700；每场产出 = `drop.poolByStage`（按图序号）× `drop.materialWeightsByTier`（按图档子权重）
- 进化素材按 `drop.areaEvolutionTiers` + `drop.evoMaterialWeights` 分到普通/精粹/传说
- 地图不产的资源（神圣石 / 锁定石 / 越龙之石 / 天仙玉露 / 涅槃丹 / 凝魂晶石 / 门票 / 重置卡 / 至尊神石…）标 `[受控]`，不折算小时
- **模型自检**：传说@图8 = **13.31 个/小时**、区域材料 ≈ 24/小时、装备 20.6（图1-3）/ 10.4（图8-10）、蛋 4.17/小时
  —— 与 `drop.pool` 注释里的既有基准（13.3 / ~20 / 20.6 / 4.2）吻合，口径可信。

**判据**：可重复任务（每日 / 每周 / 循环）的「奖励折算小时 ÷ 需求耗时」必须 **< 1** ——
否则"做任务"比"挂机"划算，任务就成了新的最优产出源（这正是当初立「循环任务只发通货」铁律的原因）。
一次性里程碑**允许 2~3 倍溢价**（任务本来就该有溢价，不然没人做）。

**查出并已改（4 类）**：

| # | 问题 | 证据 | 改法 |
|---|---|---|---|
| 1 | **受控资源被挂机白给**（最严重） | 图 8/9/10 的地图委托每轮给神圣石 **5 / 6 / 8** 个，而一轮 = 50 个区域材料 ≈ 2~2.5 小时 ⇒ **2~4 个/小时**；主来源淬炼试炼一局只 1~2 个、每日免费 1 次 ⇒ **试炼玩法被挂机架空**（且本表此前从未登记过这条来源） | `loop_ember_hollow` / `loop_soul_abyss` / `loop_blight_heart` → 神圣石 **2 / 2 / 3** |
| 2 | 日常"奖励 > 付出" | `d1` 100 杀（8.6 分钟）给重铸石 2（16 分钟产量）= **1.9 倍**；`d2` = 1.45 倍 | `d1` → 150 杀 + 重铸石 1；`d2` → 300 杀 + 重铸石 2 |
| 3 | 日常门槛 = "点一下按钮" | `d12`「每日交易」成交 2 次 = **4.89 倍**、`d14`「每日上架」上架 1 件 = **3.71 倍** | 门槛各提到 **3 次** |
| 4 | 宠物线零成本拿高档材料 | `pe19 孵化·尸犬` 给**传说 ×1**、`pe22 孵化·幽影兔` 给**传说 ×2**（成本只是把挂机掉的蛋孵出来）；`pe2` / `pe11` 试炼给该档素材 ×3 | 孵化环 → 精粹 ×2（新孵的宠要练，精粹才是它要用的燃料）；试炼环 ×3 → ×2 |

**第二轮（用户追问「那些超模奖励你打算怎么修改」）—— 单查「受控资源白拿」这一类**：

换了一个尺子：**不看东西贵不贵，看「付出 vs 回报」**。按这把尺子，两条看着吓人的其实**不超模**：
- `ex_day_nir_pill`（每天 2 个涅槃丹）：成本 12 合成之石 ≈ 3 小时挂机，而涅槃试炼一局（约 10 分钟）就给 4 个 ⇒ 玩家是**亏**的。
- 一次性任务的神圣石合计 **96 个**：**一次性**（拿完就没有），不累积、不构成通胀源。
真正超模的是「**零成本 / 低成本却拿受控资源**」，已改：

| # | 条目 | 原 | 现 |
|---|---|---|---|
| 5 | 日常 `d5` / `d6` / `d13` | 每天白给神圣石 **3 个**（淬炼试炼一局才 1~2 个） | 换成基础通货（重铸石 / 增缀石 / 剥离石 各 1） |
| 6 | `bx_week`「本周活跃」 | 传说×2 + 涅槃丹×3 + 神圣石×5（三种全是受控资源，门槛只是"交 10 条日常"） | 神圣石×2 + 增缀石×5 + 鉴定石×5 |

**改后的账**（探针 ④ / ⑤ 段）：
- 任务侧受控资源白拿：每日神圣石 **3 → 0**；每周 **7 → 4**。
- 「净白拿」（奖励折算 − 成本折算，不含受控）：每日 **1.85 → 1.33 小时挂机量**，其中 `bx_day`「今日勤勉」从 1.29 降到 0.77 小时
  （它是全部任务里净白拿最大的一条 —— 交 60% 日常成本≈0 却送 1.29 小时，已降一档：重铸石 3→2 / 增缀石 2→1 / 鉴定石 5→3）；
  每周 = **0**（每周兑换全是"用富余物换紧缺物"，成本都高于回报）。

**查出但没改（反方向失衡，等用户定）**：
- **主线收集 / 周常 / 成就的需求量大得离谱，奖励却只值几分钟** ——
  `m30` 要 420 个余烬残灰（≈24 小时挂机）→ 传说 ×2（≈9 分钟产量）；
  `m38` 要 550 个腐变之心（≈22 小时）→ 合成之石 ×4；
  `wk2` 要 300 个血潮凝晶（≈14 小时）→ 神圣石 ×2；
  `a6` 要 100 次打造（≈13.8 小时材料）→ 神圣石 ×5。
  ⇒ 这是**同一个病的另一半**：需求量与奖励值从来没放在同一把尺子上量过。
- **孵化类成就倒亏**：`wk4` 孵 15 次给蛋 5、`ah2` 孵 30 次给蛋 3、`a5` 孵 50 次给蛋 5 ——
  而孵化本身**消耗**蛋（蛋 4.2 个/小时）⇒ 奖励比成本还少。
- **久战环（`mk1`~`mk10`）**：击杀量是主线环的 2 倍，奖励却只有基础通货（比值 0.17~0.8）——
  做它远不如直接挂机刷图。

### 2026-09-10 任务系统整理（归类 + 奖励清理）

- **分类口径从 `category` 改为派生的 `kind`**（`quest-config.js` 规范化层，零数据改动）：
  `guide` 6 / `series` 50 / `pet` 24 / `daily` 12 / `loop` 11 / `achieve` 6 = 109 条。
  原先 10 条 `collect_loop` 混在「主线」、`awaken_road` 混在「宠物」（`repeatable` 的 `finished` 恒为 false → 永远占着一次性分类的列表与角标），现统一归入「循环」。
- **涅槃材料改发真道具**：`m19` / `m31` / `a3` 原发的「涅磐兽」已退役（涅槃唯一消耗品是涅槃丹），改为发 **涅槃丹**（1 / 2 / 3）。这三条都是一次性条目，不违反铁律 1。
  来源合法性：涅槃丹 现有主来源 = 试炼·涅槃（5/10/15/20 层），本表允许的少量补充里未禁止任务产出。
- **其余 6 条涅磐兽奖励改成同档打造通货**：`m11`(增缀石2) / `m23`(神圣石2) / `m29`(神圣石3) / `m33`(神圣石4) / `m37`(神圣石5) / `pe24`(神圣石2)。
- ✅ **连带问题已拍板（2026-09-10 用户）**：停发之后「涅磐兽」会变成无来源的死物 → 用户决定**把它的掉落挪到图 10，概率非常非常低**。
  落地：`Config.drop.materialWeightsByTier[10]` 加 `'涅磐兽': 0.05`（详见该处注释与下面的概率账）。
  `vtest_drop_tier.js` 同步改为守「涅磐兽只许出现在图 10 且权重 < 1」。
- ⚠️ 仍留着的一条：`Config.marketBot` 的 AI 出货表（`materialWeights.phoenix` / `botGoods.materials.phoenix` / `prices.phoenix`）仍按它出货。
  现在它有真来源了，所以不再算"凭空造物"；但 AI 无限供给一件**没有任何消耗口**的收藏物，价格锚意义不大，要不要收敛待定。
  → ✅ **已收敛（2026-09-11 用户拍板「摘掉」）**：五处 AI 出货配置 + `Config.trade.materials` 白名单全部移除，假卖家不再产出。

## 本次（2026-09-09）修正清单

### 产出削减 + 高级物品归属重排（与用户逐项讨论定稿）
- 总盘 `drop.pool` / `poolByStage`：none 700→900、material 180→85、equipment 20~30→11~15、egg 20→6
  → 挂机一小时：材料 135→约 59 件、装备 22.6→7.7~10.4 件、蛋 15.1→约 4.2 个。
- **高级物品全部移出普通地图**（原则：地图只出「燃料」，高级物品归副本/塔/市场）：
  - 神圣石：地图 7~10 全删，主来源改为试炼·淬炼 Lv43+（不断供）
  - 越龙之石 / 天仙玉露 / 强化丹B：地图全删，**塔接线前绝版**（淬炼不掉、商店关闭）
  - 归属统一登记在新增的 `Config.towerDrops`（enabled:false 纯登记），`vtest_resource_matrix.js` 新增断言守「这些键不得回到 materialWeightsByTier」
- 合成之石权重砍半（图10 档 45→20）；重铸/增缀/剥离适度降；区域材料保留最大权重（collect_loop 换门票的燃料）。
- **鉴定石按「产出 ≥ 装备产出」反推**（掉落装备全部未鉴定，鉴定石不够会卡死玩家）：阶段3 装备约 10.4 件/小时 → 鉴定石子占比 17.5~19%。
- 价值打分（讨论记录）：区域材料 3 / 进化 4 / 精粹 6 / 传说 9 / 重铸 3 / 增缀 4 / 剥离 5 / 合成 7 / 神圣 8 / 鉴定 5 / 强化丹A 4 / 强化丹B 6 / 玉露 8 / 越龙 7 / 装备 9 / 蛋 7 / 锁定 10。

- 资源试炼奖励档位化进 `Config.resourceTrials.routes[].tiers`，不再硬编码在 `resource-trial.js`；
  失败补偿改为本路线基础进度（**不再发区域材料**）。
- 淬炼高阶（Lv43+）补发**神圣石 + 锁定石**，锁定石因此有了唯一来源（此前图 16/17 删除后断供）。
- 清理任务奖励越界：日常 `d11` 进化素材→鉴定石；主线/BOSS/宠物/成就里图 9~10 与提前两阶段的传说素材→精粹或打造通货，单条封顶 2。
- `凝魂晶石` 移出 `Config.trade.materials`（账号绑定、天然不可交易）；`market.js` 收款白名单改为从 config 派生（消除第二份名单）。
- 涅槃路线发**涅槃丹**（道具化），纠正此前发已退役的涅磐兽。

## 仍未做（不在本次范围）

- ~~通天塔（阶段 3 整块）~~ → **2026-09-10 已落地，回看结论**：原计划让塔补「锁定石 / 涅槃材料」的来源，定调时**取消**（塔不发这两类，锁定石仍是淬炼高阶唯一来源、涅槃材料仍是试炼·涅槃）；塔的产出改为「高级装备 + 越龙之石 / 天仙玉露 / 强化丹B + 腐印 + ~~鉴定石~~」（⛔ 鉴定石 **2026-09-17 晚已移出塔的随机池**，只在保底/第 5 层档位保留少量）。
- 塔**服务端权威 EF** 未落地（`PROJECT_CONSTITUTION.md` 上线前必做清单 #6，暂停中）→ 塔的奖励目前仍是客户端发放，恢复时与 #6 一起做。
- 4.3 成长软门槛、6.3 副本词缀、阶段 2 验证数据：按基线仍待办。

### 🔍 2026-09-17 导出「产出归属总表」时自动查出的三处（只登记，未改）

做 Excel 现状表时把「游戏内词条的来源文案」跟代码对了一遍账，查出三处**文案/名册**问题（都不影响掉落本身）：

1. **区域材料的词条图号写错 5 处**（玩家能看到）——`core/mat-wiki.js` 的 `sources()` 用 `areaMaterials` 的**对象键序号 `i+1`** 当图号，
   而它的键顺序与 `Config.battle.areas` 的排列**不一致**（`blight-heart` 排在键的第 6 位、实际是图 10），于是：

   | 材料 | 词条里写的 | 实际所在图 |
   |---|---|---|
   | 腐变之心 | 图6 | **图10** |
   | 回响之羽 | 图7 | **图6** |
   | 腐沼黏液 | 图8 | **图7** |
   | 余烬残灰 | 图9 | **图8** |
   | 魂渊之尘 | 图10 | **图9** |

   ✅ **2026-09-17 已修**：改成按 `Config.battle.areas` 查真实序号，5 处图号全部对上了
   （`tests/export_matrix.js` 的「文案图号对不上」从 5 项降到 0 项，自动可验）。
2. **通天塔重置卡没有任何获取途径，但塔详情页/百科页写着「魔石商店购买（60 魔石，每周限购 3 张）」** ——
   ✅ **2026-09-17 已修**：定论"维持无来源"，并把三处与事实不符的说法改对
   （百科文案 / `tower-config.js` 文件头 / `resetCard` 注释里那句已失效的"唯一路 = 塔券铸成"）。
3. **`Config.trade.materials` 里有两条重复登记**（`锁魂玉`、`琼浆玉露` 各出现两次）——
   ✅ **2026-09-17 已修**：删掉重复的两行，并在 `vtest_resource_matrix.js` 新增第 6b 节
   （**按 id 与材料名双重查重**）防回归：这类重复**不报错不崩**（按 id 去重的消费方只取第一条），
   只是下拉多一个选项，**靠肉眼永远发现不了，只能静态查重**。

> 这三处都由 `tests/export_matrix.js` **自动检出**并写进 xlsx 的「异常与待定」页签（改完重跑会自动消失），
> 所以不用手改本节的表 —— 这里是留个可追溯的记录。

### 神宠培育与定向植入（2026-09-11 新增两件道具，均归通天塔）

神宠的成长和特质各给了一条花钱花肝的长线，两件新道具都只从通天塔 21~30 层出，商店配了货但商店还没开门：

- **琼浆玉露**：神宠培育，吃一颗成长 +0.8~1.3（封顶 100）。21~30 层材料池出，30 层档位奖励再保底 1 颗。
- **锁魂玉**：涅槃时指定副宠的一条特质 100% 植入，用了它副宠其他特质本次就不进。21~30 层材料池出（权重压得低），25 层档位奖励 1 颗。

顺带拍板的几条，别再问：神宠不准参与合成（主副都拦）；合成时主宠词条 100% 保留、只升不降；副宠词条 40%（成长≥60 是 50%）概率嫁接进来，至尊神石直接 100%；变异追加的词条按变异福利掷（T1 20%、保底 T2）。

### 神宠培育的次数规则（2026-09-11 补）

培育不是无限吃玉露：每只神宠**一轮最多 10 次**（Config.pet.godPets.cultivateMax），天仙玉露和琼浆玉露共用这个额度。用尽了就只能等**涅槃**——涅槃成功时计数归零，开启新一轮 10 次。计数存在 pets.cultivate_used，旧库缺列时按 0 处理，读档不会炸。
