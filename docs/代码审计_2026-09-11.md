# Forge of Souls 代码审计（2026-09-11）

**方法**：意图轴审计（intended vs implemented）。先读 `PROJECT_CONSTITUTION.md` + `docs/*.md` 当"应该是什么"，再逐条到代码里找强制点。
**范围**：`docs/js/`（72 文件 / 约 1.5MB）+ `supabase/*.sql`（25 个）+ `supabase/functions/`。
**不在范围**：通用 lint（测试已全绿，扫描器价值低）、美术资源、文案。

**一句话结论**：前端玩法的工程质量不错（转义、回滚、锁定收口都做对了），但**云端 RPC 层有一个能直接刷穿材料经济的洞**，且**迁移脚本的"重放安全"是假的**——旧脚本重放会静默抹掉安全加固。

---

## 线上实测（2026-09-11，anon 身份直调）

用仓库里的 anon key 直接打三个 RPC，传入不存在的挂单 UUID（函数只会返回 `notfound`，不写库）：

```powershell
$k='<docs/js/core/supabase.js 里的 anon key>'
$h=@{apikey=$k;Authorization="Bearer $k";'Content-Type'='application/json'}
$b='{"p_listing_id":"00000000-0000-0000-0000-000000000000"}'
Invoke-WebRequest -Uri "https://asklogeayzlqpeejuvjj.supabase.co/rest/v1/rpc/bot_buy_material" -Method POST -Headers $h -Body $b -SkipHttpErrorCheck
```

| RPC | 结果 | 读法 |
|---|---|---|
| `bot_buy_equip` | 401 `42501 permission denied` | anon 已被收回 ✅ |
| `bot_buy_pet` | 401 `42501 permission denied` | anon 已被收回 ✅ |
| `bot_buy_material` | **200 `"notfound"`** | **anon 能调，且没撞到 `ERR_BOT_BUY_ANON`（撞到会抛异常而不是返回 notfound）= 函数体内根本没有 `bot_buy_guard()`** 🔴 |

→ **P0-1 不是推测，是线上正在发生的。** 也印证了根因：hardening 是 09-03 的，`bot_buy_material` 09-10 才建，那句"统一收 anon"从没覆盖过它。

（`bot_buy_equip/pet` 的 anon 维度是安全的，但**守卫是否被 `migrate_trade_records_ref.sql` 抄丢，anon 探测不出来** —— 需要登录态或 PAT 查 `pg_get_functiondef`。不过下面写的 `migrate_security_reapply.sql` 是"覆盖式"的，不管线上什么状态，跑完都对，所以不必先诊断。）

---

## 🔴 P0-1　`bot_buy_material` 从诞生起就没有任何守卫 —— 可无限把材料 A 换成材料 B

### 文档说应该是这样
- `PROJECT_CONSTITUTION.md` B10 两条红线：**"消耗端必须存在（sink）"、"AI 只买不高于价值模型上限的单"**。
- `docs/js/core/config.js:1358` 定义了守卫参数：`minAccountAgeSec: 600`（新号 10 分钟静默期）、`dailyCap: 30`（每天 30 次）。
- `docs/js/core/market_bot.js:37` 注释明说"服务端会拒：未登录 / 封禁 / 新号不满10分钟 / 每日上限"。

### 代码实际是这样
`supabase/migrate_material_listings.sql:177-214`（提交于 **2026-09-10**）：

```sql
create or replace function public.bot_buy_material(p_listing_id uuid)
...
begin
  select * into v_listing from public.material_listings
  where id = p_listing_id and status = 'active' for update;
  if not found then return 'notfound'; end if;

  -- 直接给卖家发材料，没有任何前置校验
  insert into public.materials (user_id, name, quantity)
  values (v_listing.seller_id, v_listing.material_type, v_net) ...

  update public.material_listings set status = 'sold' where id = p_listing_id;
```

对照 `supabase/migrate_security_hardening.sql:149-194`（09-03 写的 equip 版），**缺 3 个强制点**：

| 强制点 | equip 版 | material 版 |
|---|---|---|
| `perform public.bot_buy_guard();`（身份/封禁/账号年龄/每日30次） | ✅ 有 | ❌ **无** |
| `if v_listing.seller_id::text = auth.uid()::text then return 'self'` | ✅ 有 | ❌ **无** |
| `grant execute ... to authenticated`（不是 anon） | ✅ | ❌ **第 214 行 `to anon, authenticated`** |

`migrate_security_hardening.sql` 里**根本没有 `bot_buy_material`**（已核对，出现 0 次）——因为它 09-10 才出生，而 hardening 是 09-03 的。所以 hardening 末尾"统一收 anon 权限"那一刀**从来没覆盖过它**。

### 攻击 / 受害
- **攻击者**：任何登录玩家（甚至未登录，因为 grant 给了 anon）。不需要改前端，DevTools 里一行就行：
  ```js
  supabase.rpc('bot_buy_material', { p_listing_id: '自己挂的单' })
  ```
- **路径**：`list_material` 扣掉自己 `good_qty` 个甲材料（`migrate_material_listings.sql:73`）→ 立刻 `bot_buy_material` 自己的单 → 系统凭空发 `material_type` 的乙材料，标记 sold。
- **因为缺 self 校验**，不用找人接手，**自己挂自己买**，可以无限循环。
- **受害者**：整个材料经济。稀缺材料（涅槃丹/强化丹/锁定石/凝魂晶石）可以用垃圾材料无限兑换。`dailyCap=30` 这条唯一的刹车完全不在。

### 修复（最小改动）
在 `bot_buy_material` 的 `begin` 后补三行，和 equip 版对齐：

```sql
begin
  perform public.bot_buy_guard();

  select * into v_listing from public.material_listings
  where id = p_listing_id and status = 'active' for update;
  if not found then return 'notfound'; end if;

  if v_listing.seller_id::text = auth.uid()::text then return 'self'; end if;
```

末尾加：
```sql
insert into public.security_bot_buy_log (user_id, listing_id, kind)
values (auth.uid(), p_listing_id, 'material');
revoke execute on function public.bot_buy_material(uuid) from anon;
```
`kind` 列的 check 约束目前是 `check (kind in ('pet','equip'))`，加 `'material'` 要同步改（`migrate_security_hardening.sql:101`）。

---

## 🔴 P0-2　"幂等、可重复跑"是假的 —— 重放旧脚本会静默抹掉安全加固

### 文档说应该是这样
`PROJECT_CONSTITUTION.md:18` 和 `skill: forge-of-souls-dev` 都写：**`supabase/migrate_*.sql`（幂等，可重复跑）**。

### 代码实际是这样
同一个函数被**多个文件重复 `create or replace`**，谁后跑谁生效：

| 函数 | 定义在几个文件 | 最新一份是否带守卫 |
|---|---|---|
| `bot_buy_equip` | `migrate_bot_buy.sql` / `migrate_security_hardening.sql` / **`migrate_trade_records_ref.sql`（09-10）** | ❌ 不带 |
| `bot_buy_pet` | 同上 3 个 | ❌ 不带 |
| `buy_equip` / `buy_pet` | `migrate_material_trade.sql` / **`migrate_trade_records_ref.sql`（09-10）** | — |
| `buy_egg` | `migrate_egg_species_trade.sql` / **`migrate_trade_records_ref.sql`（09-10）** | — |
| `battle_settle` | `migrate_idle_sessions.sql` / `migrate_battle_pending_script.sql` | — |

具体退化（已逐行比对 `migrate_security_hardening.sql:149-194` vs `migrate_trade_records_ref.sql:184-219`）：

| | hardening 版 | **trade_records_ref 版（09-10，更晚）** |
|---|---|---|
| `perform bot_buy_guard()` | ✅ | ❌ 缺 |
| `seller_id = auth.uid()` self 校验 | ✅ | ❌ 缺 |
| 写 `security_bot_buy_log` | ✅ | ❌ 缺 |
| `delete from equip_listings`（防重复收购） | ✅ 注释明写"防孤儿行被重复购买刷材料" | ❌ 只删 `equip_items`，**挂单行留在 `active`** |
| 前置 `drop function if exists` | ✅ | ❌ 无 |
| grant | — | ❌ `to anon, authenticated`（第 219/257 行） |

**后果**：如果线上最后执行的是 `trade_records_ref`（它 09-10 才加，很可能就是），那么 equip/pet 也是**无守卫**版本，且因为挂单行没删，同一条单可以**被反复收购**——`equip_items` 第二次删 0 行，但给卖家加材料的语句照样执行 = **纯凭空产材料，零成本**。

同一模式还波及 RLS：
- `migrate_material_trade.sql` 建了 `trade_records_open_all`（**所有人可读全部交易记录**），`migrate_security_hardening.sql` 删掉它换成 `trade_records_select_own`。重放 material_trade → 隐私泄露回退。
- `migrate_trade_records_ref.sql:180` 又 `grant execute ... to anon` 给 `buy_egg`，与 hardening 第 17-18 行"anon 全部收回"直接冲突。

**根因**：安全加固是"后加的收口脚本"，但仓库里**没有任何机制阻止更早的脚本被重放**。`PROJECT_CONSTITUTION.md` #2 自己承认了这个洞："迁移应用记录：谁按什么顺序应用了哪些 SQL、当前库停在哪个版本，无记录"。

### 先查线上到底是哪个版本
跑这条，一次看明白：

```sql
select p.proname,
       pg_get_functiondef(p.oid) ilike '%bot_buy_guard%'      as has_guard,
       pg_get_functiondef(p.oid) ilike '%security_bot_buy_log%' as has_log,
       p.proacl::text as acl
from pg_proc p
join pg_namespace n on n.oid = p.pronamespace
where n.nspname = 'public' and p.proname like 'bot\_buy%'
order by p.proname;
```

`has_guard = false` 或 acl 里出现 `anon` → 就是洞。

### 修复（按懒到勤排序）
1. **立刻**：新建 `supabase/migrate_security_reapply.sql`，把三个 `bot_buy_*` 一起重定义成带守卫版 + `revoke ... from anon`。一个文件，别回头改旧的。
2. **立刻**：在 5 个会被覆盖的旧脚本**文件头加一行注释**：`-- ⚠️ 已被 migrate_security_reapply.sql 取代，重放会抹掉安全守卫，禁止执行`。
3. **等有第二个人维护时再做**：给迁移编序号（`001_`…）或建 `schema_migrations` 表。现在一个人做，注释足够。

---

## 🟠 P1-3　`applyCraft` 的重试逻辑会吞掉「装备词缀同步失败」　✅ 已修

`docs/js/equipment/equipment_craft.js:108-115`：

```js
let syncErr = (sp && sp.error) || (sp && sp.data === false ? new Error(...) : null) || (up && up.error);
if (syncErr && sp && sp.data === false && !sp.error) {
  await Materials.flushMaterials();
  const again = await Materials.cloudSpend(stoneName, stoneAmount);
  if (again && again.data !== false && !again.error) syncErr = null;   // ← 无条件清空
}
```

`sp` = 石头扣减，`up` = `Items.updateCloudItem`（装备词缀落库）。
当**两者同时失败**（石头余额不足 + 词缀更新失败），重试只重试 `sp`；`sp` 成功就把 `syncErr` 整个置 `null`，**`up.error` 被丢掉**。

结果：函数返回 `{ok:true}`，界面显示打造成功，石头扣了，本地词缀是新的，**云端词缀还是旧的**。玩家刷新 → 词缀回退。如果刚 roll 到 T1 词缀，就是白花石头 + 丢词缀。

**修复**：只清空"石头那一路"的错误——

```js
if (again && again.data !== false && !again.error) syncErr = (up && up.error) || null;
```

---

## 🟠 P1-4　附加锁定石的扣减结果没人看　✅ 已修

同文件 `106-108`：

```js
if (extraStone) cloudOps.push(Materials.cloudSpend(extraStone.name, extraStone.amount));
const [sp, up] = await Promise.all(cloudOps);   // 第三个元素被丢了
```

锁定时每条已锁词缀额外扣 1 颗锁定石。云端扣减失败（余额被别的标签页花掉）→ 本地扣了、云端没扣 → **下次刷新锁定石数量回涨**（客户端与云端账目漂移）。

**修复**：`const [sp, up, spExtra] = ...`，把 `spExtra` 一起并进 `syncErr` 判断。

---

## 🟡 P2-5　文档与实现不符（意图轴清单）

| 文档承诺 | 代码实际 | 位置 |
|---|---|---|
| 宪法 B2：AI 不标 "AI/流浪商人"，全部以昵称呈现 | ~~三个 `bot_buy_*` RPC 硬编码 `'流浪商人'` 当 `player_id`，玩家在交易记录里能直接看到~~ ✅ **已修（2026-09-11）**：三个函数改为接收 `p_buyer_name`，客户端在收购那一刻从当次会话的 persona 取；历史 2 行已回填 | 见「修复记录」#8 |
| 宪法一：`supabase/` = **17 个 SQL** | 实际 **25 个** | `supabase/*.sql` |
| 宪法/skill：迁移脚本"幂等，可重复跑" | 假的，见 P0-2 | — |
| 宪法 D5：不做收款码 | ✅ 一致（`config.js:1522` `shop.enabled=false`） | — |
| 宪法 #6：通天塔服务端权威 | ⏸️ 暂停中（已知），前置缺陷 `gen_server_config.js` 缺 equipment 段 + 两份战核漂移仍在 | — |
| 宪法维护规则 5：改 config 必同步百科/dev/归属账本 | 无法自动校验，只能靠测试 | — |

**未接的功能**：~~`trial/`、`tower/` 对 `Quest.reportType` 的调用是 0——爬塔不推进任何任务进度。~~

> 🔴 **本条作废（2026-09-11 当天核实）**：写审计时照抄了过期记忆，没核对代码。实际情况是 **`trial-engine.js:143` 和 `tower-engine.js:279` 早就接了**，而且 `quest.js:492` 的 `mode:'max'`（"最高层数只认历史最大值，重复通关不累加、失败不回退"）也已经实现，config 里配了 6 条分档成就（`at1~at3` / `tw1~tw3`）+ 6 条周常（`wk1~wk6`）。**② 这条不是待办，是伪命题。**
>
> 教训：这份审计里凡是"没接 / 没做"的结论，都必须现场核代码，不能引用记忆 —— 记忆会过期，尤其是当天有并行会话在改同一片区域。

---

## 🟡 P3-6　存量技术债（不是 bug，但违反了项目自己定的规则）

1. **CSS `!important` 共 131 处**：`game.css` 88、`ui-fixes.css` 41、`market-cascade.css` 1、`design-tokens.css` 1。
   `ui-fixes.css` 整个文件就是一个"补丁层"，跟 skill 里第 5 条铁律「禁补丁式修复」直接对着干。
   它文件头（第 15 行）自己写明"已删 transform/transition !important 强制覆盖"，但第 52/55 行还留着两条（聊天抽屉的 `translateX`）——那条不是拖拽窗口，功能上没坏。**建议：存量不动，新增别再写。**
2. **负 margin 16 处**，同类问题。
3. **空 `catch` 只有 3 处**，数量很少，没问题：
   - `ui-fixes.js:272`、`main.js:615`（localStorage 写入兜底）、`equipment_craft.js:305`（`unlockSide` 的界面回调）。
   第 3 处违反了"表现层 catch 不能静默"，但影响仅限"点了解锁按钮界面没刷新"，P3。

---

## ✅ 查过、确认没问题的（省得下次重复查）

| 项 | 结论 |
|---|---|
| **聊天是否 XSS** | 干净。`ui-console.js:41` 的 `escHtml` 在 `renderChatMessage` 里对 `name`/`text` 都转义了，历史加载（`:67`）和 Realtime 推送（`:84`）走同一个入口 |
| **魂铸词缀会不会被打造洗掉** | 干净。`soulAffix` 与 `affixes` 是两个独立字段，`equipment_craft.js` 的四种打造只动 `affixes`；`equipment.js:366` 单独读 `soulAffix` 结算。符合宪法 C3 |
| **锁定只保一次打造** | 正确。「重铸/剥离/神圣/增缀」收口在 `expireLock`（`:28`），且在 `Items.updateCloudItem`（`:104`）**之前**调用（`:78`）；`lockSide`/`unlockSide` 不在 `LOCK_CONSUMING` 里，锁不会被自己清掉 |
| **云同步失败回滚是否完整** | 正确。`reforge`/`strip`/`reroll`/`augment` 四个都还原 `affixes` + `rarity` + 锁定 + 材料（含附加石头），漏项检查过没有 |
| **`savePet` 是否是更新** | 见 `fos-cloud` skill；本次未发现新反例 |
| **市场前端守卫** | `market_bot.js:40` 的 `GUARD_PAUSE_MS` 逻辑正确——但**它挡不住直接调 RPC**，别把它当安全措施 |

---

## 修复记录（2026-09-11）

| # | 做了什么 | 状态 |
|---|---|---|
| 1 | 线上实测确认 P0-1 是真的（anon 直调 `bot_buy_material` → 200） | ✅ 已做 |
| 2 | 新建 `supabase/migrate_security_reapply.sql`：三个 `bot_buy_*` 全部重定义为带守卫版 + `bot_buy_material` 补 self 校验 + `security_bot_buy_log.kind` 约束加 `'material'` + 末尾收回 anon | ✅ **已执行到线上**（2026-09-11） |
| 3 | 三个旧脚本加「🔴 禁止重放」头注释：`migrate_bot_buy.sql` / `migrate_trade_records_ref.sql` / `migrate_material_listings.sql` | ✅ 已做 |
| 4 | 修 P1-3：重试只清「主石头」那一路的错，不再吞 `up.error` | ✅ 已做 |
| 5 | 修 P1-4：解构第三个返回值 `spExtra`，附加锁定石扣减失败纳入回滚 | ✅ 已做 |
| 6 | 更新 `PROJECT_CONSTITUTION.md`：SQL 数量 17→26、测试 47→72、新增维护规则 6（迁移不许重放）+ 7（存量红清单）、上线前清单加 #7 | ✅ 已做 |
| 7 | 升 `游戏.html` 里 `equipment_craft.js` 版本号 `20260911gear3` → `gear4` | ✅ 已做 |
| 8 | **P2-① 假买家不再叫「流浪商人」**：新建 `supabase/migrate_bot_buy_persona.sql`（三个 `bot_buy_*` 加 `p_buyer_name`，先 drop 单参版防重载，历史 2 行回填）；`supabase.js`/`market.js` 透传 persona 昵称；`market_bot.js` 公开 `randomPersona`；`?v= persona1` | ✅ 已上线并复验 |
| 9 | **P2-② 作废**：那条"爬塔不推进任务进度"是照抄过期记忆写的，实际 `trial/trial-engine.js:143`、`tower/tower-engine.js:279` 早就接了，`quest.js:492` 的 `mode:'max'` 也已实现 | ✅ 已纠正 |

**验证**：`node --check` 通过（26310 字节 / 383 行）；打造相关 5 个守值测试全绿；全量 **66/72**，与改动前基线完全一致（6 个红均为存量，清单见 `PROJECT_CONSTITUTION.md` 维护规则 7）。

### 执行前查到的事实（比审计时的推测更准）

用 `pg_proc` 查线上函数定义：

| 函数 | `has_guard` | ACL | 结论 |
|---|---|---|---|
| `bot_buy_equip` | ✅ | `postgres, authenticated, service_role` | **完好**，`trade_records_ref` 那版没生效 |
| `bot_buy_pet` | ✅ | 同上 | **完好** |
| `bot_buy_guard` | — | 只有 `postgres, service_role` | 内部专用，收得对 |
| `bot_buy_material` | ❌ | `PUBLIC, anon, authenticated, postgres, service_role` | **无守卫 + 对全世界开放** |

→ 修正审计时的判断：**P0-2 的"守卫可能已丢失"在 equip/pet 上没有发生**，实际受损的只有 `bot_buy_material`。P0-2 作为"未来会踩的坑"依然成立（已用注释 + 宪法规则 6 挡住）。

另：`security_bot_buy_log_kind_check` 线上确实是 `('pet','equip')`，已扩为 `('pet','equip','material')`。

### 执行后复验（2026-09-11）

**① anon 直调三个 RPC** —— 全部堵死：

```
bot_buy_equip    => 401 {"code":"42501","message":"permission denied for function bot_buy_equip"}
bot_buy_pet      => 401 {"code":"42501","message":"permission denied for function bot_buy_pet"}
bot_buy_material => 401 {"code":"42501","message":"permission denied for function bot_buy_material"}
```

（修复前 `bot_buy_material` 是 `200 "notfound"`。）

**② 函数定义 + ACL** —— 三个 `bot_buy_*` 现在都是 `has_guard=true` / `has_log=true`，ACL 一律 `{postgres, authenticated, service_role}`，anon 与 PUBLIC 都已移除。

**③ 权限面回归** —— 因为脚本末尾有 `revoke execute on all functions in schema public from public` 这种批量操作，逐个查了 29 个业务函数（`add_material` / `list_material` / `buy_*` / `get_my_wallet` / `grant_gems` / `admin_*` / `session_*` / `battle_settle` / `spend_material` …）：

**全部 `authenticated=true, anon=false`**，无误伤。

### 对玩家的行为变化（预期内）

`bot_buy_material` 现在和装备/宠物同规则：登录后才可用、账号满 10 分钟、每账号每天 30 次、被封禁不可用、不能买自己的单。前端 `market_bot.js` 的 `guardKeyOf` 是按 `ERR_BOT_BUY_*` 字符串通用匹配的，材料路径自动覆盖，不需要改前端。

### 本次没做的

- P2（文档与实现不符）只记录未修；P3（CSS `!important` 131 处 / 负 margin 16 处）存量不动。

---

## ⚠️ 覆盖范围与局限（重要，别把这份报告当"全量审计结论"）

**这份报告是按「爆炸半径」做的针对性审计，不是全量审计。** 154 个 js 文件里完整读过的不到 20 个；约 1.5MB 代码里逐行看过的大概 10~15%（但集中在了最危险的那几段）。

### 审过的

| 范围 | 深度 |
|---|---|
| `supabase/migrate_*.sql`（25 个） | 全量扫函数重定义与授权，重点逐行读了 4 个 |
| 线上 RPC 权限 / ACL / 函数定义 | 实测（anon 探测 + `pg_proc` + `pg_policies`） |
| **RLS 策略（23 张表全部）** | 逐条看 `qual` / `with_check` |
| `docs/js/equipment/equipment_craft.js` | 全文 400 行 |
| `docs/js/ui/ui-console.js` 聊天渲染 | 转义路径 |
| `market.js` / `market_bot.js` / `supabase.js` | 相关段 |
| `docs/js/core/quest.js` | `reportType` 单函数 |

### 完全没碰的（按爆炸半径排序，建议作为下一批）

| 优先 | 区域 | 为什么危险 | 规模 |
|---|---|---|---|
| ~~🔴 1~~ | ~~`supabase/functions/battle-settle/` + `_shared/` + `resource-trial/`~~ | ✅ **已完成** —— 见下方「第 1 批：服务端结算链」 | — |
| 🔴 2 | `docs/js/core/idle-bridge.js` | 挂机回放核心，唯一驱动全部收益循环的模块。本次只 grep 过时间戳 | 1098 行 |
| ~~🟠 3~~ | ~~`docs/js/pet/pet_merge.js` + `pet_evolve.js` + `pet.js`~~ | ✅ **已完成** —— 见下方「第 2 批：宠物资产链」（`pet.js` 只按需查了资产相关函数，未逐行通读） | — |
| 🟠 4 | `docs/js/core/` 其余 | `items.js` / `materials.js` / `drop.js` / `battle.js` / `auth-session.js` / `server-session.js` … | 19 文件中 15 个 |
| 🟡 5 | `docs/js/ui/` 其余 | 表现层风险低，但 `ui-market*.js` 4 个涉及挂单与收款物校验 | 27 文件中 26 个 |
| 🟡 6 | `docs/js/tower/` + `trial/` | 门票扣除与奖励发放 | 9 + 7 个文件 |
| ⚪ 7 | `docs/css/`、`docs/游戏.html` | 只统计过 `!important` 数量 | — |

### 本轮补充查过的（RLS 横切面，结果干净）

23 张表**全部开启 RLS**，其中：

- `pets` / `materials` / `equip_items` 各 4 条策略，`qual` 与 `with_check` 一律 `auth.uid() = user_id` → **资产表锁得住**，第三人直连 REST 读不到别人的宠物/材料/装备。✅
  （注意：`pg_policies.roles` 显示 `{public}`，看着像开放，实际被 `qual` 条件锁死 —— 别只看会话角色就下结论。）
- `profiles_select_all`：`{anon, authenticated}` + `qual = true` → **不登录就能读全部玩家的 profile 整行**。
  查了列：`id / nickname / avatar_url / invite_code / created_at / last_seen_at / banned / ban_reason` —— **没有邮箱** ✅；`invite_code` 全仓**零引用**（死列）→ 泄露无害；唯一副作用是暴露 `banned` + `ban_reason`（谁被封了、为什么），属观感问题不是漏洞。
- `chat_messages.chat_select_all`：`{public}` + `qual = true` → 不登录可读全部聊天。聊天本来就公开，可接受。
- `redeem_codes` / `security_bot_buy_log` / `security_rate_limits`：RLS 开启且**零策略** → 只能走安全定义者函数，REST 直接读写被拒 ✅

---
---

# 第 1 批：服务端结算链（2026-09-11 当日第二批）

**审了什么**：`supabase/functions/battle-settle/index.ts`(245) + `_shared/settle-core.mjs`(211) + `_shared/trial-core.mjs`(80) + `resource-trial/index.ts`(144) + `migrate_idle_sessions.sql` / `migrate_battle_pending_script.sql` 两个 RPC + `idle-bridge.js` 的消费端。

**方法**：不看注释看实际存进去的东西 —— 直接查线上 `battle_logs` / `idle_sessions`，用真实数据反推代码行不行。

---

## 🔴 P0-1　`jsonb` 参数被 `JSON.stringify` → 幂等失效 → **所有玩家挂机产出被放大 1.4~2.1 倍**　✅ 已修并已上线

### 代码事实

`battle-settle/index.ts:202 / 208`：

```js
p_detail: JSON.stringify(plan.logDetail),
p_pending_script: JSON.stringify({ id: script.id, until: untilIso, script })
```

RPC 参数声明的是 `jsonb`（`migrate_battle_pending_script.sql:18,23`）。传 JSON 字符串过去，PostgREST 会把它当成**一个 JSON 字符串值**，而不是数组/对象 —— 于是落库是 `jsonb string` 而非 `jsonb array/object`。

线上实测（修复前）：

| 列 | 类型 | 行数 |
|---|---|---|
| `battle_logs.detail` | `string` | 7024 / 7024 |
| `idle_sessions.pending_script` | `string` | 64 / 64 |

### 为什么这是 P0

EF 读回 `session.pending_script` 得到的是**字符串**，于是这一行（`:136`）：

```js
if (pending && pending.script && pending.until && nowMs < ...) { /* 幂等：原样再发，不记账 */ }
```

`"..."` 字符串没有 `.script` 属性 → 恒为 `undefined` → **幂等分支永不命中** → 每次 `settle` 都往下走：重新模拟一个 **30 秒剧本窗**、推进游标、**再入账一次**。

### 线上铁证

如果幂等生效，同一个 30 秒窗口内的重复 `settle` **不会写 `battle_logs`**。实际：

| session | settle 次数(=日志行数) | 真实经过 | 授予的模拟时长 |
|---|---|---|---|
| `b9eb0405…` | 4 | 57 秒 | **120 秒 → 2.10×** |
| `e363ce76…` | 7 | 107 秒 | **≈2.0×** |
| `4de44b0a…` | 9 | 144 秒 | **≈1.9×** |
| `8f2f6867…` | 4 | 67 秒 | **≈1.8×** |
| `692470d9…` | 24 | 456 秒 | **≈1.6×** |

**每次 settle 都写了日志 = 每次都在记账。** 结论：不是"可以刷"，而是**全体玩家被动地多拿了 1.4~2.1 倍产出**。而 `add_material` 的限流（60 秒 1000 个）远高于这个量级，挡不住。

### 副作用 2：审计日志形同虚设

`battle_logs.detail` 存成字符串 → 任何按明细聚合的查询都直接报错（本次审计第一步就踩到了 `cannot extract elements from a scalar`）。

### 修复（两层，都已落地）

1. **`supabase/migrate_settle_jsonb_fix.sql`（已执行到线上）**
   - 清洗历史脏数据：`detail = (detail #>> '{}')::jsonb`（带 `left(btrim(...),1) in ('[','{')` 守卫，防脏值炸整条 UPDATE）
   - **RPC 入口做类型归一化**：`if jsonb_typeof(p_detail) = 'string' then p_detail := (p_detail #>> '{}')::jsonb; end if;`
     —— 这一层让**洞立刻关上，不必等 EF 重新部署**；且旧数据在下次写入时自愈。
2. **修 `battle-settle/index.ts` 根因**：去掉两处 `JSON.stringify`，直接传数组/对象。
   ⚠️ **需要在 Supabase 部署才生效**（见文末待办）。

### 复验

```
battle_detail_type      = array      ✅
pending_script_type     = object     ✅
battle_settle_overloads = 1          ✅
pending_script ? 'script' = true，jsonb_typeof(script) = object，events 长度 11/9/13  ✅
```

修完后明细查询第一次能跑通，直接给出下面的 P0-2。

---

## 🔴 P0-2　挂机**永远不掉装备、不掉蛋**（配置说 1.3% + 0.6%）　❌ 未修，**需要产品决策**

### 代码事实

服务端掉落唯一实现 `_shared/settle-core.mjs:101-120`：

```js
const AREA_MATERIALS = { 'corrupted-forest': '枯荣种荚', ... };   // 只有材料
function rewardForFight(fight, areaId, seed, index) {
  if (!fight || !fight.win) return { type: 'none' };
  const material = AREA_MATERIALS[areaId];
  if (!material) return { type: 'none' };
  if (fight.isBoss) return { type: 'material', material, qty: 5, boss: true };
  const roll = (hashSeed(seed, areaId, index) % 10000) / 10000;
  return roll < 0.18 ? { type: 'material', material, qty: 1 } : { type: 'none' };
}
```

全仓搜 `supabase/functions/**` 里 `type: 'equipment'` / `type: 'egg'` → **零命中**。
`battle-settle/index.ts:242-253` 发奖分支只处理 `reward.type === 'material'`。

前端配置却写着（`config.js`）：`pool: { none: 900, material: 85, equipment: 13, egg: 6 }` → 装备 ≈1.3%、蛋 ≈0.6%。

### 线上铁证

修好 `detail` 的类型后，`battle_logs` 全部 70,742 场战斗的奖励分布：

| `reward.type` | 场次 |
|---|---|
| `none` | 58,074 |
| `material` | **12,668**（17.9% ≈ 代码里的 0.18 ✅） |
| **`equipment`** | **0** |
| **`egg`** | **0** |

**一次都没有过。** 而 `equip_items` 表里有 96 件、`pet_egg` 有 520 颗 —— 那些来自打造 / 任务 / 商店，**不是挂机掉的**。

### 为什么没人发现

- 客户端 `idle-bridge.js:1026` 优先消费服务端下发的 `reward`；只有"旧版战报没有 reward 字段"才回退本地 `Drop.rollReward`（`:1038`）。而服务端**总会**塞 `reward` 字段 → **回退分支是死代码**。
- `Drop.rollReward` 目前只剩 4 个调用点：`main.js:109`（旧单场路径）、`idle-bridge` 的两个死分支、`market_bot.js:200` 与 `ui-dev.js:646` 都是 `{dry:true}` 不发奖。
- **守值测试守错了对象**：`vtest_drop_tier.js` 验的是 `materialWeightsByTier` / `areaEvolutionTiers` / `collect_loop` 任务，**完全没有校验掉落池分布**。所以前端配置漂到哪都没人拦。

### 这是"未完成"不是"设计"

`battle-settle/index.ts:240` 自己的注释写着：「当前先落材料；装备/宠物蛋沿用同一 detail 结构接入对应表」。也就是**服务端化做了一半**，装备与蛋的产出在迁移中被静默砍掉。

### 需要你拍板（不能自己定）

- **甲**：按原设计补上——服务端 `rewardForFight` 加 `equipment` / `egg` 分支，并接 `equip_items` / `pet_egg` 入表。工作量：改 `settle-core.mjs` + EF 发奖分支 + 部署。
- **乙**：接受"挂机只掉材料"，**把 `Config.drop.pool` 的 `equipment: 13, egg: 6` 删掉**并同步 `drop.js`/文档，让配置和现实一致。（但要先想清楚：装备改为纯打造产出，是设计意图吗？）
- **丙**：先只删配置里的假承诺 + 在代码里留 TODO，产出路径等毕业内容一起做。

无论选哪个，**都该补一条守值测试**，断言"服务端 reward 表覆盖了 config 声称的所有类型"。

---

## 🟠 P1-1　发奖与记账**不在同一事务** → 失败即丢奖　❌ 未修

`battle-settle/index.ts` 的执行顺序：

```
① battle_settle RPC   → 推进游标 + 写 battle_logs（已记账）
② pets.update         → 写回经验/等级/血量
③ add_material 循环   → 发材料
```

- **② 失败** → 返回 500，但 **① 已推进游标** → 那段时间的经验**永久丢失**（下次 settle 从新游标算）。
- **③ 失败**（撞 `add_material` 限流、网络抖动）→ 返回 500，但 ①② 已完成、游标已推进 → **该批材料永久丢失**；且是**部分发放**：材料 A 成功、材料 B 失败，A 不会回退，B 永远拿不到。

`resource-trial/index.ts:135-146` 是同一模式（更严重：票已扣 + 占位行 `run_id` 唯一约束），玩家重试得重新点一次、再扣一张票。

**修法**：把发奖搬进 RPC（同一个事务里 `insert into materials ... on conflict do update`），或者引入"待发奖队列 + 幂等批次号重投"。这是结构性改动，**建议单独一批做**，不要顺手改。

---

## 🟠 P1-2　`battle_settle` 有 3 个重载　✅ 已修

线上原来并存：

```
battle_settle(uuid,integer,integer,jsonb,timestamptz,timestamptz,integer,timestamptz,jsonb)  ← EF 命中的
battle_settle(uuid,integer,integer,jsonb,timestamptz,timestamptz,integer)                    ← 残留
battle_settle(uuid,integer,integer,jsonb,timestamptz,timestamptz)                            ← 残留
```

后两个不含 `p_cursor` / `p_pending_script`，是改版前残留。调用方参数少传一个就会**静默命中旧版本**、走没有游标幂等的逻辑 —— 和 `bot_buy_material` 那次是同一类坑。已在 `migrate_settle_jsonb_fix.sql` 里 `drop` 干净，`battle_settle` 现在只剩 1 个签名。

---

## 🟡 P2　`logDetail.slice(-100)` 会让**发奖**随截断丢失　❌ 未修（当前未触发）

`settle-core.mjs:184`：`logDetail: gapDetail.concat(scriptDetail).slice(-100)`，而发奖是从 `plan.logDetail` 归集的（`index.ts:242`）。补账窗最长 120 秒 + 剧本窗 30 秒 = 150 秒模拟，场次超过 100 就会**截掉前面的掉落**。

线上实测 `max(fights) = 65`、`avg = 10`、`fights >= 100` 的行 **0 条** → 目前没踩到。但这是"上限靠运气"的设计：`gapSec` 一旦被拉满又赶上低等级秒杀，就会静默吞掉落。**修法**：先归集 `rewardTotals` 再截断。

---

## ✅ 查过、确认没问题的

| 项 | 结论 |
|---|---|
| **客户端会不会二次发奖** | 不会。`idle-bridge` 用 `Materials.gainLocal()`（`materials.js:108` 纯本地、**不进 pending 队列、不上报**）消费服务端下发的 `reward`；云端由服务端 `add_material` 已发。两条路各管一边，不重叠 ✅ |
| **`petId` 能不能指向别人的宠** | 不能。EF 读 `pets` 时**没有** `.eq('user_id', uid)`，但 RLS `pets_select_own` 是 `auth.uid() = user_id` → 越权查询直接读成 null → `PET_NOT_FOUND`。**这里是 RLS 在兜底，不是查询条件** —— 以后改 RLS 要连带想一下这条路径 ✅ |
| **`areaId` 能不能伪造到未解锁图** | 服务端 `AREA_MATERIALS` 只影响材料品种，不影响场次/经验；怪数值按 `enemyList` + 宠物等级算。伪造 `areaId` 拿不到额外好处 ⚠️ 但**没有"图是否已解锁"的校验**，属设计缺口（读不到高级材料就算，收益不高于正常） |
| **`pending_script` 到期前的重复请求** | 修复后逻辑正确：窗口内直接原样返回缓存、**不碰 DB** ✅ |
| **服务器时间权威** | `nowMs = Date.now()` 取的是 **Deno 侧时间**，客户端传的时间只用于 `p_now` 之外无影响；`gapSec` 由服务器算并 clamp 到 ≤120 ✅ |
| **`resource-trial` 的幂等** | 设计正确：`runId` 唯一约束挡重放（`23505` → `DUPLICATE_RUN`），先插占位行再扣票、扣票失败删占位行，宠归属 + 等级校验齐全 ✅ |
| **管理员配置隔离** | `game_config_overrides` 只对硬编码管理员邮箱生效；该表 RLS 只读、无写策略 → 只有 service_role 能改 ✅ |
| **装备引用 UUID 白名单** | EF 与 `resource-trial` 都用 `UUID_RE` 过滤后再 `.in('id', ...)`，防脏引用炸查询 ✅ |
| **`market_bot` 挂机** | 走 `Drop.rollReward(..., {dry:true})`，只产出展示、不入库 ✅ |

---

## ✅ 第 1 批遗留：Edge Function 已部署

```powershell
$env:SUPABASE_ACCESS_TOKEN='<PAT>'   # 会话内联，不落盘
npx supabase functions deploy battle-settle --project-ref asklogeayzlqpeejuvjj
```

结果：`battle-settle` **VERSION 14**，`2026-09-11 10:41:01 UTC`，STATUS ACTIVE，上传 `index.ts` + 4 个 `_shared/*.mjs`。
冒烟测试（无 token）：`HTTP 401 UNAUTHORIZED_NO_AUTH_HEADER` —— 网关拦截、函数存活 ✅

> 🔴 **部署时踩的坑**：项目 ref 是 **20 位** `asklogeayzlqpeejuvjj`，我第一次照记忆写成 18 位的 `asklogeayzlqejuvjj`（少 `pee`），CLI 直接报 `Invalid project ref format`。
> **以后一律从 `docs/js/core/supabase.js:15` 的 `SUPABASE_URL` 抄，不要凭记忆写。** 记忆/文档里的错误 ref 已全部修正。

---

## 第 1 批修复记录

| # | 做了什么 | 状态 |
|---|---|---|
| 1 | 新建 `supabase/migrate_settle_jsonb_fix.sql`：清洗 7024 行 `detail` + 64 行 `pending_script` 脏数据；`battle_settle` RPC 加 jsonb 类型归一化；清掉 2 个重载 | ✅ 已上线并复验 |
| 2 | 修 `battle-settle/index.ts` 两处 `JSON.stringify`（根因） | ✅ 已改 + **已部署（VERSION 14）** |
| 3 | 用线上数据证明 P0-1（产出放大 1.4~2.1×）与 P0-2（装备/蛋 0 掉落） | ✅ 已取证 |

**验证**：`vtest_settle_core` / `vtest_settle_script` / `vtest_idle_bridge` / `vtest_server_sim` / `vtest_sim_sync` / `vtest_resource_trial` 全绿；`vtest_action_freeze` 是存量红（与基线一致）。

---
---

# 第 2 批：宠物资产链（合成 / 涅槃 / 进化 / 培育）

**审了什么**：`pet_merge.js`(537) + `pet_evolve.js`(210) + `migrate_god_pet.sql` 的两个服务端校验 RPC + 资产表（`materials` / `pets` / `equip_items` / `profiles`）的 RLS / 约束 / 触发器 / 权限。

---

## 🔴 P0-3　RLS 只做「行归属」，不做「列/值」控制 —— 玩家能直连 REST 改自己的资产　🟡 已部分修复

### 事实

宪法 E2 写的是「RLS + RPC 只防外人直连，防不了**真人玩家改前端后**合法提交」。实际情况比这更直接：**根本不用改前端**，一条 `PATCH` 就行。

线上核对（`pg_policies` + `pg_constraint` + `pg_trigger`）：

| 表 | RLS 允许本人 | 约束 | 触发器 | 后果 |
|---|---|---|---|---|
| `materials` | INSERT / UPDATE / DELETE | **无任何 CHECK** | **无** | `PATCH … {"quantity": 999999}` → **凭空造材料** |
| `pets` | INSERT / UPDATE / DELETE | growth 1~100、level **1~99**（注意不是 60） | 无 | 改满成长 / 改 traits / 改觉醒 → 造神宠卖钱 |
| `equip_items` | INSERT / UPDATE / DELETE | 无 | 无 | 伪造全 T1 金装 → 卖钱 |
| `pet_egg` | INSERT / UPDATE | 无 | 无 | 造蛋 |
| `quest_progress` | upsert | 无 | 无 | 直接改进度领奖 |
| `wallets` | **仅 SELECT** | gems ≥ 0 | — | ✅ 魔石安全（写只走 RPC） |
| `profiles` | INSERT / UPDATE | 无 | 无 | 见下 |

**为什么这是 P0 而不是"自欺欺人"**：这游戏有**跨玩家真实交易**（材料计价的市场）。凭空造材料 → 挂单 → **换走别人真材料**。攻击者获益、受害者是其他玩家。跨了信任边界。

`profiles` 那条尤其难看：`profiles_update_own` 的 `with_check` 是 `auth.uid() = id`，只保证"只能改自己那行"，**不限制改哪些列**。而 `bot_buy_guard()` 正是用 `select banned from profiles where id = auth.uid()` 做封禁拦截 →
`PATCH /rest/v1/profiles?id=eq.<自己>  {"banned": false}` → **封禁形同虚设，被封的号自己解封即可。**

### 已修（能安全收口、不打断功能的两张表）

`supabase/migrate_asset_write_hardening.sql`（已上线）：

1. **`materials`——客户端只读，写入一律走 RPC**
   依据：全仓核对，客户端对 `materials` 只有 2 处用法，**都是 `select`**（`core/materials.js:170`、`ui/ui-shop.js:190`）；所有写入走 `add_material` / `spend_material`（两者 `security definer`、owner=`postgres`，不受 RLS 与列级权限影响）。
   做法：删掉三张写策略 + `revoke insert, update, delete on public.materials from authenticated, anon`。
   （只删策略也能挡住 —— RLS 无策略 = 默认拒绝 —— 但表级授权留着会让以后审计误判，所以两层一起收。）

2. **`profiles`——列级权限收紧**
   `revoke update/insert on profiles from authenticated` + `grant update (id, nickname, avatar_url)`。
   依据：客户端对 `profiles` 只写 `{id, nickname}`。`admin_*` 全是 `security definer`，不受影响。

### 复核

```
materials   SELECT=true  INSERT=false  UPDATE=false  DELETE=false      ✅
profiles    nickname/avatar_url UPDATE=true   banned/ban_reason UPDATE=false   ✅
```

### ❌ 没修的（收掉会直接炸功能）

`pets` / `equip_items` / `pet_egg` / `quest_progress` / `*_listings` —— **客户端的整个架构就是"直接写自己那行"**（`updatePet`→`pets.update`、`Items.updateCloudItem`→`equip_items.update`、`consumeEgg`→`pet_egg.update`…）。收掉策略等于当场打断宠物/装备/孵蛋全部功能。

**这是架构性的，不是配置性的。** 正确解法就是宪法 E1「服务端权威」——把这些写操作也搬进 RPC。本批**没有**硬上，因为那不是审计顺手能改的（属于上线前必做 #1 的工程量）。

**但建议在宪法里升级它的措辞**：现在写的是"玩家开 DevTools 就能改游戏进程"，容易被读成"只能自欺欺人"；实际是**一条 curl 就能凭空造货并卖给其他玩家**。这决定了它的优先级。

---

## 🟠 P1-3　`nirvana` 两种失败模式**都会出事**（一只不可逆的操作排在了可逆操作前面）　✅ 已修

`pet_merge.js:251-268`：

```js
const delSub = await Supabase.deletePet(sub.cloudId);        // ① 删副宠（不可逆）
if (delSub.error) { console.warn(…); /* 副宠留下 */ }
else { removePet(sub.id); }
…
const { error: updErr } = await Supabase.updatePet(main.cloudId, patch);   // ② 更新主宠
if (updErr) console.warn('云端更新宠物失败：', updErr.message);             // ← 只 warn，然后返回 {ok:true}
```

| 失败点 | 结果 |
|---|---|
| ② 失败 | 副宠**已经永久删除**，主宠成长**没涨** → 玩家**白亏一只副宠**；函数仍返回 `{ok:true}`，界面显示涅槃成功 |
| ① 失败 | 副宠留在列表里，但主宠**照样涨了成长** → 玩家可以拿**同一只副宠再涅槃一次** → **白刷成长** |

也就是说：两种失败都不是"安全失败"。而 `deletePet` 是**不可逆**的 —— 先删它就等于把整单的原子性交给了后续操作。

### 修复（2026-09-11 已落地）

**核心原则：不可逆的操作必须排在最后。** `deletePet` 删了就回不来，所以它只能当最后一步；它前面的每一步失败都必须整单回滚。

```
① 快照主宠旧值（含 traits 深拷贝——implantNirvanaTraits 在原数组上 push）
② 更新主宠（可逆）
     └ 失败 → 还原主宠本地值 + 退回已扣道具 → 报错。副宠没动，零损失
③ 更新成功 → 删副宠（不可逆）
     └ 失败 → 把主宠写回旧值（云端 + 本地）+ 退回道具 → 报错。副宠仍在，可重试
```

两个容易漏的点，都补了：
- **道具必须一起退**。前面已经扣了涅槃丹 / 凝魂晶石 / 锁魂玉；只还原宠物不退道具，等于把"白亏一只副宠"换成"白亏材料"。
- **`traits` 要深拷贝**。`implantNirvanaTraits` 是在 `main.traits` 原数组上 `push` 的，浅拷贝存快照会被一起改掉，回滚就成了空操作。

**测试**（`vtest_god_pet.js` 第 7 节，8 条新断言）：注入 `updatePet` 失败 / `deletePet` 失败，分别断言"如实报错 + 道具退回 + 成长未变/已回滚 + 副宠仍在"。

**仍未解决的（更彻底的做法）**：这条链**没有事务**，和第 1 批的 P1-1（发奖与记账不在同一事务）是同一个病。上面的补丁把两种**明确的**失败模式都堵住了，但"第 ② 步成功、进程在 ③ 之前崩掉"这种仍会留下不一致。终局要 `nirvana_pet(main_id, sub_id, …)` 服务端事务 RPC —— 归入「服务端权威」那批。

---

## 🟠 P1-4　`evolve` 缺防连点闸门　✅ 已修

`pet_merge.js` 的 `nirvana` / `synthesize` / `cultivate` **三个都有** `inFlight` 闸门（注释里还写了血泪："连点两下两次调用都能通过校验 → 产出两只新宠、材料扣两份"），**只有 `pet_evolve.js` 的 `evolve` 没有**。

`ui-pet-evolve.js:224` 的 `disabled` 只按 `canEvolve`（能不能进化）置灰，**点击后不禁用**，挡不住并发。

后果：进化要串「扣素材 → 云端 `updatePet`」两次往返（约 1 秒）。期间本地 `pet` 尚未提交，连点两次都会通过校验 → **素材扣两份、成长只涨一次 → 玩家白亏一份进化素材**。

修法：照抄 `pet_merge.js` 的同一套模式（`inFlight` 集合 + `evolveInner` 拆分），`?v= evo9`。

---

## ✅ 第 2 批查过、确认没问题的

| 项 | 结论 |
|---|---|
| **`synthesize` 的失败处理** | 全项目最好的一处。建档**先行**且**确认成功才动素材**（`pet_merge.js:376-384`），失败 = 整单回滚（新宠移出、合成之石 + 道具全退）；删素材严格"先删云端成功才删本地"，失败会**如实上报复活名单**。注释里还留了事故复盘（旧代码建档失败只 warn，刷新即凭空消失）✅ |
| **`evolve` 的素材退还** | 完备。云端 `updatePet` 失败会把主素材 + 异名额外素材 + 进化道具**全部退回去**（`:188-193`）；云端成功后才提交本地状态 ✅ |
| **`evolve` 的 extra 同名合并扣款** | 正确。`total` 合并判定 + 单笔原子扣，避免"每笔都够、合起来不够"（`:79-80` 注释记录的已修真 bug）✅ |
| **`cultivate`** | 有 `inFlight`、有次数上限、有成长 100 封顶、道具云端原子扣、失败不涨 ✅ |
| **服务端校验 RPC 的定位** | `check_nirvana` 用**库里的 `is_god_pet` 做权威判断**（不信客户端传参），这条是对的；`check_god_synth` 则依赖客户端传参 ⚠️ 但两者都是 **fail-open**（`catch { /* RPC 不可用：放行 */ }` + `chk.data` 为 null 时不拦），属于"额外保险"而非门禁 —— 与它们的设计意图一致，但要知道它们**挡不住改前端的玩家** |
| **`pets.level` 约束是 1~99** | ⚠️ 与 `Config.pet.maxLevel = 60` 不一致。不影响功能（客户端自己卡 60），但意味着**直改 REST 可以把等级写到 99**。是上面 P0-3 的一个具体表现 |

---

## 第 2 批修复记录

| # | 做了什么 | 状态 |
|---|---|---|
| 1 | 新建 `supabase/migrate_asset_write_hardening.sql`：`materials` 收掉写策略 + 表级写授权（客户端只读，写全走 RPC）；`profiles` 列级收紧（只能改 `id/nickname/avatar_url`，不能自解封） | ✅ 已上线并复验 |
| 2 | `pet_evolve.js` 加 `inFlight` 防连点闸门（复用 `pet_merge.js` 模式）；`?v= evo9` | ✅ 已做 |
| 3 | 全仓核对「哪些表客户端直写、哪些能安全收口」 | ✅ 已做 |
| 4 | **P1-3 修 `nirvana` 失败路径**：改为「主宠先更新（可逆）→ 成功再删副宠（不可逆）」，两条失败路径都整单回滚（含退回涅槃丹/凝魂晶石/锁魂玉 + traits 深拷贝快照）；`?v= trait4` | ✅ 已做 |
| 5 | `vtest_god_pet.js` 新增第 7/8 节：**8 条失败路径断言**（注入 updatePet / deletePet 失败）+ 2 条进化并发断言 | ✅ 已做 |

**验证**：`vtest_evolution` / `vtest_evolution_true_fork` / `vtest_god_pet` / `vtest_stage2` / `vtest_awaken_flow` / `vtest_awaken_mutant` / `vtest_mutation` / `vtest_synth_growth` / `vtest_pet_balance` / `vtest_trait_soulcast` / `vtest_exp_pool` **11 个全绿**；`materials`/`profiles` 的新权限线上复核通过。

> ⚠️ **改 RLS / 权限后必须人工过一遍动作**：node 测试全用测试桩，**桩里没有 RLS**，所以"收权限收过头把功能打断"这类事故测试是抓不到的。本次收口前已逐表核对客户端用法（`materials` 只有 2 处 select、`profiles` 只写 id/nickname），但**建议你实机点一次打造/合成/交任务**确认无异常。

---
---

# 第 3 批（进行中）：`core/` 数据层

**审了什么**：`core/items.js`(96) 全文 + `core/supabase.js` 的 `updatePet` / `deletePet` / `consumeEgg` 段 + 全客户端 `.update(` 的写法普查。

---

## 🟡 P2-1　客户端更新无法区分「改成功」和「一行没改到」　✅ 已按 **opt-in** 方案修（不全局面铺）

### 事实

`supabase-js` 的 `.update()` 不带 `.select()` 时，PostgREST 不返回受影响行 → **"行不存在"和"更新成功"都表现为 `{data:null, error:null}`**。

全客户端普查（`.update(` 后有没有接 `.select(`）：

| 位置 | 写法 | 后果 |
|---|---|---|
| `core/supabase.js:197` | `pets.update(p).eq('id', cloudId)` | ❌ 无 |
| `core/supabase.js:204` | 同上（缺列重试分支） | ❌ 无 |
| `core/items.js:42` | `equip_items.update(patch).eq('id', eq.cloudId)` | ❌ 无 |
| `core/supabase.js:174` | `pets.update({equipment}).eq('id', ...)` | ❌ 无（有 try/catch 兜底，风险低） |
| `core/supabase.js:413` | `pet_egg.update({...}).eq('id', data.id).select().single()` | ✅ **有**（`.single()` 在 0 行时报 `PGRST116`） |

**调用方全部只解构 `{ error }`**（`main.js:78`、`pet_merge.js:293/307/572`、`pet_evolve.js:199`、`equipment/equipment.js:278`、`ui/ui-dev.js:532`、`ui/ui-pet-awaken.js:143`、`equipment_craft.js:104/314/384`、`salvage.js:35`… 无一使用 `.data`）—— 所以**加了检测不会破坏任何调用方**，这一点是安全的。

### 为什么它要紧

- `updateCloudItem` 0 行 → 打造报成功、石头扣了，**云端词缀没落库**，刷新即回退。
- `updatePet` 0 行 → **这正是 P1-3 的一条残余通道**：主宠行已被另一个标签页删掉时，`updErr` 是 `null`，我刚加的检查挡不住 → 于是流程继续往下走、**把副宠删了**。
  （另一条常见的"行消失"通道 —— 宠物在别的标签页被卖掉 —— 被 `Market.isListed` 挡着，所以实际触发需要**多标签**这种不常见操作。概率低，但性质是丢资产。）

### 全量调查（改任何代码之前做的）

**判据**：「0 行会导致丢资产吗？」

**`updatePet` 的 14 个调用点**：只有 3 个是"先扣东西、后写云端"的不可逆链条 —— 涅槃主宠 / 进化 / 培育。
其余 11 个（挂机经验写回、装备槽防抖同步、切出战宠、补 traits、dev 面板…）0 行只是"没同步上"，下次操作会重来，**不该打断**。

**`updateCloudItem` 的 6 个调用点**：只有 2 个该拦 —— 打造（石头扣了词缀没落库）/ 魂铸（晶石扣了 + 宠物删了、词缀没落库）。
鉴定、解锁锁定这类 0 行只影响体验，不值得为它加一次往返。

**结论：不该全局加。** 全局面铺会连带改 5 个"用假 `cloudId` 造数据"的测试（`vtest_craft_lock` / `vtest_trait_soulcast` / `vtest_affix` / `vtest_idle_bridge` / `vtest_equip_persist`），而那些失败**不是产品缺陷、是测试卫生** —— 让测试先真建档属于另一件事。

### 实际做法：opt-in

```js
updatePet(cloudId, patch, { verify: true })          // 只有不可逆链条传
updateCloudItem(eq, patch, { verify: true })
```

传了才加 `.select('id')`，0 行 → 返回明确错误（"这只宠物已不在你的存档里（可能在别的标签页被出售或删除），本次改动未生效"）。**其余 15 个调用点一个字不改、行为不变。**

开了 verify 的 6 处：`pet_merge.js` 涅槃主宠 / 涅槃回滚 / 培育、`pet_evolve.js` 进化、`equipment_craft.js` 打造 / 魂铸。

### 测试桩配套（纯增量）

`vstub.js` 原来 `update().eq()` **只返回 `{data:null,error:null}` 且不支持 `.select()`** → 真代码一调就 `res.select is not a function`。给它加了 `select`（不动原 `then`，所以其他测试零影响）：

```js
select:()=>({then:(res)=>{ …; res({data: r ? [r] : [], error:null}) }})
```

### 验证

- **全量回归 66/72，与基线完全一致** → **实际波及 0 个测试**（比"只改 6 处"的预期还干净）
- `vtest_god_pet.js` 新增 3 条断言：verify 更新不存在的行→报错 / 不开 verify→保持原行为 / verify 更新存在的行→正常成功

> ⭐ **这次的做法值得复用**：先出「全量消费方清单 + 该不该拦」的判定，再选**最小作用域**的改法 —— 而不是先把检测全局加上再看谁炸。用户 2026-09-11 明确要求「不要过度堵住，要结合所有代码之后才知道结果」。

---
---

# 第 4 批：战核副本同步（`docs/js/core/battle-sim.mjs` ⇄ `supabase/functions/_shared/battle-sim.mjs`）

**审了什么**：两份战核的逐行 diff + `build-sim-global.js` 生成链 + `vtest_sim_sync.js` 守护范围 + `window.BattleSim` 的全部运行时消费者。

---

## 🟠 P1-5　战核副本漂移未修，且**守护测试漏掉了漂移的那几个函数**　✅ 已修

### 事实

战核有 **3 份**：

| 文件 | 角色 | 谁在用 |
|---|---|---|
| `docs/js/core/battle-sim.mjs` | 前端源（ESM） | `vtest_*` 测试导入 |
| `docs/js/core/battle-sim.global.js` | 由上一份**生成**（`build-sim-global.js`） | ✅ **浏览器真的在用**：`game.html` 加载 → `tower/tower-preview.js:51` `const BS = window.BattleSim` |
| `supabase/functions/_shared/battle-sim.mjs` | **手工抄的**服务端副本 | `battle-settle` EF（挂机结算权威） |

`git diff` 两份 `.mjs` → **24 增 7 删**，差异全部集中在神级宠：

```
+ resolveLineId 里加神级宠查表（godPets.list[].line）
+ 新增整个 godDefOf(pet, config)
+ getBaseSpeed 里加神级宠速度优先
+ getStatCoeff 里加神级宠成长系数优先（普通宠 ×1.5）
```

即：**服务端 2026-09-06 修了神级宠，前端源没同步**（宪法 #6 记录的遗留，一直没修）。

### 为什么守护测试一直是绿的

`vtest_sim_sync.js` 逐函数比对 `f.toString()`，但 `NAMES` 只有 13 个：

```js
const NAMES = ['simulateSession', 'simulateSessionScript', 'simulateFight', 'petStats',
  'calcDamage', 'expFromBattle', 'mulberry32', 'pickWeighted',
  'skillOf', 'getEquipBonuses', 'getBloodline', 'rollBoss', 'bossRand'];
```

**漂移恰好在那 3 个不在名单里的函数上**（`resolveLineId` / `getBaseSpeed` / `getStatCoeff`）。守护测试守的是别的函数 —— 覆盖不到漂移点，等于没守。

### 玩家能感知到的后果：**塔的进塔预估偏低**

`tower/tower-preview.js` 用前端那份算属性：

```js
const stats = BS.petStats(pet, simCfg);   // ← 缺神级宠修正的那份
```

而塔是**神级宠专属**内容。于是：带神级宠进塔 → 属性被算低 → 预估层数偏低、风险等级虚高 → 界面显示「风险：高（会死在半途）」，实际能通。

这条不是空谈：该文件注释自己写着「用户把『失败要买重置卡』定为规则，这是**缓释付费墙的关键一件**」—— 预估不准 = 玩家误判要不要买重置卡。

### 修复（已落地）

1. **同步 4 处神级宠逻辑**到 `docs/js/core/battle-sim.mjs`（逐字照抄服务端，保证 `toString()` 全等）
2. **两份都 export** 这 4 个函数（`resolveLineId` / `godDefOf` / `getBaseSpeed` / `getStatCoeff`）—— 不 export 就比不了
3. **`vtest_sim_sync.js` 的 `NAMES` 补上这 4 个**（13 → 17 个），并留注释说明"名单必须覆盖所有会被两边分别修改的函数"
4. 重跑 `node build-sim-global.js` 重新生成 `battle-sim.global.js`；`?v= simsync1`
5. 重跑 `vtest_sim_sync.js` → **`A. 两份战核副本逐函数一致（17 个函数源码全等）`** ✅
6. `_shared/battle-sim.mjs` 动过 → **重新部署 `battle-settle`**（改 export 行为中性，但按项目规矩保持源码 == 线上）

全量回归 **66/72**，与基线一致。

### 根因没除（记录）

服务端副本仍是**手工同步**的 —— EF 打包不能引用仓库外的文件，所以只能抄。**`vtest_sim_sync.js` 是唯一护栏，它漏函数就等于没守。**
彻底解法是像 `gen_server_config.js` 那样**从 `docs/js/core/battle-sim.mjs` 生成服务端副本**（构建期产物），而不是手抄。**未做**，留给后续。

---

## 🟡 P2-2　战核其实有 **4 份**，`battle.js` 那份没有任何护栏（**当前未漂移**）　✅ 已补护栏

### 四份战核与各自的角色

| 实现 | 角色 | 护栏 |
|---|---|---|
| `core/battle.js` | **塔 / 副本的实时战斗**（`isTrialMode` / `isTowerMode` / `beginTrialFloor`） | ❌ **没有** |
| `core/battle-sim.mjs` | 前端模拟源（挂机结算口径） | ⇄ 服务端那份有 |
| `core/battle-sim.global.js` | 上面那份的生成产物 | 生成链保证 |
| `_shared/battle-sim.mjs` | **挂机结算权威** | ⇄ 前端那份有 |

`battle.js` 与 `battle-sim` 的关系只是**注释里的口头约定**：

```
 * 单场伤害结算（从 battle.js calcDamage 移植）
 * 单场战斗（与 battle.js tick/step/doTurn 逐随机数一致）
```

**没有任何测试守着。** 改 `battle.js` 的公式不会红，但挂机（服务端 sim）与塔/副本（`battle.js`）里同一个宠物会打出不同数字 —— 玩家侧就是"同一只宠在塔里和挂机里强度不一样"。

### 逐行核对结果：**当前是一致的**

| 对照 | 结果 |
|---|---|
| `battle.js#calcDamage` ⇄ `battle-sim#calcDamage` | ✅ 公式逐行等价（命中率 / 暴击 / 穿透 / 攻防递减 `atk²/(atk+effDef)` / dmgBonus / dr 全同），差异只在签名（闭包 `Config`+`Math.random` vs 注入 `config`+`rnd`） |
| `pet.js#getStats` ⇄ `battle-sim#petStats` | ✅ 等价。`pet.js` 的 `statParts` 与 sim 的 `core*`/`total*` **逐字相同**，乘区写法不同但数学等价（`sp.core.atk*(1+pct)+sp.growth.atk+flat` ≡ `coreAtk*(1+pct)+(totalAtk-coreAtk)+flat`）；其余 `spd/critRate/critDamage/hit/dodge/lifesteal/pen/dmgBonus/dr/growth` 字段逐字相同 |

**所以这是隐患不是现存 bug** —— 如实这么记。

### 补的护栏：`docs/tests/vtest_sim_port.js`

**为什么用行为对比而不是源码 diff**：两者签名本就不同，`toString()` 永远不可能相等。用**同一套随机序列喂两边、比对返回结构**，才是在守「同输入 → 同输出」这条真正的不变量。

- **A**：10 个用例（普通命中 / 未命中 / 暴击 / 带穿透 / 穿透超防御 / 伤害加成 / 受伤减免 / 减免超上限 / 吸血 / 全拉满），断言 `{damage,isCrit,isMiss,heal}` 完全一致
- **B**：**随机数消耗次数必须一致**（当前两边都是 2 次）—— 次数不同会让同种子模拟**整体错位**，不只是这一场不同

> ⭐ **还做了变异测试**：故意把 `battle.js` 的伤害 `+1`，测试立刻红并指出「普通命中 前端 68 / sim 67」；确认能抓得住后 `git checkout` 还原。**能过的测试不代表能失败** —— 新加的护栏必须验证它会红。

全量：**67/73**（= 基线 66 + 新测试 1），6 个红仍是存量。

---

## 🟠 P2-3　`compensateFights` 的兜底分支是一条「静默双倍发奖」通道　✅ 已修

### 事实

`idle-bridge.js` 有 **2 处**这样的代码（`compensateFights` 与演出层的 `finishShowFight`）：

```js
const serverReward = row && hasOwnProperty(row, 'reward') ? row.reward : null;
if (serverReward) {
  Materials.gainLocal(...)              // ✅ 纯本地，云端由服务端 add_material 已发
} else if (area && foe && Drop.rollReward) {
  Drop.rollReward(foe, area, { boss, enemyLevel })   // ❌ 没传 dry → 真发奖
}
```

而 `drop.js#rollReward` 在**不传 `dry`** 时会真的落库：

| 行 | 动作 |
|---|---|
| `drop.js:134` | `Materials.gain(am.name, 5)` → **上报云端 `add_material`** |
| `drop.js:140` | `Items.saveItem(eq)` → **写库一件装备** |
| `drop.js:201` | `Supabase.addEgg(baseName)` → **写库一颗蛋** |

注释自己写着「新战报的 reward 已由服务器决定；页面只负责入包和展示」+「兼容旧版战报（没有 reward 字段）时才走旧掉落逻辑」——**它本意就只是"演出来"，但实现给了它真发奖的能力。**

### 为什么现在没炸，但它很危险

服务端 `withReward` **总会**塞 `reward` 字段 → 这个 `else` 分支**当前是死代码**。

但它是"危险兜底"：一旦服务端因为任何原因不再下发 `reward`（字段改名、回滚到旧版本、某个路径漏塞），客户端会**静默地自己发一份并上报云端** → **每场战斗双倍产出**，而且**没有任何日志、没有测试能发现**。这种"静默翻倍"是最难查的一类。

### 修复

两处都加 `dry: true`：

```js
Drop.rollReward(foe, area, { boss, enemyLevel, dry: true })
```

`dry` **仍然返回抽样结果**（`showLoot` 照常展示掉落），只是不落库、不上报 —— 正好就是"兜底只该负责演"想要的语义。

> ⭐ **原则**：**发奖权已经归服务端之后，客户端那份兜底就不该保留发奖能力。** 留着不是"保险"，是"静默双倍"的入口。

`?v= idle-bridge dry1`；全量 67~69/73（见下方 flaky 说明）。

---

## 🟢 甲（2026-09-11 完成）：挂机补上装备 / 宠物蛋掉落

**现象**：线上 70742 场战斗明细里 `equipment` / `egg` **各 0 次**（配置写着 1.3% / 0.6%）。

**根因**：服务端化时只把「材料」搬过去了 —— `settle-core.mjs` 硬编码了一张 17 图的材料表 + `roll < 0.18` 掉材料，**装备/蛋那半截没写**。

### ⚠️ 材料掉率随之变化（这是"改收入"，不是"补掉落"）

| | none | material | equipment | egg | 总有掉落 |
|---|---|---|---|---|---|
| 服务端旧逻辑 | — | **18%** | 0 | 0 | 18% |
| 现在（读 `config.drop`） | 900 | 8.5% | 1.3% | 0.6% | 10.4% |

**材料收入约减半**，但这是配置（`Config.drop.poolByStage`）的既定设计，服务端只是终于去读它了。改这个数字**只需要动 `config.drop.poolByStage` 一处**，重跑 `node supabase/gen_server_config.js` 同步即可。

### 核心难点与解法：不能造第二份手工副本

装备生成逻辑在前端 `equipment.js`（IIFE + 闭包 `Config`/`Util`），服务端要能生成**完全一样**的装备。**手抄必然漂移**（战核刚吃过这个亏）。

**解法 = 构建期抽取**（照抄 `gen_server_config.js` 的模式）：

```
docs/js/equipment/equipment.js  ──(vm 加载)──▶ supabase/gen_equip_gen.js
                                                  │ 常量取求值结果
                                                  │ 函数取源码原文
                                                  │ Util 取源码原文
                                                  ▼
                                    _shared/equip-gen-server.mjs
                                    export function makeEquipGen(cfg, rnd)
```

几个关键点：
- **`Math` 遮蔽注入随机源**：抽取来的源码写的是 `Math.random()`，用
  `const Math = Object.create(globalThis.Math); Math.random = rnd;` 把它换掉 ——
  **源码零改写**。⚠️ `Math` 的方法**不可枚举**，`Object.assign({}, Math)` 是空的，**必须走原型链**。
- **抽取器失败就报错退出**：`SLOT_INFO`/`LABELS`/`affixTiersFor`/`rollBaseHit`/`levelOfAreaTier`/`rarityIdFromCount` 原本没导出 → 抽取器真的拦住了，才补进 `window.Equipment`。
- **没有手抄任何映射**：`AFFIX_TIER_TABLES` 是**原样抽源码**（不是重写一份），抽不到就 `exit(1)`。

### 改动清单

| 文件 | 改动 |
|---|---|
| `supabase/gen_equip_gen.js` | **新**。构建期抽取器 |
| `_shared/equip-gen-server.mjs` | **新**（生成产物） |
| `gen_server_config.js` | 白名单加 `equipment` + `drop.{pool,poolByStage,areaMaterials}`（不带 `drop.quests`，145 条任务会白白胖几百 KB） |
| `_shared/settle-core.mjs` | 删掉硬编码 `AREA_MATERIALS`（**第三份手抄**，17 图 vs 前端 10 图）；掉落改走 `config.drop` 单池，新增 equipment/egg 分支 |
| `battle-settle/index.ts` | 发奖分支加 `equip_items` / `pet_egg` 写入 |
| `docs/js/equipment/equipment.js` | 只加导出（`SLOT_INFO`/`LABELS` + 4 个内部纯函数），行为不变 |
| `docs/js/core/idle-bridge.js` | 掉到装备/蛋时拉一次云端背包（否则"飘字说掉了金装，背包里没有"） |
| `docs/tests/vtest_equip_gen.js` | **新**。行为等价 + 分布 + 落库结构护栏 |

### 验证

**`vtest_equip_gen.js`（12 条断言全过）**：
- **A**：前端与服务端抽取副本 **360 组输入 × 同随机序列，整件装备逐字段相等**
- **C1~C3**：20000 场模拟 → **装备 601 件 / 蛋 108 颗**（修复前恒为 0）
- **C4**：比例贴近配置（装备 3.0% vs 期望 2.9%；蛋 0.5% vs 0.6%；材料 8.1% vs 8.3%）
- **C5/C6**：掉落装备是**未鉴定**、带 `rarity.id`
- **C7**：同游标可重放（确定性保持）

**线上真实登录态冒烟**（一次性账号，已清理）：
- 挂机结算后 `pet_egg` 出现 **1 颗自然掉落的蛋** ✓
- 用 EF 同款写法插入装备 → **201**，回读 `affixes` 类型是 **`object`（不是字符串）** ✓
- 材料持续发放（枯荣种荚 ×2）✓
- EF 已重新部署，上传清单里含新的 `equip-gen-server.mjs` ✓

### Boss 掉落同步对齐（同批补做）

服务端 boss 原来是「区域材料 ×5」，前端 `drop.js` 是「**必掉一件未鉴定装备** + 本图区域材料 ×5 + 百变魔石 10% / 至尊神石 5%」。已对齐：

```js
{ type: 'boss', eq, material: { material, qty: 5 }, bossItems: [{name, qty}...] }
```

EF 发奖相应处理这个复合结构（装备入 `equip_items`、材料与稀有道具走 `add_material`）。

**断言（D1~D5）**：走 boss 分支 / 必掉未鉴定装备 / 材料 ×5 / `ilvl = Boss 等级` / 稀有道具骰比例（百变魔石 11% vs 10%、至尊神石 5% vs 5%）。

---

---
---

# 第 5 批：任务系统（`core/quest.js`）

## ✅ 先说好的：`completeQuest` 这块写得很扎实

145 条任务、兑换三铁律、任务奖励 —— 我原以为这里是重灾区，实际**没找到 bug**。它的顺序是对的：

```
① 校验 + submitting 闸门（同步，任何 await 之前 → 连点重入在这被挡）
② markFinished() 立刻记「已交」（同样在 await 之前）
③ 扣材料 → 失败才回滚（此时一份奖励都没发，回滚安全）
④ 发奖 → 失败【绝不回滚】（回滚 = 撤销已交 = 玩家能再点一次再领一份）
```

尤其是④⑤那条 —— **"宁可让玩家少拿一次，也不能让奖励可重复领"**，这正是防止经济被刷的正确取舍。注释里还写清了两种失败为什么处理相反。

## 🟡 P2-4　`grantGear` 有个潜伏 bug：送的装备恒为最低档（当前是死路径）

```js
const eq = E.generateEquipment(rarity, cfg.areaTier || 1, cfg.materialTier || 3);
//                                                                          ↑ 没传 ilvl
```

`generateEquipment(rarity, areaTier, materialTier, ilvl, countBonus)` —— 第 4 个参数 `ilvl` 没传，函数内部退化成 `ilvl = levelOfAreaTier(areaTier)` = `areaLevels[0]` = **1**。

后果链：
- `rollAffixTier(1)` → 只有 T4 门槛达标（T3=25 / T2=60 / T1=70）→ **词缀恒 T4/T5 最低档**
- `rollAffixCount(1)` → 最低档区间（1~2 条）→ **颜色恒为白**
- 底材 T 同样是最低档

而 2026-09-11 改版后 `generateEquipment` 的**注释明写「rarity / materialTier 参数仍接收但被忽略」** —— 也就是说 `grantGear` 传的 `rarity: 'blue'` 是**无效参数**，它要的蓝装根本拿不到。

**当前为什么没炸**：全仓检索 `rewardGear` —— 只有 `config.js:584` 的一句说明注释和 `quest.js` / `ui-quest.js` 的管道代码，**没有任何一条任务真的配了 `rewardGear`**。所以是**死路径**。

**建议**：真要用的时候，要么让 `grantGear` 传 `ilvl`（推荐，与掉落同口径），要么在 `config.js:584` 那句说明注释上标注「暂未启用」。否则下一个人照着注释配 `rewardGear: {count:2, rarity:'blue'}` 会拿到两件白装，还查不出为什么。

> ⭐ **顺带一个观察**：`config.js` 里「写了字段说明但没有任务在用」= 配置文档与现实脱节，和 P0-2（配置写 1.3% 装备、实际 0 次）是同一类问题。**说明注释也算"承诺"，一样要守。**

## 🟠 P1-5　任务奖励可以无限重领（P0-3 的一个具体利用）

这是第 2 批 P0-3「RLS 只控行归属、不控列值」的一个**具体后果**，此前只抽象提过，现在把路径写清楚：

1. 玩家完成任务 → `completed[q.id] = true` 写进 `quest_progress.progress` jsonb
2. `quest_progress` 的 RLS 是 `upsert_own`（`with_check auth.uid() = user_id`）—— **只限制"是自己的行"，不限制内容**
3. 玩家直接 PATCH 自己的那行，把 `completed` 清空
4. 刷新页面 → `loadCloudProgress()` 读到空 → 任务显示为未完成 → **再领一次**

**为什么这条比"改材料数量"更麻烦**：
- 材料奖励走 `add_material`，有 **1000/60s** 限流兜着
- 但**装备奖励走 `Items.saveItem` → `equip_items` 直接插行，没有任何服务端限流**
- 而且任务系统是规划中的资源来源之一（`vtest_resource_matrix.js` 守的是**设计比例**，不是"能不能重领"）

**为什么现在没修**：这属于宪法上线前清单 #8 的范畴 —— `quest_progress` 要收口就必须「服务端权威化」：做一个 `complete_quest(qid)` RPC，在服务端校验「是否已完成 / 进度是否达标 / 是否已扣材料」，然后原子发奖。**客户端改不了服务端判定的东西，才是根治。** 收掉写权限只会打断玩家正常的任务进度保存。

---

---
---

# 第 6 批：新手引导 `core/tutorial_mode.js`（689 行）

## ✅ 整体评价：这块是项目里写得最稳的几处之一

| 机制 | 评价 |
|---|---|
| **`grantOnce` 发放账本** | ✅ 与 `completeQuest` 同一套正确取舍：**记账先行**（`saveProgressStrict` 失败 → 抛错 → 不发货）→ 发货失败**不回滚**。账本存 `quest_progress.extra.grantLedger`，复用 extra 通道不加表不加列。还有 `granting` Set 做并发第二道闸。 |
| **`enter()` / `exit()` 数值覆盖** | ✅ 进教程前存 5 项（`expRate`/`speedScale`/`dropPool`/`synMin`/`nirMin`），退出逐项还原，且只在 `!= null` 时还原。`active` 防重入 ✓ |
| **`boostGuidePetToLevel`** | ✅ 只升不降（`cur >= lv` 跳过）、顶完清 `exp` 防到门槛立刻再升、已建档走 `updatePet` **绝不 savePet**（savePet 是无条件 INSERT，会复制出重复宠 —— 这条血泪它接住了） |

**没找到资产类 bug。**

## 🟡 P2-5　引导顶等级时，云端存档失败被静默吞掉，UI 却报"已生效"（已修）

```js
} catch (e) { console.warn('[guide] 引导经验包等级云端存档失败', e); }
```

**为什么这条比一般的"静默失败"更坏**：
1. `grantOnce` 的账本**已经记账成功**（严格来说这一步是对的：宁可少拿不可重发）
2. 本地等级已顶到目标值，UI 也弹了 toast「引导经验包生效：XXX 直升 Lv40」
3. 但云端没写进去 → **玩家一刷新等级回退**
4. 而账本说"发过了" → **再也领不到第二次**

结果就是**凭空丢了一次引导等级**，而且玩家完全不知情，只会以为游戏坏了。

修法：catch 里加一条如实的上报（参照 `grantGear` 已有的做法）—— 至少让玩家知道要重新登录，而不是以为游戏出 bug。

```js
if (window.UI && window.UI.addLog) {
  window.UI.addLog('⚠️ 引导等级云端存档失败，刷新后可能回退（重新登录可再试）');
}
```

---

---
---

# 第 7 批：`core/battle.js` 战斗流程（613 行）

## ✅ 结论：没找到资产类 bug，防护挺全

| 点 | 评价 |
|---|---|
| **`endFight` 模式隔离** | ✅ 塔/副本（`trial`/`tower`）模式**不掉落 / 不上报 / 不回血 / 不连战**，只写回血量再回调引擎；野图循环一概不碰 |
| **`fightEnded` 刹车** | ✅ 先把 `fightEnded = true` **再** `state.enemy = null`。注释写明：标签页被节流时 `tick()` 的 `steps` 可能 >1，不刹车则下一轮 `step()` 去读 `state.enemy.spd` → 满屏 "Cannot read properties of null"（2026-09-10 浏览器实测踩到） |
| **血量归属** | ✅ 写回 `state.petRef`（本场快照的宠物），不是 `getActivePet()` —— 中途切换出战不会把血量写到别的宠物身上 |
| **`beginTrialFloor` 重入防护** | ✅ `state.mode !== 'wild' \|\| autoRunning \|\| waitingRecover \|\| interval \|\| nextFightTimer \|\| recoverTimer` 任一为真就拒绝，防止套嵌多个战斗循环 |
| **战斗页让位** | ✅ `SESSION.onChange` → 被副本/塔抢占时自动 `stopAutoBattle()`，且注释点明"谁拥有资源谁负责收尾" |

**唯一的结构性风险**（已记在宪法 #6，非本次新增）：塔 / 通天塔的战斗**仍在客户端跑** —— 服务端权威 EF 处于暂停状态。玩家改前端可以在塔/副本里必胜，白拿门票对应的奖励。根治 = 塔 EF（`tower-run`）落地。

## 📌 一个顺带确认的事实

`endFight` 里有条**诊断埋点**：走到本地野图战斗时会打
`[本地战斗·收尾] ⚠️battle.js 本地战斗（非托管）`。

也就是说 —— **托管上线后，野图走本地战斗本身就是异常信号**。这条埋点是排查"为什么玩家在走本地循环"的抓手，值得留着，别当调试日志删掉。

---

## 🔴 过程中我自己写错又抓回来的一个 bug：`fight.lv` vs `fight.enemyLevel`

装备的 `ilvl` 决定词缀 T 阶与底材档位。我第一版写的是：

```js
Number(fight.lv) || 1      // ❌
```

而 `simulateSessionScript` 的事件字段是 **`enemyLevel`**（`battle-sim.mjs:721`），`lv` 是 `settle-core` **映射成战报之后**才用的名字。`rewardForFight` 拿到的是**原始事件** → `fight.lv` 恒为 `undefined` → **ilvl 永远是 1** → 掉出来的装备全是 T4/T5 最低档，等于白掉。

**更值得记的是：我的测试也照抄了 `lv: 30`，所以测试跟着代码一起错，360 组等价对比全过，谁也没发现。**

修法：
1. 抽出 `ilvlOfFight(fight)`，主取 `enemyLevel`、`lv` 只作兜底
2. 测试改用真实字段名 **`enemyLevel`**
3. **新增断言 C6b：装备 `ilvl` 必须等于击杀怪等级**（读到 1 就红，防止再犯）

> ⭐ **教训：测试里造的输入必须按【被测函数真实收到的结构】造，不能照抄自己代码里的假设。** 否则测试只会确认你的错觉。

### 遗留（未做）

- **发奖仍不在事务内**（第 1 批 P1-1）：现在多发的三张表同样在游标推进后才写入，失败即丢。终局解法是服务端事务化。
- 第 4 批未审完：`idle-bridge.js` 演出层、`core/battle.js` 流程、`quest.js`(766)、`tutorial_mode.js`(689)。

---

## ⚠️ 一个差点误报的地方（记录手法）

重跑 `build-sim-global.js` 时输出 `35182 bytes`，而原文件是 `38641 B` —— 看着像"生成产物缩小了 3.5KB = 原来那份是旧的"。

**实际是量纲不同**：构建脚本打印的是 `s.length`（**字符数**，中文 1 字符），`Get-ChildItem .Length` 是**字节数**（中文 1 字 = 3 字节）。`git diff --stat` 显示 `17 insertions(+)` 无删除 → **生成产物原本就是同步的**。

**教训**：核对文件大小别跨来源比数字，优先看 `git diff`。

## ⚠️ 第二个差点误报：`vtest_tier_rarity` 是 flaky

修完 P2-3 跑全量得到 **68 通过 / 5 失败**，比之前的 67/6 多过一个，`vtest_tier_rarity` 由红转绿 —— 差点当成"改动带来的改善"报出去。

单独连跑 3 次：**OK / FAIL / FAIL** → **它是 flaky**（掉落底材 T 阶的抽样测试，非确定性），和 `vtest_enemy_balance`（蒙特卡洛）同类。

**教训**：**存量红的清单必须区分「稳定红」和「flaky」**，否则每次全量数字浮动都会被误读成"我改好了/我改坏了"。已把这条写进宪法维护规则 7：稳定红 5 个 + flaky 2 个，通过数在 **67~69/73** 浮动属正常。

---

## ✅ 第 3 批已查、没问题的

| 项 | 结论 |
|---|---|
| `items.js` 整体 | 干净。`saveItem` 回写 `cloudId` 前检查了 `error`；`fromCloud` 的旧数据兜底做得很细（`_ilvl` 缺失→null、`base_stat` 缺 `type`→给默认攻击基底、脏词缀清洗、稀有度一律按词缀条数重算）；`updateCloudItem` 把 `_ilvl/_lockPrefix/_lockSuffix` 塞进 `affixes` JSON 而非加列 ✅ |
| `deleteCloudItems` | 一次 `IN` 批量删（不逐条），空数组直接短路 ✅ |
| `deletePet` / `deleteCloudItems` 的 0 行 | 无害（本来就没行），不需要检测 ✅ |
| `consumeEgg` | **已有** `.select().single()`，0 行会报 `PGRST116` ✅ —— 说明"要检测 0 行"这件事项目里**已经有人想到过**，只是没铺开 |
