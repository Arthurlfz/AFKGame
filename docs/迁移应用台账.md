# 迁移应用台账（supabase/）

> **这份文档解决宪法 §三 #2**：以前没人知道「线上库到底跑到哪一版、哪些 SQL 执行过」。
> 盘点方式 = **只读查询线上 public schema**（建表/建函数清单）+ 逐个迁移文件里 `create table/function` 的名字对照。
> ⚠️ 只看「对象在不在」，**不看内容是否最新**（同名函数被多个文件反复 `create or replace` 过，谁最后跑谁生效 —— 见宪法 §四-6）。

**盘点时间**：2026-09-23
**线上快照**：`public` 下 **32 张表 / 53 个函数**，pg_cron **2 条定时任务（都 active）**

| 定时任务 | 频率 | 命令 |
|---|---|---|
| #5 | 每 5 分钟 | `select public.market_bot_sweep();`（装备常驻买家） |
| #6 | 每 5 分钟（错开） | `select public.market_bot_sweep_material();`（材料常驻买家） |

**线上 32 张表**：
`battle_logs` `board_messages` `chat_messages` `egg_listings` `equip_items` `equip_listings` `feedback`
`follows` `friendships` `game_config_overrides` `idle_sessions` `market_bot_sweep_log` `material_listings`
`materials` `messages` `orders` `pet_egg` `pet_listings` `pets` `products` `profiles` `quest_claims`
`quest_progress` `redeem_codes` `security_bot_buy_log` `security_rate_limits` `trade_records`
`user_perks` `user_sessions` `wallet_ledger` `wallets`

---

## 一、逐文件对照（45 个迁移文件）

图例：✅ 它创建的对象线上都在 ｜ ➖ 只改数据/权限/列，不新建对象 ｜ ❌ 线上缺

| 迁移文件 | 创建的对象 | 线上 |
|---|---|---|
| `migrate_material_trade.sql` (08-22) | trade_records / buy_pet / buy_equip | ✅ |
| `migrate_pet_active.sql` (08-22) | — | ➖ |
| `migrate_evolve_times.sql` (08-25) | — | ➖ |
| `migrate_quest_progress.sql` (08-25) | — | ➖ |
| `migrate_pet_equipment.sql` (08-29) | — | ➖ |
| `migrate_pet_exp.sql` (08-29) | — | ➖ |
| `migrate_profiles.sql` (08-29) | profiles / handle_new_user | ✅ |
| `migrate_pet_egg.sql` (08-30) | pet_egg | ✅ |
| `migrate_trait_soulcast.sql` (09-01) | — | ➖ |
| `migrate_equip_identified.sql` (09-02) | — | ➖ |
| `migrate_dev_delete_user.sql` (09-03) | admin_delete_user | ✅ |
| `migrate_security_hardening.sql` (09-03) | security_rate_limits / security_bot_buy_log / bot_buy_guard / quest_progress / add_material | ✅ |
| `migrate_idle_sessions.sql` (09-06) | idle_sessions / battle_logs / battle_settle / battle_session | ✅ |
| `migrate_god_pet.sql` (09-06) | check_god_synth / check_nirvana | ✅ |
| `migrate_resource_trial.sql` (09-08) | **resource_trial_runs** | ❌ **线上没有这张表** |
| `migrate_battle_pending_script.sql` (09-10) | battle_settle | ✅ |
| `migrate_pet_icon_nullable.sql` (09-10) | — | ➖ |
| `migrate_egg_species_trade.sql` (09-10) | egg_listings / list_egg / buy_egg / cancel_egg_listing | ✅ |
| `migrate_user_sessions.sql` (09-10) | user_sessions / session_login / session_heartbeat / session_logout | ✅ |
| `migrate_cultivate_used.sql` (09-11) | — | ➖ |
| `migrate_security_reapply.sql` (09-11) | bot_buy_guard / bot_buy_* 三个 | ✅ |
| `migrate_bot_buy.sql` (09-11) | bot_buy_equip / bot_buy_pet（🔴 禁止重放） | ✅ |
| `migrate_trade_records_ref.sql` (09-11) | buy_pet / buy_equip / buy_egg（🔴 禁止重放） | ✅ |
| `migrate_material_listings.sql` (09-11) | material_listings / list_material / buy_material / bot_buy_material（🔴 禁止重放） | ✅ |
| `migrate_bot_buy_persona.sql` (09-11) | bot_buy_*（带 persona 买家名） | ✅ |
| `migrate_settle_jsonb_fix.sql` (09-11) | battle_settle | ✅ |
| `migrate_asset_write_hardening.sql` (09-11) | —（materials / profiles.banned 收口） | ➖ |
| `migrate_quest_claim.sql` (09-12) | complete_quest / admin_unclaim_quest | ✅ |
| `migrate_settle_reward_tx.sql` (09-12) | battle_settle | ✅ |
| `migrate_quest_claims_table.sql` (09-12) | quest_claims / complete_quest | ✅ |
| `migrate_shop.sql` (09-12) | wallets / wallet_ledger / products / orders / redeem_codes / get_my_wallet / redeem_code / spend_gems / grant_gems | ✅ |
| `migrate_market_bot_service.sql` (09-13) | market_bot_sweep_log / market_bot_nickname / market_bot_sweep / market_bot_sweep_material | ✅ |
| `migrate_spend_materials.sql` (09-15) | spend_materials | ✅ |
| `migrate_hatch_egg.sql` (09-15) | hatch_egg | ✅ |
| `migrate_bound_items.sql` (09-16) | — | ➖ |
| `migrate_admin_tools.sql` (09-16) | admin_* 九个 | ✅ |
| `migrate_newcomer_first_sale.sql` (09-17) | market_bot_sweep / market_bot_sweep_material（改） | ✅ |
| `migrate_market_pulse.sql` (09-17) | market_pulse | ✅ |
| `migrate_material_daily_cap.sql` (09-17) | add_material（改） | ✅ |
| `migrate_feedback.sql` (09-17) | feedback / admin_list_feedback | ✅ |
| `migrate_ops_board.sql` (09-17) | admin_ops_board | ✅ |
| `migrate_growth_no_cap.sql` (09-17) | — | ➖ |
| `migrate_shop_perks.sql` (09-20) | user_perks / spend_gems / get_my_perks | ✅ |
| `migrate_shop_perks2.sql` (09-20) | get_my_perks / set_name_tag / spend_gems | ✅ |
| `migrate_god_synth_sub.sql` (09-20) | check_god_synth（改） | ✅ |

> 注：`follows` / `board_messages`（任务单 05 轻社交 2026-09-20）、`friendships`（任务单 07 好友私聊）
> 线上都在 —— 这两单的数据库部分确实执行过了。

---

## 二、唯一的真缺口

**`resource_trial_runs` 表线上不存在**（`supabase/migrate_resource_trial.sql` 没执行过）。
> ✅ **已补建（2026-09-23）**：表与 4 条 RLS 策略已上线；另跑 `migrate_trial_free_entry.sql`
> 加了 `used_free` 列与 `(user_id,route_id,created_at)` 索引（副本每日免费次数改由服务端记账）。
> 下面这段"影响面/什么时候会炸"保留为**当时的判断**，不再代表现状。

- 影响面：**现在不痛** —— 副本当前是纯客户端玩法（资格存 localStorage），客户端不调 `resource-trial` EF。
- 什么时候会炸：**副本服务端权威化那天**。EF `resource-trial/index.ts` 第 94 行会往这张表插占位行做幂等，
  表不存在 = 一调用就报错、副本整个走不通。
- 处理：要么现在补建（幂等，无副作用），要么**在副本服务端权威的施工图里列为第一步**。
  ⚠️ 建表要动数据库 → 按规矩**先问人、串行做**。

---

## 三、复盘点怎么跑（以后重做一遍只要 2 分钟）

1. 取 token（PAT 在项目根 `.env`，已 gitignore）：
   ```powershell
   $env:SUPABASE_ACCESS_TOKEN = (Select-String -Path .env -Pattern '^SUPABASE_ACCESS_TOKEN=').Line -replace '^SUPABASE_ACCESS_TOKEN=',''
   ```
2. 盘点表与函数（**只读**：`information_schema` + `pg_proc`）：
   ```sql
   select 'TABLE' as kind, table_name as name from information_schema.tables where table_schema='public'
   union all
   select 'FUNCTION', p.proname from pg_proc p join pg_namespace n on n.oid=p.pronamespace where n.nspname='public'
   order by 1,2;
   ```
   ```powershell
   npx -y supabase db query --linked --project-ref asklogeayzlqpeejuvjj -f <文件.sql>
   ```
3. 定时任务：`select jobid, schedule, active, command from cron.job order by jobid;`

⚠️ 坑：SQL 里带双引号必须走 `-f 文件`（PowerShell 会吃掉引号）；子命令是 `db query` 不是 `db execute`；
必须带 `--linked`；输出是超宽 ASCII 表 → 重定向到文件再搜。
