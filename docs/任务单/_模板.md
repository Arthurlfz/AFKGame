# 任务：<一句话写完 — 动词开头，能验收>

> **状态**：待派发 ｜ 进行中 ｜ 已完成
> **派给**：（哪个 agent / 哪个会话）
> **创建**：YYYY-MM-DD

---

## 一、要解决什么

> 写**目的**，不要写方案。
> 一句话说清"不做会怎样"。方案留给干活的人想。

## 二、🔴 边界（这一栏最重要，防打架）

**允许碰的文件**：
- （列具体路径。列不出来 = 这个活还没拆清楚，别派）

**绝对不许碰**：
- （已知共享雷区：`docs/js/core/config.js`、`docs/游戏.html`、`docs/js/core/battle.js`、
  `docs/js/core/supabase.js`、`docs/js/ui/ui-common.js`、`docs/css/game.css`）

**会不会改数据库 / 线上配置**？
- 会 → **先停下来问人**，不许自己拍板。（这是唯一的生产库，改坏没法撤销）

## 三、验收标准

> 必须**可执行**。不许写"看起来没问题""运行正常"。
> 默认都带上这条：

```bash
cd docs && npm run check
```
**期望：输出里没有「新搞红了」那一段。**

补充验收：
- （具体到界面/数据，比如"点 X 出现 Y，且刷新后还在"）

## 四、依赖与冲突

前置任务：无
已知会跟它撞车的并行任务：无
> ⚠️ 并行期间：**改了 JS/CSS 必升 `?v=`**，两个人同时改同一个文件的版本号必然互相覆盖。

## 五、做完必做（漏一条就当没做完）

- [ ] 改了 JS 或 CSS → 升了 `docs/游戏.html` 里对应的 `?v=`
- [ ] **浏览器里真看了一眼**（测试绿 ≠ 界面存在，血泪：商店页的 CSS 从来没写过）
- [ ] 提交时**只 add 自己路径**（先看 `git status --short` 有没有意外的 `D`）

## 六、结果（干完填这里）

实际改了什么：
验收结果：
有没有留尾巴（没做完的部分要说清楚，别藏）：
