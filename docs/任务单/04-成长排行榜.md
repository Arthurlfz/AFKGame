# 任务：成长向排行榜

> **状态**：已完成（**验收：实现为并行会话所写**，本会话只做核对/验收/填结果）
> **派给**：当前会话
> **创建**：2026-09-20

---

## 一、要解决什么

> 玩家没有"我在全服排第几"的参照，成长缺少外部标尺。
> 你的核心卖点是交易，那"身价最高的宠物"排行天然就该有。

## 二、🔴 边界

**允许碰的文件**：
- 新建 `docs/js/ui/ui-leaderboard.js`
- `docs/js/ui/ui-shell.js`（加侧边栏入口）
- `docs/css/ui-polish.css` 或新建 `docs/css/leaderboard.css`
- `docs/游戏.html`（加script引用+版本号）

**绝对不许碰**：
- `docs/js/core/config.js`、`docs/js/core/battle.js`、`docs/js/core/supabase.js`
- `docs/js/core/idle-bridge.js`

**会不会改数据库**：
- 可能需要新建排行榜视图或RPC → **先停下来问人**，不许自己拍板

## 三、验收标准

```bash
cd docs && npm run check
```

补充验收：
- 侧边栏有"排行"入口
- 至少展示：宠物总身价排行（前50）、个人当前排名
- 数据从服务端拉，不是假数据
- 刷新后还在

## 四、依赖与冲突

前置任务：无
已知会撞车：无

## 五、做完必做

- [x] 升了 `?v=`（`ui-leaderboard.js?v=20260920lb`，已由实现方加上）
- [ ] 浏览器里真看了一眼 —— **AI 没有浏览器，没做**
- [ ] 提交只 add 自己路径 —— 未提交（工作区有并行会话改动）

## 六、结果

**实际改了什么（实现方：并行会话，本会话核对）**

- 新建 `docs/js/ui/ui-leaderboard.js`（92 行）：查 `pets` 按成长降序（每人取最高那只，前 50），
  join `profiles` 拿昵称，显示奖牌/排名/宠物名/成长值，**自己高亮（你）+ 底部"你的排名：第 N 名"**。
- `docs/游戏.html`：侧边栏加 `data-page="leaderboard"` 按钮「排行榜」+ `#tab-leaderboard` 页面容器
  + `<script src="js/ui/ui-leaderboard.js?v=20260920lb">`。
- `docs/js/ui/ui-shell.js:72`：切页时 `if (page === 'leaderboard') UI.renderLeaderboard()`。
- ⭐ **没有建表、没有 RPC** —— 直接查已有的 `pets` / `profiles`，所以 §二 里"先问人"的那条**没有触发**。

**验收结果**

- `cd docs && npm run check` → 85 通过 / 3 失败，**没有新搞红**（3 条存量红）。
- 补充验收逐条：侧边栏入口 ✅ ｜ 前 50 ✅ ｜ 个人当前排名 ✅ ｜ **数据从服务端拉（Supabase 查询，非假数据）** ✅ ｜ 刷新后还在 ✅（读库，天然持久）。

**留尾巴（没做完的，说清楚）**

1. **没在浏览器里看过**（AI 没有浏览器）—— 建议你自己强刷进「排行榜」看一眼。
2. **取样口径是近似**：取"全服成长值前 300 只宠物"再按人去重 ⇒
   理论上会把"最好的宠排在 300 名开外"的玩家漏掉。要算准得建**视图或 RPC**，那要动数据库 →
   **按 §二 的规矩先问人**，我没做。
3. ⚠️ **需要你确认一件事**：`pets` 表的 **RLS 是否允许读他人的宠物**。
   如果 RLS 只允许读自己的，这个榜就只会显示你一个人（代码本身没问题，是权限问题）。
   离线测不出来 —— 你在库里或浏览器里确认一眼。
