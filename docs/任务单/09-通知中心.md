# 任务：通知中心

> **状态**：待开始（需建表，交给其他agent）
> **创建**：2026-09-20

---

## 一、要解决什么

> 现在市场卖出去东西了、有人发好友请求了、系统发补偿了，玩家没有统一收件箱。
> 只有前端轮询提示，刷新就丢。需要一张持久化通知表。

## 二、需要新建的表

```sql
create table if not exists public.notifications (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references auth.users(id) on delete cascade,
  type text not null,           -- market_sold / market_bought / friend_request / system
  title text not null,
  body text,
  data jsonb,                   -- 附加数据（商品id、卖家名等）
  read boolean not null default false,
  created_at timestamptz not null default now()
);
create index if not exists notifications_user_idx on public.notifications (user_id, read, created_at desc);
```
RLS：只能读写自己的通知；服务端函数（SECURITY DEFINER）才能插入。

## 三、功能范围

- 顶栏加铃铛图标，未读红点
- 点开下拉列表看通知
- 一键已读
- 市场售出/买入时自动插一条通知
- 好友请求到达时自动插一条

## 四、边界

**允许碰的文件**：新建 `docs/js/ui/ui-notify.js`、`docs/js/ui/ui-common.js`、`docs/游戏.html`、CSS
**不许碰**：config.js / battle.js / idle-bridge.js

## 五、验收

```bash
cd docs && npm run check
```
- 市场成交后双方都收到通知
- 刷新后通知还在
- 未读数显示正确，点已读后清零
