# 任务：玩家屏蔽 + 改名

> **状态**：待开始（需改表，交给其他agent）
> **创建**：2026-09-20

---

## 一、要解决什么

> 1. 聊天/市集遇到喷子或骗子，能屏蔽他（不看他的聊天和商品）
> 2. 现在昵称是邮箱前缀，玩家想改个好听的名字

## 二、需要改数据库

### 屏蔽表
```sql
create table if not exists public.player_blocks (
  id uuid primary key default gen_random_uuid(),
  blocker_id uuid not null references auth.users(id) on delete cascade,
  blocked_id uuid not null references auth.users(id) on delete cascade,
  created_at timestamptz not null default now(),
  unique(blocker_id, blocked_id)
);
```
RLS：只能查/增删自己的屏蔽记录。

### 改名（profiles已有nickname列，加个改名费用字段即可）
```sql
-- 不需要新表，profiles.nickname 已存在
-- 改名逻辑：扣魔石（已有shop表的魔石体系），改 profiles.nickname
-- 可加限制：每7天只能改一次
```

## 三、功能范围

- 聊天/市集点玩家头像或昵称 → "屏蔽此人"
- 设置页加"已屏蔽列表"（可解除）
- 屏蔽后：聊天不显示他的消息、市集不显示他的商品
- 设置页加"改名"按钮（扣魔石，7天CD）

## 四、边界

**允许碰的文件**：新建 `docs/js/ui/ui-block.js`、`docs/js/ui/ui-chat.js`、`docs/js/ui/ui-market.js`、`docs/游戏.html`
**不许碰**：config.js / battle.js / idle-bridge.js

## 五、验收

```bash
cd docs && npm run check
```
- 屏蔽某人后，他的聊天和商品都消失
- 解除屏蔽后恢复
- 改名后各处显示新名字（市集、聊天、排行榜）
