# 永夜灵市（Evernight Bazaar）· 项目宪法（PROJECT CONSTITUTION）

> **定位：唯一的"已拍板决策账本"。** 任何 Agent（CodeBuddy / WorkBuddy / ZCode / 豆包 / 未来所有工具链）开工前**必须先读本文件**。你记不住的东西，这里替所有人记。
>
> **为什么存在**：2026-09-03 用户自曝"用很多 agent 导致幻觉、自己记不住拍过的板"。本文件就是解药——把拍板收敛成一条条可核对的事实，而不是散在各自 agent 的记忆里。
>
> **维护**：拍板新决策 → 追加到对应主题并标日期；推翻旧决策 → 在原文标注"已推翻（日期）"，不删除。冲突时以本文件 + 最新日期为准。

---

## 一、权威源（谁说了算）

| 领域 | 唯一权威 | 说明 |
|---|---|---|
| 代码（运行源） | `docs/` | git 提交已声明 "docs as official runtime source" |
| 数值 | `docs/js/core/config.js` | 旧"数据地图"已废弃归档，不再改 |
| 测试 | `docs/tests/` | **84 个脚本，其中 `vtest_*` 75 个**（2026-09-13 实测）。⚠️ 基线待重跑，见文末维护规则 7 |
| Supabase 迁移 | `supabase/` | **33 个 SQL**（2026-09-13 实测，原记 26 已过期）。⚠️ **不保证幂等/可重放** —— 同一函数被多个文件重复定义，重放旧文件会覆盖新逻辑，见维护规则 6 |
| 借鉴参考 | `docs/玩法文档/` | 只读参考源，不照抄数值 |
| 归档区 | `archive/` | 旧物唯一去向，gitignore 不入库 |

**禁止**：往非权威源写代码/数值/测试/迁移；新 agent 自带的历史记忆一律以本文件为准。

---

## 二、已拍板决策

### A. 等级与地图（2026-09-03）

1. **宠物等级上限 = 60**（已落地 `config.js: pet.maxLevel = 60`）。原设计 1-100 本来就是"教学"，等级爬升不是游戏本体。
2. **图 1-10 = 教学图**（1-60 级快速上手主流程）。
3. **图 11-17 保留不删** = 60 级毕业图占位（配置全留，数值占位，当前被 60 上限自然锁死不可达）。
4. **真玩法在 60 之后**：涅槃成长 + 做装 + 市场博弈的无限循环。AI 假人经济是毕业后的核心玩法，**不是冷启动工具**。

### B. AI 假人经济（2026-09-03）

1. **AI 数量 = 20 个 persona**（原型够用；正式多人 20-80）。
2. **AI 不标 "AI / 流浪商人"**，全部以昵称作为真实玩家呈现。
3. **价格锚 = 双层模型**：
   - **魂值**（死尺）：抽象数字，策划定，不随供需动 = 成本底线；
   - **市场价**（活尺）：成交移动平均价，随供需/抄底/短缺波动。
   - 玩家看到动态比价（"比均价低30%" / "较公允价+40%"）→ 抄底倒卖可读。
4. **"尺"不挂在任何材料上**：凝魂晶石等材料会结构性贬值，魂值必须是策划定的抽象点，不与材料挂钩。
5. **材料比例固定**（钱稳）；**装备/宠物价格随需求浮动**（货活）。
6. **AI 之间不互相交易**（防对倒/洗单，市场价只吃有玩家参与的真实成交）。
7. **AI 犯错机制**（博弈空间来源）：挂漏 ~5-8%、买贵 ~3-5%、**识货盲区**（对特定血统/词缀组合估值有盲区 = 玩家"比 AI 更懂价"能赢钱的空间）。
8. **AI 真玩 = 底层真实账号 + 上层决策大脑**：AI 复用真实游戏代码（掉落/打造/进化/涅槃）在独立沙箱跑，决策层参数化（`config.marketBot.personas`）。
9. **实施分阶段**：阶段 1 = 前端沙箱 20 persona（当前，单人各自市场）；阶段 2 = 服务端 AI（Cloudflare Worker + Cron，真共享市场，所有人看到同一个）。
10. **两条红线（防崩，写进代码）**：
    - 消耗端必须存在：AI 买走的东西必须离场（sink），禁止回流；
    - 价格合理判定：AI 只买不高于价值模型上限的单。

### C. 魂铸与宠物特质（8 条铁律，违反即返工）

1. **特质一律不含攻击%**，全部机制/生存属性（暴击/暴伤/吸血/命中/闪避/速度/生命%/防御%）。
2. **T 阶口径 T1 最强最稀有**（与装备词缀 T1~T5 惯例一致；特质只用 T1/T2/T3）。
3. **魂铸词缀：每件最多 1 条、永久**，不可被剥离/重铸/神圣石洗掉；总词缀上限 = 前3 + 后3 + 魂1 = 7 条。
4. **成长 hook 是门槛式，不是放大式**：成长只做解锁条件（≥10/≥40/≥60），禁止"成长越高→铸魂越强"的线性加成。
5. **继承强度先松后紧**：合成继承主 70% / 副 40% 起步。
6. **氪金宠三原则**：天花板不变只许加速不许加高；皮肤不加数值；功能只做经济向不碰战斗数值。
7. **评分不展示给玩家、不参与市场排序**（仅内部批量分解参考）。
8. **精准特质保留**（后期图加高闪避怪让它有价值）。

### D. 设计取舍（明确"不做"清单）

1. **不做 build 深度构筑**（词缀不做系列化 build）。
2. **不做"盘"系统**（2026-09-03 明确拒绝）。
3. **不做掉落保底**（挂机游戏不需要）。
4. **不搞涅槃成长曲线**（成长只做门槛，禁止放大式曲线）。
5. **不做收款码/私下转账**（2026-08-31 拍板；正式收款需官方支付 SDK + 企业主体 + 版号资质）。
   - ⚠️ **2026-09-12 更正**：商店**已打开**（`config.js:1532 enabled: true`）。**收款通道仍未开** —— 魔石来源只有卡密兑换（`redeem_code`）与管理员发放（`grant_gems`）两条。
   - ⭐ **同时拍板商店定位**：魔石＝「便利货币」，**只卖不影响战力的权益（`payload.perks`）**；`payload.materials`（卖材料＝卖数值）与「不学氪金分层」冲突，**已全部下架，不再新增**。
   - 用户给的理由：背包/宠物/离线上限**本来就不存在**，卖扩容＝先造限制再卖解除，不做。**便利商品应当从玩家的真实抱怨里长出来。** 现仅 `perk_listing_5`（挂单额度 +5 / 20 魔石 / 限购 2）。
   - ⚠️ **2026-09-16 改判（上面那句原文保留不删，供回溯）**：装备与宠物**已经设了容量上限**，
     并上架了扩建道具（`Config.capacity`：背包装备位 150＋50×5 / 育兽栏 20＋5×4）。
     改判理由：实测有号攒到 1786 件装备，上限本身是有意义的；扩容仍属「便利」，不进战力。
   - ⭐ **2026-09-20 商店成排**：商店页分三排 —— **扩容 / 礼包 / 名牌**（`ui-shop.js` 按 payload
     推导分组，**不写 sku 名单**，加了商品不用改分组代码）。现共 9 件：
     扩容 3（背包 +50 格＝50 石 / 育兽栏 +5 位＝50 石 / 挂单 +5＝20 石）、
     礼包 2（旅者行囊 85、拓荒者行囊 170，都比单买便宜）、
     名牌 4（青玉 15 / 幽紫 30 / 鎏金 60 / 血月 100，买下永久解锁、可自由切换、重复购买不扣钱）。
     名牌 = **流光名字**，与掉落播报同一套视觉（`game.css` 的 `background-clip:text` + `loot-shimmer`）；
     ⚠️ **只有顶两档（鎏金/血月）带扫光** —— 低两档也扫会抢掉宝播报的戏（掉宝是第一爽点），
     而且榜单一屏几十个名字全在动既花又耗。
     用户同日明确**这三样不做**：称号、摊位招牌、市场置顶（后两者在假人撑市集的当下收益近乎为零）。
     🔴 边界不变：**不卖材料 / 不给副本次数与通天塔重置卡 / 不卖经验**；
     也**不为填货架去"先造限制再卖解除"**（离线托管时长、好友上限、挂单到期这些限制代码里根本不存在）。
6. **材料词条里不做「行情表（在售价/成交量）」**（2026-09-14 用户拍板"这个没必要做吧"）。三条理由：
   ① **行情在市集页已经有了**：参考价＝同类在售挂单中位价，并打「低于市价 X%」/「高于市价 X%」标记
   （`Config.trade.refPriceMinSamples / dealDiscount / overpriceMarkup`）—— 词条里再放一份 = 第二份真源，正好犯项目头号病因；
   ② **现在市集是假卖家/假买家冷启动撑着的，成交量是假的**，给玩家看假行情比不给更糟；
   ③ 真要行情，等真人多了应该加在**市集页**（玩家当时就在做交易），不是塞进百科词条。
7. **百科页不做「材料索引/搜索」入口**（2026-09-14 同批）：词条的价值是"我背包里这东西能干嘛"，
   那一刻人就在背包里（最短路径）；百科定位是"规则与数值速查"，再开一个入口解决同一个问题、还要维护第二份列表，不做。

### E. 架构与治理（2026-09-03）

1. **服务端权威 = 上线前必做**（用户便签原话）。2026-09-13 核对后拆成两半，**别再说"当前是客户端权威"了 —— 那是过期描述，会让人低估进度**：
   - 🟢 **已搬完成的野图挂机**：`docs/js/core/idle-bridge.js:26` 直连 `battle-settle` EF（已部署），
     **预结算当场入账、客户端只回放**；且**客户端兜底禁止保留发奖能力**（否则是静默双倍收益）。另 `resource-trial` EF 已部署。
   - 🔴 **仍在前端的部分**：打造 / 进化 / 定价 / 副本(trial) / 塔(tower) 结算 —— 玩家改 DevTools 仍能改结果，这才是"没底气推广"的根因。
   - ⚠️ 本清单 #1 原来写「掉落/打造/进化/定价全在前端」，**其中"掉落"已不成立**，故上面三条取代原表述。
2. **RLS + RPC 只防"外人直连"**，防不了"真人玩家改前端后合法提交"（add_material 不校验数值来源）。要堵必须把数值结算搬服务端。
3. **5 套 Agent 工具链各带记忆**（.codebuddy/.workbuddy/.zcode/新demo/豆包）是"多 agent 幻觉"的物理根源——**统一先读本文件**。
4. **账本优先**：任何 agent 开工前读本文件，拍板在此记录，不靠个人记忆。

### F. 市场买家缺失 → 服务端常驻买家（2026-09-13）

> 背景：核实「新人为什么前十分钟留不住」时挖出来的**地基缺陷**，不是新人专属问题。

1. 🔴 **事实（已核实，别再怀疑）**：前端机器人（`market_bot.js`）只跑在**玩家浏览器**里，且**不买自己挂的单**
   （`market_bot.js:376` 过滤 `seller_id === user.id`，防自挂自买刷材料）；
   **没有任何服务端常驻买家**。
   → **一个人玩时，挂出去的东西永远卖不掉**。「你打到的东西能卖给真实的玩家」这句话，落到游戏里是空的。
2. **用户拍板（2026-09-13）**：机器人收购走**保守**路线 —— **宁可玩家偶尔卖不掉，也不让材料贬值**
   （发出去的材料收不回来，通胀不可逆）。
3. **用户拍板**：机器人必须能**一键关掉**（将来有真实玩家了，关掉让真人互相交易）。
   → 开关必须放**服务端**：只管本机的开关等于没关（别人浏览器里的机器人照跑，刷新还复原）。
4. **落地方式（已完成）**：
   - 总开关：`game_config_overrides.config->'bot'.enabled`，前端 `docs/js/core/server-config.js` 登录读 + 每 60 秒跟一次；
     关=停补货+停收购+**清掉市场上所有假货**。改开关走 `admin_save_config`（管理员专属）。
   - 常驻买家：**`supabase/migrate_market_bot_service.sql`** —— `pg_cron` 每 5 分钟调一次 `market_bot_sweep()`
     （**没用 Cloudflare Worker**，本文件 B-9 的"阶段 2"就用数据库原生定时器实现）。
   - ⚠️ **一个现有函数都没改**（`bot_buy_equip/pet/material` 原样）：定时任务是**系统行为**，没有 JWT 也不该占玩家每日额度，
     与"玩家主动召唤商人"是两套职责，只共用同一套口径（税每满 8 收 1 / `trade_records` 双写 / 买家名「流浪商人」）。
5. **保守起步参数**（写在服务端配置里，改参数不用改函数）：
   **`dailyCap: 30`（每天全局最多 30 件）／ `maxPrice: 5`（只收标价 ≤5 的）／只收白蓝装（金装留给真人交易）／
   沿用 80% 销毁**。配额用**漏桶**按当天进度匀速释放 —— 防"系统秒收"（玩家会察觉并挂高价试探）和"前两小时用完全天额度"。
6. ⚠️ **账算不准的原因（别假装算出来了）**：缺**消耗端**数据（玩家一天在打造/进化/涅槃上吃掉多少材料）。
   消耗 > 产出则多注入是健康的，反之 10% 都嫌多。→ 正确做法是**保守值上线 + 盯「每日注入材料量」+ 看真实数据再调**。
7. **待补（没做完）**：**宠物 / 宠物蛋挂单**的常驻收购（**材料与装备已于同日补上**，材料走独立函数 + 独立配额
   `materialDailyCap`）；开发者面板调参；注入量的面板观测。

### G. 材料/道具元数据：只有一份登记表（2026-09-14 用户拍板）

> 起因：背包「素材」分类笼统 + 悬停说明笼统；用户追问「**以后加道具怎么办？要不要给物品打 tag？要不要写进宪法？**」

1. **不打散 tag**。不给物品在 `Config.items` / `Config.craft` / 掉落表 / 商店 payload 各处分别打标
   —— 加一个道具要改 5 处，必漏一处（项目已经因「腐印名字必须与 items 一字不差」栽过，还专门写了测试补漏）。
   改为**唯一登记表**：`Config.materialInfo`（按材料名索引：`group` 分区 / `use` 用途 / `from` 来源）
   + `Config.materialInfoGroups`（分区名与顺序）。
2. **以后加材料只有两步**：① 掉落表 / `craft` / `items` 等处照旧加；② 回 `Config.materialInfo` 加一条。
3. **守值**：`docs/tests/vtest_material_info.js` 会扫全部「产出/消耗材料的地方」，
   **漏登记 = 测试红**，且报错会指出漏掉的名字来自哪张表。这就是"打 tag"想要的效果，但只需在一处登记。
4. **UI 不许另写名单**：背包「素材」二级分区、开发者面板的发材料下拉都读这一份；
   以后的图鉴词条页（对齐流亡编年史）也读它。
5. 以后要给材料加新维度（如"绑定/不可交易"）→ **扩这张表的字段**，不新开第二份名单。
6. 数据形态是 **JS 里的一份声明式表**，不用 JSON / Excel（用户 2026-09-14 问过）：
   本项目零构建、浏览器直读；JSON 写不了注释、Excel 要导出会必然产生"两份数据"。

---

### H. ⏳ 待拍板：装备属性（尤其吸血）超模 —— 2026-09-14 数值体检结论

> **状态：⏳ 待用户拍板，未做任何改动。** 本节只记结论，防止下次会话重新摸一遍。
> 完整报告：`docs/档案/数值体检_吸血与装备属性_2026-09-14.md`（配套图表 html 已删，数据都在报告里）。

**起因**：用户实测「约 28% 吸血的金装 → 通天塔 11 层；裸装 → 3 层」，怀疑是怪太弱 / 属性太强 / 属性给太多 / 公式有结构问题。

**测量方式**：控制变量 + 真造 12 件满级金装 + 蒙特卡洛（每档 24~30 局，z≥1.96 才算显著）。探针 `docs/tests/probe_lifesteal_audit.js` 为一次性件，**报告产出后已删除**。
⭐ **模型已复现用户实测**（裸装 3 层 ✓、中位金装 11~12 层 ✓、官方满配 21~23 层 vs 记录 21 层 ✓）→ 数据可信。

**四条结论（对应四个怀疑）**：
1. ❌ **怪太弱？不成立**。第 30 层守卫 vs 第 1 层杂兵：血 ×298 / 攻 ×88 / 防 ×87，每层 +12.4%。
2. ✅ **装备属性太强？只强在吸血**。裸装 3.0 层 → 满装 9.9 层（+6.84 层），其中**吸血独占 3.46 层 = 51%**；第二名速度仅 1.25 层（18%）。
   ⚠️ **附带事实：12 件装备的攻/血/防/命中贡献是 0 层**（满装比裸装只多 267 攻 / 545 血 / 127 防 ≈ 1~2%）→ 装备收益全在机制属性上，**掉落爽感被这条结构吃掉**。
3. ✅ **吸血是白送的**：护符部位**基底**（非词缀）**100% 出吸血**，中位贡献 **13.0%**，占整套吸血量的 **63.4%**。12 件合计中位 18.8%，词缀只贡献 7.0%。达到 28% 的套约占 4%（p99 32.5% / 上限 32.9%）。
   ⭐ 根因：护符基底 4 点会跟着**图档（×3.25）与底材档（×0.6~1.5）一起被放大** → 7.8%~19.5%。
4. ✅ **公式有两个结构问题**：
   - **问题 A（根因）：吸血的价值由「塔不给回血」这个规则放大。** 同一套装备——塔里占装备收益 51%，**野图挂机 1 小时只差 1~3 场（0.1%）**（野图系统每秒免费回 20% 血，吸血被淹没）。⇒ **不是吸血数值问题，是场景结构问题。**
   - **问题 B：吸血是滚雪球**（heal = 伤害 × 吸血率）。攻击 ×1.5 时吸血值 6.50 层，攻击 ×0.6 时只值 5.00 层（+30%）。
   - **换算**：10.8% 吸血 ≈ 攻/血/防全部 ×1.6；28% 吸血 ≈ ×2.5。
   - **吸血曲线是阶梯**：0%→3% 一步就 +1.6 层（全程最陡），0→6% 已吃掉「0→50% 总收益」的 34%。

**可调旋钮（待用户选，未动）**：A 砍护符基底 4→1/0（最小改动，砍掉 63% 白送吸血）；B 给吸血加硬封顶；C 加分段递减；D 改词缀出率（效果弱）；E 改成「按最大生命百分比封顶回血」（唯一能同时治 A+B，但需前后端两份战核同步）；F 改塔规则（层间回血）；G 什么都不动。

**改动前必须知道**：改任一项都会让塔的难度锚点漂移（「涅槃5 → 21 层」是拍过板的）→ **必须重跑 `docs/tests/sim_tower_balance.js` 重新校准**，并跑 `vtest_affix` / `vtest_pet_balance` / `vtest_enemy_balance` + `vtest_resource_matrix`，百科页/开发者页/资源矩阵同步。

**另有一条待核实**：塔的难度校准是 2026-09-10 做的，而装备生成在 **2026-09-11 重做过**（底材 T 从固定系数改为按 ilvl 抽）。本次测出 12 件金装中位吸血 18.8%，校准记录里写的是 16.8% ⇒ **动数值前建议先重跑一次塔校准，确认当前基线**。

#### H-1 ✅ 用户拍板（2026-09-14）：「塔不给回血」是刻意的核心体验

> 用户原话：「**是的，并且这个设计之初是后期主要玩法啊**」—— 确认「通天塔一管血打到底、中途不给回血」**是刻意设计，且塔是后期主要玩法**。

**推论（重要，别再把它当 bug 讨论）**：既然塔刻意不给回血，那么**吸血在塔里成为最强属性是设计正确的表现**，不是数值缺陷。
⇒ 原来 7 个旋钮里 A/B/C/D（都朝"削弱吸血数值"去）**优先级下降**；真正还要治的只剩**问题②（滚雪球）**。

#### H-2 ⭐ 重新定性：这**不是平衡问题**

调数值能拿到的最好结果 = 「吸血 28%→15%、塔少上 1~2 层」，但野图里吸血**仍是 0 价值**、塔里吸血**仍是唯一回血手段** → **病根一动没动**。
- **一句话定性**：游戏里能回血的只有两处 —— ① **系统白送**（每秒 20%，**野图开着、塔里关着**）② **吸血属性**。
  ⇒ **吸血不是「可选属性」，它是塔的「入场券」。** 病在「回血」的**供给结构在两个场景不一致**。
- **拆成两个独立问题（药不同，别混着治）**：
  - **问题①「吸血只在塔里值钱」= 场景规则问题**（回血供给不一致）→ 药是改规则；但 **H-1 已拍板不改塔** ⇒ 问题①**接受为设计**。
  - **问题②「吸血是滚雪球」= 公式问题**（`heal = 伤害 × 吸血率`）→ 药是改算法（如「每次回血不超过最大生命 ×N%」，POE 做法）。

#### H-3 ⭐⭐ 真正该先修的根因：**装备的攻/血/防加在了「底座」上**

实测（腐噜兽 Lv60 成长 100，`docs/js/pet/pet.js` 的 `statParts` + `getStats`）：

| | 底座 core（`base + lv×系数`） | 成长增量 growth（`lv×成长×系数`） | 倍数 |
|---|---|---|---|
| 攻 | **165** | **14,137** | 86× |
| 血 | **404** | **29,106** | 72× |
| 防 | **72** | **6,059** | 84× |

`atk = round(core.atk × (1 + pct.atk) + growth.atk + flat.atk)`
⇒ **装备的 `攻%/血%/防%` 词缀只放大那个 165 / 404 / 72**，成长增量完全不参与。
⇒ 实测 12 件金装总共只给 **+267 攻 / +545 血 / +127 防 = 1~2%**；**即使装备给 +100% 攻，也只多 165 攻（总攻的 1.15%）**。

**对比：机制属性（暴击/暴伤/速度/闪避/命中/吸血/穿透/减伤）是直接加在【最终面板】上的点数** →
12 件装备把它们从 暴击 16%→53%、闪避 13→58、速度 80→121、吸血 0→10.8%，**100% 有效**。

⇒ ⭐ **结论：不是「整套公式要重写」，而是「攻/血/防这三项的词缀表达与现实不符」这一处。**
⚠️ **但「把 pct 的作用域放大到成长增量」这条路已被 2026-09-01 拍板否掉** —— 成因见下面 **H-6**（用户当年就担心「装备的百分比 × 宠物成长值会失控」）。
因此**正解不是拆刹车，而是换一种表达**（见 H-6 的 A/B/C）。

#### H-6 🔴 用户早在 2026-09-01 就装过这道刹车（2026-09-14 追认，**主 agent 曾提错方案已撤回**）

> 用户原话：「**很久之前我就讨论过这个问题，我当时是觉得这个装备上的百分比 × 宠物 × 成长值到时候会失控，会大到没边**」

**⇒ 「pct 只作用于 core、不放大成长增量」不是"防通胀的副作用"，而是用户刻意的设计刹车。**
**主 agent 在 2026-09-14 曾建议「让装备的 % 也乘成长值（候选1）」= 拆掉这道刹车 → 已撤回，不得再提。**

**两边其实都对，只是层面不同**：
- 主 agent 实测的是「**当前这一套装备（+41% 攻）**下相对收益恒等于 41%，不随成长值变」→ 结论「不爆炸」。
- 用户顾虑的是「**这个方向**迟早失控」→ 层面更高，且**成立**：现状是「成长值」与「装备 %」两条**互不相干的加法线**；
  一旦让 % 乘成长值，就变成「**成长 × 装备**」的**乘法耦合**，而成长线是**无上限**的（神级涅槃），
  再往后每加一个新系统都是一个新乘区 → **乘区相乘 = 数值失控的标准剧本。**

**⚠️ 别把「加硬顶」当成解法**：实测装备的 `攻击%` 理论硬顶 = 12 部位 ×（每部位同类型词缀只出 1 条 —— `equipment.js` 用 `pool.splice` 去重）× T1 8% = **+96%**。
单看这一条确实有界（有效战力 ×1.96 封顶）。**但这不改变"开了乘法通道"这件事** —— 用户防的是通道，不是这一条的上限。

**⭐⭐ 真正该改的不是"百分比作用在哪"，而是"给一个只作用在 1% 分母上的东西贴百分比标签"**：
现状下「攻击 +8%」这条词缀**实际只加 13 点攻**（8% × 底座 165）。**玩家看到「攻击+8%」却只有 13 点 → 名实不符。**
**这才是"属性好像需要重新设计"的真正来源。**

**三条路线（待用户拍板，均未动手）**：

| | 做法 | 是否动刹车 | 代价 |
|---|---|---|---|
| **A（零风险，建议先做）** | 攻/血/防这几条空词缀**要么改成固定值、要么从词缀池拿掉**（装备专管机制属性）→ 名实相符 | **不动** | 装备维度变少；「武器给攻击」的直觉消失 |
| **B（保留刹车且让装备有感）** | 把装备的「%」改成「**固定值，但按穿戴者等级给足量**」（如武器基底随 ilvl 缩放）→ 永远跟着等级长、有手感，**且永不与成长值相乘** | **不动** | 需要重定标 `baseValues` 与词缀数值；前期（低等级）平衡要复核 |
| **C（已撤回，不推荐）** | 让 % 乘成长值 | **拆掉** | 存量玩家战力当场 +42%（攻 14712 → 20566）→ 塔难度全废、经济受影响；**且违反 2026-09-01 拍板** |

**✅ 用户已答（2026-09-14）**：
> 用户原话：「**百分比乘成长值，因为最开始的想法是 最终攻击 = 宠物基础 × 等级 × 成长 × 装备词条，一算这个就是指数爆炸**」

⇒ **否掉的是「百分比乘成长值」这个具体做法**（不是「装备给攻血防」本身）。
⇒ 原设计意图是**四轴连乘**（基础 × 等级 × 成长 × 装备词条）→ 用户自己算出「这是指数爆炸」→ 所以落成今天的「**装备与成长值 = 两条互不相干的加法线**」。**这道刹车是他自己算出来装的，不是顺手定的。**

#### H-7 ⚠️ 因此 **B 路线（固定值随等级/图档给量）是伪选项，已划掉**

**理由（纯算术，不用测）**：装备要在攻血防上有感，数值就必须与「成长值」**同量级**；
但「不乘成长值」的参照系只有两个 —— **等级**（Lv1-60 = 6 倍跨度）和 **图档**（1 → 3.25 = 3.25 倍跨度），
而**成长值是 5 → 350+（≈70 倍）且无上限**。**没有任何"不乘成长值"的尺子能追上它**；追上了就是耦合。
⇒ **B 是"形式上不乘、实质上必须同步"，绕一圈还是 C。**

**⇒ 最终结论：在「成长值无上限」这个前提下，
「装备在攻血防上有终局手感」与「数值不失控」不可兼得 —— 没有第三条路。**

#### H-8 ⭐ 但要纠正主 agent 一个说错的地方：装备攻血防**不是"完全没用"**

- **前期（低等级 / 低成长）装备的攻血防是主力**：宠物面板还小（Lv4 成长 5 总攻仅约 70），装备给的固定值显得很大。
- **是后期被成长值淘汰**：Lv60 成长 100 时成长增量 14,280 碾压装备的 267。
- ⇒ 准确说法是「**在塔（Lv60 成长 100+）里它们是空的**」，而不是「从来没用」。
  （主 agent 2026-09-14 说"6 项空属性"说过头了，此处纠正。）

#### H-9 ⭐⭐ 好消息：第一爽点「掉宝/鉴定」**是安全的**

装备的**机制属性**（暴击 / 暴伤 / 速度 / 闪避 / 命中 / 吸血 / 穿透 / 减伤）**永远不会被成长值淘汰** ——
宠物基础暴击只有 8%、闪避 5，装备能拉到 53% / 58%，**这些加成与成长值毫无关系、不衰减**。
⇒ **掉到暴击 / 吸血 / 速度装永远有感。根本没有"掉宝被架空"这回事。**
⇒ 真正让玩家失望的只有一件事：**掉到「攻击 +8%」这种"假百分比"词缀**（实际只加 13 点）。
**所以最该修的只有一件事：别让这些词缀骗人。**

#### H-10 ⏳ 真正的大问题（迟早要答，主 agent 已抛给用户）

> **「神级涅槃无成长上限」这条长线，要不要留着？**

- **留着** → 装备在攻血防上永远是陪衬 → 接受它，工作重点转成「**把攻血防词缀做诚实**」（改固定值或移除，消除假期待）。
- **去掉（给成长值设软上限）** → 装备才可能在攻血防上有终局手感，公式才有得做；但要重做塔与经济，且动到长线设计。
- ⚠️ **必须提醒用户**：**一条没有上限的成长线，是所有数值失控的燃料** ——
  以后每加一个新图 / 新装备档 / 新养成系统，都会撞同一面墙。这个问题不解决，它会反复出现。

#### H-11 ✅ 用户拍板（2026-09-14）：「成长值上限」不做；并**翻玩法文档查出了正解**

> 用户原话：「**第三步目前不想做，这个游戏最开始得出发点就是为了成长吧，虽然说无上限到后期的确很空虚**」

⇒ **「成长无上限」是游戏出发点，是 H-10 的答案：留着。** 并由此把工作重心定为：**在无上限的前提下，解决"后期空虚"与"装备价值"**。
⇒ 用户同时要求：**去翻玩法文档（口袋精灵2）+ 查市面做法**（已完成，一手资料见 `.codebuddy/memory/topics/竞品参考-口袋精灵2.md`）。

**⭐⭐ 查证结果：本项目 2026-09-01 那道刹车「装错了位置」**

- 口袋精灵2 手册原文（8.属性相关）：「装备、称号、buff 的**百分比加成的基数为基础属性**……**基础属性受 cc、五行属系、品级、等级等影响**」
  ⇒ **它的装备百分比确实乘成长值** —— 正是本项目 H-6 否掉的那个做法。**而它没崩。**
- **它不崩的原因：刹车装在【成长值的获取速度】上，不在【装备的作用域】上**。原文：
  > 主宠 cc 越高，涅槃的提升就越低：成长 ＞ **14w** → 实际获得成长 **×0.7**；＞ **20w** → 再 **×0.65**；
  > ＞ **25w** → 再 **×0.45**；**月增长数据不受削减影响**。
- ⭐ **这是「分段阻尼」，不是「上限」**：成长值仍可无限涨（玩家一个月涨 3w cc），但增速被一段段砍下去（三段叠加 ≈ 剩 20%）。
  且**不砍「月增长」** → 月度目标永远可达（它有「每月增长达到 300cc 即可正常速度刷图」这条官方节奏标尺）。

**⇒ 结论：把刹车从「装备的作用域」搬到「成长值的获取速度」上 ——
「数值不失控」的效果完全一样，但代价天差地别**（前者把装备的一半属性变成废物，后者什么都不废）。
**这解开了 H-6 的死结：不需要在「装备有终局手感」和「不失控」之间二选一。**

**其余可借的机制（详见竞品文件）**：
- **速度用「相对比值」**（我速/敌速 → 伤害 ×0.1~×1.65）→ 永不失效；本项目的速度是绝对出手频率、且被 620ms 固定冻结压制。
- **命中用「门槛式破防」**（命中 > 敌闪避即无视敌防御）→ 「够用即可」；本项目的连续命中率**顶到 95% 上限后完全变废**（实测贡献 0 层）。
- **后期内容靠「层叠短周期系统」**：它有 **11 个装备维度**，每个都有自己的升级资源 + **明确上限**（强化 +20 / 宝石 10 级 / 部位突破 20 级 / 印记满噬魂）；
  **成就**（魔卡志）是「属性的主要来源之一」；多个 buff 是「**月榜**」奖励；品级 10 档但只动一面（不影响命中/速度/闪避）。

**⏳ 修订后的方案（主 agent 拍板版；H-6 的 A/B 二选一作废）**：

> ✅ **2026-09-15 执行记录（grill-me 拷问后用户逐条拍板落地）**：
> ① **词缀统一规则**：**T1 = 百分比（乘全属性）、T2~T5 = 固定值**（全属性适用；pen 穿透暂保持固定值另行立项）。
>    H-6 的「不得再提」就此改判：**T1 百分比乘全属性 + 涅槃分段阻尼 + %总预算收紧，三件绑定** —— 阻尼阈值键控在
>    【主宠当前成长值】（防交易绕过），成长仍无上限。旧 maxGrowth=100 硬停一并退役。
> ② **命中/闪避升格为主体属性**：与攻/防同构跟等级+成长走（config.pet.mechCoeff，成长增量刻意压小）；
>    **怪物也有命中/闪避**、跟怪等级走（battle.enemyMech / tower.mech / trial.mech，写死 5/8/12/160 退役）。
> ③ **护符基底吸血清零**（吸血变纯词缀）；T1 概率按装备等级分段上调（ilvl55+ 15% / 90+ 18%）。
> ④ **塔重校准**：perFloor 0.124 → 0.21，梯子回到旧锚点（低 0 / 中 11 / 涅1 15 / 涅3 20 / 涅5 21）。
>    实测满装对总攻贡献 1.9% → ~14%（30 套中位），命中率曲线：新手图 ≥95%、图10 不堆 ~45%/毕业 ~64%、塔 30 层毕业 ~47%。
>    校准探针 docs/tests/probe_mech_recal.js（定稿后删），?v= 20260915mech1。

| 步 | 内容 | 风险 / 代价 |
|---|---|---|
| **1** | **装备属性大扫除**：机制属性保留；攻/血/防改固定值；**命中、穿透从词缀池删掉**（实测贡献 ≈ 0，是"假签"） | 几乎为零（这些属性本来就没在起作用） |
| **2** | **把「吸血是塔的必答题」做成明牌**：塔入口/百科明说"塔不给回血、续航第一"；腐印反制再添变条；让其他属性在野图当主角 → 形成**两套 build** | 纯文案/引导，零风险 |
| **3+4** | **一起做**：① 让装备的 % 作用于基础属性（= 恢复它有终局手感）② 同时给**涅槃增量加分段阻尼**（照口袋精灵2 的形式，**不是上限**）③ 后期内容补「层叠短周期目标」（周榜/图鉴收集/月增长标尺） | **最大**：存量玩家战力会当场上涨（实测 +42%，攻 14712→20566）→ **必须重做塔的难度校准**；且①与②**必须同批做**，分开做等于先拆刹车再装新刹车 |

#### H-4 ⚠️ 用户提出「属性好像都需要重新设计，包括计算公式」→ **已提醒不宜大改**

三条理由（都已说给用户）：
1. **老装备会立即变值**：改口径 = 玩家身上已有装备的数值当场变化（可能变强可能变弱）—— 这是**玩家可感知的资产变动**，线上有真实访客，不能试错。
2. **塔的难度锚点会整体失效**：而塔刚被确认为**后期主要玩法**（H-1），它的「涅槃5 → 21 层」是拍过板的校准线，必须随之重做。
3. **战力是平方级**（攻×血同涨）：宪法早已记录「**是悬崖不是斜坡**」→ 动一处会全盘偏移，**只能按目标画像反推，不许凭手感**。

#### H-5 ⭐ 更该先想的问题（直接服务「塔是后期主要玩法」）：**塔这张考卷只考「吸血」一道题**

塔里不回复血 ⇒ 玩家必须有「续航」。但实测**续航只有吸血一条有效路**：

| 属性 | 摘掉后掉几层（塔） |
|---|---|
| 吸血 | **−3.46 层** |
| 速度 | −1.25 |
| 暴击率 / 暴伤 / 闪避 | −0.75 / −0.67 / −0.67 |
| 受伤减免 | −0.33 |
| 生命 / 防御 | **0** |

⇒ 后期玩家的**最优解 = 堆吸血**，而吸血有 **63.4% 是护符白送的**（无脑戴一个护符即可）。
⇒ **一个只有唯一解的后期主玩法会很快玩腻。**
⭐ **让塔好玩的方向 = 让「续航」有多条来源**（减伤 / 闪避 / 血池 / 输出速度都能转化成续航），玩家才有 build 选择。
这属于「**丰富它**」而不是重设计 —— 而且**不需要改任何已有公式**。

**下一步（已提议给用户，等他选）**：**体检第二期（纯测量、不动线上数值）** ——
① 装备攻血防该用什么加法位置（3 个候选各算爆炸边界 + 对老装备的影响）
② 塔的「续航」多源化：若减伤/闪避/血池能提供与吸血同级的续航，攻略会变成什么样
③ 成长值把战力做成平方级这件事的影响面

### I. 神级宠的用途与边界（2026-09-17 用户拍板）

**神级宠可以当涅槃副宠（神宠互相吃）—— 这是设计好的成长路径，不是漏洞。**

- 用户原话：「**本身就不用啊，之后肯定是神宠互相吃啊**」（回应我提议"把神级宠从副宠候选里过滤掉"）。
- ⇒ **两条不许做的事**：
  1. ⛔ 不许在涅槃的副宠候选里过滤掉神级宠（`Merge.getMergeCandidates` 只筛 等级 / 在售 / 已穿装备）；
  2. ⛔ 不许把它当"资产流失风险"去加保护性拦截 —— 玩家有权把一只神宠喂给另一只。
- ✅ **必须做**：被选中的副宠若是神级，界面要**显式标出来**（副宠卡片 `.pet-card2.god` + 「· 神级」、
  备选条「· 神级」、底部确认条「★ 神级」）—— 主宠卡片一直标着，副宠以前不标，
  玩家会把一只神宠当普通肥料点掉（不可逆）。
- ⚠️ **与合成页的差别（刻意相反，别搞混）**：合成里神级宠**不准当素材**（`pet_merge.js` `synthesizeInner`
  开头就拒，防止把神宠合没）；涅槃里**可以当副宠**（副宠被吸收 = 本意）。
- 守值：`docs/tests/vtest_nirvana_ui.js`（神级宠必须出现在候选里 + 三处标记都在）。

---

### J. 进化分支：**不加数值差异**（2026-09-22 用户拍板）

- 背景：内测清单 🟠14「血牙狐和幽火狐不知道哪个强，选哪个都一样」。
- 🔴 **取证事实**：两条分支**数值完全一样**（`Config.pet.speeds` 同值、成长提升同、门槛同），
  后续形态链只是换名换立绘。**真实差异只有两样**：① 最终长成哪个终形态 ② 那个终形态的主动技能。
- 用户拍板：**不加数值差异**（我建议不加：会动平衡，且与"8 宠拉平 / 不学分品级"定调冲突）。
  ⇒ 分支的意义 = **选技能口味**（血月斩 13%×2.0 爆发 ｜ 幽火焚身 20%×1.5 稳定），不是选强弱。
- ✅ 必须做：**把差异摆出来**，不造假 —— 方向卡显示「终形态 X」+「终形态会学 Y：概率/倍率」，
  并配一句实话「两条强度一样，选错也不会变弱」。
- ⛔ 不许做：不许偷偷给某条分支加攻击/血量来"制造差异"。
- 实现唯一入口：`PetUI.finalFormOf / finalSkillOf / skillEffectLine`（ui-pet.js），
  进化页与宠物资料页共用（🟠11 主动技能说明也用同一份）。

---

## 三、上线前必做清单（未完成项）

| # | 事项 | 状态 | 说明 |
|---|---|---|---|
| 1 | 服务端权威（Server Authority） | 🔴 待做 | 数值结算从客户端搬到服务端，否则推广即被拆 |
| 2 | 迁移应用记录 | ✅ 已建台账（2026-09-23） | 见 **`docs/迁移应用台账.md`**（45 个迁移文件逐个对照线上 32 表 / 53 函数 / 2 条 cron）。**真缺口 1 个**：`resource_trial_runs` 线上不存在（副本 EF 依赖它；现在不痛，副本服务端权威化那天会炸 ⇒ 列为该工程第一步） |
| 3 | AI 阶段 2（服务端 AI） | 🟡 待做 | 真共享市场需 Worker；当前阶段 1 各浏览器各自市场 |
| 4 | 毕业图内容（图 11-17） | 🟡 待做 | 当前锁死占位；AI 毕业档 persona 需毕业装需求 |
| 5 | 云端 bot 交易记录买家昵称 | ✅ 已完成（2026-09-11） | 三个 `bot_buy_*` 改为接收 `p_buyer_name`，由客户端在收购那一刻从当次会话的 persona 取（`market.js` 的 `botBuyerName()`）；历史 2 行已回填。服务端不存 persona 名单——它每次会话随机生成、不落库，服务端无从得知「这次是谁来买」 |
| 6 | **通天塔服务端权威 EF**（塔的奖励与成绩） | ⏸️ 暂停 | 2026-09-10 用户拍板"奖励与成绩都走 EF"；做到一半因「野外战斗经常有问题」**先修野图**而暂停（用户指示先记入本清单）。详见下方备注。 |
| 7 | **安全守卫收口重放**（`bot_buy_material` 无守卫） | ✅ 已完成（2026-09-11） | 已执行 `supabase/migrate_security_reapply.sql` 到线上。修复前 anon 直调 `bot_buy_material` 返回 `200 "notfound"`（无守卫），修复后三个 `bot_buy_*` 一律 `401 42501`；29 个业务函数权限面回归无误伤。同批修复见 `docs/代码审计_2026-09-11.md` |
| 8 | **REST 直改资产的剩余面**（`pets`/`equip_items`/`pet_egg`） | 🔴 待做（**2026-09-13 拍板：不加软护栏过渡，随 #1 一起做**） | **施工图见 `docs/待办/资产表收口调研_2026-09-13.md`**（写路径 / 需新建的 8 个 RPC / 会挂哪些功能 / 开工检查清单）。要点：① **实测 RLS 是开着的**（未登录读不到三张表，**不是**「任何人能看所有人」），真问题是登录玩家能改**自己那行**的任意值；② 这三张表**全部写操作都由客户端直连 REST、无任何 RPC 替代**，现在 revoke 会当场挂掉出战切换/战后升级/进化/涅槃/融合/觉醒/穿脱装备/**全部打造行为**/掉装入库/掉蛋 —— **线上有真实访客，不能试错**；③ 收口前需新建 `create_pet`/`update_pet`/`delete_pet`/`create_item`/`update_item`/`delete_items`/`grant_egg`/`consume_egg`；④ ⭐ **加 CHECK 不等于安全**：只能拦 `level=99999`，拦不住 `level=60`（合法值），真正的防线是"服务端知道这只宠本该多少级"；⑤ 🔴 最早出事的单点：`pets.equipment` jsonb **无归属校验**，可把**他人装备 id** 填进自己槽位 = 直接侵害他人资产（修时注意别写成全 `before update`，否则 `buy_pet` 换主人会挂）。已收口：`materials` + `profiles.banned`（见 `migrate_asset_write_hardening.sql`） |

> **#6 备注（2026-09-10 暂停时的进度与前置缺陷）**
>
> - **已完成**：塔的数值源 / 词缀 / 资格 / 引擎 / UI / 腐印产出 / 契约测试（`docs/js/tower/` 9 个文件，`vtest_tower.js` 86 断言全过）。
> - **未开始**：`supabase/functions/_shared/tower-core.mjs`（整局 30 层 × 5 只 = 150 只怪的模拟）、`tower-run` EF（资格校验 / 原子发奖 / runId 幂等 / 周榜写入）、`supabase/migrate_tower.sql`、客户端切权威源（回放服务器录像）。
> - **必须先修的两个前置缺陷**（都已用代码核对）：
>   1. `supabase/gen_server_config.js` 的提取白名单**缺 `equipment`** → 服务端没有装备生成能力，塔的"高级装备"发奖落不了地。要么把装备生成器搬到 `_shared`，要么按现有 `battle-settle` 口径**先只发材料**。
>   2. ~~**两份战核已漂移**~~ ✅ **已于 2026-09-11 修复**：前端源 `docs/js/core/battle-sim.mjs` 已同步服务端的神级宠修正（`godDefOf` + `resolveLineId`/`getBaseSpeed`/`getStatCoeff` 三处神级宠分支），`battle-sim.global.js` 已重新生成，`vtest_sim_sync.js` 名单补上这 4 个函数（13 → 17 个，现在两份逐函数全等）。
>   **实际影响（审计确认）**：漂移会让**塔的进塔预估偏低** —— `tower/tower-preview.js:63` 用前端那份战核算 `petStats`，而塔是神级宠专属内容，于是带神级宠的玩家看到"风险：高（会死在半途）"，实际能通。三份战核里**只有前端那份在浏览器里真的被调用**（`window.BattleSim`），其余调用点全是测试。
>   ⚠️ **根因未除**：服务端副本仍是**手工同步**的（EF 打包不能引用仓库外文件）。`vtest_sim_sync.js` 是唯一的护栏，**它漏了函数就等于没守** —— 以后往战核加函数，必须同步加进该测试的 `NAMES`。

---

## 四、维护规则

1. 新拍板 → 追加到对应主题，标日期，写明来源（用户原话/文档名）。
2. 推翻 → 原文标"已推翻（日期）"，不删原文（留痕防再次摇摆）。
3. 冲突 → 以本文件 + 最新日期为准；无法判断先停下来问用户，不自行裁决。
4. 本文件由 MainAgent 维护；其他 agent 发现问题可提议修改，但落地统一走 MainAgent。
5. **改 `config.js` 数值/公式/归属后的必查清单（2026-09-09，因"第二副本"失同步立规）**：
   - 跑全量 `docs/tests/vtest_*.js`；
   - **百科** `ui-codex.js`（公式描述、材料说明是否与 config 一致）；
   - **dev 面板** `ui-dev.js`（凡是"预估/模拟"，必须调用唯一定义的函数如 `Battle.calcDamage`，禁止手抄公式）；
   - **归属账本** `docs/资源归属矩阵现状.md` + `vtest_resource_matrix.js`（新增/移除资源来源必须同步）；
   - 原则：**能从 config 派生的展示一律派生，不手写第二份**（先例：market.js 收款白名单、ui-dev 强度模拟）。
6. **迁移脚本不许重放（2026-09-11 审计立规）**：
   - 同一函数被多个 `migrate_*.sql` 重复 `create or replace` 过（`bot_buy_equip`/`bot_buy_pet` 各 3 份、`buy_equip`/`buy_pet`/`buy_egg` 各 2 份、`battle_settle` 2 份），**谁后跑谁生效** —— 所以文档里那句"迁移脚本幂等、可重复跑"是假的，已从事实源表格里划掉。
   - 规则：**安全守卫类函数（`bot_buy_*` + 权限收口）的唯一定义在 `supabase/migrate_security_reapply.sql`**。该文件幂等，任何时候跑完都是对的。
   - 改函数请**新建文件**，别回头改已执行过的文件；确实要重放旧文件，跑完必须再跑一次 `migrate_security_reapply.sql`。
   - 已加「🔴 禁止重放」头注释的：`migrate_bot_buy.sql`（最初版无守卫）、`migrate_trade_records_ref.sql`（5 个函数抄的是守卫上线前的旧版）、`migrate_material_listings.sql`（`bot_buy_material` 原文无守卫）。
7. **测试存量红（2026-09-11 基线，跑全量时别当成自己改坏的）**：
   - 🔴 **2026-09-23 更新：存量红已清零（全量 94 通过 / 0 失败，baseline 已重设）。**
     三个长期红的真因**全是"测试写死旧常量"**，不是代码坏：
     · `vtest_action_freeze` / `vtest_pet_skill`：把 `speedScale` 写死成旧值 **12**（现 **18**）
       ⇒ 行动条永远填不满 ⇒ "该出手时没出手"。改成从 `Config.battle.speedScale` 推导。
     · `vtest_bugfix`(Bug3)：断言比对占位 emoji `🐶`，而 emoji 占位 2026-09-10 随立绘接入已移除
       ⇒ 改成查立绘归属（`#pet-icon` 的 `dataset.pet` == 出战宠名）。
     ⭐ 教训：**测试里不许写死会变的常量**；看到 `for (i<13)`、`=== 12` 这类数字先怀疑它。
     （**旧名单留痕**，防以后有人照旧文档找"存量红"：~~稳定红 5 个：`vtest_action_freeze` / `vtest_boss`(D4) / `vtest_bugfix` / `vtest_equip_score` / `vtest_pet_skill`~~）
   - **flaky 2 个**（时红时绿，**别把它的转绿当成自己的功劳**）：`vtest_enemy_balance`（蒙特卡洛随机）、`vtest_botbuy`（异步等待，单独跑 4/4 通过但全量里偶发红）
     ~~`vtest_tier_rarity`~~ 已于 2026-09-12 摘掉 flaky 帽子（见下），现在 3 个变 2 个。
   - ⚠️ 判定 flaky 的方法：单独连跑 3~4 次，全过 = 大概率是 flaky；再跑一次全量复验。**别急着当成自己改坏了或改好了。**
   - ✅ **2026-09-13 实测基线（本次核对宪法时真跑的）**：**70 通过 / 5 失败，共 75 个**，耗时 158 秒。
     失败的 5 个与上面「稳定红名单」**逐条吻合**；且 `tier_rarity`/`enemy_balance`/`botbuy` 本次全绿 → 印证 tier_rarity 的 flaky 定性成立与否都已了结。
   - ⏱️ 全量约 2.6 分钟。最慢 `vtest_fun.js` 61s —— **>5s 的多半是有余留定时器没退出**，值得单独看（不是慢，是进程不肯退）。
   - ⚠️ **`run_all.js` 只跑 `vtest_*`（75 个）**，仓库里另有 9 个非 vtest 脚本（`sim_*`/`probe_*`/`test_trade` 等）不在回归里，别指望它帮你跑。
   - ✅ 已修正：~~`vtest_tier_rarity` 是 flaky~~ —— 2026-09-12 查明它**从来不是 flaky，是断言本身在要求一个不存在的必然**
     （一字千金：PoE 规则下 ilvl 55 的池本就含 T4/T5，断言却要求必然 T3）。已改为 300 次抽样分布断言，连跑 4 次全绿。
     **它若再报红，是真回归，别再按 flaky 放过。** ⭐ 教训：标成 flaky 之前先算一遍规则。

---
## 五、⏳ 待补录（2026-09-13 核对时发现：这些已落地/已拍板，但本文件还没记）

> 存在这里的意义：**防止它们永远停留在某次会话的记忆里，然后过几天再被讨论一遍**（这正是本文件诞生的原因）。
> 谁有空谁补进上面对应小节，**补完删掉这一行**。

| 决策 | 日期 | 出处 |
|---|---|---|
| 技能 = **16 个主动自动技**（触发概率×(倍率−1) 全压在 +13~18% 区间）；此前「纯被动 16 个」方案**已推翻** | 2026-08-31 | `config.petSkill` |
| 进化 5 阶 = Lv10/25/40/60；⭐ `evolve_times = 进化阶段 − 1`，**上限判定只认 `nextStageOf` 返回 null** | 长期 | `config.evolution` |
| 装备 12 部位；稀有度＝词缀条数（1白/2蓝/3+金）；词缀 T 阶走 PoE 模式；未鉴定拦截打造；**锁定石只保一次打造** | 长期 | `config.equipment` |
| 任务领取记录**独立成表** `quest_claims`，客户端零写权限；旧 `quest_progress.claimed` 列方案**已废**（全表 403） | 2026-09-12 | `migrate_quest_claims_table.sql` |
| ⭐ **PostgREST 只认【表级】`has_table_privilege`** —— 保护某列**绝不能**靠"收表级写权限"，必须用独立表 / 触发器 / RLS with-check | 2026-09-12 | 事故复盘 |
| ⭐ 判定事实搬服务端后，**本地那份副本就成了"显示状态"**，必须有回灌通道，否则玩家「看得见点不动」 | 2026-09-12 | 事故复盘 |
| 🔴 改 JS/CSS **必升 `docs/游戏.html` 对应 `?v=`**，否则浏览器不拉新版 | 长期 | 全项目最常被误判成 bug 的现象 |
| 🔴 **给 `.tab-page` 写样式永远不要碰 `display`**（ID 特异性碾压 → 多页同时显示） | 2026-09-12 | 事故复盘 |
| ⭐ **长期被开关隐藏的功能，打开前必须实机看一眼**（接口/权限/测试全绿 ≠ 样式存在） | 2026-09-12 | 商店页教训 |
| 🔴 `.gitignore` 里**父目录一旦被排除，其中内容无法用 `!` 放行**（判生效看 `git status`，不能看 `check-ignore`） | 2026-09-13 | `.gitignore:31-33` |
| ⭐ **新手引导 = 聚光灯压屏挖洞**（`ui-onboarding.spotlight`），推翻 2026-09-02「引导不要弹窗/高亮/HUD」拍板 | 2026-09-13 | `ui-quest.js#guideHint`；引擎 09-03 就写好了，本次只把 G1~G9 从 hotspot 画圈换成 spotlight 压屏 |

---

*最后更新：**2026-09-13**（事实核对修正；新增 §二 F「市场买家缺失 → 服务端常驻买家」+ §五 待补录区）*

*来源：现状地图_v1.md（已移除） / AI假人经济系统策划_v1.md / 宠物特质与魂铸系统·实施提示词.md + 用户多轮拍板 + 2026-09-11 代码审计（`docs/代码审计_2026-09-11.md`）+ 2026-09-12~13 事故复盘（`.codebuddy/memory/`）*
