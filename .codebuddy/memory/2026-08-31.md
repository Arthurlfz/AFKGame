# 2026-08-31

## 掉落池重做（改法一：单池·一场一抽）— 已完成
- 改了 `docs/js/core/config.js`（drop.pool + drop.materialWeights，删掉旧 8+ 独立 chance 字段）、`docs/js/core/drop.js`（rollReward 单池一次抽取 + weightedPick/pickMaterial 助手，保留 eggMap/totalEquipDrops/equipDrop 上报/云端写入）、`docs/js/ui/ui-battle.js`（showLoot 按 type 处理）、`docs/js/ui/ui-market.js`（进化材料改从 Config.trade.materials 取，因为删了 Config.drop.evolutionMaterials）。
- 配套修了两处 stale 引用 + 两个测试脚本（vtest_quest_material.js / vtest_evolution.js）适配新 config。
- 自测：20万场模拟验证单件不变量 + 分布吻合（none75.8%/material21.2%/装备1.49%/蛋1.53%）；两个原测试全绿。
- 用户拒绝的方向：保底/幸运值、新增物品（秘匣/极品装备/精英Boss）、养成缺口（觉醒/成长丹/图鉴）。养成系统（孵化/进化/涅槃/合成变异）在 FoF 里其实已存在，不是空白。
- 提醒：测试从 `docs/tests` 目录跑（harness 用 `../js/core` 相对 cwd）；PowerShell 下 `cd /d` 会失败导致路径错位，属测试脚本预存路径问题，与本次改动无关。

## 掉落分布按图档重做（low→high + 出现时机）— 已完成
- 用户纠正：**没有"第二幕"概念，就是一条 17 图连续进度**；掉落应做成按图档从低到高爬升 + 每种材料有"出现时机"门槛。
- 改动：`docs/js/core/config.js` 把全局 `drop.materialWeights` 换成 `drop.materialWeightsByTier`（17 张图各一张表，权重随图档爬升）+ `drop.evoMaterialWeights`（普通50/精粹70/传说100，档位内高档更常见）；`drop.js` 的 `pickMaterial` 改为按 areaIdx+1 取本图档表（缺省键=该图还不出），进化素材占位键'进化素材'按 areaEvolutionTiers+evoMaterialWeights 解析具体档名；区域材料权重仍在表内按'区域材料'键承载。
- 清掉死配置 `drop.areaMaterials[].chance`（单池改造后根本不读，worldmap/图鉴只读 .name），注释说明。
- 出现时机门槛：涅磐兽 图1-9 不出现、图10（Lv60 涅槃）起出现并爬到图17 权重75；合成之石/神圣石 图1-6 不出现、图7（Lv40 合成）起出现；传说进化素材 图1-4 不出现、图5 起出现；重铸石/增缀石 早期最高、深处淡出。
- 测试：`vtest_drop_tier.js` 加了第 5 段守卫（覆盖 1-17、出现时机、low→high 单调、drop.js 必须读 materialWeightsByTier 且弃用旧全局）全绿；另写临时 `_sim_drop.js` 模拟 20 万场/图验证分布（跑完验证，待用户删）。
- 注意：`drop.pool`（none/material/equip/egg 四向总盘）本次**保持全局平**（76%/21%/1.5%/1.5%），未随图档缩放——用户只要求材料子权重 low→high。若要"深处掉得更频繁"（连总盘也随图升），需另议，暂未动。

## 世界地图信息卡新增「材料掉落分布」展示 — 已完成
- 用户要求把本次按图档重做的掉落分布（materialWeightsByTier）在大地图悬停信息卡上展示，并用 ui-ux-pro-max skill 排版。
- 改动：`docs/js/core/worldmap.js` 的 buildPreview 新增 `dropDist`（按 areaIdx+1 取 materialWeightsByTier[tier]，算占材料分支百分比 pct 与相对条形长度 bar；区域材料显示专属名，进化素材附 variants 档位）；`docs/js/ui/ui-worldmap.js` 的 tipHTML 用「材料掉落分布」块替换旧的"专属材料/进化素材"两行——条形长度=相对权重、数字=百分比（双表达，遵循 skill 的 Charts 规则：不只靠颜色，且静态不动画 width）；`docs/css/ui-polish.css` 加 .wm-tip-sub/.wm-drop-* 样式并放宽卡片 max-width 200→240px，沿用现有暗色主题 CSS 变量保持一致性。
- 进化素材档位在信息卡用短标签 普通/精粹/传说（去掉冗长素材全名）。金装概率行保留。
- 测试：`vtest_worldmap.js` 加第 7 段守卫（行数匹配 materialWeightsByTier 键数、占比和≈100%）全绿；旧守卫（mat/evoTiers/gold 字段）未破坏。临时 `_sim_drop.js` 已删。
- 百科（ui-codex.js）仍只显示专属材料名+用途，未接掉落分布（用户未要求）。

## 装备颜色（稀有度）随图档平滑梯度重做 — 已完成
- 用户指出旧设计偷懒：颜色只由怪的 `lootTier` 三档枚举决定，图1=low(白)/图2=mid(蓝)/**图3~17 全锁 high(必出金)**，15 张图颜色无差异；而基础数值(baseTierMultipliers) 与底材 T 阶(materialTierWeights) 其实已随 17 图平滑爬升（底材轴本就在、且独立）。
- 方案（用户拍板：3 档平滑梯度，不加第 4 档）：删 `lootTier`，改读 `Config.equipment.rarityWeightsByTier`（17 张图各一组 白/蓝/金 概率，合计 100；白随深递减、蓝中段达峰后回落、金单调爬升 图1≈2%→图17≈79%）。
- 改动：`docs/js/core/config.js` 加 `equipment.rarityWeightsByTier`；`docs/js/core/drop.js` 删 `getEnemyLootTier` + 重写 `pickDropRarity(areaTier)`（按 areaTier 取概率表加权抽，返回 rarity 对象）；`rollReward` 装备分支先算 areaTier 再传入（并复用给底材轴）；`docs/js/pet/enemy-data.js` 清掉已死的 `lootTier` 字段（20 处）。
- 掉率总盘(drop.pool.equipment≈1.5%) 不变，只动"出装时什么颜色"。「掉落稀有度」词缀的 白→蓝→金 升档逻辑保留。
- 测试：`docs/tests/_tmp_drop_sim.js` 去掉 lootTier、新增 17 图金装占比梯度断言；跑 20 万场/图验证 2.1%→79.7% 单调爬升 + 单件不变量 + 掉率吻合，全绿（exit 0）。
- 注意：怪自身头上的 白/蓝/金% 是 `enemy.rarityWeights`（用于**捕捉宠物的品质**），与装备掉落颜色无关；那个显示与本次无关，之前说的"显示误导"其实是两套数据。

## 鉴定系统（未鉴定装备 + 消耗鉴定石 + 拖拽/点按揭晓）— 已集成到真项目
- 背景：用户嫌前期挂机无聊，准许多加系统。选「鉴定仪式」做掉落冲击（盲盒仪式感），贴合第一爽点掉宝/鉴定；流放味适配为「未鉴定掉落 + 消耗鉴定石 + 拖到商人回收/拖到市场上架双落点」。
- 改动落地（都是基于 `docs/` 原项目，不是孤立 demo）：
  - `config.js`：`trade.materials` 主表注册 `{id:'identify', name:'鉴定石', icon:'🔍', category:'stone'}`（**之前只加了权重表漏了主表，导致云端 add_material/spend_material RPC 拒收 → 已登录用户掉落刷新丢失 + 鉴定用不了**，这是关键坑）；`materialWeightsByTier` 前期(1-6图)鉴定石权重 30/28/26/24/22/20 → 55/50/46/42/38/34（前期好掉治无聊，后期稀缺保 G 点）。
  - `drop.js:164`：装备掉落 `eq.identified = false`（未鉴定）。
  - `ui-bag.js`：未鉴定装备灰框 `.q-unid` + 「鉴定」按钮 → `identifyEquip` 消耗 1 鉴定石 → 设 `identified:true` + `.bc-scan` 扫光演出后重渲染（修了 spend 失败也误揭晓的 bug，现检查返回值并提示）；**新增拖拽鉴定**：装备视图顶部常驻可拖拽 `.bag-idstone`（持有>0且有未鉴定装备时显示），未鉴定装备卡作 drop 目标 `.bc-drop` 高亮，拖鉴定石到装备即鉴定；点按钮仍可用。
  - `css/ui-polish.css`：`.q-unid` 灰框、`.bc-scan` 扫光动画、`.bag-idstone`/`.bc-drop` 拖拽样式。
  - `游戏.html`：侧边栏之前缺 `data-page="bag"` 按钮（背包页入口被移走，页容器/渲染链路都在），已加回「🎒 背包」按钮恢复入口。
- 关键机制：`materials.js` 的 `gain()` 本地不校验主表，但已登录走云端 `add_material`/`spend_material` RPC，**必须注册 trade.materials 才认**；背包素材 tab 走 `Materials.getLocal()`，图标写死🧪、名字取自库存。
- 用户反馈点：装备掉率本身极低(~1.5%/场，设计稀有)，挂机才可能掉到未鉴定装备才看得到鉴定交互；若测可直接调高 drop.pool.equipment 临时看效果。
- 待办（用户未拍板）：拖到「商人」回收材料 / 拖到「市场」上架 的拖拽（鉴定系统后半段）尚未做。
