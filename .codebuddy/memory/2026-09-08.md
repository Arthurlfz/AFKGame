# 2026-09-08

## 新手引导发放层重构（用户拍板定稿）
- 起因：用户报"未完成新手任务重登会重复送宠/装备，可反复刷初始资源"，问重构还是局部修。
- 分析结论：根因是系统不存在"已发放"状态，`applyGrantsFor` 用库存差量反推（玩家消耗钥匙即误判 → G5 白装/G6 蛋/G7 素材宠/G4·G8 装备全部可无限刷，且补发物无绑定字段可上市集）。另有状态底座三硬伤：readFlag 本地优先（与"云端唯一真相"注释相反）、writeFlag 云端写失败被静默吞掉、checkGuide 无 cloudLoaded 门闩；并发无闸门（completeQuest fire-and-forget 触发 checkGuide）；resetGuideChain 控制台可调。
- 现状"引导经验包"是假的：boostGuidePetToLevel 直接 pet.level=lv，无道具不可见；starterPack.expItems 空数组。
- 用户四问拍板：①动机=两个都要分开做（先漏洞后体验）；②实物钥匙收口成一次性「新手补给箱」（1 个已领标记）；③经验包做成真实道具、分档锁死（Lv10/40/60 各一档不可跨档）；④零容忍，必须有发放账本（云端 quest_progress.extra.grantLedger，先记账成功才发货）。
- **用户追加两条铁律（2026-09-08）**：
  1. **经验包要做成通用真实道具、机制可复用**（未来商店/掉落可发非绑定经验包直接复用同一使用机制），教学期三档只是"绑定版"（市集黑名单 + 档位上限 + 关卡门槛）。
  2. **开工前先 commit+push 当前工作区当安全点**（与既有"每批前先 push"铁律一致）。
- 方案要点：grantOnce(keyId) 统一发放入口；saveProgressStrict() 让云端写失败可感知（失败不发货）；readFlag 改云端优先；checkGuide 加门闩+并发单飞；resetGuideChain 加 adminEmails 鉴权；missingKeysFor 只读检测 + 引导条手动"补发钥匙"按钮（每关限 1 次走账本）；删除隐式 boostLevel。老账号不回收已超发资源，缺钥匙走补发按钮自愈。
- 不动：任务链结构、NPC 台词/target、加速祝福、开场总览、装备绑定字段（不做）。
- Plan id: 46edbe4bdd7f4411b58a7c5b500a9a54，**已执行完毕（4 批全部落地，57 套件全绿）**。

## 落地记录（2026-09-08 四批）
- **批1 状态底座（8b2b27a）**：quest.js 新增 `saveProgressStrict()`（失败 reject，公共队列吞错不污染）+ `isCloudLoaded()`；tutorial_mode `readFlag` 改云端优先（本地降级为缓存/兜底）；`resetGuideChain` 加 `Config.dev.adminEmails` 鉴权（控制台不能再随便调）；ui-dev 按钮接住鉴权失败。
- **批2 发放账本（e8dd4d2）**：`grantOnce(keyId, fn, {max})` 三步=查账本→strict 记账→才发货；账本存 `quest_progress.extra.grantLedger = {keyId:{count,at}}`（零新表）；`ledgerBackfill` 老档兼容（packClaimed/blessingGiven 发过的只补账不发货）；checkGuide 加门闩+并发单飞（guiding/guideQueued 尾部补跑）；引导祝福、毕业礼包迁移账本。
- **批3 新手补给箱（9e57204）**：config `tutorialMode.grants` 删除 → `supplyBox.items`（每项带 taskIds 标注服务哪关）；applyGrantsFor/grantPet 死代码删除；首次进引导整箱发放（账本 supplyBox 守门），老账号（已 readStarted 但无账本）不自动发防叠加；`missingKeysFor(taskId)` 只读检测 + `reissueKeys(taskId)` 引导条「补钥匙」按钮（keyId=`reissue:{taskId}:{key}`，每关每种限 1 次）。**白装不能绑定——G8 上架任务本身要求挂装备，防刷靠账本不靠绑定**。
- **批4 分档经验包（4679719）**：config `expPacks` 三档（初/中/终阶 cap=10/40/60）；`useExpPack(name)` 通用使用入口（教学档走 cap 锁死=全宠顶到 cap 不超；未来非绑定包可扩展 exp 字段零新增）；checkGuideInner 发包按 `expPack:{cap}` 记账（G1/G2 同档共享一份）；背包消耗品栏渲染经验包卡片点击使用（matEntries 排除防重复渲染）；**绑定实现=道具名不进 Config.trade.materials（材料既不能上架也不能当收款物，天然绑定，零新字段）**；g1/g7/g9 hint 改"去背包使用"文案。
- **测试**：新增 `docs/tests/vtest_guide_grant.js`（门闩/整箱一次性/花掉钥匙不补/补发限1次/同档不重发/档位锁死守卫/账本幂等/strict失败不发货+重载自愈/鉴权）；全 57 套件 exit=0。
- **版本号**：config/quest/tutorial_mode/ui-quest/ui-bag/ui-dev 全部 bump `?v=20260908guide1`。
- ⚠️ **批4 push 未完成**：GitHub 连接被重置（本地 4679719 已提交），网络恢复后 `git push origin main` 补推。批1-3 已在远程。→ **后来已补推成功（9e57204..4679719），4 批全部在远程**。
- **全库存档已重置（2026-09-08，用户要求）**：按 skill 全清脚本执行（经 supabase MCP 核验项目身份后），另多清 4 张后加表：battle_logs→idle_sessions→pets 顺序删（FK 依赖）。核验全部 0，products 8 条保留。**所有账号（含管理员 776492620@qq.com）已删，需重新注册该邮箱才能用 grant_gems 发魔石**；玩家侧 localStorage 也要清（Ctrl+F5 或清站点数据）避免本地残留。

## 复制宠 bug 根因修复（2026-09-08，用户报"刷新后多出几个宠物"）
- **根因 1（主）**：`boostGuidePetToLevel`（useExpPack 在用）对每只顶等级的宠调 `Supabase.savePet`——而 **savePet 是无条件 INSERT（建档语义），不看 cloudId**。每用一次经验包，全宠各插一行副本，刷新后 loadPets 全量拉回 = 凭空多宠。修：顶等级改 `updatePet(cloudId,{level,exp})`，无 cloudId 才 savePet。savePet 加 JSDoc 防呆（市场买宠依赖 INSERT 语义，不改本函数）。ui-dev 面板 helper 同修。
- **根因 2**：孵化 `hatchEgg` 无防连点——扣蛋前有 await(getCurrentUser)，双击两次都通过 eggMap>0 检查 → **一颗蛋孵出两只**（蛋行 pet_id 被第二次覆盖）。修：全局 `hatching` 闸门 + 扣蛋移入闸门后（try/finally）。
- **根因 3**：合成/涅槃删素材宠"本地先删、云端失败仅 warn"→ 网络抖动时云端行残留，刷新复活。修：**先删云端成功才 removePet 本地**，失败保留本地行 + addLog 提示可重试放生（synth 返回 resurrected 数组）。
- 取证方法（值得复用）：直接查库——pets 行的 created_at/level/growth/source + pet_egg.pet_id 指向 + quest_progress.progress->extra.grantLedger，比对客户端账本时间戳（注意客户端 Date.now() 与服务器 now() 有时钟差，只能同源比较相对顺序）。
- 测试：vtest_guide_grant.js 增"顶等级后云端行数不变/原行等级更新"断言；egg/evolution/synth_growth/deep/bugfix 等回归全 exit=0。
- 版本号：supabase.js/drop.js/pet_merge.js/tutorial_mode.js/ui-dev.js → `?v=20260908guide2`。commit 52ed4ab 已推。
- ⚠️ 遗留：该测试号库里已有 5 行含疑似副本（异变 Lv1/Lv60 各一行、蛋 pet_id=451cfb76 指向已不存在的行）。用户如需干净数据可再全库重置。

## 新手引导·奖励闭环设计 v1（策划，未落地）
- 用户要求：引导每关奖励必须清晰可见，且奖励直接开启下一关（闭环）。产出 `docs/新手引导·奖励闭环设计_v1.md`。
- 现状 5 个问题：①钥匙走 supplyBox 开局整箱发 → 玩家看不到因果；②G5 剥离石/G7 涅磐兽/G8 重铸石+增缀石 三处奖励与下一关无关（断链）；③**G10 魂铸需凝魂晶石×10（soulCast.materialCount），而 G9 只给 2 + 毕业包 5 = 7 → 必卡死**；④G5 分解白装零产出（salvage.white={}）教学负反馈；⑤奖励只显示"名字×数量"无用途指向、完成仅 toast。
- 设计核心：①**奖励即钥匙**（第 N 关奖励 = 第 N+1 关准入，一物一用）；②钥匙**在上一关完成瞬间发**（不是开局整箱）；③零新字段——UI 用现有 `supplyBox.items[].taskIds` 反查"给谁用"。
- 新链（钥匙→完成可得）：G1 初阶经验包→进化素材1 / G2→蓝装1 / G3→重铸石1+白装1 / G4→白装1 / G5→腐噜兽蛋1 / G6→合成之石1+中阶经验包1 / G7→白装1(不绑定，上架用) / G8→精粹1+传说5+终阶经验包1 / G9→凝魂晶石10+毕业礼包 / G10→神圣石2。数量守恒已核：G9 进化4次总需 精粹1+传说5；G7 合成需 合成之石1+双宠Lv40。
- 机制改动 3 处：openSupplyBox 整箱 → `grantKeysFor(taskId)` 按关发（账本 `keys:{taskId}`，保留 reissueKeys 自愈）；新增 supplyBox type `exppack`（经验包也变钥匙）；新增测试 `vtest_guide_chain.js`（requires 链连续 + 数量守恒 + G9 晶石≥10）。
- 展示三处：引导条用途标签 / 卡片用途标签 / **领取弹窗替代 toast**（显示奖励+用途+"去用掉它"跳下一关）。
- **三个待用户拍板**：①G5 白装零产出保持不动（靠蛋给正反馈，推荐）还是 salvage.white 给重铸石（会被刷）；②G10 魂铸 minGrowth=10 门槛（教学期保底顶成长 vs 全局降到 5）；③打造页能否选"已穿戴"装备重铸（决定 G3 是否多给一件白装）。
- **用户拍板（当日）**：①靠送（保持零产出）；②实测没问题，不动；③现在不能（装备列表只走 `getInventory()` 背包，已穿戴不在列表），做成"能选"更省操作 → 拆批4 可选。

## 引导批1「奖励即钥匙」已落地（a05bfc7 已推）
- **config.js**：`supplyBox.items` 重写成**钥匙表**（每项 `taskIds` = 它是哪一关的钥匙），新增 `type:'exppack'`（cap 10/40/60 三档经验包也变钥匙）；删 `fodder` 素材宠（副宠由 G6 蛋出，多给会凭空多宠）；G10 项 = 凝魂晶石×10（= soulCast.materialCount，旧版 G9 只给 2 + 毕业包 5 = 7 必卡）。**引导关 g1~g9 的 `reward` 全部清空**（钥匙统一一个来源，两套并行玩家会看到重复资源、也讲不清因果），g10 保留神圣石×2。hint 文案改成"刚领到的…"。
- **tutorial_mode.js**：`openSupplyBox`（开局整箱）删除 → `grantKeysFor(taskId)`（按关发，账本 `keys:{taskId}`）；`grantBoxItem` 加 `exppack` 分支；`haveOf/itemKeyOf` 加 exppack；**删掉 boostLevel→grantExpPack 那一段**（两套并行会让同档经验包到手两份），boostLevel 字段保留只作门槛说明；checkGuideInner 加**老档补账**（账本有 supplyBox 但无 keys:g1 → 给所有 tutorial 关补 `keys:*`，防 v1 已整箱的号二次到手）；**G10 不是 isGuide，getGuideQuest 永不指向它** → 在"引导段全完成"分支里补 `grantKeysFor('g10')`。
- 因果时机：completeQuest 里 `TutorialMode.checkGuide()` 是 fire-and-forget，交完上一关时 getGuideQuest 已切到下一关 → 钥匙正好在"上一关完成瞬间"到手 ✅ 无需额外钩子。
- **测试**：vtest_guide_grant 改语义（新增：按关发=后续关钥匙不提前到手；g1 交完 → g2 钥匙到账；钥匙表守恒 传说5/精粹1/凝魂晶石≥materialCount；引导关 reward 已清空断言）。vtest_quest 的"g2 送装备"断言改成"g3 钥匙表有 gear"（本测试不加载 tutorial_mode，运行时发放由 vtest_guide_grant 覆盖）。**全 57 套件全绿**。
- 版本号 config/tutorial_mode → `?v=20260908guide3`。
- **剩批2（UI：引导条/卡片用途标签 + 领取弹窗替 toast）、批3（vtest_guide_chain 链连续性）、批4（可选：打造页支持已穿戴装备）**。

## 引导批2/3/4 已落地（f37d3dc 已推）
- **批2 UI（奖励清晰可见）**：`ui-quest.js` 新增 `nextQuestOf/keyLabelOf/rewardPreviewOf`（数据唯一来源 = `TutorialMode.keyItemsFor(next.id)`，UI 不另造一份）；三处展示 = 引导条 `.qt-reward` 常驻行、卡片 `.quest-card-next`、详情 `.quest-next-row`，统一文案"完成可得 X → 下一步「Y」要用"。**领取弹窗复用现成 `UI.showDialog` 气泡**（icon 🎁/引路人/按钮=下一关 guide.btn → goGuide），替代原来只有一行 toast；三个提交入口（引导条 qt-submit / 卡片 / 详情）全部改走 `showGuideReward`，非引导任务仍走 toast。`tutorial_mode.js` 新增只读 `keyItemsFor(taskId)` 导出。CSS 加 `.quest-card-next/.quest-next-row/.quest-next-use/.qt-reward`，并给 `.qt-item` 补 `flex-wrap:wrap`（否则 flex-basis:100% 的奖励行会在桌面端挤压同行元素）。
- **批3 测试**：新增 `docs/tests/vtest_guide_chain.js`（只加载 config.js 纯数据层，避免环境桩不足的假失败）——链连续（g1→g10 requires 一环套一环）、每关都有钥匙、taskIds 归属合法、数量守恒（G9 传说≥5/精粹≥1、G10 凝魂晶石≥soulCast.materialCount、G7 合成石≥合成需求、G4 重铸石≥1、G3/G8 各有装备）、经验包档位 ≥ 该关门槛（G7≥合成minLevel40 / G9≥60）、引导段 reward 已清空。
- **批4（零代码）**：查证**打造已穿戴装备本来就能用**——`ui-pet.js` 装备槽 onclick → `UI.renderBagEqDetail` → `renderEqDetailInto`（`body.style.display='flex'` + `renderCraftInto('.eq-detail-craft')`）。打造页（背包页）选不到只是因为列表只走 `getInventory()`。→ G4 改成直接淬炼身上那件（guide 改 `{page:'pet',tab:'equip'}`、target 改 `.pet-tab[data-pet-tab="equip"]`），钥匙表删掉 g4 那件白装，少发一件少一步操作。
- 版本号：css/game.css、ui-quest.js → `?v=20260908guide3`。
- ⚠️ **已知 flaky（与本次改动无关，别再查）**：`vtest_enemy_balance` / `vtest_deep2` 用随机装备模拟器，批量跑偶发 FAIL，单跑必过。已连跑 3 轮全量，每次挂的都不同。

## 魂铸（soulcast）"完全不能用" bug 修复（26f8f6c 已推）
- **根因**：`ui-craft.js` 的 `bindSoulCast` 只绑了 `.craft-soul-tier` / `.craft-soul-pet` / `#craft-soul-cast`，**漏绑 `.soul-trait`（特质选择按钮）**。而 `canCast = !!selPet && (source==='awaken' || !!(soulState.traitId || traits.length===1))` —— 宠有 ≥2 条特质时必须先选特质，特质按钮点了没反应 → traitId 永远为空 → canCast 永远 false → 确认按钮一直 disabled。**且文案不论缺什么都写"先选宠物"，完全误导**。逻辑层 `Craft.soulCast`（equipment_craft.js:294）本身没问题（vtest_trait_soulcast 全绿），是纯 UI 漏绑。
- 修 4 处：①补 `.soul-trait` onclick；②`renderCraftInto` 换装备时重置 `soulState`（残留 petId 指向不在候选里的宠 → 又是灰按钮）；③魂铸成功后清空 soulState（宠已消失）；④按钮文案区分"先选宠物"/"先选要铸的特质"。
- 顺带补自查信息（不改数值）：候选为空时显示"你名下 N 只：最高 Lv X · 最高成长 Y"（原只写门槛，玩家看不出差哪项）；缺晶石提示补"当前 X 颗"。
- 回归断言：`vtest_craft_page.js` 造假容器验证 trait/pet/cast 三个 onclick 都绑上（该测试桩的 querySelectorAll 恒空，只能这样测）。
- 版本号 equipment_craft.js / ui-craft.js → `?v=20260908soulfix`。

## 魂铸"点确认后卡死" + battle-settle 500 刷屏（e3f09f7 已推）
- **魂铸卡死根因**：`ui-craft.js` 成功分支读 `res.aff.label`，但 `Craft.soulCast` 返回字段名是 **`soulAffix`**（equipment_craft.js:368 `return { ok:true, soulAffix: aff, petName, tierKey }`）→ 铸造实际已成功，却在渲染成功提示时抛 TypeError → `btn.disabled=false` 永远不执行 → 按钮永久卡在「⚒ 魂铸中…」。修：读 `res.soulAffix` + **整个 onclick 包 try/catch**（任何 await 后的异常都必须先把按钮放回来，否则一次小错=界面永久卡死——这条要当成通用铁律）。
- **battle-settle 500 刷屏取证链**（值得复用）：①edge 日志全 500 无 body → ②postgres 日志 `invalid input syntax for type uuid: "null"` 与 500 同步 → ③查库发现 `battle_settle` RPC 其实**成功**（idle_sessions.last_settled_at 在推进、battle_logs 30 分钟 47 条）→ 500 在 RPC 之后的"宠物写回/奖励入账"，且 EF **没有全局 try/catch**，异常=裸 500 无任何线索 → ④手动 SQL 模拟（`set local role authenticated; set local request.jwt.claims=...`）证明 add_material 本身正常。**根因处置**：EF 包全局 try/catch（异常带 error/detail 回响应体，下次一击定位）+ `equipIds` 改 uuid 白名单正则过滤（equipment jsonb 脏引用会炸掉 `.in('id',...)` 整条查询）+ 客户端 `doSettle` 失败设 `nextScriptTryAt` 冷却 30s（gaugeTick 每帧等剧本，失败无冷却=每 1.2s 轰炸一次服务器）。
- ⚠️ **EF 已部署**（2026-09-08 用户给了 token，`npx supabase functions deploy battle-settle --project-ref asklogeayzlqpeejuvjj` 成功，注意 flag 是 `--project-ref` 不是 `--project`）。提醒过用户：token 已在聊天暴露，建议去 dashboard revoke 重新生成。
- **vtest_equip_persist / vtest_deep 修复**（不是产品 bug）：`drop.js` 自 52ed4ab 起掉落即 `identified=false`（PoE 式鉴定流），`equipment.equipItem` 拦截未鉴定穿戴 → 两个测试直接穿掉落装备必挂。修：穿之前 `inv[0].identified=true`（模拟玩家已鉴定，测试守的是持久化/结算不是鉴定流）。marketbot/deep 系列仍有随机 flaky。
- 版本号：idle-bridge → `?v=20260908idlefix`；ui-craft → `?v=20260908soulfix2`。
- 用 `git add -A docs` 提交时，把用户**在磁盘上已删除**的 `docs/assets/ui/0X_切图_N.png`（15 张）当删除提交并推送了。代码里搜不到 `assets/ui/` 引用，删了不影响运行，但这不是我的意图 → 已 `git checkout HEAD~1 -- docs/assets/ui` 恢复并推（26f8f6c）。
- **以后提交前先 `git status --short` 看有没有意外的 D（deleted）**；只 add 自己改过的具体路径。
- 仍有一个本地删除未提交：`docs/assets/City/City.mp4`（同样是用户删的，我没动，待用户定夺）。

## 资源试炼 + N1-N6 引导：P0 试玩复盘与 P1 阻塞修复（未提交）
- 基线：`vtest_resource_trial / guide_chain / guide_grant / newbie` 全绿；但按交接文档 `docs/HANDOFF_资源试炼与新手引导_20260908.md` 做 P0 推演时，挖出 4 个真阻塞（测试全绿却都是假绿）。
- **① N5 卡死（最严重）**：N5 要求「击败第一个地图 Boss」，而守关 Boss 是 `battle-sim` 的 `BOSS_COOLDOWN=200 / BOSS_CHANCE=1/1600 / BOSS_PITY=2400`，且由服务器权威结算 → 新手要挂几百到几千场，引导直接死在第 5 步。改：N5 类型 `disposeBoss` → 新增 `disposeKill`（处置 + 本图再击败 5 只）；`quest.js` 组合任务通用化（`isComboQuest/secondTypeOf/secondKey/secondNeedOf`，second 条件走 `q.secondType`，默认按 type 推导），`ui-quest.js` 的 taskDesc/progressText/TYPE_LABEL/guide 跳转同步。`currentProgress` 用「两者各自完成度取短板」而不是 `min(两个计数)`（need=1/secondNeed=5 时会误判达标）。Boss 保留为主线 boss1（图首通长线目标），引导文案里点明。
- **② 试炼 100% 通关、失败分支是死代码**：旧 `roundResult` 用 `taken=(敌攻-玩家防)×回合`，玩家防御成长远快于试炼敌攻 → 全程 0 伤害，实测成长 3 起步宠 Lv5~60 三条路线全通关。改：每回合按最大生命比例掉血 `hitRatio`，比例随轮次 ×(1+round×roundRatio)，总伤 = 比例 × 回合数，血量跨场累计不回满 → 攻击决定回合数、回合数决定挨打，强度与打造都能体现。参数在 `Config.resourceTrials.hitRatio=0.045 / roundRatio=0.15 / roundDelayMs=420`。实测剩血（裸装成长3）：Lv10 蜕变 28%/淬炼 14%，Lv15 42%/28%，Lv25 42%/涅槃 11%/淬炼 36%，Lv60 57%/21%/35%。
- **③ 门票没有来源**：门票只在 N6 发 1 张，用完即止 → 试炼变成一次性演示。改：config 末尾统一给所有 `collect_loop`（地图委托）奖励注入 1 张票（不新增货币、不动掉落与战斗公式），面板显示来源。
- **④ 涅槃路线发「涅磐兽」**：涅磐兽早已不是涅槃消耗品（pet_merge.js：消耗的是涅槃丹，×1.2 吸收），发它玩家不知道干嘛用 → 改发涅槃丹 ×1。
- 另修：`ui-resource-trial.js` 开始按钮没有 try/catch（await 出错按钮永久卡死）+ 失败无 toast；`tutorial_mode.grantStarterPack` 在 starterPack 被 N 链清空后仍 log「新手礼包已发放」→ 空包直接 return。
- **vtest_quest.js 是 N 链改造遗留的假红**（不是本次引入）：G1-G10 被替换后 任务数 112→108、类型 16→15、tutorial 10→6，且大量 `g1/g2/g3` 断言。已整体迁移到 `n1/n2/n4`（链完整性、引导条指向、n1 kill 上报、n2 钥匙装备、追踪栏任务名）。顺手修 config 里 n1 名字缺「物」字的笔误。
- 回归：全量 vtest（除 vtest_files）**全绿**；`node --check` 六个改动文件通过；静态页 HTTP 200、`#btn-resource-trial`/`#resource-trial-panel` 各 1 个；版本号统一 bump 到 `?v=20260909trial1`。未提交（交接文档禁止 commit）。
- 待办（P2 才动）：试炼结果仍是纯文本（5 场 × 420ms），可考虑补战斗演出；Supabase 权威结算仍未做，等本地循环被真实试玩验证后再设计。
### 本轮未完成项（交接文档 P0/P2）
- **P0「真实试玩记录」没做**：需要开新账号在浏览器里走一遍，我做不了（无注册邮箱）。已用静态推演 + 真实属性数值模拟代替，留给用户。
- 留给用户的新号试玩清单（每步要盯的点）：N1 选宠→地图→挂机 3 场是否有进度提示；N2 两件装备（轻盔/重甲）是否到手、能否比较底材与词缀、穿上后挂机效率有无变化；N3 重铸后词缀变化有没有明确「随机、不保证」的反馈；N4 Lv10 进化（引导是否自动顶等级/发进化素材）；N5 上架或分解 + 再打 5 只，看处置后有没有资源进账提示；N6 选「资源试炼」→ 面板是否自动打开、门票 1 张、跑完 5 场是否看到剩血与奖励。
- **P2 是否进 Supabase 正式化**：等上面试玩结论再定，未启动（也不该现在启动）。
