# 2026-09-22

## 🔚 收尾总账（用户要关页面前的状态，下次从这里接）
✅ **已提交并推送 main：`dd55604`（2026-09-22 03:34）**，5 个文件 / +436 −30。
远端已确认：`git ls-remote --heads origin main` = `dd556048b487d46ff044275ab37a7fb3c87f0c9c`（与本条一致）。
⚠️ 提交时 `docs/游戏.html` 里**混着另一个会话的版本号升号**（`game.css→qtrack` / `market-cascade→growth` /
`config.js→botcap` / `market_bot.js→botcap` / `ui-quest.js→qtrack`，都不是我改的）——一并提交了。
判断：**无害**（版本号只影响"让浏览器重新拉哪个文件"；对方那些 js/css 本体若未提交，线上仍是旧内容，
玩家最多白刷一次缓存），故未回改、避免将来和对方冲突。
⚠️ 另：`raw.githubusercontent.com` 本机抓不动（TLS），**核实推送只信 `git ls-remote`**。
**今天一条主线**：用户觉得「我这个游戏的布局不像一个游戏」⇒ 把「网页后台骨架」改成「画面铺满 + 件浮在边上」，
共 10 批（详见下面各节）。核心产物 = **新文件 `docs/css/shell-immersive.css`**（一个文件管这一件事）。

**改了但未提交（都在磁盘上，不会丢）**：
- 已 `git add`（我的独立文件）：`docs/css/shell-immersive.css`、`docs/js/ui/ui-bag.js`、
  `docs/js/ui/ui-shell.js`、`docs/tests/vtest_bag.js`
- ⚠️ **未 add**：`docs/游戏.html`（改了两处：① 引入 shell-immersive.css ② 战斗页的 `.fight-status` 块
  从 `.bp-bottombar` 移进 `.inline-console`；另升了 ui-bag/ui-shell 两个 `?v=`）。
  **不 add 的原因**：该文件里同时混着另一个会话的在途改动，add 会把别人的东西一起暂存。
  ⇒ **下次提交时**：`git add docs/游戏.html` 前先 `git diff` 看清是不是只有我这两处。
- 临时文件已清理（`_demo-shell.html` 等全部删净）。

**⏳ 等用户实机验收（我没法看画面）**：
1. **宠物页「装备」页签那套三连屏**（属性 / 12 槽 / 换装背包）—— 内容区让出右侧栏后最可能挤，**最担心这一处**；
2. 世界地图页（页眉搬到底部 + 右侧栏）；3. 面板页顶栏与浮起顶栏是否打架；4. 战斗页右栏顶部「挂机账」观感。

**没用完/没做的**：
- 「底部空空的」只填了一半（底图下半本身内容少 ⇒ 换底图是美术侧的活）。
- 若要「按页给默认频道」（战斗=掉落、其它=系统）——碰 `ui-console.js` 共享契约，**未做，等发话**。
- 未做的旧账（来自第二批）：贴真美术（图标凹槽九宫格）。

## 外壳「像游戏」改造 —— 需求溯源 + grilling 五轮 + PM 定调（未动工）

### 需求溯源（重要：别再从方案开始）
用户起初说「有 agent 建议三联屏像 DNF」，经逼问逐层剥出真需求：
三联屏 → 右侧加栏 → 整个底部移到右边 → **「我是觉得我这个游戏的布局不像一个游戏」**。
⭐ 教训：他说方案时（"做成 X 布局"），先问/挖他**要解决什么问题**，方案往往不对题。

### grilling 五轮的账（结论 = 战斗页右侧栏不做）
1. 消息必须常驻（用户拍板）→ 折叠省空间的路封死。
2. 挪右边 = 永久砍 240~460px 宽（消息栏功能下限 240px：4 频道 tab + 输入框）。
3. 立绘 350/320 只跟高度走不跟宽度缩 → 两宠空隙掉一半、冲刺位移短一半（上周刚调好的演出）。
4. 竖栏 ~690px 高但消息只占 ~120px → 空白 500px。
5. 填交易/资源 → 用户否：「就完全是个市集页面了」。
6. 填角色面板（宠物卡+装备槽+掉落预览）——曾提案，未拍板。
7. 收益（立绘变大）只在可视高 <950px 成立，1080p 全屏下立绘已顶 350px 上限 = 0 收益。
**净亏，不做。**

### 真诊断：「不像游戏」= 外壳是网页骨架（代码已取证）
- 顶栏 = 网站 header：标题+英文副标+文字按钮+`border-radius:999px` 圆角胶囊（`.idle-chip`/`.gem-chip`）。
- 侧边栏 = 后台菜单：图标+文字竖排、选中态 `border-left:3px`。
- **主城/世界地图已是"画面"，但被关在 page-container 盒子里** ⇒ "网页里嵌了个游戏画面"。
- 游戏客户端 = 世界铺满窗口 + UI 浮在上面。默认进的是主城页（tab-capital active）→ **第一眼是框不是城**。

### PM 定调（我的推荐，等用户拍）
1. 砍掉：战斗页右栏 / 三联屏 / 消息搬家 / 交易资源栏 —— 全是"为改而改"。
2. 第一批：**主城画面铺满窗口 + 顶栏/侧栏半透明浮起**（只动主城页）。
3. 第二批：侧栏改图标条（10 入口 → 6 + 凹槽底）、顶栏去英文副标、胶囊改方形资源格。
4. 页游风险分水岭 = 少而精 + 材质（凹槽、水墨 98 图标、禁霓虹），不是"是不是图标条"。

### ✅✅✅✅✅✅✅ 第六批：挂机账进右栏 + 宠物页也统一成右栏（用户：「可以，并且那个宠物资料的也要修改」）
- **战斗页**：把 `.fight-status`（状态徽章 + 本轮/累计/掉落装备 + 进化提醒）**从画面左下角的浮片搬进右栏顶部**
  —— 这一步动了 `游戏.html`（DOM 搬位置，**id 一个没改 ⇒ JS 不用动**）。CSS 里把它从
  `absolute/半透明底/金竖条` 改成静态块（含 `::before { display:none }`，那条金竖条是浮片装饰）。
  结果：右栏 = 挂机账（上）+ 消息（下），**画面底部只剩「开始挂机 / 主动技能」两个按钮**。
- **宠物页**：从"右下角浮窗"改成**与三个画面页一致的右侧固定栏**，用 **CSS Grid**（DOM 一行不动）：
  `grid-template-columns: minmax(0,1fr) var(--side-console-w)` + 左列三行（页眉/页签/内容），右栏跨三行。
  🔴 选择器必须写 `#tab-pet.active` —— 项目红线：绝不能写会命中未激活页的 display 规则
  （会碾压 `.tab-page{display:none}` 导致切页失效）。
  🔴 内边距必须**从 grid 容器挪到左列各元素**（`.pet-page-head/.pet-tabs/.pet-tab-pane`）：
  留在容器上会把右栏一起推进来（实测右栏顶部空 74px，与其它三页不一致）；同时给宠物页
  `.page-container { padding: 0 }`，右栏才顶得到屏幕顶（实测 1140,0 300x900 ✅）。
- 窄屏（≤900px）：宠物页 grid 回退成单列四行（内容在上、消息在下）。
- 自检（playwright+Edge 1440×900）：战斗页右栏 300x900、挂机账在栏内 y=49（避开顶栏）、
  底部只剩 fight-action；宠物页右栏 1140,0 300x900、频道 tab y=57。截图目检通过。
- 回归：**87 通过 / 3 存量红，零新红**（改过 `游戏.html` 也确认无影响）。

### ✅✅✅✅✅✅✅✅ 第七批：右栏宽度改用【黄金分割】（用户：「按照设计美学黄金比例来调转一下这些比例」）
- `--side-console-w` 从拍死的 `300px` 改成 **`clamp(260px, 23.6%, 480px)`**。
  **23.6% = 1/φ³**（黄金数列 61.8% → 38.2% → 23.6% → 14.6%，每项 = 前一项 ÷1.618）；
  画面必须是绝对主体 ⇒ 取第三档。画面 : 右栏 = 76.4 : 23.6 ≈ **3.24 : 1**。
- 🔴 **必须写成百分比 + clamp，不能写死像素**：4K 屏上固定 px 会缩成细缝，小笔记本上又太占地方。
  260px = 功能下限（频道 tab 换行临界）；480px = 上限（超过就不叫"画面+辅助栏"、变成双栏）。
- 实测（360 行验证）：1280→284 / 1440→322 / 1920→435（占比都是 23.7%）；2560→卡 480（19.4%）；
  900 及以下走窄屏回退（上下分栏，右栏占满宽）。截图目检：画面仍是绝对主体。
- 变量是四页共用（grid 列 / flex 基准 / 页眉右缘偏移都读它）⇒ **改一个数四页一起变**，这是唯一的调参口。

### 🐛 第十批：修「悬停提示残留」+ 解释「掉落消息每页都可见」（用户截图问的两件事）
- **① 残留提示（真 bug，已修）**：用户在主城画面上看到淡淡的「徽记坠饰」（**饰品底材名 ⇒ 那是装备 tooltip**）。
  根因：`#bag-tooltip` 挂在 **body 层**、靠锚点的 **mouseleave** 收起；而玩家的动作常常是
  「**鼠标停在装备格上**就把背包关掉 / 就切页」⇒ 那个 mouseleave 永远等不到 ⇒ 提示带 `.show` 留在屏幕上，
  跟着飘到任何页面（我的沉浸式改动让画面铺满，它变得更显眼，所以这次被发现）。
  修法（两个必经入口，最小作用域）：`ui-bag.js closeBagWindow()` 加 `hideBagTip()`；
  `ui-shell.js renderPage()` 加 `if (UI.hideBagTip) UI.hideBagTip()`（装备 tab 不在浮窗里，切页那条路必需）。
  `?v=`：ui-bag / ui-shell → `20260922tippurge`。
  守值 3 条进 `vtest_bag.js`（关窗清 / 切页清 / UI.hideBagTip 已暴露），**并做了反向验证**：
  把 `hideBagTip()` 删掉后立刻 FAIL ✅（守值不是假断言）。
- **② 「掉落了物品每个页面都能看到」= 原有设计，不是新 bug**：`consoleLog()` 把每条消息写进同一份 `history`，
  `renderChatPanel()` 遍历**所有 `.inline-console`**（每页 DOM 一份）+ 抽屉，且**四页共用同一个 `activeTab`**
  （2026-09-13 定的「一处切频道，两处同步」）。以前消息栏是底部一小条、不显眼；现在每页右侧一整栏 ⇒ 到处都看得到。
  理由上站得住：**挂机是全局后台行为**（在战斗页开局、人可以去逛主城/市集），掉落到处可见才合理。
  ⏳ 若用户仍要「按页看各自相关的消息」，可做**按页面给默认频道**（战斗=掉落，其它=系统）——
  但这会碰 `ui-console.js` 的共享契约，**未做，等用户发话**。

### ✅✅✅✅✅✅✅✅✅ 第九批：页眉从画面【顶部搬到画面【底部】】（用户截图圈三处：字压在亮天空上 / 按钮贴右栏 / 下方空空的）
- ⭐ **三个红框是同一个问题**：页眉（标题+说明小字+「回城休整/回世界地图」）贴在画面**顶部**，
  而顶部正好是底图的**亮天空/月亮** ⇒ 字糊、挡景；同时画面**下半是暗区（雾/地面）本来就空着**。
- 修法：`.capital-head` / `.worldmap-head` 从 `top: var(--topbar-h)` 改成 **`top:auto; bottom:0`**，
  渐变方向翻过来（`linear-gradient(0deg, …)` 从下往上渐隐）⇒ 标题落左下、按钮落右下，
  底部暗区被利用、顶部亮区腾干净。
  连带把上一批"为亮天空准备"的重描边阴影收回普通阴影（`hint` 也恢复 `--text-dim`）。
- 🔴 **同文件覆盖陷阱（又踩一次）**：后面那段 `.capital-head, .worldmap-head { background: none }`
  会盖掉我刚给两页各写的底部渐变 ⇒ 必须把 `background:none` 一并删掉并留注释。
- ⚠️ 建筑热区最低在 y=80%（距底 20% ≈ 180px），底部这条约 60px 压不到它们。
- 自检：页眉 814~900 贴画面底 / 标题 x=97 靠左下 / 按钮 x=768、928 靠右下 / 渐变方向正确；截图目检顶部干净。
- 回归 87 通过 / 3 存量红。
- ⏳ 用户提的"下方空空的"**只解决了一半**（页眉填了底部，但底图下半本身内容偏少）；
  若仍嫌空，另一条路是**换一版适配竖长比例的底图**（美术侧，`assets/City/frame1.png`）或调 `background-position`。

#### 同批追加：**左侧栏宽度也按黄金分割**（用户：「左边你修改了嘛」——左边第二批改过，但宽度还是拍死的 76px）
- `--side-rail-w: clamp(72px, 5.6%, 100px)`，**1/φ⁶ = 5.6%**（φ 数列：61.8 → 38.2 → 23.6 → 14.6 → 9.0 → 5.6 → 3.4）。
  侧栏是纯图标条、宽度只由"图标 32px + 呼吸"决定 ⇒ 取靠后的档位。
  下限 72px（图标 + 左右呼吸）、上限 100px（再宽就是一条空栏）。
- ⭐ **两条边的宽度现在都集中在文件顶部 `:root` 的两个变量里**，以后调参只改那两行。
- 实测：侧栏 1280→72(5.6%) / 1440→81(5.6%) / 1920→100(卡上限)；右栏 285/321/430(占比 23.7%)；
  图标在侧栏内**居中**（80px 宽的图标格 + 10px 字，名字仍常显）。
- 回归：**87 通过 / 3 存量红**；`vtest_enemy_level` 又一次在并发全量里红，单跑 2/2 全过 ⇒
  **该测试在并发下必红**（累计已 3 次），别当回归；纯 CSS 改动不可能影响它。

### ✅✅✅✅✅✅ 第五批：右侧【固定栏】（用户看完实物后改主意 —— 浮窗 → 固定栏）
- 用户原话：「我觉得还是要在右边整合一些东西进去，比如说什么市集有人买了你的东西，比如说聊天，比如收掉落，
  把这个 console 做成一个**固定的框架**，把中间舞台**缩小一点**」。⇒ **主城 / 世界地图 / 战斗 三页统一**
  把消息从"右下角浮窗"改成"与画面并排的右侧固定栏"（`--side-console-w: 300px`，定义在文件顶部，改一个数三页一起变）。
- ⭐ **他说的三样（市集成交 / 聊天 / 收掉落）本来就都在 console 的四个频道里**（系统 / 世界 / 掉落 / 战斗）
  —— 所以这一步**零新数据、零 HTML 改动**，只是换位置；换来的好处是栏高=整屏，消息能看很多条（浮窗时代只有 220px≈6 条）。
- 关键几何（1440×900）：画面 1364→**1056**，右栏 300×900；页眉右缘收窄到 `calc(100% - 栏宽 - 8px)`。
- 🔴 **两个自己踩的坑（都靠量数据抓到）**：
  1. **`.bp-topbar` / `.fight-action` 不要动定位** —— 它们的包含块是 `.bp-arena`（本身就是"画面那一块"，不含右栏），
     原来的 `left:50%` 已经是画面中心。我画蛇添足又减了一次栏宽 ⇒ 中心偏到 450（应为 604）。
  2. 画面窄 300px 后**顶部三块（我方状态 / 中间 HUD / 敌方状态）挤不下**：居中 HUD 条右端压到右上角敌怪血条上（重叠约 60px）
     ⇒ 收掉 `.bp-title`（"野外探险"）与 `.bp-hint`（重复说明），HUD 只剩「地图名+成长标签+返回地图」。
     修后实测 178-426 / 440-768 / 831-1079，三块零重叠。
- 🔴 **右栏顶部必须 `padding-top: var(--topbar-h)`**：三条栏都整屏高、起点 y=0，
  不让位的话频道 tab 正好落在浮起的顶栏（0~44）底下 —— **看得见消息、点不到频道**。
- 窄屏（≤900px）三页统一退回"画面在上、消息在下"的上下分栏。
- 回归：**86 通过 / 4 失败**，其中 `vtest_enemy_level` 为**已知并发偶发**（单跑 3/3 全 PASS，本次再证），
  另 3 个是存量红 ⇒ **零新红**。
- ⏳ 未做（已向用户提案）：把「挂机账」（本轮场次 / 累计 / 掉落装备 / 下一级经验）从战斗页左下浮片
  **搬进右栏顶部**，让底部只剩操作按钮。需要动 `游戏.html`（移动 `.fight-status`），故先问后做。

### ✅✅✅✅✅ 第四批：战斗页 + 宠物页（用户：「战斗页面还有宠物资料你不修改嘛」）
- **战斗页**：消息条从【流内 190px】改成【浮在画面底部 170px 半透明】⇒ 舞台拿到完整 900px（原 702）。
  🔴 **一起抬三样，漏一个就被压住**：① 消息条 absolute ② 立绘 `bottom: clamp(190px,22cqh,230px)`
  （原 108px 会落在消息条底下、脚被吃掉）③ `.fight-status` / `.fight-action` 的 `bottom: 194px`
  （它们原本也在 `bottom:12px`！不抬则**「开始自动战斗」按钮被消息条整个盖住** —— 主操作绝不能被盖）。
  三个数都从 `170 + 12 + 12 = 194` 推出来。高度取 170 而非 140：140 时消息列表只挤得下 1 条。
- **宠物页**：去掉 `#tab-pet` 自己那层「网页卡片」外框（border + 20/22px padding + 不透明底），
  内容直接铺在页面背景上；消息区从"下半屏 35%"改成右下角小窗 ⇒ 面板多拿 35% 高度。
- ✅ 新增 **`九、窄屏回退`（@media max-width:900px）**：浮层在窄屏会互相压成一团，
  战斗/宠物两页的浮层退回流式排布（战斗页原本只有 520px 断点，760 档是空的）。
- 自检（playwright+Edge 1440×900）：battle-arena 1364x900 全高 / 立绘底 702 < 消息条顶 718 /
  操作按钮与战报都在消息条之上 / 宠物页面板区 682 高 + 消息小窗右下。截图目检通过。
- 回归 87 通过 / 3 存量红。⚠️ 诚实交底：战斗页这么改**画面背景铺满到 900**，但底部 170+24px 被半透明 UI 盖着，
  「干净视野」其实只从 702 → ~706（收益主要在观感：画面不再被切一刀）。已告知用户可回退。

### 🔴 第三批返工 ②（用户截图：「看不到主城的图标」）
- 现象：侧栏第一项「主城」**只有文字、图标不见了**（被浮起的顶栏盖住）。
- 根因：**同一文件里 CSS 简写覆盖** —— 第一节写 `.sidebar { padding-top: calc(12px + var(--topbar-h)) }`，
  第三节又写 `.sidebar { padding: 12px 6px }`；后者在文件里**靠后**，简写把 padding-top 打回 12px
  ⇒ 第一项从 y=12 开始，图标（12~44）正落在顶栏（0~44）底下。
- 修法：**让位的 padding-top 合并进第三节那条简写里**，第一节只留指针注释（别再两处维护同一个值）。
- ⭐ 教训：**一个文件里对同一元素写多条规则时，后写的简写会静默吃掉前面写的长写属性。**
  改完量一遍（图标 y=65~97 ≥ 顶栏 44）才敢交付。
- 用户给的两个备选（"降低侧栏高度 / 缩短顶栏"）都没采用 —— 根因是覆盖 bug，不是尺寸问题。

### 🔴 第三批的返工（用户实测：「主城被挡住了，被顶部的标题页」）
- **根因**：顶栏（0~44px 半透明底+底边线+阴影）与页眉（48~124px，从 84% 黑渐变到透明）
  **两条"带底的横条"叠在画面顶上 ⇒ 顶部 124px 被压暗一大片**（1440×900 下占 14%）。
  ⭐ 教训：**"沉浸式"不等于"把原来的栏浮起来"** —— 栏本身有底色/边线/阴影，浮起来就是在画面上盖黑布。
- **修法（画面页专属，`六` 节）**：主城/世界地图两页上，顶栏 `background:transparent + background-image:none
  + border-bottom:transparent + box-shadow:none`，页眉 `background:none`；可读性改由**文字阴影**兜底
  （阴影不挡画面）。页眉说明小字从 `--text-faint` 提到 `--text` + **描边式三层阴影**（下拉阴影在亮云上不够）。
- ⚠️ 只对三个画面页生效；**面板页顶栏是实打实的导航栏，保持实色**（切页时顶栏外观会有变化，是有意的）。
- 验证：截图比对（修前顶部一片黑 / 修后城寨完整露出，月亮山景屋顶都在）；回归 87 通过 / 3 存量红。

### ✅✅✅✅ 第三批已落地（用户：「其他的你也要修改成这种沉浸式的啊」）：全站顶栏浮起 + 世界地图页沉浸式 + 战斗页
同文件 `shell-immersive.css`。**核心决策：沉浸式分两类，不能一刀切**
- **三个「画面页」（主城 / 世界地图 / 战斗）**：画面顶到窗口边 ⇒ `.page-container { padding:0 }` + 不让位。
- **面板页（市集 / 宠物 / 背包 / 商店 / 百科 / 排行榜 / 好友 / 任务）**：⚠️ **不套"画面铺满"那套**
  —— 它们没有画面，排版靠内边距撑，铺满只会让卡片贴到窗口边。
  但**顶栏浮起是全站的**（每页多出 48px 高度）⇒ 面板页必须 `padding-top: calc(var(--gap-md) + var(--topbar-h))` 让位，
  否则内容被浮起的顶栏盖住。
- 世界地图页与主城页结构几乎一样，规则照抄一份（`#app:has(#tab-worldmap.active)`），
  ⚠️ 保留各自 `:has` 限定、不合并成一条（任一页调整不牵连另一页）。
- 战斗页只做两件事：`.battle-page { height:100% }`（`.tab-page.active` 是 block，不写高度 flex 撑不开）
  + **`.bp-topbar { top: calc(12px + var(--topbar-h)) }`**（那里有「返回地图」按钮，被顶栏盖住就点不到）。
- 侧栏 `padding-top: calc(12px + var(--topbar-h))` 也改成**全站**（顶栏浮起后侧栏第一项会被盖）。
- 自检（playwright+Edge，1440×900，三类页面各量一遍）：
  世界地图 stage 1364x900 铺满 / 页眉 y=48 / 消息窗右下；战斗 battle-page 1364x900、bp-topbar y=60；
  面板页 `padding-top=60px`、首块面板 y=60（不被顶栏盖）。截图目检通过，回归 87 通过 / 3 存量红。
- ⏳ 待用户实机验收：**顶栏浮起是全局改动**，请逐页扫一眼（尤其市集/宠物页顶部工具栏与浮起顶栏是否挤）。

### ✅✅✅ 第二批已落地（用户看实机后说「还可以继续做」）：侧栏图标条 + 顶栏去网页化（全站）
- 同文件 `shell-immersive.css` 追加（`?v=` 未升：本地 serve 是 no-store，线上尚未部署过该文件）：
  - **`.sidebar` 276px → 76px**；每页内容区凭空 +200px 宽。
  - 🔴🔴 **第一版做成"纯图标 + hover 才出名字"，被用户当场否掉**：「现在新手更加不知道那几个 tab 是什么了」
    ⇒ **图标再好，新手也不是靠图标认路的，名字必须常显**。定稿 = **图标在上（32px 主导）、名字在下（10px 弱化居中）**，
    仍是"游戏功能条"而不是"横排左对齐的后台菜单"（区别在图标主导 + 居中 + 小字，不在有没有字）。
    ⭐ 教训：把"去网页化"做成"去信息量"就会掉进新手可用性的坑 —— **风格改动不许以新手看不懂为代价**。
  - 每项高度压到 **56px**（`padding:4px 0`）：1366×768 矮窗口下 11 项（含开发者）不溢出；
    65px 那版在 1366×768 溢出 99px（侧栏被迫滚动、"设置/百科"被藏起来）。用 playwright 在 1440×900 与 1366×768 两档量过。
  - `.sb-divider-label`（"其他"）→ font-size:0 收成一条细金线。
  - `.title-en`（英文副标）`display:none`；`.idle-chip/.gem-chip` 圆角胶囊 → 方角（radius-xs）。
- 🔴 **故意全站生效、不加 :has 限定**：侧栏宽度必须各页一致，否则切页画面宽度跳。
  项目本就有 `@media (max-width:1024px)` 的 64px 窄栏版本，这里等于推广到全尺寸 + 补 hover 出名。
- 🔴 主城建筑热区避让问题随侧栏变窄**自动消解**（76px 占位，热区不被盖），侧栏无需浮起。
- 自检（playwright+Edge，1440×900）：sidebar 76x900 / stage 1364x900 / sb-dot 不溢出 /
  title-en 隐藏 / hover 后 label 可见；截图目检通过。
- 回归：**87 通过 / 3 存量红，零新红**。临时预览与脚本已删。
- ⏳ 待用户实机验收：各页面（市集/宠物/背包等面板页）在新宽度下排版是否正常（AI 看不到画面）。

### ✅✅ 第一批已落地（用户看过 demo 后拍板「这个可以」）：主城页沉浸式外壳
- 新文件 **`docs/css/shell-immersive.css`**（`?v=20260922imm1`，已 `git add`）：全部规则包在
  **`#app:has(#tab-capital.active)`** 下 ⇒ 其它页面一像素不变。`游戏.html` 在 ui-fixes.css 之后引入。
- 做了：顶栏 `position:fixed` 半透明浮起（blur）｜`.capital-page` 显式 `height:100%`
  （⚠️ `.tab-page.active` 是 `display:block`，不写高度的话 `.capital-page` 的 `flex:1` 不生效、画面撑不开）
  ｜`.capital-stage` absolute inset:0 铺满 ｜页眉浮在 `top:var(--topbar-h)` ｜消息区从"下半屏 35%"改成右下角 420×220 小窗。
- 🔴 两个自己埋的坑（截图自检抓到的）：① 页眉/侧栏第一项被浮起的顶栏盖住 → 页眉 `top:var(--topbar-h)`、
  侧栏 `padding-top: calc(12px + var(--topbar-h))`；② 页眉渐变层会挡住画面顶部的建筑热区点击
  → 页眉 `pointer-events:none` + 子元素 `auto`。
- 🔴 侧栏**故意没动**（仍实色占位）：主城建筑热区是绝对定位坐标，侧栏浮起会盖住左侧热区。
  第二批改窄图标条时一起处理热区避让。
- 画面几何（1440×900 实测）：stage 从 480 高 → **1164×900 全高**。
- 守值：`vtest_assets_tracked` 抓到"新 css 没入库"→ `git add` 后转绿；全量 86 通过 / 4 失败（3 存量红 + 该条已修）。
- 未提交。⚠️ 守值里 `game.css?v=20260921petgate` 断言与 html 现值 `20260922qtrack` 不一致（别的会话改的），当前全量跑未因此报红，暂不处理。

### ✅ 已做：对比 demo（用户没见过样子，无法想象）
`docs/_demo-shell.html`（**临时，用户看完要删**）：左右两屏并排（A 现在的网页骨架 / B 世界+浮起件），
用真实主城底图 `assets/City/City.png` + 真实 nav 图标 + design-tokens 真配色（手抄 :root 值）。
入口 `http://localhost:8030/_demo-shell.html`（8030 当时在跑）。

### ⭐ 新招（已两次验证有效）：playwright-core + 系统 Edge 截图自检
`npm i --no-save playwright-core` → `chromium.launch({channel:'msedge'})` → 截图 → **`read_file` 读 PNG 自检**（本会话模型能读图，2026-09-22 再证）→ 删脚本 + uninstall。
这次靠它发现 demo 资源全部 200、A/B 对比效果成立，才敢交给用户。别再"盲交"。

---

# ⚠️ 以下为另一条线（内测清单修复）的记录 —— 曾被本文件整份重写覆盖，2026-09-22 补回
来源：`docs/待办/内测实测问题清单_2026-09-21.md`（test_player001 实测）。全部**未提交**，等用户实机验收。

## 取证先行（关键结论）
线上 GitHub Pages = 本地 HEAD（0 未推送提交，关键文件全入库），战斗六件套/引导修复都已部署。
⇒ 清单里 🔴1 引导卡死、🟠1~3 战斗无打击感 **无代码事实支撑，判自动化测试环境误判**（同一工具已误判过一次）。

## 真问题（已修）
1. **🟠4 市集购买失败没反应**：根因 = `showToast` 只写消息中心，市集页**没有**内嵌 console ⇒
   失败原因**就地写进购买弹窗**（`.buy-confirm-error`，弹窗保留不关）。`ui-market.js` `?v=20260921growth`。
2. **🟠15 进化按钮被挡**：根因 = `.es-main/.es-target` 写死 `min-height:430px` 撑爆一屏，forge-bar 在折叠线下
   （"聊天栏"其实是宠物页常驻内嵌消息栏）。修 = 面板吃满高度 + grid `minmax(0,1fr)/auto` + forge-bar 常驻底部
   + 头像 `flex:none`。Playwright 几何验证通过（按钮 463~507 在面板 85~558 内、头像 180×180）。
3. **🟠16 任务提交没反馈**（同根因）：三条路径（卡片/详情/顶部引导条）加就地结果行。
   ⚠️ 提交成功后会 `renderQuestPanel()` 整块重绘 ⇒ **结果行必须重绘之后再插**。`ui-quest.js` `?v=20260922qtrack`。
4. **🟠6 市集成长区分度**：⛔不引进品级 ⇒ 成长提亮放大 + 「高于同款」（同进化线在售成长中位，样本<3 不标）
   + 「可搏神级宠」（≥`Config.synthesize.god.minGrowth`）。`market-cascade.css` `?v=20260921growth`。
5. **🟠12 养成 tab + 空态**：tab 加「未开启/缺材料」角标（不置灰不拦截）；合成/涅槃/觉醒三页空态 =
   这是什么玩法 / 还差什么 / 现在干嘛。门槛统一由 `UI.petTabGate()` 读 Config。
6. **🟠14 分支看不出区别**：取证 = **两条分支数值完全一样**，差异只有"终形态 + 主动技能"。
   ⇒ 方向卡显示「终形态 X」+「终形态会学 Y：概率/倍率」+ 一句实话「两条强度一样，选错也不会变弱」。
   **用户拍板：不加数值差异（已入宪法 §J）**。
7. **🟠11 主动技能没说明**：未解锁时也写出「将来会学 血月斩（13% 概率打出 200% 伤害 · 3 回合冷却）」
   （宠物资料页 + 战斗页按钮悬停）。查询统一走 `PetUI.finalFormOf/finalSkillOf/skillEffectLine`。
8. **🟠8 任务条挡市集**：常驻浮层默认收成小胶囊，**有引导任务才自动展开**；玩家手动操作过则完全尊重。
   折叠按钮点击区加大。⚠️ toggle 的 onclick 必须用**本次渲染算出的 collapsed**，不是模块变量。
9. **🟠10 术语太多**：不删术语，改成就地能看懂 —— 转生/血脉天赋/成长/血统 四处加白话与悬停说明。
10. **🔴1 守值**：新增 `tests/vtest_onboarding_guard.js`（暗层 pointer-events 契约 19 断言）。

## 🔴 用户实机抓出的错（已修，教训值钱）
觉醒 tab 显示「未开启」不对。我两个错：① **自己拼门槛**（用 `getEvolveStage>=5` 判终形态，
而觉醒页口径是"宠名在 `activeSkills` 表里"；合成页还额外排除穿装备/在售/神级）⇒ 现在一律调 core 判定
（`Merge.canSynthesize`/`canNirvanaMain`）。② **把"没资格"和"缺材料"混成一态** ⇒ 现在分「未开启」/「缺材料」。
⭐ 通用：**任何"这个功能能不能用"的提示都要调系统自己的判定，不要在 UI 重拼条件**。

## ❌ 用户否决/暂缓
- **材料货币锚点**（用户质疑"万物可交易为何要材料当钱"）：我给了三条理由（能比价/能按数量收税/防 RMT），
  承认"货币太杂、装备不能直接换装备"是真问题；建议内测前不动结构。用户说「这个不做了」⇒
  **价格旁显示"我有多少"、上架默认选重铸石，均不做**。以物易物留到内测后由他拍板。
- 进化分支**不加数值差异**（用户拍板，宪法 §J）。

### 收尾两项（清单 🟠 至此全部处理完）
- **🟠9 宠物页加载空白**：根因 = `ui-shell.js` 切到宠物页**只切显示、不渲染内容**，要等下一次全局刷新才填
  ⇒ 中间一整块空面板。修法与百科/市集/地图页一致（切页当场渲染，失败只记 console 不连累切页）。`?v=20260922petfast`。
  ⭐ 通用：**新增/修复"切过去先空白"的问题，先看 shell 里这一页有没有"切页即渲染"这一句**。
- **🟠7 商品刷新太快**：根因 = 假卖家 tick **每 30 秒无条件 +perTick 件、只增不减**（87→496）。
  修法 = 每池加上限（装备48/宠20/材料16/蛋12），到顶不补、被买走才回补。
  新 config 字段 `marketBot.maxActive / petMaxActive / maxMaterial / maxEgg`；`?v=20260922botcap`。

## 🔴 提交被并行会话阻塞（2026-09-22 用户验收通过、准备提交时发现）
用户已验收（"应该没问题了"），但**这批改动无法干净提交**：
另一会话正在做「外壳 immersive 改造」（在途文件：`css/shell-immersive.css`、`docs/_demo-shell.html`、`_preview-shell.html`、
`_shot*.png`、`Assets/City/frame*.png`、`ui-capital/ui-codex/ui-console/ui-dev*/ui-friends/ui-leaderboard/ui-shop/ui-social.js`、
`package.json`、`启动游戏.bat`…），并且**和我改了同一批共享文件**，diff 行数明显大于我的改动量：
- `config.js` +56（我只加 9 行）｜`ui-pet.js` +159（我约 100）｜`ui-market.js` +59（我约 34，含他的名牌 +22）
- `PROJECT_CONSTITUTION.md` +29（我只加 15）｜`game.css` +70｜`游戏.html` +40/-（含他新增的 css/js 引用）
⇒ **现在提交/push 会把对方未完成的半成品一起推上线**（GitHub Pages 对访客立刻生效）。
⭐ 决策：**先不提交**（推荐等那个会话收工或让它先交它那部分；退路 = 我逐块 `git add -p` 只挑自己的块，慢且易误提）。
⭐ 教训：**多会话并发时，改共享文件之前先看 `git status` 里有没有别人在改同一批文件**；
本次两边的改动在 12 个共享文件里交织，无法按文件切分。

## 状态
本批共 **12 项改动全部未提交**，等用户实机验收后一起 add（只 add 自己改的路径）。
回归常绿：90 测 87 过 / 3 失败（全是存量红 action_freeze / bugfix / pet_skill）。
⚠️ 2026-09-22 本文件曾被另一会话**整份重写覆盖**（丢过一次记录），已补回；多会话时别在同一份日志上覆盖。

---

## 宠物军团 UI 重构 V1+V2（2026-09-22 第二段，同一会话继续做）
用户给了两份任务单：V1 = 功能项（组件库/进度条/收益预览/二次确认…），V2 = **线框级布局规格**（左栏 360px、
CTA 栏统一 72px、三列等高、间距 8/16/24、任何区域空白 ≤240px…）。冲突时以 V2 布局为准。

### 落地内容
- **新文件 `docs/css/pet-legion.css`**：宠物页组件库（只作用于 `#tab-pet`）。含组件：统一宠物卡、CTA 按钮
  （`.lg-cta` / `--ghost` / `--danger`）、进度条 `.lg-bar`/`.lg-prog`、chip 网格、checklist、步骤条 `.lg-steps`、
  V2 骨架（`.lg-ctabar` 72px CTA 栏、`.lg-row3` 三列 300px、`.lg-synth3` 合成三列、`.lg-mats5` 觉醒材料 5×2、
  `.lg-focus` 焦点卡 160px、`.lg-taskbar`、自定义下拉 `.lg-select`）。
- 5 个 tab 全部重排为 V2 骨架：**页签 = 内容区 + 底部 CTA 栏**，`#evolve-confirm/#synth-confirm/#merge-confirm/
  #awaken-confirm/#profile-confirm` 五个容器几何位置完全一致（探针实测 y=994 h=72，五页相同）。
- 组件统一走 `PetUI.*`（ui-pet.js 里新增：renderList/listCard/stepsHtml/barHtml/chipsHtml/condListHtml/
  selectHtml+bindSelect/confirmIrreversible/fmtNum/stageTotal/pushRecord/recordsHtml）。
- 二次确认统一复用 `#salvage-modal`（`UI.salvageConfirm`）；化石/合成/涅槃/觉醒/培育五个入口都有。
- 千分位、邮箱脱敏（`UI.maskEmail`）、掉落同类合并（consoleLog 的 mergeKey，2 分钟窗口 + 就地改 DOM）、
  聊天频道未读徽章（`.chat-tab-unread`）、`openQuestPanel(questId)` 定位到「觉醒之路」。

### 🔴 两个真坑（都在 CSS，且都不在宠物页里）
1. **`grid-area` 命名线会泄漏到新网格**：`game.css` 原 `.pet-wrap--altar .altar-stage` 用
   `grid-template-areas: "main arrow target"` + 给 `.es-main/.es-arrow/.es-target/.forge-bar` 挂 `grid-area: main/arrow/target`。
   新布局把三列下移到 `.lg-row3`/`.lg-synth3` 后，那些 `grid-area` 变成新网格上的命名线 ⇒ **三列变五列、炼妖炉那格被压成 34px**。
   修法 = 删掉那套命名网格（`display:flex` + 去掉所有 `grid-area`），不要在子项上打补丁。
2. **`.tab-page.active` 是 `display:block`、高度 auto** ⇒ 里面所有 `height:100%` 都失效，内容区撑不满、
   `.page-container` 反而出现"内容没满却可滚"。修法 = `shell-immersive.css` 里给 `#app:has(#tab-pet.active) #tab-pet { height:100% }`。
   （同类需求以后照这个写：**先给页容器确定高度，再谈内容撑满**。）
   附带：`game.css` 里 `.pet-wrap--altar .evo-card .avatar { width:180px !important }` 已删 `!important`（新布局按上下文定尺寸）。

### 1080p 现实（别再说"无滚动条"）
右栏（消息栏）吃掉 23.6% 宽、顶栏+页签+CTA 栏吃掉 ~150px 高 ⇒ **进化 / 涅槃 / 觉醒三页内容量天然超一屏**。
按 V2「区块内滚动」的口径：**滚动限制在面板/区块内，CTA 栏永远固定可见**；资料页、合成页做到整页零滚动。
探针体检结论（1920×1080）：整页滚动=无（5 页）、属性卡缝隙=16px、合成三列等高=320/260/294 × 280、内容→CTA 栏空隙 ≤29px。

### ⭐ 取舍定案（用户拍板）：整屏放下优先
出战视觉区最终取 **232px**（不是 V2 写的 300px）：1080p 下按 300px 排会把「属性卡 + 下一步/次级属性」
两行挤出屏幕（实测溢出 46px）。**用户选"整屏放下"**，接受视觉区 232px + 立绘 140px。
⇒ 以后遇到"V2 写死的 px 数"与"1080p 整屏放下"冲突，**一律以整屏放下为准**，并在回复里说明改了多少。

### 验证方式（复用配方）
`node tests/_probe_legion.js`（薄 DOM 桩渲染真模块 → 吐真 HTML）→ 探针 HTML（引真 CSS）→ `playwright-core`
（channel: msedge，`npm i --no-save playwright-core`，实测 6s 装完）量 `getBoundingClientRect` + 截图。
**用完删探针 + `npm uninstall playwright-core`**（已做）。两个坑：探针里 `.tab-page` 忘了加 `active` 会全是 0×0；
内联脚本里塞 JSON 时 `window.__R = ` 后 13 个字符才是 JSON 起点。

---

## 🔴 修 bug：用了经验包不上传服务器（2026-09-22 用户实测报）
`TutorialMode.useExpPack` 的**通用 5 档**（微光经验屑/残魂经验囊/聚魂经验囊/魂晶经验匣/幽冥经验髓）分支：
`Materials.spend` 在**服务端原子扣**包，`Pet.grantExp` 只改**本地** level/exp，**全程没有写云端**
⇒ 包没了、等级只在内存里涨，刷新/重登后回退 = 玩家眼里"经验被吞"。
（引导三档那一路本来就写云端，所以只有通用档出问题；quest.js 里 direct 模式是死代码，`QUEST_EXP_MODE='pack'` 写死。）

修法（`js/core/tutorial_mode.js`）：
- 新增共享 `savePetProgress(pet)`：有 cloudId → `updatePet({level, exp})`；无 → `savePet` 建档并回填 cloudId；
  未登录（无 `UI.getAuthUser`）→ 返回 true（本地玩，没有云端可写）。
- 通用档：`grantExp` 后调用它，整段包进 `IdleBridge.duringPetEdit`（托管期间本地是预演值，先结清真账再写，
  否则写完被下一次服务器真账覆盖回去）；写失败 → `addLog` 明确提示（2026-09-11 那条"绝不静默"的决定）；
  返回值新增 `saved`。
- 引导档那一路**改用同一个 helper**：顺带修掉它原来"只 catch 异常、没检查 `updatePet` 返回的 error"⇒ 写失败静默。
- `ui-bag.js`：背包点卡片吃包后 `r.saved === false` → 就地弹「⚠️ 等级没存上云端」（不能只靠消息中心那条 log）。
- 新增守值 `tests/vtest_exp_pack_cloud.js`（14 条：写一次 updatePet / 写的是自己的 cloudId / level+exp 正确 /
  不 INSERT / 无 cloudId 走建档 / 失败要吭声 / 扣包失败不改等级）。
- 版本号：`tutorial_mode.js` + `ui-bag.js` → `20260922expsave1`。

⭐ 玩家的**已损失经验找不回来**（服务端已扣包）→ 要补偿走开发者面板「加经验」（那条路有 savePet，能落库）。

---

## 🔴 修 bug：通天塔战斗页「层阶刻度条」被顶栏盖住（2026-09-22 用户报）
根因：沉浸式外壳（shell-immersive.css）把三个画面页 `padding:0` 铺满、`.app-topbar` 改 `position:fixed`
（0~44px 浮在画面上）。战斗页里**绝对定位**的件都做了让位补偿（`.bp-topbar { top: calc(12px+var(--topbar-h)) }`、
右栏 `padding-top: var(--topbar-h)`），但**静态流**子元素没补偿 —— 塔的 `#tw-ladder` 是 `.bp-arena` 里唯一的
静态流顶部子元素，从 y=0 起排 ⇒ 整条压在顶栏底下（探针实测 ladder y=0，顶栏 0~44 不透明黑条）。
野图没这问题：没有刻度条，stage-wrap 从 0 铺满就是设计本身。

修法（第一原则 = 外壳自己的让位原则「顶栏脱离文档流 ⇒ 顶上的内容必须让位」）：
- `ui-tower-battle.js`：进塔 `ensureLadder` 给 `.bp-arena` 挂 **`tw-mode`** 标记，`hideLadder`/退出塔摘掉
  （classList.add 幂等，renderLadder 每次塔事件都会走到）。
- `shell-immersive.css` ⑤：`.bp-arena.tw-mode { padding-top: var(--topbar-h) }` +
  塔口径 `.bp-topbar` 收回文档流（`position:static; margin:10px 12px 2px`）——
  HUD 和刻度条按 DOM 顺序排、**零魔法数、永不相叠**（HUD 高度是内容驱动的，写死偏移必碎）。
  野图不挂 tw-mode，HUD 照旧 absolute 浮起、画面照旧顶到窗口边。
- 验证：真实 游戏.html 去 script → 注入刻度条（照 renderLadder 结构）→ playwright 量矩形+截图：
  顶栏 0~44 / HUD 58~116 / 刻度条 118~150 / 舞台 150~1080，无重叠。
- ⚠️ 探针坑：整页 html 探针必须先 `#login-screen.remove()` + `#app.removeAttribute('hidden')`，
  否则全 0×0（登录浮层盖着，app 是隐藏的）。
- 版本号：`shell-immersive.css`、`ui-tower-battle.js` → `20260922tw1`。`vtest_tower_ui`(27) / `vtest_tower`(105) 全绿；
  `vtest_enemy_level` 全量里红过一次，单跑 3 次全绿 = flaky（该测试跑真实时间 23.8s）。
- 🔴 npm 坑：`npm i` 报 `mkdir npm-cache\_cacache\tmp UNKNOWN` = 用户级 npm 缓存目录坏了，
  加 `--cache "$env:TEMP\fos-npm-cache"` 绕过即可（别动共享缓存）。

---

## 宠物军团 UI 重构 V3（视觉降噪修订，同会话第三段）—— 已实机验证 ✅
任务单：V3（合成页空壳 / 涅槃页四个红框 / 配色收敛 / 文字减负 / CTA 位置规范）。
改动文件：`css/pet-legion.css`、`js/ui/ui-pet-{synth,merge,evolve,awaken}.js`、`游戏.html`；`?v=→20260922legion4`。
新守值：**`tests/vtest_pet_legion_v3.js`**（CTA 常驻 / CTA 在右列 / `:empty` 不画框 / 无 `!important` / 「等级重置 Lv.1」≤2 处）。
回归：**88 通过 / 3 存量红，零新红**。

### 🔴 三个"看截图才抓得到"的真 bug（都已修根因，别再犯）
1. **`game.css:4118` 还有一条 `.pet-wrap--altar .evolve-stage .es-main/.es-target { justify-content:center; align-items:center; overflow-y:auto }`**
   —— 它让卡片宽度=内容宽度（实测副宠卡 227px / 列 371px），**"拉伸占满右列"从来没生效过**；
   `overflow-y` 也是红框2 那根竖条的另一半来源。pet-legion.css 里必须显式写
   `align-items:stretch; justify-content:flex-start; overflow:visible` 盖掉（我当时只写了 flex-direction，漏了这两条轴）。
2. **`game.css` 的 `.pet-wrap--altar .hint / .warn` 选择器太宽**：把区块标题里的行内小字、预览区的一行警告也套成
   "虚线框/红框盒子" ⇒ 一屏冒出好几个小方块。修法 = 在 pet-legion.css 按上下文收窄（标题内/预览区内去掉框）。
3. **`.es-preview` 空的时候照样画框**：各页 `pb.innerHTML=''` 后那只 100+px 的空箱体还在
   ⇒ 用 `#tab-pet .es-preview:empty { display:none }`（判据 `:empty`，别靠各页自己 hide）。

### 🔴 内容性 bug（我上一版自己写错的）
合成页「副素材条件」文案写死"终阶 + Lv.60"，而 core `Merge.getMergeCandidates(mainId, cfg)` 只按
`cfg.minLevel`（synthesize=**40**）筛 ⇒ 界面一边把 Lv.40 的宠列成可选、一边写它"不达标"，自相矛盾。
⭐ 规矩：**checklist 的判据必须逐条对齐 core 的筛选函数（连顺序都一致），不许沿用旧文案猜**。

### ⭐ 验证配方（这次定型，下次直接照抄）
1. **探针**：`tests/_probe_v3.js`（薄 DOM 桩 + vstub + VTF.FILES 加载真模块）→ 真调 `UI.renderXxxTab()` →
   把各容器 innerHTML 灌进从 游戏.html 切出来的 `#tab-pet` 段（引真 CSS）→ `_probe_v3.html`。
2. **playwright-core（channel: msedge）1920×1080** 逐 tab 截图 + 量 `getBoundingClientRect`
   （CTA 栏五页 y=980 h=72 / 主按钮 200×48 / 右边距 24px 全一致）+ `read_file` 读 PNG 自检。
3. 🔴 **坑：桩里 `UI.renderAll()` 不会重渲染"非当前页"** ⇒ `pick(...)` 之后必须**显式再调一次** `UI.renderXxxTab()`，
   否则截到的永远是"未选主宠"的空态（我为此白查了一轮）。
4. 🔴 探针外壳必须还原 app-shell 的高度链（`#app{height:100vh;display:flex;...}.app-body{display:flex;flex:1}`），
   否则 `#tab-pet{height:100%}` 解析成 auto → 页面撑到 3000px 高、CTA 栏跑到 y=3964（全是假数据）。

### 落地清单（对照 V3 §1-§6）
- 配色：每页唯一主题色（金/铜橙/赭橙/紫）+ `--lg-gold`（数值金）+ `--lg-danger:#e5484d`（唯一警示红）+
  涨跌绿/红（只进属性对照表）+ 背景三级。神级宠在宠物页内**改用金**（紫只留给觉醒主题）。
- 字号阶梯 `--lg-fs-title/20 · sec/15 · body/13 · help/11`（页面标题从 24 收到 20）。
- CTA 栏搬进右列（DOM 挪位，非 margin 硬拉）+ 右对齐 + 24px 边距 + 48/200 + 呼吸光（hover 时停动画把发光还回去）。
- 合成页：中列去箱体（丹 128px 居中）+ 常驻确认按钮（含置灰+原因）+ 右列空态垂直居中（`.lg-center` 用 flex:1，别用 height:100%）+ 主素材卡删红字。
- 涅槃页：删标题步骤提示（五页统一删 `.altar-sub`）/ 去竖条与双滚动（**一页只留 .evolve-stage 一个滚动容器**）/
  行高自适应（min-height 300px 替代固定 300px）/ CTA 入栏 / 正文只留一行警示条。
- 文案：规则全部进「详细规则」折叠区或 `?` tooltip；「等级重置 Lv.1」收敛到统计格+警示条；黑白 checklist。

### ⏳ 未做 / 待用户实机验收
- ✅（第二轮已做完）觉醒页 1080p 一屏放下：删「总进度/最短板」两行（与任务条里的同一个数）只留一句规则、
  注释行内化、区块间距 16→12 —— `#awaken-detail` overflow 实测 25 → **0**。
  ⚠️ 1440×900 仍要滚 172px（窗口太矮，物理放不下，内部滚兜底）。
- ✅（第二轮已做完）资料页名录换 `.pet-vault`（与四个祭坛页同一套单列组件）—— 实测卡片 326px 全宽、
  纵向单列，名字不再截断；列表长了自己滚。
- ~~未提交~~ → **已全部提交并推送（2026-09-22 19:0x）**：`1974c57`（宠物军团 V3，7 文件）+ `e38d0ef`
  （在途改动一并入库，63 文件：内测修复/沉浸式外壳/serve 与 tools/神宠立绘/云端迁移/AI 记忆）。
  已按内容核实线上生效：Pages 的 `pet-legion.css`(50.9KB, 含 --lg-danger) 与 `ui-pet-synth.js`(含 renderSynthCtaIdle) 都是 V3。
  ⚠️ 只入库了 `assets/pets/god/bloodmoonfox/`（1.8MB，被 pet-sprites.js 引用的那套）；
  `god/` 下 71MB 的加工素材（视频/gif/多版设定图）、`anim/god`、`pets/ref`、`City/frame2-4+sprite` **都没入库**
  （未被代码引用，要不要入库等用户发话）。
  ⚠️ 顺手删掉了根目录两个 0 字节垃圾文件（`_probe_v3.js`、`游戏.html`）。
  ⚠️ 仍未入库：`docs/node_modules/`、`.agents/`、`.claude/`、`skills-lock.json`、`docs/_demo-shot.png`。
- `?v=` 最终 = `20260922legion5`（pet-legion.css + 四个 ui-pet-*.js）。
