# 未决 / 待办

> 入口：`MEMORY.md`｜**上线前必做清单的权威在 `PROJECT_CONSTITUTION.md` §三**，本文件只放不在那里的零散未决项。
> 本文件按需读取，不占每轮注入预算 —— 事项了结后记得删掉对应行。

## 已定但要注意
- ✅ 魔石 = **便利货币**（`Config.shop.enabled=true`，只卖便利）；**收款通道仍关**；
  来源 = 卡密 `redeem_code` / 管理员 `grant_gems`。

## 🔴 未决项
1. **塔重置卡获取断链** —— 现状：**来源暂未开放**（09-16 删掉了每周兑换，因为"任务能产重置卡"违反边界基线）。
   要开只能走付费（但与"魔石只卖便利不卖数值"冲突，见 `PROJECT_CONSTITUTION` §二 D5 与 09-16 记录）。**待拍板。**
2. ~~`add_material` 无限刷限制~~ ✅ **已完成 2026-09-17**：加了**每日上限 20000**
   （`supabase/migrate_material_daily_cap.sql` + `Config.security.addMaterial.maxPerDay`）。
   依据：正常挂机 24 小时产出量级约 2000，留 10 倍余量 → 正常玩撞不到，撞到的基本是脚本。
3. **打造 / 进化 / 定价 / 副本 / 塔仍是客户端权威** → **收真钱前必须搬完**（权威清单见宪法 §三）。
4. **删号冷静期**。
5. ~~聊天显示名 = 邮箱前缀 → 泄露隐私~~ ✅ **已核实不存在（2026-09-17）**：
   注册页有昵称框，留空会自动生成（`supabase.js` 的 `randomNickname()`，形如"灰烬行者1234"）并写库；
   聊天取的是这个昵称，只有自动生成也失败时才回落邮箱前缀。
6. **背包 / 宠物无上限、聊天无限频** —— 背包/宠物上限已于 09-16 落地（`Config.capacity` + 扩建道具）；
   **聊天仍无限频**（未做）。
7. **副本 19 空隙回血漏洞**。
8. **「冥花线」原创宠接入**（2026-09-21 用户：「先到这里」⇒ **暂不接，只登记**）——
   形象五阶已定稿：`docs/assets/pets/ref/minghua/`（孢子团 → 菌兽 → 菌王 → 菌将 → 冥花神，512 透明立绘 + 源图）。
   要接时是**一项多改动**（别再从头查）：
   - `config.js` 加血统线（**数值红线**：三围 / 成长系数 / 速度 / 血统被动 / 终阶技能 / 进化形态树）
   - **形态树补第 4 层**：`菌王 →(Lv40) 菌将 →(Lv60) 冥花神`（⚠️ 不补的话 `getEvolutionRoutes` 会**静默降级成"形态不变"**）
   - **`form` 按线覆盖**（用户已在 A/B 里选 **A：只给这一线破例，不做全局改**）——
     ⛔ 别改 `stages[3].form`（全局默认值，动了 8 线全变）；**全量消费方 5 处**清单在 `docs/宠物设定-冥花线.md` §七之五
   - 获取途径（蛋 / 掉宝 / 任务，先定一个）+ 补守值（该线走换形态、其余 8 线仍"形态不变"）
   - 参考：`docs/宠物设定-冥花线.md`（含"怪物化五条硬约束"、出图经验、参考图模式）

## ⏸️ 内测准备（2026-09-17 A/B 档落地情况）
- ✅ A1 系统收货额度提到 **150 件装备 / 100 笔材料**（`game_config_overrides` 的 bot 配置）
- ✅ A2 新人第一笔成交优先 + 成交实时提示（`migrate_newcomer_first_sale.sql` + `ui-market-notify.js`）
- ✅ A4 玩家反馈入口（`migrate_feedback.sql` + `ui-feedback.js` + 开发者面板「反馈」页）
- ✅ B2 市场脉动条（在线人数 + 最近成交；`migrate_market_pulse.sql` + `ui-market-pulse.js`）
- ✅ B3 内测看板（开发者面板「看板」页；`migrate_ops_board.sql` + `ui-dev-ops.js`）
- ✅ B4 材料上报每日上限
- ⏸️ B5 手机端：**断点覆盖已查（市场/背包/宠物/战斗/追踪栏都有 980/900/640 断点），但没有真机实测** —— 要用户拿手机走一遍
- ⏸️ **内测看板看不到"材料产出 vs 消耗"的账**：产出只记在 `battle_logs.detail`（结构未固定），
  消耗散在各 RPC，**没有材料流水账本**。要看得先建账本。
- ⏸️ 里程碑任务 9 条 + 登神之路 8 环：仍未落地，但**定位是内容侧（给副本/塔成绩发奖）**，
  不是补资源空档（该判断已被证伪，见 `docs/档案/交互审查与修复_2026-09-17.md` §六）。

## 导出「产出归属总表」时自动查出、**等用户拍板再改**（2026-09-17）
- **`core/mat-wiki.js` 区域材料词条图号错 5 处**（玩家能看到）：`sources()` 用 `areaMaterials` 的对象键序号
  `i+1` 当图号，而键顺序 ≠ `Config.battle.areas` 顺序（`blight-heart` 排第 6、实际是图 10）。
  错的是：腐变之心（写图6→实图10）/ 回响之羽（图7→6）/ 腐沼黏液（图8→7）/ 余烬残灰（图9→8）/ 魂渊之尘（图10→9）。
  修法 = 改成按 `Config.battle.areas` 查真实序号（一行）。**用户本轮明确「什么都不改」，所以只登记。**
- **通天塔重置卡 = 当前唯一断供道具**：没有任何获取途径，但塔详情页/百科页写着「魔石商店购买（60 魔石/张、每周限购 3 张）」，商店从未上架。
  二选一：上架 or 把文案改成「暂未开放」。
- **`Config.trade.materials` 有 2 条重复登记**（`锁魂玉` / `琼浆玉露`，`config.js` 1379-1382）→ 市集下拉出现重复项；删掉重复两行即可。
> 三处都会由 `docs/tests/export_matrix.js` 自动检出并写进 `docs/产出归属总表.xlsx` 的「异常与待定」页签，**修好重跑就消失**。

## 掉落归属：已查出、**等用户拍板再改**（2026-09-17 晚）

- **精粹进化素材在图 9~10 只有 0.89~1.04 个/小时，图 8 是 6.37** —— 与普通档**一模一样形状的断层**
  （权重：图 9/10 = 4~5，图 8 = 41.3）。用户 2026-09-17 晚只点名了「普通进化素材」（已修），
  **精粹档原样保留、等发话**。要修的话同类推法：把图 9~10 的 `'精粹进化素材'` 抬到 **15~16** ⇒ ≈2.5 个/小时。
  ⚠️ 改前先跑 `cd docs/tests && node probe_quest_audit.js` 定基线，改后 `npm run check`。

## 塔：2026-09-17 晚改完后**留了两个待定**（等用户发话）
- **保底（不足 5 层）×3 与第 5 层档位 ×5 里的「鉴定石」还要不要？** 本轮只把鉴定石撤出**随机材料池**，
  这两处**确定性给**的留着了 —— 因为塔每局掉约 9 件**未鉴定**装备，全清会导致"打完塔开不了装备"。
  全清的话记得同时给一条替代的开箱路径（或接受必须回地图刷）。
- **至尊神石要不要也抬？** 现一局 5.4% / 8.2% / 18.6%（≈19 / 12 / 5 天一颗），**与改前完全一致**（刻意没动）。
  它 =「必定出神级宠」，上一轮刚从守关 Boss 挪进塔收紧，用户单独立过规矩 —— 所以**没被"顺手抬稀有档"带走**。
  它现在是塔里唯一低于 10%/局 的东西（探针 ③ 会自动标注"刻意例外"）。

## ⚠️ 塔的稀有度 ↔ 「通天塔重置卡」硬耦合（用户 2026-09-17 晚追问，已写进 tower-config）

> 📌 **用户会忘自己砍过它**：2026-09-17 晚他又问「？？？任务里现在没有了？」——**是他 9-16 砍的**
> （原话「**任务给的重置卡太多了**」，`config.js:1023` 有完整缘由链：09-10 建 → 09-16 成本提到 24 个腐印他仍否 →
> 他点明"问题不在价格，在**任务能产重置卡**本身"）。**再被问到时先引这条，别重新论证一遍。**
>
> ✅ **同晚已改判并落地**：用户在三选一里选了 **A = 加回兑换**（每周限 1 张、成本 36 个腐印）——
> 条目是 `config.js` 的 `ex_week_reset_card`，三道护栏写在它上面。**下面第 1 条"卡是死的"已作废；第 2、3 条的口径永久有效。**
- ✅ **现状（09-17 晚改判后）**：卡**有来源了** ⇒ 一周 8 局封顶（1 免费/天 + 兑换 1 张/周）。
  历史：~~卡是死的~~ ⇒ `freePerDay: 1` ⇒ **塔一天 1 局封顶**。所以塔所有「一局 X%」的稀有度都按这个算的。
  （60 魔石/每周限 3 张那两个字段是**预留死配置，从未上架、读不到任何 UI**；任务侧的「塔券铸成」2026-09-16 已删。）
- 🔴 **给卡开来源时必须先回来复核稀有档**：上限 =「1 免费/天 + 3 张/周」= **一周 10 局 vs 现 7 局 = ×1.43**
  ⇒ 每个「一局 X%」乘 1.43（魂晶经验匣 11 天 → 7.7 天、至尊神石 19 天 → 13 天）。
  ⛔ 别改 `limitPerWeek: 3` 那道限量刹车（改了就是从 ×1.43 变成翻倍）。
- 给来源的两条路（tower-config 里记着）：ⓐ 商店上架（**踩"只卖便利不卖数值"的边界，要用户点头**）；
  ⓑ 挂塔的**深层档位奖励**（如第 25 层 1 张）—— **⛔ 绝不能挂 10 层**（= 每天白送一局，架空每日 1 次）。
- ✅ **已落地（09-17 晚）**：用户选「加回兑换」，**复核结论 = 稀有档不动**（花 36 个腐印换 +14%，不值得为它压低所有人的产出）。
  以下是当时的建议原文（保留）—— **我的建议：不要为了一个"可能上架"的付费商品去压低免费玩家的产出。** 现在按"一天一局"就是对的；
  等真要上架卡时再一起复核（这条就是那个"别忘了"）。

## 暂停中
- **通天塔服务端权威** ⏸️ 暂停（未落地：塔整局模拟 / `tower-run` EF / `migrate_tower.sql` / 切权威源），详见宪法 §三 #6。

## 剧情线（非技术，长期）
- 通天塔的「腐印」（腐蚀度）是给毕业玩家自愿加的杠杆：**只加难度、不加战力** →
  「通关」纯靠硬实力，**不存在「必须叠腐印才通」的付费墙**。
