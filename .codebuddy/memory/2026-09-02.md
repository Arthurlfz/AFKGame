# 2026-09-02

## 教程遮罩 bug 修复（用户截图反馈："导航提示下面页面完全点不到"）
- **问题**：`#tutorial-overlay` 设了 `pointer-events: auto` + 高亮框用 `box-shadow: 0 0 0 9999px` 撑出遮罩，结果整层（包括 box-shadow 撑出去的部分）都拦截点击，下层选宠卡点不到。
- **修正**（`docs/css/ui-polish.css`）：
  1. `#tutorial-overlay` 加 `pointer-events: none`（只做视觉压暗，不挡点击）
  2. `.tut-panel` 显式 `pointer-events: auto`（弹窗按钮可点）
  3. `.tut-spot` 确认 `pointer-events: none`（高亮目标本身可点）
  4. 删除旧版用 9999px box-shadow 撑遮罩的代码 + 重复的旧 `.tut-panel`/`.tut-progress` 定义
- **行为变化**：
  - 询问框阶段：玩家直接点选宠卡 = 跳过教程（无礼包），不再被遮罩拦住
  - 章节引导阶段：高亮目标可点（点选宠卡/穿装备/进化等真实操作正常推进）
- **JS 配套**（`docs/js/main.js` 选宠 onClick）：若 `asked && !started`，自动调 `TutorialUI.abort()` 标记 skipped，符合"直接选宠 = 跳过"语义。
- 测试：vtest_newbie / vtest_quest 全绿，lint 干净。

## 第二波修复（用户注册新小号反馈："没有新手引导" + "显示不了后面内容" + getBoundingClientRect null 报错）
1. **教程状态改成按账号隔离**（根因：state 存 localStorage 用固定 key，主号点过"跳过"→ 注册新号读到旧 asked=true → 询问框直接 return，新号完全没引导）：
   - `tutorial_mode.js` 加 `bindUser(email)`，savedKey 变成 `fos_tutorial_state_<email>`；导出 bindUser。
   - `ui-common.js` 的 `setAuthUser` 里调 `TutorialMode.bindUser(email)`（登录/换号/登出自动切换）。
   - 验证（临时脚本 7 断言全过）：换号后新号 asked 为空能重新弹；切回旧号保留 skipped；登出 guest 独立。
2. **getBoundingClientRect null 崩溃防护**（报错会打断流程，可能正是"引导不显示"的原因）：
   - `ui-popover.js` position() 加 `!anchor || typeof anchor.getBoundingClientRect !== 'function'` 保护。
   - `ui-tutorial.js` 给 highlight / render / startPoll / showEntryAsk / ensureDom 全部加 try-catch 降级（渲染失败只 warn，不让引导消失；body 未就绪不建 DOM）。
3. **缓存版本号**：批量更新 游戏.html 里改动文件的 `?v=` 为 20260902a（config/quest/tutorial_mode/tutorial-content/ui-tutorial/ui-common/ui-quest/equipment_craft/main/ui-polish.css/ui-popover 等），避免用户拿到旧代码（旧 CSS 带 72% 黑遮罩）。
- ⚠️ 遮罩背景本身在上一轮已改为 transparent（415 行无 background/blur），用户看到的"显示不了后面内容"多半是缓存的旧 CSS。让用户强刷验证。

## 引导系统改版：弹窗/HUD → 任务系统（2026-09-02 用户拍板）
用户连续三轮反馈后拍板："算了，你把这个引导系统做成任务系统吧"，并确认：
- **保留加速+礼包**（30 分钟目标不变，只是引导形态换成任务）
- **引导只需要体验到涅槃为止**（t1~t10 到涅槃），涅槃之后（t13 魂铸）作为普通新手任务

### 实现
1. **删除分章教程**：
   - 删除 `docs/js/ui/ui-tutorial.js`、`docs/js/ui/tutorial-content.js`（HUD/遮罩/高亮/询问卡全没了）
   - `游戏.html` 移除两个脚本引用；`ui-polish.css` 清掉教程 CSS 死代码（留注释说明）
   - `main.js` 移除 showEntryAsk/abort/遮罩恢复逻辑
2. **引导载体 = 新手链任务 + 顶部引导条**（quest-tracker，ui-quest.js 已有：当前任务+进度条+跳转+跳过）：
   - config.js 新手链任务加 `isGuide: true` 标记（t1~t12，含升级缓冲；**t13 不加**）
   - 保持 `guide: {page,btn}` 跳转对象不变（ui-quest guideOf 依赖）
3. **`tutorial_mode.js` 重写为引导驱动**：
   - `checkGuide()`：有未完成引导任务 → 自动 enter() 加速 + 发该任务补给；引导段全完成 → exit() + 发礼包
   - 补给 `grants` 从 `chapter` 改为 `taskId` 关联任务（t4 进化素材/t5 打造石/t6 白装/t7 蛋/t9 副宠+合成石/t10 副宠+涅磐兽/t13 魂铸材料）
   - 礼包已领标记 `hasClaimedPack/markClaimedPack`（按账号）；跳过标记 `markSkipped`（跳过引导不发礼包）
   - `grantStarterPack()` 从旧 ui-tutorial 搬来（经验包+蓝装三件套+材料+成长8副宠设出战）
4. **quest.js**：导出 `isFinished/isUnlocked`（checkGuide 用）；`skipGuide()` 调 `TutorialMode.markSkipped()`；`completeQuest` 成功后调 `checkGuide()`（交任务即推进引导）
5. **接入点**：main.js 选宠完成后调 `checkGuide()`；quest.js 每次交 tutorial 任务后调

### 测试
- 临时脚本（12 断言全过）：引导段 12 条、t13 非引导段、guide 跳转对象保留、checkGuide 自动加速、补给幂等、礼包内容+出战宠、跳过不发礼包。临时脚本已删。
- 回归：vtest_quest / vtest_newbie / vtest_trait_soulcast / vtest_ui / vtest_task12_ui / vtest_evolution / vtest_mutation 全绿，lint 干净。

## 新手任务「怎么做」引导（步骤文字 + 金色高亮框）— 2026-09-02 落地
用户拍板：引导条要写清「怎么做」步骤 + 当前该点的按钮/卡片闪金光高亮（不挡点击），只给新手链引导段 t1~t12。
- **config.js**：新手链引导段任务各加 `hint`（怎么做，HTML 目标动作用 `<b>`）和 `target`（高亮选择器）：
  - t1/t2/t11/t12 挂机 → `#btn-battle`；t3 穿装 → `.pet-tab[data-pet-tab="equip"]`；t4 进化 → `evolve` tab；t5 打造 → `.sb-btn[data-page="equip"]`；t6 分解 → `#btn-salvage`；t7 孵化 → `.egg-hatch`（⚠️ 宠物页没有 egg tab，孵化面板在资料 tab 内，guide.tab 已从 'egg' 改为 'profile'）；t8 上架 → `market-sell`；t9 合成 → `synth` tab；t10 涅槃 → `merge` tab。
- **quest.js**：`getGuideQuest()` 返回对象补 `isGuide/hint/target` 透出。
- **ui-quest.js**：`renderQuestTracker` 新手引导段任务在进度条下渲染 `.qt-hint`（怎么做）；新增 `updateGuideHighlight()`（定位 `#guide-highlight` 金框，null/try-catch 防护）+ 500ms 位置跟随定时器（仅高亮可见时跑，隐藏即停不空转）。
- **ui-polish.css**：`.qt-hint`（步骤文字，`--text-dim` + 目标 `--accent-hi` 加粗）+ `#guide-highlight`（fixed 金框，2px 熔岩金边+外发光+脉动，`pointer-events:none` 不挡点击，`prefers-reduced-motion` 关闭动效）。
- **游戏.html**：ui-quest.js / ui-polish.css 版本号升 20260902f。
- 测试：vtest_quest 新增 3 断言（引导段都有 hint+target、t1/t4/t9/t10 有 target、t13 无 target）全绿；临时 UI 测试 7 断言（getGuideQuest 透出、引导条含 qt-hint 文案、高亮调用不抛错）全过后删；vtest_newbie 回归绿。

## 加速 buff 加时间限制（2026-09-02 用户拍板「时间限制」）
用户问"玩家一直不完成任务 buff 是不是一直都在" → 拍板加时间限制：
- config.js `tutorialMode.buffDurationMin: 30`（0 = 不限时）
- tutorial_mode.js：新增 buff 起始时间戳（`fos_buff_start_<email>` 按账号）；`buffExpired()` 判断是否超时；checkGuide 里：首次开启记录起始时间，超时后**不再 enter（任务还在但不加速）**，已激活则自动 exit；**教学补给照发不随 buff 超时停**（保证零卡手）。
- 验证（临时脚本 7 断言）：首次进加速+记录时间戳、模拟 31 分钟前 → 自动退加速、exp 还原 1、任务还在、补给仍发。临时脚本已删。
- 版本号 tutorial_mode.js 升 20260902g；回归 vtest_quest/newbie/trait_soulcast/ui 全绿。

## 引导加速改成绑定道具「引导祝福」（2026-09-02 用户拍板）
用户："把这个做成道具把绑定道具" → 加速=绑定道具，用掉才生效 30 分钟；开局（选宠进引导时）自动送 1 个并明确提示；删掉自动进加速。
- config.js：`tutorialMode.blessing = { name:'引导祝福', durationMin:30, icon:'⚡' }`（保留 buffDurationMin 兼容）；`trade.materials` 注册 `{id:'blessing', name:'引导祝福', icon:'⚡', category:'buff'}`（**不进 market.js 白名单 → 不可上架=天然绑定**）。
- tutorial_mode.js：
  - checkGuide 删掉自动 enter()（有引导任务只发补给）；已激活且超时 → exit 兜底。
  - 新增：`hasBlessing()`（getQuantity>0）、`useBlessing()`（spend 扣 1 → writeBuffStart → enter；加速中拒绝/无道具拒绝）、`blessingActive()`（起始时间存在且未超时）、`blessingRemainSec()`（按钮剩余分钟）、`grantInitialBlessing()`（账号幂等标记 fos_blessing_given_<email> → gain 1 → addLog 提示用途+绑定）、`startGuideRoutine()`（1s 超时自动关定时器，仅引导任务存在时跑，完成后停）。
- main.js 选宠完成处：先 grantInitialBlessing（发道具+提示）→ 再 startGuideRoutine（发补给+启动超时定时器）。
- ui-quest.js 引导条：新手引导任务加「⚡ 用引导祝福」按钮（未激活且有道具可点 → useBlessing + toast；激活中 disabled 显示"加速中（约 N 分）"；无道具不显示）。
- ui-bag.js CONSUMABLES 加引导祝福展示（desc 提示去引导条用）。
- 验证：临时测试 21 断言全过（配置/注册/幂等发放/不自动加速/使用扣道具+加速+起始/重复使用拒绝/超时自动关/补给照发/礼包保留）；6 个 vtest 回归全绿。⚠️ 测试子代理 model 仍损坏（custom-local:gpt-5.6-terra not found），改用主 agent 自测。
- 版本号升 20260902h（config/tutorial_mode/main/ui-quest/ui-bag）。

## 打包 skill：Forge of Souls 开发规范（2026-09-02）
用户问"做了这么多事有没有能打包成 skill 的、但不能进 git" → 我打包了一个项目专属 skill：
- 位置：`.codebuddy/skills/forge-of-souls-dev/SKILL.md`（`.codebuddy/` 已在 .gitignore，`git check-ignore` 已验证不会提交；只对本项目生效）
- 内容（59 行）：项目结构（docs/js 代码、docs/tests 测试、config.js 数值源、js junction 坑）、CSS 5 条铁律、云端防连点、测试/验证/版本号套路、Supabase 表清单+全库重置 SQL、用户偏好、核心系统速记。
- 决策：只做一个"项目开发规范"综合 skill，不做 Supabase 单独 skill、不放用户级 .agents/skills/（项目专属，全局无意义）。

## 未鉴定装备两个 bug（2026-09-02 用户反馈）
1. **未鉴定装备丢失（根因=数据库缺列）**：`equip_items` 表没有 `identified` 列，而 `items.js saveItem` 插入该字段 → PostgREST 报 PGRST204「列不存在」→ 整条 INSERT 失败 → 装备（尤其未鉴定）保存失败，刷新丢失。修复：新增迁移 `supabase/migrate_equip_identified.sql`（`alter table equip_items add column if not exists identified boolean not null default true` + 索引），已执行。
2. **宠物装备页/打造页直接显示未鉴定装备属性**：ui-pet.js（已穿槽 + 换装背包）、ui-equipment.js（打造页装备卡）完全没判断 `identified`，未鉴定装备属性直接泄露。修复：
   - ui-pet.js renderEquipSlots：未鉴定已穿装备显示 🔒 + "未鉴定 · 去背包用鉴定石揭晓"，tip 不显示词条。
   - ui-pet.js renderPetEquipInv：换装背包未鉴定行加 `q-unid` 类，icon 🔒、meta "未鉴定·Txx"、tip 封印。
   - ui-equipment.js 装备卡：未鉴定卡片不渲染词缀行，tip 显示封印提示。
   - 背包页本就有 unid 处理（q-unid/封印图标/鉴定模式），未动。
- 版本号：ui-pet.js/ui-equipment.js 升 20260902j。测试：临时脚本 6 断言（云端 identified 读回、各渲染不抛错）全过后删；vtest_quest/newbie 回归绿。
- 语义：未鉴定装备默认 identified=false（equipment.js:175 生成时），已鉴定/教程补给/礼包 = true；fromCloud 只在明确 false 时才保持未鉴定。