# 2026-09-13

## 用户自曝的核心痛点：没有框架，决策反复（"左脑攻击右脑"）

用户原话：项目没有框架、没有 PD(C)D，有时自己决策前后打架。
这与 `PROJECT_CONSTITUTION.md:5` 记录的同源（2026-09-03 用户自曝"用很多 agent 导致幻觉、自己记不住拍过的板"）。

**诊断（我的判断，非用户原话）**：**不是缺文档，是缺"让文档生效"的机制。**
项目已有 250KB 设计文档 + 735KB AI 记忆，但它们**写了就睡死**；真正被反复执行的只有 MEMORY.md 和 4 个 `fos-*` skill。
所以解法不是再加一份 PRD，而是建立三层执行通道：

| 层 | 载体 | 状态 |
|---|---|---|
| 人看得懂 | `PROJECT_CONSTITUTION.md` | 已有 |
| AI 每会话必读 | **`AGENTS.md`（本次新建）** | 新增 |
| 代码改不动 | `vtest_*` 守值测试 | 已有雏形（`vtest_shop.js` 已有「禁止卖数值」静态扫描） |

**后续建议（用户未拍板）**：把最容易反复横跳的拍板固化成守值测试。已知高发区：技能方案（被动→主动已推翻一次）、商店"不卖数值"、不做可交易经验包、等级上限 60。

## 落地改动（4 项，未 commit）

1. **🔴 `.gitignore` 放行 `.codebuddy/memory/` 入库**（安全补漏）
   - 原来 `.codebuddy/` 整目录忽略 → **735KB / 19 天的 AI 记忆零备份零版本历史**，误删或换机全没。
   - ⭐ **踩坑（可复用）**：`.codebuddy/**` + `!.codebuddy/memory/` **无效** —— Git 规定**父目录一旦被排除，其中内容无法再 re-include**（`check-ignore` 会显示命中 `!.codebuddy/memory/**`，看着像生效，实际 `git status` 仍看不到）。
   - 可用写法是 `.codebuddy/*` + `!.codebuddy/memory/` + `!.codebuddy/memory/**`（一级通配不递归到目录内部）。
   - 验证：`git status -uall .codebuddy` 精确列出 **20 个文件**（19 天 + MEMORY.md），`skills/` `agents/` 一个都没混进来 ✅
2. **`AGENTS.md`（根目录新建）**：CodeBuddy 约定每会话自动读。内容＝一句话项目定位 + 三爽点 + 开工前三件事 + **六条红线** + 干完必做 + 第一原则 + 权威源表。**刻意写得短、大白话**。
3. **`00-项目导航.md`（根目录新建）**：Obsidian 首页。含**「拍过的板（不许翻）」表** + **「曾经自己推翻过自己」清单**（技能被动→主动、商店卖数值→只卖便利、经验包可交易→不可交易等）+ 拍板四问 + 文档地图。
4. **`.obsidian/` 配置（新建，3 个 JSON，语法已校验）**
   - `app.json` 的 `userIgnoreFilters` 排除了 `docs/assets`（290 张 png）等 8 个重目录 → 打开不卡。
   - 只建议装 **Dataview** 一个插件。
   - `.gitignore` 已加 `.obsidian/workspace*` → 个人窗口布局不入库。

⚠️ Obsidian 相关坑：`.codebuddy/memory/` **不能**加进 `userIgnoreFilters`，否则 Obsidian 看不见 → 双链全断。要和"避免扫描 290 张图"分开处理（只排除 `docs/assets`）。

## ⭐ 反复 concluding 的方法论（本轮产出）

**"..要不要建 X"类问题的通用判法**：先看**是不是已经有了一个**。
本项目已经陷入过两次"同一逻辑两份实现"（ui-dev.js 面板 vs 假想工作台；vault vs memory），
而审计的头号病因就是「同一逻辑复制两三遍、只有一份对」。
**「再建一个」的默认答案应该是不建**，除非能证明现有那个覆盖不了。

## `PROJECT_CONSTITUTION.md` 事实核对（最后更新停在 09-11，已过期）——已修正

**核对方法：拿代码当事实源逐条反查文档**（不靠记忆）。发现 4 处硬伤：

| 原文 | 实测 | 性质 |
|---|---|---|
| D5「商店 `enabled = false`」 | **`config.js:1532 enabled: true`**（09-12 已开） | 🔴 **方向相反**，最危险——照着它会以为商店还没做 |
| E1「当前是客户端权威，掉落/打造/进化/定价全在前端」 | **`idle-bridge.js:26` 直连 `battle-settle` EF 已部署**，野图已服务端权威 | 🔴 **严重低估进度**，会让人以为啥都没搬 |
| 「26 个 SQL」 | **33 个** | 🟠 数字过期 |
| 「75 个 vtest / 基线 70~72」 | 84 个脚本（vtest 75）；**实跑 70/75** | 🟠 含糊 + 过期 |

已修：上述 4 条 + flaky 名单（tier_rarity 已于 09-12 摘帽，3 个变 2 个）+ **新增 §5「待补录」表**
（10 条已拍板却没进宪法的决策：16 主动技、进化 5 阶、PoE 词缀 T 阶、quest_claims 独立表、
PostgREST 只认表级权限、.gitignore 父目录陷阱等）+ 末尾日期。

- ✅ **实测基线（本次真跑，158s）：70 通过 / 5 失败。失败的 5 个与宪法「稳定红名单」逐条吻合**
  → 宪法第 124 行那份名单**完全准确**，别再怀疑它。
- ⭐ **可复用手法**：**把「文档里的陈述」当断言去代码里证伪**，比"读一遍觉得没问题"有效得多。
  这次最值钱的两条恰好都是**方向相反**（商店开着写成关着、已搬完写成没搬），扫读根本发现不了。
- ⚠️ `run_all.js` **只跑 `vtest_*`（75 个）**，另有 9 个非 vtest 脚本（`sim_*`/`probe_*`/`test_trade` 等）不在回归内。
- ⏱️ 全量最慢 `vtest_fun.js` 61s —— >5s 的多半是**余留定时器没退出**，不是真慢。

同步改了 `00-项目导航.md`（它把服务端权威写成"完全没做"，会跟宪法打架 → 两份文档冲突正是本项目头号病因，发现必须立刻对齐）。

## 补齐并行前置两套工具（承接多 Agent 讨论）

### 1. 一键回归 + 基线对比
- `docs/package.json`（新建，项目此前**从未有过 package.json**）：
  `npm test` / `npm test:v` / **`npm run check`**（跑完自动对比基线）/ `npm run baseline`（存基线）/ `npm run serve`。
  ⚠️ 用法是 `cd docs && npm run check`（docs 是运行源）。
- `docs/tests/run_all.js` **加 `--json=<file>` 落盘**（不传参数行为不变）。
  ⭐ 为什么改现有文件而不是另写 collect 脚本：**另写=把"怎么判定失败"复制第二份**，而「同一逻辑两份、只有一份对」是本项目头号病因。
- `docs/tests/diff_baseline.js`（新建）：对比 `baseline.json` ↔ `_current.json`，输出「新红了 N 个 / 存量红 / 修好了」。
  ✅ **实测基线 70/75 已固化入库**（`_current.json` 已 gitignore，`baseline.json` 必须入库）。
- **变异测试已做**：往 `_current.json` 注入一个假失败 → 如实报警 + 退出码 1 ✅；正常路径输出「没有新搞红的测试」+ 退出码 0 ✅。

> 🔴 **两个首次引导 bug（同一根因，别再犯）**：`diff_baseline.js --save` 是「从无到有」建基线，
> 但我把 `read(BASE_FILE)` 和存在性校验都排在了 SAVE 分支**之前** → 第一次运行直接 ENOENT，死锁（永远建不起来）。
> **修法：SAVE 分支必须排在最前**，且存在性校验要用 `!SAVE` 限定。
> ⭐ 通用教训：**「首次运行」这条路径必须自己走得通**。给工具加"引导/初始化"模式时，
> 先问「它第一次跑的时候，依赖的东西存在吗」。

### 2. 任务单机制（`docs/任务单/` 新建）
- `_模板.md` —— 六栏，**「🔴 边界」设为一级字段**（允许碰哪些文件 / 绝对不许碰 / 会不会动数据库）。
  ⭐ 设计理由：并行冲突的本质是边界不清，所以模板必须**逼着划边界**，而不是让 AI 自由发挥。
- `README.md` —— **派活四问**（边界干净吗？动数据库吗？能自动验收吗？跟别人撞车吗？）
  + 一张「当前可以安全派出去的活」清单（部署配置重配 / 清一次性脚本 / 补 spend_material SQL）。
- `AGENTS.md` 已同步：开工步骤加了「先读任务单边界」，`去哪查` 表加了任务单。

⚠️ 踩坑：PowerShell `Set-Content` 被拦（要求显式 Encoding，否则可能损 UTF-8/BOM）。
**写文件内容改用工具或 node fs.writeFileSync(…,'utf8')**，别用 `Set-Content`。

### 3. 多 Agent 并行：实证清单（比理论有说服力）
这轮连同上一轮，整理出**本项目实测过的并行代价**（可直接引用）：
| 事故 | 后果 |
|---|---|
| 09-12 并行会话给 `game.css` 的 `#tab-pet` 加 `display:flex` | 页面全堆叠、点市集跳宠物页，排查绕大圈（自己第一轮搜 diff 还漏了 `tab-pet` 关键词） |
| 09-12 并行会话 emoji→图标，`.sb-icon` 容器 32 与图标 44 不同步 | 视觉溢出 |
| 09-12 被迫破例 `git add -A`（51 文件） | 因并行会话横跨几十文件，「只提一部分会让 HEAD 不自洽（新版 config 配旧版 ui）」 |
| 09-13 宪法两条**结论写反** | 商店 enabled 说关着、服务端权威说全在前端 —— 多会话各写各的、没人回头对齐 |
- ⭐ **并行最大的隐性成本不是冲突，是口径漂移**：数字写错一眼能看出，**结论反了看十遍发现不了**。
- 「成立条件 4 条：边界干净 / worktree 隔离 / 自动验收 / 无共享可变状态」→ 本项目只满足 1.5 条（自动验收是优势，缺隔离 + 单 Supabase 生产库 + `?v=` 互覆盖）。
- 🔑 给用户的核心话术：**视频把卖点搞反了 —— 值钱的是"总管"（拆活/划边界/定验收），不是"并行"。**
  类比：10 个人挤一个厨房炒一桌菜。

（抖音链接依旧打不开：返回"视频数据加载中"空壳，作者/文案/字幕全无 → 只能就模式本身作答。）

## ⚠️ 2026-09-13 提交/推送记录（含一个需要注意的副作用）

- **2 个 commit**：`2762d66` 建立开发规矩与一键回归工具链（12 files）
  → `10ea48e` 修正宪法事实错误并把 AI 记忆入库（22 files）。
  push 成功：`5faee70..10ea48e`（用 `git -c http.version=HTTP/1.1 -c http.postBuffer=524288000 push origin main`）。
- **刻意未提交**（守住「只 add 自己路径」）：
  `docs/js/ui/ui-common.js`（并行会话改到一半的半成品）、`docs/墓碑代码审计_2026-09-12.md`（非本次产出）、根目录一堆 `screenshot_*.jpeg`（临时截图）。

🔴 **副作用：`.codebuddy/memory/` 首次入库 = 735KB 内部记录被推到了 GitHub。**
里面含：管理员邮箱白名单、Supabase project ref 与 EF URL、历次事故细节、未上线功能、全部数值决策。
**若仓库是 public，这些等于公开了。** 后续要么转 private，要么在写入 memory 时就避开明文邮箱/密钥
（⚠️ 注意：已进 git 历史的删不掉，得 `git filter-repo` 强改历史，且会重写所有 commit hash）。
⭐ 教训：**「让知识入库」和「知识里有没有隐私」是两件事**。放行入库前先看一眼内容敏感度。

### 🔴 我给错过一次建议（已纠正，留档防止再犯）

用户问「仓库转私有是不是收费」，我顺着答之前先查了部署方式 —— **结果证明我上一条的推荐（转私有）是错的**：

- GitHub 免费账户**可以**有无限私有仓库（转私有本身不收费）
- ⚠️ **但 `has_pages: True` —— 项目正跑在 GitHub Pages 上**，而**免费账户的私有仓库不支持 Pages**
  → 一点「转私有」，游戏立刻下线，朋友当场打不开。
- ⭐ **教训：动仓库可见性之前，先查 `has_pages`。** 「治安全问题」的方案如果会砸掉线上服务，就不是方案。

顺带查到的事实：
- 根目录 **无 `.github/`**（没有任何 CI/CD workflow）
- `netlify.toml` / `wrangler.toml` 的 publish 仍指向 **`新demo/Mvp/原型代码`**（已废弃）→ **这两个部署配置实际是废的**，当前唯一在跑的就是 Pages。
- 仓库 `size` ≈ 117 MB（主要是 `docs/assets` 图片资产）。

因此建议改为 **「别藏，修」**：真正危险的是 `pets` / `equip_items` / `pet_egg` 三张表没收权限（= 能伪造资产去市场上骗真材料），
**漏洞修完，笔记公开也无所谓；藏笔记而留着漏洞，是本末倒置。**
笔记要隔离的话，另开私有仓库放 `memory/` 即可，游戏仓库保持公开不动 Pages。

### 部署配置修正 + 移除被禁止的译名（用户认可后落地）

1. `wrangler.toml` / `netlify.toml` 的发布目录：废弃的 `新demo/Mvp/原型代码` → **`docs`**。
   用户已确认这两个**都未在启用**（实际部署只有 GitHub Pages）。
   ⭐ **没删而是改对**，理由：Cloudflare Pages 免费版支持私有仓库 = 将来私有化的唯一免费出路，是一张备用牌。
2. ⚠️ **发现一处违反宪法拍板**：`docs/游戏.html:32` **登录页大标题**写的是「**灵魂熔炉**」，
   而宪法与 MEMORY 都明文写着「英文名 Forge of Souls，**别译灵魂熔炉**」—— 而且是玩家一进来最先看到的主视觉文字。
   已改为 `Forge of Souls`，副标题去掉重复的英文名（`docs/index.html` 的 `<title>` 同样改了）。
   ⚠️ 现在登录页大标题是**英文**，若要正式中文名需用户另行拍板（不擅自起名）。
3. 全仓扫描确认：`docs/**` 的 html/js 里「灵魂熔炉」**已清零**。

> ⭐ 教训：**"文档里写了禁令"和"代码里遵守了禁令"是两回事。** 宪法里那句话已经躺了很久，
> 主页面却一直在违规。**禁令类拍板必须做一次全仓扫描确认 —— 这正是「拿文档当断言、反查代码」的同一手法。**
> 另外注意：改 `<title>` / 页面文案**不需要升 `?v=`**（那只管被引用的 css/js），但玩家需要强刷才能看到。

## `pets`/`equip_items`/`pet_egg` 权限收口调研（用户选【2】= 不做过渡，随服务端权威一起做）

产出：**`docs/资产表收口调研_2026-09-13.md`**（施工图，下次开工照着做，别重摸一遍）。
宪法清单 #8 与 `00-项目导航.md` 已同步（两份文档必须同口径）。commit `6e1548c`。

### 调研定论（都已核实，别再重头查）
1. 🔴 **修正一个误判：这三张表的 RLS 是开着的**。未登录 anon key 读 `pets`/`equip_items`/`pet_egg`/`materials` 全部返回 `[]`。
   真问题只有一条：**登录玩家能 REST 直改自己那行的任意值**。
   ⭐ 坑：这些表是在 Dashboard 建的，**RLS 策略从来没落到仓库 SQL** → grep 仓库查不到 ≠ 线上没配。
   核实要用 `select relname, relrowsecurity from pg_class where ...`。
2. 🔴 **现在 revoke 会当场挂掉大半功能**：写操作全在客户端直连 REST，零 RPC 覆盖。
   位置：`pets` → `core/supabase.js:151/198/187`；`equip_items` → `core/items.js:18/44/57`；`pet_egg` → `core/supabase.js:452/410`。
   会挂：出战切换/战后升级/进化/涅槃/融合/觉醒/穿脱装备/**全部打造行为**/掉装入库/掉蛋。**线上有真实访客，不能试错。**
3. 收口前需新建 **8 个**服务端函数：`create_pet`/`update_pet`/`delete_pet`/`create_item`/`update_item`/`delete_items`/`grant_egg`/`consume_egg`。现有覆盖 0。
   ⚠️ 别把 JS 函数 `Supabase.savePet` / `updatePet` 误当成 RPC（它们是前端函数）。
4. ⭐ **最重要的认知：加 CHECK 约束 ≠ 安全。** 只能拦 `level=99999`，拦不住 `level=60`（合法值）。
   真正防线是「服务端知道这只宠本该多少级」→ 绕一圈还是回到服务端权威。
   **别再提议"先加个 CHECK 顶一下"**，那是假的安全感（用户已据此拍板不做过渡）。
5. 🔴 **最早会出事的单点：`pets.equipment` jsonb 无归属校验** —— 可把**他人装备 id** 填进自己槽位。
   性质比"改自己等级"严重：**侵害他人资产**。
   ⚠️ 修的坑：触发器不能写成全 `before update`，否则 `buy_pet`（`update pets set user_id`）换主人会当场挂掉；
   必须 `before insert or update of equipment`，且要先定义清「买来的宠原本带的装备归谁」。
6. 加约束务必 `not valid`：Postgres 加约束会全表校验历史数据，一行不合规就整体失败。
7. 关键取值（已核实别猜）：`equip_items.rarity` = **`white`/`blue`/`gold`**（config.js:1250，不是中文）；
   `tier` = 1~5；`pets.growth` 已有 CHECK 1~100；**`pets.level` 至今零约束**（Config maxLevel=60 从未落地到库）。

### 可抄的先例
`migrate_asset_write_hardening.sql`（materials 收口范式，其注释里明说 pets/equip_items 不在该文件范围）、
`check_god_synth`/`check_nirvana`（jsonb 入参纯校验不写库的服务端校验器模板）、
`battle-settle` EF 已在用 service role 写 pets（野图那条链路已经是服务端权威）。

## 用户问「vibecoding 流程是什么」（已作答，未落文件）
用户刚建完一批规矩（AGENTS.md / 00-项目导航 / 任务单 / npm run check+baseline），问之后怎么干活。
答时按其已有设施归纳成 7 步：想法 → 拍板四问（导航页）→ 派活四问+任务单边界（决定串行/并行）→ 开工重读共享文件 → 写代码守六条红线 → 升 `?v=` + `npm run check` + 浏览器看一眼 → 只 add 自己路径提交 → 回填任务单结果 + 记忆/宪法。
⚠️ 答案**只在对话里**，未写成文件；若用户后续要固化，应放 `00-项目导航.md` 或 AGENTS.md（**别再新建第五份文档** —— 「同一逻辑两份、只有一份对」是本项目头号病因）。
同时按系统要求把超长被截断的 MEMORY.md 精简重写（结构不变、去重复与过期项，新增 §4 派活/任务单、§12 测试回归）。

## 补：流程已固化进文件（用户再问一次同一问题 = 说明"只在对话里"的答案会丢）

- **`00-项目导航.md` 新增「🚀 干活流程（vibecoding 七步）」**：查板 → 拍板四问 → 派活四问/任务单边界 → 开工重读共享文件 → 写码守红线 → `npm run check` + 浏览器看一眼 → 只 add 自己路径 → 回填任务单结果 + 宪法/记忆。
  另加「🛑 什么情况下必须停下来问人」（改数据库 / 推翻已拍的板 / 碰钱 / 想建第二份实现）。
- **`AGENTS.md` 开工三件事 → 加第 4 条指针**指向导航页那节，并注明「唯一一份，别复制到别处」。
- ⭐ 判据：**用户连着两轮问同一个"流程是什么"，等于这份知识没有载体**。答案只活在对话里 = 下次还得重问。
  落点选已有文件（导航页 = 人的入口，AGENTS = AI 的入口），**不新建第五份文档**。
