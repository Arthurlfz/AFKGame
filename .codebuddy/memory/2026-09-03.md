# 2026-09-03

## Git push 误传与清理（用户不满）
- 用户让 push，我 `git add -A` 把临时文件/archive/demo 全收进去了，被骂。教训：**push 前不要 add -A 全收，先列 untracked 给用户确认**。
- 已补撤（commit 5513ff3，git rm --cached 本地保留 + .gitignore）：archive/、_html、_game_screenshot*.jpeg、新建文本文档.txt、系统整合_full_tmp.md、docs/_dl_skill.tmp、docs/_preview_*.png、onboarding-demo、battle-demo、AI假人经济系统策划_v1.md、游戏UI体检报告.md。这些已加入 .gitignore。

## 新手引导重构（目标驱动主线）· 先做视觉 Demo（用户拍板：确认后再接入正式版）
- 用户最终决策链：重做任务内容（非套皮）→ 目标驱动主线（领资粮Lv10→进化→武装→扩充阵容→毕业涅槃）→ **教学期间所有等级门槛都给经验包**（G2 进化Lv10 / G7 合成Lv40 / G9 涅槃Lv60 全用引导经验包顶上去，不刷级；加速×6 降为可选提速刷材料；早期"硬刷真实Lv60"方案被推翻）。
- **Demo 已交付（等用户确认）**：
  - ⚠️ 用户反馈"spotlight 一个没看到" → 根因疑似预览器/webview 不加载外链 `<script src>`（battle-demo 同款老坑）→ **onboarding-demo.html 已改完全自包含单文件**（design-tokens/ui-onboarding/onboarding-demo 三份 CSS + 引擎/脚本两份 JS 全内联，无任何外链）+ 打开 450ms 自动点击 btn-tour 播开场总览。独立的引擎/样式文件保留（正式接入仍引用）。
  - 新引擎 `docs/js/ui/ui-onboarding.js`（生产形态）：`Onboarding.startTour/spotlight/hotspot/clear/isActive`；spotlight=压暗遮罩(pointer-events:none)+box-shadow 9999px 镂空(目标可点)+底部 NPC 字幕条；hotspot=目标脉冲环+轻量气泡(不压暗)；自带 null 防护 + 每帧重查 target 跟随 DOM 重建；ticker 仅展示期跑。
  - 样式 `docs/css/ui-onboarding.css`（引擎通用，独立成文件，不污染 ui-polish）。
  - Demo 页 `docs/onboarding-demo.html` + `docs/css/onboarding-demo.css` + `docs/js/ui/onboarding-demo.js`：伪游戏四页（战斗/宠物/打造/市集）+ 顶部引导条 + 右侧 G1~G10 任务链逐条预览（NPC 台词草稿，接入 config 可改）；NPC 引路人为内联 SVG 水墨剪影（assets 无现成立绘）；经验包顶级用数字动画演示（G1→Lv10 / G7→Lv40 / G9→Lv60）。
  - 服务：`python -m http.server 8177 --directory docs`，页面 `http://localhost:8177/onboarding-demo.html`。
- 台词草稿（接入 config 用）：G1 领资粮直升Lv10 / G2 进化 / G3 穿装 / G4 打造 / G5 分解 / G6 孵化蛋得副宠 / G7 合成(需Lv40,顶入) / G8 上架 / G9 涅槃(需Lv60,顶入) / G10 魂铸(毕业后普通任务)。
- **已正式接入游戏.html（2026-09-03，版本 tag 20260903j）**：
  - `config.js`：删 t1~t13，写 G1~G10（type/need/requires + hint 怎么做 + target hotspot 锚点 + npc 引路人台词 + boostLevel + reward/rewardGear）；新增顶层 `tutorialMode` 段（enabled/expRate6/fightSpeedMult3/blessing{引导祝福,30min}/starterPack 毕业礼包/openingTour 4 步台词/grants 只有 g7 副宠）。
  - `quest.js`：新增 `level` 任务类型（currentProgress=出战宠 level）；getGuideQuest 只认 isGuide 并透出 npc/hint/target/boostLevel；completeQuest 成功后 fire-and-forget 调 `TutorialMode.checkGuide()`；skipGuide 调 `markSkipped()`。
  - `tutorial_mode.js`：新增 `boostGuidePetToLevel(lv)`（出战宠 level=lv、exp=0、Supabase.savePet 落盘、幂等）；checkGuide 改为「有引导任务→顶等级+发 grants；无→exit+发毕业礼包」。
  - `游戏.html`：引 `css/ui-onboarding.css` + `js/core/tutorial_mode.js`（quest.js 后）+ `js/ui/ui-onboarding.js`（ui-popover 后）；宠物页补「装备」「宠物蛋」tab 与 pane（蛋 pane 含 `#egg-panel`；装备 pane 是直达「背包→装备」子页的按钮，因 12 槽 UI 在背包窗口里、不重复造 id）。
  - `ui-pet.js`：`openEquipWindow()`（openBagWindow + 点 `.bag-subtab[data-bag-subtab="equip"]`），切「装备」tab 自动打开、切「宠物蛋」tab 调 renderEggPanel。
  - `ui-common.js`：setAuthUser 调 `TutorialMode.bindUser(email)`。
  - `main.js`：新增 `startGuideOnboarding(withTour)`；选宠完成走 withTour=true（开场 tour → grantInitialBlessing + startGuideRoutine），会话恢复走 false。
  - `ui-quest.js`：taskDesc 加 level 文案；引导任务达标时主按钮变「领取奖励」（qt-submit→completeQuest）；点 qt-go 先 `Onboarding.hotspot(target,{npc})` 再 goGuide。
  - 校验：8 个改动 js `node --check` 全过、lint 0；config 链校验（requires 引用/type/boost/target/npc）bad=0；openingTour 4 步、grants 1 条。
  - 修复「看不到开场 tour」根因：tour 原本只在新号选宠分支触发，老账号登录走不到。已改为：会话恢复也传 `withTour=true`，本账号没看过开场总览就补播一次（标记 `fos_tour_seen_<user>`）；播之前 setTimeout 700ms 等主界面排版稳定，避免聚光框量到 0 尺寸；`main.js` 暴露 `UI.replayOpeningTour()`；`ui-dev.js` 开发者面板增加「重播开场总览」「清除引导标记」两个按钮。
  - 修复「有压暗没聚光」真正原因：`ui-onboarding.css` 里 `.ob-hole`、`.ob-hot-ring`、`.ob-hot-bubble` 默认 `display:none`，但 JS 里显示时写成 `el.style.display = ''`（空字符串），没有覆盖 CSS 的 `display:none` → 所以即使 target 存在，镂空高亮/脉冲环也画不出来。已改为 `display = 'block'`。
  - 修复配置 target：开场 tour 第一站从 `#btn-battle` 改为 `.sb-btn[data-page="worldmap"]`（左侧边栏「世界地图」按钮）。
  - 同步把 `ui-onboarding.js` 和 `config.js` 在 `游戏.html` 的 `?v=` 升到 `20260903k` 防缓存。
  - 按用户反馈调整 spotlight 视觉：压暗层从 `rgb(0 0 0 / .62/.72)` 降到 `.38/.42`，不再死黑；底部 NPC 字幕条改成贴边导购气泡，紧跟当前 target（默认优先出现在目标右侧，带小箭头指向目标），`下一步/跳过` 按钮就在气泡右下角。
  - 修复「教学副宠重复发放（同名多只）」：`grantPet` 按宠名做幂等，账号名下已有同名教学副宠就跳过发放（标记 `fos_grants_done_*` 是纯 localStorage，清缓存/换设备会丢，丢了后 checkGuide 每次登录都会重发一只 `泥沼从者`，云端一路 INSERT 出 N 只）。开发者面板「新手引导」组新增「清理重复教学宠」按钮，删云端同名教学副宠、只留最早一只。
  - **第一原则级加固（2026-09-03，联网经济防刷）**：云端唯一真相 + 记账先行。
    - 给 `quest.js` 增加云端 `extra` 存储（存 quest_progress.progress JSON 内，**无需改表**），暴露 `Quest.getExtra/setExtra`；换号 reset 清空、loadCloudProgress 恢复。
    - `tutorial_mode.js` 所有一次性标记改为「本地缓存 + 云端真相」双写：tutorialStarted / packClaimed / guideSkipped / blessingGiven / guideGrants（数组）/（main.js tourSeen）。
    - 经济类发放全部「先落标记（本地+云端 await），再发货」：`applyGrantsFor`、毕业礼包 `grantStarterPack`、开局祝福 `grantInitialBlessing` —— 宁可断网少拿一次，绝不重复领。
    - `grantPet` 幂等 + 自愈：发现同名多只时自动删多余（保留有 cloudId 或最早一只）；savePet 失败不抛（本地已在、标记已落，下次同名幂等跳过）。
    - `replayOpeningTour` 重播结束只记 seen（不再触发引导例行），修掉「重播→下次登录自动播→checkGuide 重发」的循环。
    - 开发者「清除引导标记」现在本地+云端一起清（含 packClaimed/tourSeen 等）。
    - 已改文件版本号统一 `20260903n`（quest/tutorial_mode/main/ui-dev.js）。
  - 引导单步 hotspot 修复：目标常在「还没切过去的页」里（如宠物页 synth/merge/egg tab），量到 0×0 → 引擎报 `hotspot target 不可见`。`ui-quest.guideHint` 改为轮询目标可见（≤1.5s）再弹 hotspot；goGuide 紧随其后同步切页。ui-quest.js 版本 → `20260903p`。
  - **引导链卡死根治（经验包=全宠顶门槛，奖励=下一步钥匙）**：
    - 根因：G7 合成要求两只宠都 Lv40、G9 涅槃要求主副都 Lv60，但经验包只升出战宠一只 → 另一只不够 → 卡在融合/涅槃。
    - 修复：`boostGuidePetToLevel` 改为把**名下所有宠**顶到门槛（本地+逐只 savePet）；新增 `ensureGuidePets(taskId)`（配置声明过的教学宠缺失即补建，不依赖已发标记，自愈历史丢宠）；checkGuide 在 boost 后先 ensure 再 applyGrantsFor。
    - 闭环补漏：G8 上架需要未穿戴装备，而 G5 分解可能把手头装备拆光 → tutorialMode.grants 新增 g8 gear 兜底一件白装，进 G8 自动放背包；g8 hint 文案同步改。
    - 版本：config.js、tutorial_mode.js → `20260903q`。
  - **引导链奖励=下一步钥匙（全链闭环，config v20260903r）**：
    - G2 奖励加 rewardGear 白装（G4 打造的底材）；G4 奖励加 rewardGear 白装（G5 分解的废品）；G5 奖励从误导性的「宠物蛋」材料改为 剥离石。
    - 教训：孵化吃的是「按品种的蛋」（Drop.hatchEgg，蛋在 pet_egg），材料「宠物蛋」孵不了蛋 → G6 必须走 grants 发真蛋。
    - tutorialMode.grants 按"消费型任务进任务即发钥匙"补齐：g5 白装（G5 分解兜底/自愈旧账号）/ g6 腐噜兽蛋 ×1 / g7 泥沼从者 Lv40 / g8 白装。任务 reward + 进任务 grants 双保险，旧账号卡住也能自愈。
  - **第一原则重构（20260903t）：引导不卡链改为「库存差量补齐」机制**：
    - 反模式（废弃）：一次性"已发"标记 + 靠上一关奖励喂下一关 —— 奖励被花掉就断链、标记丢就重复发。
    - 新模式：config.tutorialMode.grants = 每关「钥匙表」，声明本关自己要用的资源（g2 进化素材/g3 蓝装/g4 白装+重铸石/g5 白装/g6 腐噜兽蛋/g7 合成之石+泥沼从者/g8 白装/g9 涅磐兽）。任务激活时 `applyGrantsFor` 对当前库存求差补齐（材料<需求→补；背包装备件数<need→补；蛋<need→补；缺同名宠→建）。幂等不靠标记、可自愈历史卡链。
    - 顺序修正：`startGuideOnboarding` 从 restoreCloudPets 内移到 `restoreAllCloudData` 末尾（云装备/蛋/材料全部 load 完才跑 reconcile，否则空库存误判乱发）。开场 tour 只标记 seen、不再触发例行（例行登录即跑）。
    - 删掉了 grantKey/readGrantsDone/markGrantDone/flushGrantDone/ensureGuidePets 等一次性标记残留；ui-dev「清除引导标记」同步只清仍存在的标记（tourSeen/tutorialStarted/guideSkipped/blessingGiven/packClaimed）。
  - **补给打偏修复（20260903v）**：真机日志显示玩家在 g6 孵化但蛋为空、且 checkGuide 只打了"Lv10 经验包跳过"——内部 currentGuideTask 与 UI(Quest.getGuideQuest) 判定不一致，补给发给了带 boostLevel 10 的另一关而不是 g6。checkGuide 改为**以 Quest.getGuideQuest()（引导条显示的当前关）为唯一真相**做经验包+补给，两者不一致时打 warn 日志；空蛋 reconcile 诊断输出已就位（[guide] reconcile 无事可补 + eggMap）。
  - **真正根因（20260903w，致命低级 bug）**：`quest.js` 的 `isFinished`/`isUnlocked` 没 export！tutorial_mode 里 `window.Quest.isFinished(q)` 拿到 undefined，三目恒为 false → `currentGuideTask()` 永远认为"所有引导关都没完成"→ 永远返回 g1 → 经验包只打 Lv10、补给永远发给没有钥匙表的 g1，g5/g6/g7/g8/g9 的钥匙全都没发过（玩家卡死/没收到白装/蛋全是同一根因）。已把两者加入 `window.Quest` 导出（quest.js v20260903w）。
  - **合成页“不显示宠物”根因（20260903x）**：合成 tab 主素材过滤误用 `Merge.canMerge`（读 nirvana.minLevel=60），全队 Lv40 一只都不显示；合成门槛其实是 synthesize.minLevel=40。修：pet_merge 拆出 `Merge.canSynthesize`（读 synthesize.minLevel），合成页（ui-pet.js / ui-pet-synth.js 的 renderSynthTab）改用 canSynthesize；涅槃页继续用 canMerge。三个文件版本升 20260903x。
  - **移除自命名副宠「泥沼从者」（20260903y，用户质疑"我设计过这个吗"）**：G7 原来在 grants 里造了一只不存在于美术/数值里的「泥沼从者」当合成副宠——确属 AI 自造。改为 `type:'petCount', baseName:'腐噜兽', min:3`：素材池总数 <3 就用真实基础宠补足（保证合成吃掉 2 只后还有 1 只 + 合成产物 = G9 涅槃的两只），不发明新物种。config/tutorial_mode 版本 20260903y。
  - **安全闸（20260903z，用户质疑"会生成东西的代码玩家也能用"）**：applyGrantsFor 增加闸门——只允许给「Quest.getGuideQuest() 的当前引导关」补钥匙，其他 taskId 直接拒绝并 warn。原型阶段客户端持有 Supabase anon key + RLS，真正的防线必须是服务端权威 RPC（教学补给由服务端按 quest_progress 校验后铸造；客户端仅表达意图）+ RLS 收紧写权限；前端关闸只是第一步，不是安全边界。tutorial_mode v20260903z。
  - 引导经验包可见性：boostGuidePetToLevel 生效时加浮层 toast；开发者「清除引导标记」新增 `Quest.resetGuideChain()`（清 G1~G9 完成记录并落云），才能真正重走引导链看到经验包效果（此前只清标记、云端 completed 还在，永远走不回 G1）。quest/tutorial_mode/ui-dev 版本 → `20260903o`。
  - 校验（追加）：`main.js`、`ui-dev.js` 语法全过、lint 0。
  - ⚠️ 待真机验证：老账号登录应能看到开场总览；若仍看不到，开 F12 控制台输入 `UI.replayOpeningTour()` 强制播一次。G3 穿装备依赖背包窗口装备子页，G8 上架需背包有装备。

## B 类 emoji（宠物 / 血统 / 进化树小图标）全量移除
用户拍板依据：每只宠物都有立绘 + 头像，emoji 图标属于多余占位，按第一性原则全删。

### 改动
- **config.js（65 处）**：9 处基宠 icon（8 starters + legacyBase）+ 48 处进化树分支 icon + 8 处血统被动 icon。
- **UI 层统一改走立绘/头像，不再回退 emoji**：
  - `ui-pet.js` `iconHtml(name, inline)`：头像 → 立绘 → 空（去掉 emoji 参数，全部调用点同步）
  - `ui-battle.js` `mountIcon/mountIconAvatar`：立绘 → 头像 → 空；`resetBattle` 去掉 `petIcon/enemyIcon` 两个参数（`battle.js` 调用同步）
  - `ui-common.js` `bloodlineHtml`：去掉 `bloodline-icon` span，只显示名称 + 描述（血统无美术资源，不拿 emoji 凑）
  - `main.js` `artHtml`：去掉 icon 参数与 onerror 的 emoji 兜底
  - `ui-bag.js`：背包宠物头像用立绘兜底；孵化 toast 去掉 `baby.icon`
  - `pet_evolve.js`：删掉 `pet.icon = nextIcon`（进化不再改 icon 字段）
  - `tutorial_mode.js`：`createPet` 的 emoji 兜底改空串
- **保留 `pet.icon` 字段本身**（pets 表 icon 列、`createPet` 签名不动）：只是不再填 emoji、不再用于显示。彻底删字段需先确认云端列约束（历史血泪：改列曾导致 loadPets 400）。
- 版本号：游戏.html 里 14 个改动文件的 `?v=` 统一 bump 到 `20260903a`。

### 补漏（第二轮，用户追问 SQL 匹配后自查发现）
1. pet.js 的 PET_POOL 漏网：8 个基宠 emoji（`{ name: '瘟熊', icon: '🐻' }` 这种格式，第一轮正则没匹配到）→ 已删，只留 name。
2. pets.icon 变 undefined 的写库风险：删 emoji 后新宠 pet.icon = undefined，supabase-js 用 JSON.stringify 会整个丢掉该字段 → 若 pets.icon 列 NOT NULL 无默认值，新宠建档直接 400。已双兜底：createPet 改 icon: icon || ''、buildPetRow 改 icon: pet.icon || ''。
   - 教训：删配置字段时要顺着「值 → 对象 → 写库」链路查一遍，undefined 在 JSON 序列化层会静默消失。
   - supabase/ 目录里没有 pets 建表 SQL（只有 shop/market 等迁移），pets 列约束无法从文件确认，只能靠代码兜底。
3. 追加 node --check（pet/supabase/config 全过）+ 版本号 bump（pet.js、supabase.js → 20260903a）。

### 校验
- 44 个 js 语法编译全过；UI 层 `.icon` 显示残留 0；config 仅剩材料类 icon（A 类待议）。
- ⚠️ **测试跑不起来（既有问题，非本次引入）**：`tests/` 在仓库根，但 vtest 脚本按 `docs/tests` 写的相对路径 —— `'../js/...'` 要求 cwd 在 docs 子目录、`'vstub.js'` 要求 cwd 在 tests 目录，两者无法同时满足。要么把 tests 挪回 `docs/tests/`，要么改脚本路径基准。

## 美术资源缺口清单（用户要逐条解决，进度在此打勾）
A 类 - 材料/通货图标（13 种 emoji 占位 + 6 种区域材料连 emoji 都没有）
- [ ] 重铸石🎲 / 剥离石✂️ / 神圣石🔮 / 增缀石➕ / 合成之石💠 / 涅磐兽🐉
- [ ] 进化素材🧬 / 精粹进化素材💎 / 传说进化素材✨ / 宠物蛋🥚 / 凝魂晶石🔷 / 鉴定石🔍 / 引导祝福⚡
- [ ] 6 种区域专属材料（枯荣种荚 / 泣腐之泪 / 白骨残片 / 幽影魂丝 / 血潮凝晶 / 腐变之心）—— 只有中文名
C 类 - 主城建筑（BUILDINGS 无 icon 字段，热区只有名字）
- [ ] 孵化 / 兽栏 / 鉴定 / 铸造 / 商会 / 任务中心
D 类 - UI 装饰与货币（可保留）：🪙魔石、📜🐾任务、📜对话 speaker、⚔️🥚🐾市场占位、💠🌟合成、🌟进化、🥚孵化、⚡祝福、🔥💎🪙 dev 统计
非 emoji 缺口
- [ ] 地图背景：17 图中 6 图有 back+mid 缺 front；11 图完全无素材
- [ ] 变异宠立绘：4 个基础宠异变（腐噜兽/疫毛兽/毒沼蛙/尸犬·异变）无专属立绘
- [ ] 变异宠立绘质量：现有 4 个用 monster-06「多爪乱舞」，偏丑
- [ ] mut1-rotten（7 立绘 + 7 头像）未接进 pet-sprites.js
- [ ] 逐帧动画：ANIM_ENABLED=false，仅影刃兔/霜影兔有素材且不可用
- [ ] 战斗怪物立绘：wild-* 无立绘，战斗中全靠 emoji 占位（emoji 暂时保留，删了会变空白）

## meowa 官方规范（查证后修正，之前那版是错的）
⚠️ 上一版「长关键词前缀 + 一次出 5x5 网格 24 个」两处都违反官方规范，已作废。官方依据：Meowa-AI/meowa-skills 仓库 `skills/game-assets/`（SKILL.md + references/capability-routing.md + references/pixel-and-hd-assets.md + references/ui-and-image-editing.md）。

### 铁律（SKILL.md 原文要点）
1. **禁用 diffusion 式提示词工程**：no long keyword stacks、no repeated quality terms、no negative prompt blocks、no token weights、no camera/rendering jargon —— 这些会「interfere with the model's own interpretation and reduce consistency」。正确写法：**最短可用的自然语言，只写主体/动作/视角/材质/元素关系；先看结果，确实缺什么再一条条加约束**。
2. **数量与尺寸是 preset 的固定契约**：`Treat preset-defined dimensions and output counts as fixed contracts. Do not imply that prompt wording can override them.` 提示词里写「出 24 个网格」无效。没有合适 preset 就说明差距，别硬凑。
3. 官方示例语气就一句话：`A clean HD game asset sheet with eight clearly separated sci-fi props`。

### 命令路由（references/capability-routing.md）
- **HD 角色/道具/图标/资产包 → `hd-gen-run`**（A 类道具图标走这条）
- 通用 HD 批量/场景/sprite sheet → `nano-banana-run` 或 `image-2-run`（无 preset 契约，构图自由，数量不保证）
- 需要自动去背 + 组件分割 → `ui-gen-run`（**只返回一张聚合 sheet + 分割数据，不返回切好的单文件**）
- 1 个源出 1~8 个变体 → `one-click-upgrade-prompts` → `one-click-upgrade-run`（变体数 = `--variant-prompt` 个数，上限 8）
- **选 preset 前必须先发现**：`hd-gen-template-info`（报告输出尺寸与 default count）

### hd-gen-run 用法
`python3 skills/game-assets/meowart_api.py hd-gen-run --template-name <preset> --requirement "<描述>" --resolution 2K --aspect-ratio 1:1 --quality detailed --remove-bg-method standard --output-dir <dir>`
- quality：standard(便宜试提示词) → detailed(正式) → ultimate
- 参考图：`--reference-file` 一张，`--reference-files` 多张（最多 8 张）—— **分批出图时用第一批当参考图保风格统一**
- 环境变量名官方是 `MEOWART_API_KEY`（项目里 meowa-monster-pack skill 写的是 `MEOWA_API_KEY`，注意不一致）
- 中断恢复：`<command>-poll --job-id <原 job id>`，不重复扣费

### 本项目落地计划
- A+D 共 23 个图标（19 材料/通货 + 魔石/装备/宠物爪印/任务卷轴），一次最多 8 个 → **3 批（8+8+7）**
- 第一批先出 8 个定风格，后两批把它当 reference 传进去对齐
- 坑：即使写了 no text，官方提醒仍可能冒出标签（`Inspect for unintended labels`）；交付只认 `final_outputs.json` 里列的文件
- D 类事件装饰（进化🌟/祝福⚡/统计🔥/合成💠/对话头像📜）**不并入**，保留 emoji
- C 类（主城建筑图标）用户拍板不做

### 本机环境事实（2026-09-03 查证）
- `C:\Users\Administrator\.agents\skills\` 下**只有 meowa-monster-pack**，**没有官方 game-assets CLI**。
- 环境变量：`MEOWA_API_KEY` 已设置；`MEOWART_API_KEY` 未设置（官方 CLI 认后者，需 `$env:MEOWART_API_KEY = $env:MEOWA_API_KEY` 复用）。
- 现有 HTTP 方式（`POST https://api.meowa.ai/api/hd-gen`）**不支持传参考图**，所以分批出图无法用参考图锁风格，只能靠提示词；要传参考图必须先装官方 CLI。
- 网络：`raw.githubusercontent.com` 不通；`api.github.com`、`cdn.jsdelivr.net` 通（取官方文件走 jsDelivr）。
- 官方 CLI 已安装到 `C:\Users\Administrator\.agents\skills\game-assets`（2026-09-03 由我从 codeload zip 装好）。

### hd-gen 模板契约（实测 hd-gen-template-info，2026-09-03）
- **icon 类模板一律：output_size 256、default_count 8、1K、1:1、去背 standard**（即一次 8 个 256px 透明图，**不用自己切图**）
  - 可选：`ancient_minimal_icon`(古风简约) / `ancient_watercolor_icon`(古风水彩，**项目出怪物立绘用的就是这个**) / `cute_cartoon_icon` / `ink_wash_icon` / `minimal_pet_icon` / `modern_minimal_icon` / `storybook_watercolor_icon` / `warm_healing_watercolor_icon`
- `hd_char_1~7` 是角色立绘模板：512-1024、**default_count 2**、带 directions(front/left/right/back)
- ⚠️ **没有任何暗黑/暗黑幻想风模板**（全是古风、水墨、卡通、治愈系）。但 pack0-base 用 `ancient_watercolor_icon` + 暗黑提示词成功出了 realistic dark-fantasy 立绘 → **模板只定规格，风格由提示词拉**。
- `hd-gen-run` 实测参数：`--template-name --requirement --resolution --aspect-ratio --quality{standard,detailed,ultimate} --generation-model --generation-speed --direction --remove-bg-method{none,standard,advanced} --reference-file(单张) --reference-files(可重复) --output-dir`
- **余额：2026-09-03 查 `credits-balance` = 0（paid/subscription/trial 全 0）→ 要跑生成需先充值**。
- **首次实测 hd-gen-run（2026-09-03）**：模板 `ancient_watercolor_icon` + 强化水墨提示词 + `blight-heart` 参考图 → 产出**3D 写实宝石风**（金属光泽、立体光影），**不是水墨**。每批扣 10 credits（不是预估 40），job 同步完成。**模板名误导**：「古风水彩」实际产出是暗黑写实，**水墨要换 `ink_wash_icon` 模板**+ 强化提示词「calligraphy brushstrokes, NOT 3D rendering」+ 参考图改用 worldmap 不用 blight-heart。
- 验证 transparent：sprite 文件是 PNG（head 89 50 4E 47），尺寸 256×256；transparent 验证待补（要 PIL 查 mode='RGBA'）。
- **第二次实测 ink_wash_icon + worldmap + 强化水墨提示词 → 仍 3D 写实宝石风**（磨石/蛇/虫/卵全是金属光泽+立体光影，和 ancient_watercolor_icon 几乎一样）。**结论：meowa 所有 icon 类模板（ancient_minimal/ancient_watercolor/ink_wash/cute_cartoon/minimal_pet/modern_minimal/storybook_watercolor/warm_healing_watercolor）都被锁在「3D 写实/暗黑宝石」风**，提示词"ink-wash/flat/NOT 3D"+ 参考图都压不住。**要水墨/扁平/卡通必须换工具**（SVG 手绘 / Midjourney 调参 / DALL·E）。
- **最终方案（用户拍板）**：meowa 3D 风与项目羊皮纸水墨调性冲突被否 → 改用内置 image_gen 文生图（不花 credits），prompt 模板「Traditional Chinese ink wash painting (shuimo) style game icon, bold black ink brushstrokes, subtle gray wash, pure white background, centered, no text」水墨效果极佳。22 张已缩 256×256 覆盖到 docs/assets/icons/final/（item_×8 / blood_×8 / evo_×7）。
- **接入结构**：材料图标 → ui-bag.js `MAT_IMG` 映射（`indexOf` includes 匹配，"进化素材"档位共用，未覆盖回退 emoji 💎🔮🧪）；血统被动 → config.bloodlinePassive[].icon 字段 + ui-common.bloodlineHtml 渲染 img；样式在 game.css（.bloodline-icon-img / .mat-img，白底圆角金边"墨画贴片"风）。
- **涅磐兽 = 涅槃系统材料**（凤凰浴火图 item_phoenix.png），不是血统被动；血统被动 8 条绑定 8 基宠（bloodlinePassive 表）。
- **evo_* 7 张无接入点**：进化树/宠物头像全走 PetSprites 形态立绘（iconHtml 明确不回退 emoji，62 形态全覆盖），抽象分支图标不需要，图留备用。
- **通货全量接入（2026-09-03）**：补生成 21 张水墨图（鉴定石/凝魂晶石/17 区域材料/引导祝福符纸/宠物蛋），全部亮度转透明入 final/。接入：ui-bag.js MAT_IMG（+19 条含 17 区域材料精确名）；**config.trade.materials 12 条 icon 全换 `<img class="mat-img">` HTML**（消费端 market-sell/records 均 innerHTML）；tutorialMode.blessing.icon 同改；game.css 加 `img.mat-img` 通用 18px 兜底（防市场场景原图炸开，背包格 38px 特异规则仍生效）。精粹/传说进化素材暂共用蛹图。
- **装备页蛋入口移除（2026-09-03 用户点名）**：ui-equipment.js renderInventory 里的"🥚 宠物蛋×N+孵化按钮"条目删除（重复入口，孵化统一走宠物页「宠物蛋」tab），同步清掉解绑的 getEggCount/hatchEgg 引用。
- **全量 emoji 清理（2026-09-03 用户最终要求）**：三步脚本。① 野怪 icon-data 字段全删（18，战斗页走 PetSprites 立绘不需要）；② `(['\"]) +` 批量清 33 文件 223 处 toast/log 前缀 emoji + 18 处 `icon:'xx'` 参数 + enemy 字段；③ 第二轮清所有剩余 emoji（保留 ✕ 关闭钮/❔ 立绘占位/✓ 达标符号），共 44 文件 150+ 处。
- **清理副作用修复链（重要教训）**：`(['\"]) +` 误伤 HTML 模板属性间/拼接边界空格 → 三轮恢复：属性间空格 549、拼接边界 19、class 前导 17、SVG `#`颜色粘连、双引号 class 多词值粘连 151（正则值字符集漏空格/`#`）。同时排查运行时引用残留：ui-equipment 移除蛋入口后 `eggs` 变量仍被 L217 空背包判断引用（已修）；EQUIP_ICON 表清成全空 → 删表 + 卡片兜底 '◆'；ui-console 频道图标清成双空三元 → 用 2500 区段几何符号 ◆/◈（避开 emoji 扫描范围）；锁定按钮 🔒/🔓 → 文字 '解锁'/'锁定'。最终：`node --check` 44 文件全过、双引号粘连扫描 0、emoji 复扫 0。**教训**：① 批量正则锚定字符串开头；② 删除一段代码要 grep 它引用的变量是否还有别处使用；③ 每轮修复后用 git diff / 复扫验证。
- **第二次运行时故障（2026-09-03 用户报）**：镜像 `alt==`/`id=="status-badge"` 属性错位 → 元素查不到 → `updateStatus`/`renderInvToolbar` 对 null 设 textContent；img `src=="` 渲染成 `="assets/..png` 畸形 URL。**根源：我加的属性修复正则与清理脚本叠加时把 `X="y"` 改成了 `X=="y"`**（多一个 =）。
- **第三次运行时故障（同日报）**：fix4/fix5 把 `=="` 的等号**全删**（sub `\1\2` 应保留一个 =），导致 `id"status-badge"`（属性名后缺 =）→ 元素仍 null。修复链：fix6 用 lookbehind `(?<=[\s<])` 补 = 320 处，但**误伤 class 值尾单词**（`danger"` → `danger="`）；fix7 还原误伤 + 补 boolean 属性粘连空格（`"hidden`→` hidden`）119 处。**最终用 HTMLParser 验证游戏.html 188 个 id 无重复、13 个关键 id 全在**，与 HEAD 原版逐行一致；node --check 0 / 语法 0；版本号 bump 20260903b→20260903c。**教训：① 修复正则替换字符串必须保留原结构（`==`→`=` 不是 `==`→空）；② 用 lookbehind 界定属性名时会把 class 值尾单词误判为属性名；③ 每次修复后用 HTML 解析器（不是正则）验证 DOM 结构 + 关键 id 存在。**
- **打造页统一（2026-09-03 用户点名）**：config.craft 4 条 icon（🎲✂️🔮➕）换对应打造石水墨图；ui-craft.js 资源格/操作按钮改读 `C.xxx.icon`；ui-bag.js CONSUMABLES 从写死 emoji 改读 config；ui-craft.js 文案里装饰 emoji（loading/结果/日志/toast）replace_all 清空。icon 字段放 HTML `<img>` 的前提：消费端必须 innerHTML 渲染（textContent 会显示标签文本）。
- **抠图定稿（2026-09-03 用户要求真透明）**：白底贴片方案被否 → PIL 亮度转 alpha（`a=255-lum`，墨浓越不透明），颜色统一暖白 `(240,234,220)`——"拓片/白墨"效果，深色 UI 上清晰。final 共 22 张（item_×7 血统 blood_×8 evo_×7 备用），3 张 meowa 残留（serpent/corrupted_egg/blood_nirvana）已删。
- meowa trial 余额 150（9/9 到期）；3D 批次与 image_gen 原图等中间产物已按用户指示删除（2026-09-03），`docs/assets/icons/` 只留 `final/` 成品。

## 开发者面板重构（2026-09-03 用户点名：另一个 agent 弄坏了，重构）
- 背景：另一 agent 这轮重构了 config（血统被动/特质魂铸/主动技能triggerChance/任务链isGuide改造）+ UI（背包窗口化/删宠物页装备tab/加血统特质觉醒显示），**开发者面板没跟上**，且自 ui-dev.js 引入（21594a0）就带 3 处坏引用。
- **坏的 3 处（重构前必崩/不准）**：
  1. 快进 Tab 点「合成」→ Config.pet.synthesize（实际在顶层 Config.synthesize）→ 点击即 TypeError
  2. 快进 Tab 点「涅槃」→ Config.pet.nirvana（实际 Config.nirvana）→ 点击即崩
  3. 资源 Tab「加成长」封顶 → Config.pet.maxGrowth（实际 Config.nirvana.maxGrowth）→ 显示/上限不准
  另：模拟器 Pet.createPet('腐噜兽','🐹',...) 还传废弃 emoji。
- **重构内容（ui-dev.js + 游戏.html 版本号）**：
  1. 3 处 Config 引用改顶层（synthesize/nirvana/maxGrowth）
  2. 调参 Tab 新增「特质/魂铸」组（8 项）：traitHatch.mutant.t1Boost/count3、traitInherit.mainKeep/subKeep/up、traitNirvana.implantChance、soulCast.materialCount、awakenSkillDamage
  3. SCHEMA 构建标记 _missing，渲染时缺字段兜底（不出现 value="undefined"）
  4. 模拟器 createPet 去 emoji
  5. ui-dev.js ?v=1f74b5bc → ?v=20260903d
- **验证（浏览器实测，本地 http 8090）**：游戏无 JS 报错；Tab1 8 新滑杆值正确（t1Boost=20/mainKeep=0.7/implant=0.3/materialCount=10/awaken=0.2）；Tab2 7 功能区在；Tab3 模拟实跑 60 场出数据（枯荣之地 无80%/材料15%/装备1.7%/蛋3.3%）；Tab4 快进端到端 mock 点合成/涅槃 → 正确补发「合成之石」「涅磐兽」、Merge 被调用、零 error。node --check 全过。
- **教训**：另一 agent 并行改动时，开发者面板（引用 config 的调试工具）必须随 config 结构同步；重构前先用浏览器实测面板各 tab + 点击事件，别只看渲染。
- ⚠️ 遗留（本任务范围外，未动）：游戏.html 被另一 agent 加了 BOM（<DOCTYPE> 前有 0xEFBBBF）；根目录 js/ 是 docs/js/ 的完整副本（与 docs/js 逐字节一致，未跟踪）；工作区还有 20+ 文件 M 未提交（另一 agent 的改动）。这些不是我弄的，如需回滚/提交要用户定。
## 开发者面板补「玩家」「数据」后台 Tab（2026-09-03 用户追问）
- 用户说"之前其实增加了两个面板：给魔石 + 看在线人数/增长人数"。**查证结论**：记忆/git 历史/代码全查 → 前端这俩 tab **从没实现过**；但后端 SQL supabase/migrate_admin_tools.sql（已提交 31c1b99）早有全套 RPC：dmin_list_users() 列玩家 / dmin_search_users(q) 搜玩家 / dmin_ban_user(uid,ban,reason) 封禁 / dmin_stats() 运营统计（total_users/today_new/active_7d/active_1d/banned_count/魔石流通/魔石发放/orders/products）+ grant_gems。SQL 注释写"开发者面板的「玩家」「数据」Tab 即可使用"——纯后端规划、前端一直没接。
- **本次实现**（ui-dev.js + dev.css + 游戏.html 版本号 20260903e）：
  - Tab5「玩家」（id=player）：搜索框（admin_search_users）+ 玩家列表（admin_list_users 最近50注册），每行显示 昵称/邮箱/注册/最后在线/魔石/封禁标记 + 「发魔石」按钮（grant_gems p_user_id/p_amount/p_reason='admin-panel'）+「封禁/解封」按钮（admin_ban_user uid/ban/reason）
  - Tab6「数据」（id=stats）：9 张统计卡片（admin_stats 单行：总用户/今日新增/7日活跃/1日活跃/封禁/魔石流通/魔石发放/订单/在售商品）+ 刷新按钮
  - dev.css 新增 .dev-stat-grid/.dev-stat-card/.dev-player-row/.dev-player-ops/.dev-banned 等
  - renderBody 的 panels/binders 加 player/stats
- **验证（浏览器实测，mock Supabase rpc）**：6 tab 全渲染；玩家列表 2 行 + 封禁标记；9 卡片数值正确；搜索/发魔石/封禁三操作 rpc 参数与 SQL 签名完全一致（grant_gems {p_user_id,p_amount,p_reason}、admin_ban_user {uid,ban,reason}、admin_search_users {q}）；控制台零 error。node --check 全过。
- **提醒用户**：面板能调 RPC 的前提是 Supabase 已执行过 migrate_admin_tools.sql（若报 "RPC not found" 去 Supabase Dashboard SQL Editor 跑一遍）。
## 2026-09-03 · 宠物资料卡网游式重构（第三主线）
- **需求**：用户要求宠物资料卡第一个资料（出战宠物详情 .pet-hero-panel）按"大多数网游的设计"重构，明确指定 ui-ux-pro-max + design-taste-frontend 两个 skill 作设计规范。
- **设计决策**：沿用游戏既有暗黑金 tokens（--action-gold/--tex-grain），不引入 skill 建议的绿金新配色（design-taste 的 one palette per project）。
- **新布局**（HTML 在 游戏.html 约 L264-314，ui-pet.js renderPetPanel 零改动靠 id 兼容）：居中大立绘（158px 圆角16 金色呼吸光晕 petGlowPulse 3.4s）+ 名字/Lv 金胶囊/转生徽章 + 横贯经验条 + 徽章行（技能/血统/特质/觉醒/经验池）+ 属性矩阵。
- **徽章化**：血统/特质/觉醒 并排胶囊行（.pet-badge-row），血统描述收进 title tooltip，特质用既有 trait-pill（T 阶色），觉醒用 awaken-badge 胶囊，技能为浅金条。
- **修复的 bug**：觉醒徽标裸打内部字段名（显示 stat+def% value+8%）→ getAwakenState 返回 {stat,value} 扁平结构，需映射中文属性名（STAT_CN）+ spd 例外不加 %。现在正确显示"防御+8%"。
- **排列修正（第二轮用户反馈"下方排列有问题"）**：主属性原 5 列在窄容器每格 60-80px，"6237/6237" 溢出且比次级 3 列格子还窄（层级反）→ 改为 flex wrap 3+2 居中大卡（.pet-tab-pane .pet-stats-main display:flex; .pstat flex 1 1 30% max-width 33.333%-6px），次级 3 列紧凑（值 .82rem 可换行）。浏览器实测 "11443/11443" 完整放下、3+2 居中。
- **版本号**：game.css v=20260903i、ui-pet.js v=20260903a（原 0ea3ced5）。
- **验证**：node --check 通过；浏览器实测（mock 登录 + 注入真实结构高等级宠 Lv98）排列/徽章/觉醒全正常，零 console error。
- **注意**：8090 静态服务器被并行 agent 关过两次，需时重启（python -m http.server 8090 --directory docs）；另一个 agent 曾把 game.css 版本号从 20260903f 改成 64c823e5（并行改文件冲突，改回时注意以磁盘当前值为准）。

## 清空数据库（用户要重新玩一遍）
- 范围：连 auth.users 一起删 + orders/wallet_ledger 一起清。执行后全部归零，**products 8 行商品配置保留**（删了商店没货）、redeem_codes 保留。
- **删除顺序（外键依赖，乱序会失败）**：挂单（pet_listings/equip_listings/egg_listings）→ trade_records/chat_messages → pets/equip_items/materials/pet_egg/quest_progress → orders/wallet_ledger/wallets → profiles → auth.users。
- 工具：`supabase_execute_sql` 直接跑多语句 delete 即可（表 rls_enabled 但 rls_forced=false，能正常删）。删 auth.users 用 SQL 可行（Supabase 的 identities/sessions 自带级联）。
- ⚠️ **清库后必须让用户在浏览器清 localStorage**，否则本地还留着旧账号 session + 新手引导标记，新号无法重走引导、也拿不到新手礼包。相关 key：`fos_blessing_given` / `fos_buff_start` / `fos_grants_done` / `fos_guide_skipped` / `fos_pack_claimed` / `fos_tutorial_started`，另加 supabase 的 `sb-*-auth-token`。
- 重新注册想进开发者面板，必须用 `776492620@qq.com`（admin RPC 与 Config.dev.adminEmails 都硬编码这个邮箱）。

## 晚间：别人修复后盘点（回滚范围核查）
用户反馈"早上全做坏了，别人帮忙修复了，去看还少什么"。核查结论：**回滚到早上 emoji 清理之前的状态**（BOM 已清、HTML 属性畸形已修、语法 0 错）。
- **保留完好**：①宠物资料卡重构全套（HTML L250-318 结构 + game.css 主体 + `.pet-tab-pane .pet-stats-main` flex 3+2 排列修正在 L2470 + 觉醒徽标 STAT_CN 修复 + 血统 title + 魂铸 tooltip，后三项在工作区未提交）；②开发者面板 6 tab（tune/res/sim/fast/player/stats）+ 特质魂铸 8 项调参 + _missing 兜底；③黑框修复（html/body min-width:0）；④图标资产 43 张 `docs/assets/icons/final/` 全在；⑤HTML 健康：670 标签/205 id 无重复/畸形属性 0/无 BOM（HTMLParser 验证）。
- **被回滚丢失**：①emoji 清理全部（config.js 205 处：基宠 9+进化树 48+血统 8；ui-pet 176；游戏.html 142 含侧边栏🏯🗺️🐾；ui-bag 83；main 72；enemy-data 野怪 icon），UI 回退链回到「头像→emoji」（iconHtml 带 emoji 参数/mountIcon fallbackEmoji/artHtml/pet_evolve pet.icon=EQUIP_ICON 复活）；②水墨图标接线全断（ui-bag MAT_IMG 删、game.css `.mat-img`/`.bloodline-icon-img` 删、trade.materials 12 条回 emoji、bloodlinePassive 回 emoji、ui-common.bloodlineHtml 回 emoji、ui-craft 硬编码 emoji 不读 C.icon、ui-bag CONSUMABLES 硬编码、tutorial blessing）—— **仅 config.craft 4 条 icon 还是 `<img class="mat-img">`，但 ui-craft 不读它 → 死数据，无害**；③装备页蛋入口复活（ui-equipment L20/103-113 getEggCount/🥚，用户点名要删的）；④版本号 bump 20260903x 全回退成随机哈希（不影响功能）。
- **工具坑**：`search_content` 工具在本仓库对 js/css 返回 0 匹配（不可信），改用 PowerShell `Select-String` 或 findstr；PowerShell 直接用中文文件名（游戏.html）会解析错路径，要 `Get-ChildItem` 拿 FileInfo 再 ReadAllText。git diff 带中文 pathspec 时整条命令输出会被吞，用不带 pathspec 的 `git --no-pager diff --stat`。

## 装备页删宠物蛋入口（重做，用户点名）
- 改动 `docs/js/ui/ui-equipment.js`：删 `const { getEggCount, hatchEgg } = window.Drop;`（Drop 在文件内再无引用）、删 renderInventory 里 23 行蛋卡片块、空背包判断 `eggs === 0 && getInventory().length === 0` → `getInventory().length === 0`（**关键：上次的坑就是删了 eggs 变量但 L218 还在引用**）、头部注释去掉「孵化」与 drop 依赖。版本号 `ui-equipment.js?v=20260903f`。
- 验证：全量 node --check 0 错；`Drop|getEggCount|hatchEgg|eggs` 在 ui-equipment.js 内 0 残留；lint 0。
- **删除前必须先验证替代入口存在**（防删出死路）：孵化现在有两条路 —— ①背包浮窗（顶栏「🎒 背包」/B 键，ui-bag.js L360-391 宠物蛋详情+孵化弹窗，**这是唯一还活着的入口**）；②宠物页「宠物蛋」tab。
## 魔石系统下线（用户要求"先隐藏起来"）
- **做法：一个总开关，不删代码**。`config.js` → `Config.shop.enabled = false`（shop 段首行，带注释说明怎么恢复）。
- `ui-shop.js` 加 `enabled()` + `applyVisibility()`：
  - 控制 3 处：顶栏 `#gem-balance` 余额 chip、侧边栏 `[data-page="shop"]` 商店按钮、`#tab-shop` 商店页 section
  - **用 `el.style.display`（inline）而不是 `hidden` 属性** —— `.tab-page.active{display:block}` 和 `.sb-btn{display:flex}` 优先级高于 `[hidden]` 的 UA 样式，加了等于没加
  - `refreshShop()` 开头 early return → 下线后不再请求钱包/商品/订单接口（顺带避开未建表的 missing 报错）
  - `renderShop()` 开头清空 `#shop-root` 并 return
  - **防白屏**：关掉时若正停在商店页（hash 直达 `#shop` / 旧收藏），自动 `UI.switchPage('capital')` 退回主城
  - `applyVisibility()` 在 renderShop 里调（renderAll 每次都走 renderShop），也在 renderGemChip / refreshShop 里调
- 版本号：config.js、ui-shop.js → `?v=20260903g`。
- 验证：全量 node --check 0 错、lint 0。
- **未动**：开发者面板里的魔石（Tab3「发魔石」、Tab5 玩家行魔石余额+发魔石按钮、Tab6 魔石流通/发放两张卡）—— 后台管理工具，玩家不可见，留着等用户定。
- 恢复方法：只改 `Config.shop.enabled = true` 一处，其余代码不动。

- ⚠️ **新发现缺口（非本次造成，未修，等用户定）**：宠物页「宠物蛋」tab **已经没了** —— 游戏.html 只有 4 个 pet-tab（profile/evolve/synth/merge）+ 4 个 pane，**没有 `egg-panel` 元素**，而 ui-pet.js `renderEggPanel()` 靠 `$('egg-panel')` 拿容器，拿不到就静默 return → 这段是死代码。是别的 agent 重构宠物页时连装备 tab 一起删掉的。要么补回 tab + pane，要么删掉 renderEggPanel 死代码。

## 昵称系统（注册时自己填，留空自动生成暗黑风随机名）
- **痛点**：注册后聊天/界面显示名直接取邮箱 @ 前段（如 `776492620`）像乱码；`profiles` 表此前从来没被写入（只有 SELECT/UPDATE 策略，无 INSERT）。`getMyDisplayName` 当时也根本没接进任何 UI。
- **方案（用户选 B：注册时自己填）**：注册表单加昵称输入框；留空 → 用 `Config.auth.nickname` 词库（灰烬/腐叶/白骨… + 行者/术士/游侠…，末尾补 4 位编号）自动生成。
- **改动文件**：
  - `config.js`：`Config.auth.nickname = {minLen:2, maxLen:12, prefixes:[...14], suffixes:[...10]}`（暗黑风词库，唯一数值源）
  - `supabase.js`：新增 `loadMyProfile()`（登录时拉一次存内存 `profileCache`，无则自动 upsert 随机昵称）/ `setMyNickname(name)`（upsert 写入）/ `getMyDisplayName()` 改为**同步读缓存**（避免聊天高频打接口，auth.getUser 实测 550ms）；导出补这两个函数
  - `main.js`：`onAuthenticated` 开头 `await loadMyProfile()` + 消费 localStorage `fos_pending_nickname`（邮箱验证后才登录的号补写昵称）；`onSignup(email,pwd,nickname)` 接第三参，注册成功暂存昵称到 localStorage（`clearAccountState` 只清内存不清 localStorage，安全）
  - `ui-login.js`：昵称输入框引用 + `nickCheck()`（空=自动生成，填了查 2-12 长度）+ 注册模式 `applyMode` 显示 + `doSignup` 传参 + keydown 绑定
  - `游戏.html`：登录表单加 `#login-nickname`（注册模式显示，默认 `display:none`）
  - `ui-console.js`：聊天发送显示名优先 `Supabase.getMyDisplayName()`（昵称），回落邮箱前缀 —— 顺手解决"聊天暴露邮箱前缀"老问题
- **SQL（supabase_apply_migration `profiles_insert_own_policy`）**：`profiles` 原来缺 INSERT 策略，补 `create policy profiles_insert_own on public.profiles for insert to authenticated with check (auth.uid()=id)`。已确认生效（pg_policies 现 3 条：select/update/insert）。
- **验证**：node --check 5 文件全过；profiles 列确认 upsert{id,nickname} 可行（created_at=now()/banned=false 有默认，其余 nullable）；RLS 完整。
- ⚠️ 未做：设置界面"改昵称"功能（用户只要求注册时填）；市场/交易记录的卖家展示仍走 `sender_name` 历史（老消息邮箱前缀，新消息昵称）。
- 复玩提醒（清库后）：重新注册必须用 `776492620@qq.com` 才能进开发者面板；且要清 localStorage（`fos_*` 引导标记 + `sb-*` session），否则旧登录态/引导标记残留。

## 战斗演示 battle-demo.html（用真实游戏代码搭，完全符合本体）
- 文件：`docs/battle-demo.html`。打开方式：直接双击，或 `cd docs && python -m http.server 8077` → `http://localhost:8077/battle-demo.html`（避开 file:// 缓存）。
- **做法（关键）**：不重写引擎，而是**加载游戏本体代码** `js/core/config.js` + `js/core/pet-sprites.js` + `js/pet/enemy-data.js` + `js/core/battle.js` + `js/ui/ui-battle.js`，只补最小桩：`window.UI`($,escapeHtml,addLog,consoleLog,switchPage,showToast,renderAll)、`window.Pet`(getActivePet/getStats/getCurHp/setCurHp/getPets/setActive/getBonusText/getBloodline/getAwakenState/expNeed/expRange)、`window.Util.pickWeighted`。所以跑的是真实战斗循环 + 真实演出（飘字/震屏/扫光/技能/掉落实光效）。
- DOM 按 `ui-battle.js` 实际依赖的 id/class 搭（`#tab-battle .battle-stage`、`#pet-icon/#enemy-icon`、`#pet-hp-bar/#atk-bar-*`、`#at-racer-*`、`#enemy-tip`、`#status-badge`、`#btn-battle/#btn-active-skill/#btn-change-map`、`#pet-roster/#roster-tooltip` 等）。
- 样例宠物 `SAMPLE_PETS`（含 Lv60「腐烂之母」带主动技能「腐蚀喷吐」）+ 自动加权掉落 `rollReward`（复用 `Config.drop.pool` + 敌人 `rarityWeights`）。默认选 `blight-heart`（Lv55-60 变异怪），展示变异怪红名/大体型 + 硬仗。
- 演示 CSS 只补「战斗舞台布局」+ ui-battle 切换的演出 class（charging/attacking/hit/stage-shake/loot-flash/fs-float…），设计 token 沿用 `css/game.css`；立绘失败兜底显示宠物名（不回退 emoji，符合铁律）。

## 战斗演示修复（用户反馈打开只有空骨架）
- **根因**：外部 `<script src="js/...">` 在 CodeBuddy 预览器/某些 webview 里加载失败（与之前 pet-sprites 单独 js 缓存失效同一类坑），`window.Config`/`window.Battle` 等全局对象未建立，导致后续调用全崩、页面只剩 HTML/CSS 骨架。
- **修复**：`docs/battle-demo.html` 改为**完全内联单文件版** —— PetSprites（从 游戏.html 内联整份）、EnemyData、最小 Config（战斗/掉落/主动技能所需）、Util.pickWeighted、UI/Pet 桩、真实 battle.js、真实 ui-battle.js 全部写进一个 HTML。仅保留 `css/game.css` 外链（样式已成功加载，无妨）。
- **验证**：启动本地 `python -m http.server 8012 --directory docs`，`http://localhost:8012/battle-demo.html` HEAD 返回 200。建议用户用浏览器打开该地址验证战斗/演出/掉落。
- 注意：样例宠仍用 Lv60「腐烂之母」+ 默认图 `blight-heart`，用于展示主动技能、变异怪红名和硬仗演出。

## 开发者一键删号工具（2026-09-03 用户痛点：自测反复注册/销毁账号，手动去 Dashboard 删太麻烦）
- 需求澄清：勾选方案=「一键删当前测试号」（玩家管理加删除按钮，云端级联删+清本地+回登录页）；场景=单机自测+新人引导反复验；不臆造一键注册/多号切换。
- **删号链路确认（回答了用户"删号后要重新注册么/能触发引导么"）**：删号=auth账号+全部云端数据删净+本地清空→必须重新注册新号→新号云端无宠→自动弹选宠→startGuideOnboarding(true)→开场tour+G1~G12引导链自动接。测试引导不用删号时用 dev「清除引导标记」（同号重走 G1，不删数据）。
- **新增 `supabase/migrate_dev_delete_user.sql`**（根 supabase/，幂等）：
  - `admin_delete_user(p_user_id uuid)` security definer + 首行管理员邮箱校验（同 grant_gems 样板）
  - 防删三档：`forbidden`(非管理员) / `self`(auth.uid()=p_user_id，防删当前会话) / `protected`(目标邮箱=776492620@qq.com 主号)
  - 逐表显式 delete（owner 列 text/uuid 混用，统一 `::text` 比较）：egg/equip/pet_listings(seller_id) → trade_records(player_id=uid + counterparty=uid 且 player_id='流浪商人' 的 NPC 行) → equip_items/pet_egg(owner_id text)/pets/materials(user_id) → quest_progress/chat_messages（**to_regclass 判存在再删**，老库缺表不报错）→ wallet_ledger/orders/wallets/profiles → 最后 `delete auth.users`
  - 关键验证点：trade_records.listing_id 只是索引**无 FK**，删序不冲突；表建表 SQL 不在仓库（只有代码兜底）。
- **ui-dev.js 玩家管理 Tab**：playerRow 每行加红色「删除」按钮（`btn-mini danger`），管理员主号行与当前登录行 disabled（title 提示）；playerAct 加 `del` 分支：window.confirm 两次 → `rpc('admin_delete_user',{p_user_id:uid})` → 处理 ok/forbidden/protected/self/notfound；ok 时若删的是当前登录号（正常走不到）→ Game.onLogout + 清 localStorage `fos_*` + reload 回登录页。
- 样式复用已有 `.btn-mini.danger`（game.css:80），只补 `.btn-mini.danger:disabled`。
- 版本号：game.css、ui-dev.js → 20260903u。
- ⚠️ **待用户执行**：SQL 在 Supabase Dashboard SQL Editor 跑（与所有 migrate 一致）；跑完浏览器 Ctrl+F5 强刷验证三路径（删普通测试号 ok / 删管理员被拒 / 列表刷新）。
- 前端 node --check + lint 0；SQL 无法本地跑，需 Dashboard 执行后核对库内无残留。

## 后端安全边界审计（2026-09-03，纯审查不动代码）
后端=Supabase 无自建服务（PostgREST + RPC），边界=RLS+函数内校验。六维结论：
- **高危 1 add_material 无限刷材料**：`schema_materials.sql:32` security definer、grant anon+authenticated、无单次/频率上限、材料名无白名单 → 登录玩家可直调无限加任意材料。已列入"上线前必做"未处理。
- **高危 2 bot_buy_pet/bot_buy_equip 印钞机**：函数体无 auth.uid() 判空、grant 给 anon（其余 buy_* 都有判空）。玩家挂自己的单→直调 bot_buy→扣12.5%税后材料到账→无限循环。market_bot.js 的挑单/限价全在前端策略层，防不住直调。**比 add_material 更直接，未处理。**
- **高危 3 奖励链全客户端权威**：任务完成判定+quest_progress 整行 JSON 覆盖写+礼包标记(progress.extra)全在客户端（quest.js/tutorial_mode.js）；且 quest_progress 表**仓库无 DDL 无 RLS 定义**（只在库里手工存在）→ 归属/策略盲区，需连库查证。配合1可完整刷奖。
- 中危：trade_records policy `for all to anon,authenticated using(true) with check(true)` 全开放读写删（migrate_material_trade.sql:49，历史遗留）；banned 封禁只写 profiles 表、登录/操作零拦截（形同虚设）；密码强度+邀请码纯前端 config（绕 signUp 即破，Supabase Auth 配置不可从代码审计）；管理邮箱 3 处硬编码 SQL。
- 低危/正常：RLS 矩阵其余表正确（own 策略），无 SQL 注入面（全参数绑定无动态 SQL），聊天/市场渲染全过 escapeHtml 防存储 XSS，无 service_role 密钥泄露前端。
- 未验证清单：add_material/bot_buy 上限与登录校验、quest_progress 库内真实 RLS、trade_records 过度授权修复、封禁拦截、XSS/注入攻击性回归测试、仓库 SQL↔云端 pg_policies reconcile。

## 后端安全加固落地（2026-09-03 晚，migrate_security_hardening.sql）
- 仓库根 `supabase/migrate_security_hardening.sql`（幂等，已整体重放云端 + pg_policies 核验通过）：
  1. 全库函数 `revoke execute from anon, public`，末尾统一只 grant authenticated（bot_buy_guard 收回，内部专用）。
  2. add_material 限流：security_rate_limits 表（PK user_id，行锁串行化并发）+ 60s 窗口 1000 / 单次 5000 / 超限锁 5 分钟，错误码 `ERR_RATE_LIMIT`。
  3. bot_buy_guard() 三层守卫（身份 auth.uid+email → profiles.banned → auth.users 账号年龄≥10min + security_bot_buy_log 每日 30 次），bot_buy_equip/pet 调它 + 自购返 'self' + 卖单删除在事务内显式删（防孤儿重复买刷材料）。错误码：`ERR_BOT_BUY_ANON/_BANNED/_TOO_NEW/_DAILY_CAP`。
  4. trade_records：删 select_all/open_all 旧策略，建 `trade_records_select_own`（authenticated，player_id=auth.uid()::text）。注：trade_records 主键是 player_id 文本（含'流浪商人'），不是 uuid。
  5. quest_progress：DROP+CREATE 为 authenticated（顺带删旧 qp_* 残留）。
  6. egg_listings 删 update_own 策略（改价/状态一律走 cancel_egg_listing RPC）。
- 前端适配：config.js 加 Config.security（addMaterial windowSec/maxPerWindow/maxPerCall/lockSec + botBuy minAccountAgeSec/dailyCap）；materials.cloudGain 限流退避不清本地（rateLimited 标记）；market_bot.guardKeyOf 四错误码 + 收到即 pauseBuyer 冷却、非守卫错误不暂停；supabase.loadMyProfile 读 banned/ban_reason；main.js blockBannedAccount 登录/会话恢复封禁号在恢复云数据前拦截；vstub.js 补 profiles 表 + bot_buy_equip 'self' 拒收。
- 新增测试（全绿）：vtest_botbuy（重写：只买别人单、self 拒收、税）vtest_security_guard / vtest_trade_records_rls / vtest_egg_listings_rls。
- ⚠️ vtest_shop 存量失败（HEAD 同样挂，非本次引入）：加载列表缺 ui-market-sell.js → main.js renderAll 调 UI.renderSellArea 崩。要修需给测试补 ui-market-sell.js 及 PetSprites/MarketUI 依赖，属测试夹具问题。
