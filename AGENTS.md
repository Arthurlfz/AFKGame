# AGENTS.md — 开工前必读（所有 AI agent / 子代理 / 未来的你）

> 这是给"干活的人（含 AI）"看的规矩。**每次会话开始先读这一页**，花 30 秒，能省几小时返工。
> 详细规则不抄在这里，见最下面「去哪查」。

---

## 一、这个项目是什么（一句话）

**暗黑风挂机养宠 + 宠物装备真实交易的网页游戏。**

循环：`挂机 → 掉材料 → 合成/进化/涅槃 → 打造装备 → 玩家间摆摊交易`。

三个爽点，按重要性排序：**掉宝/鉴定 > 市场捡漏 > 成长跳变**。
（做功能前先问：它服务哪个爽点？一个都不服务的，大概率不该做。）

**游戏名：永夜灵市，英文 Evernight Bazaar。**（2026-09-13 定名）
⛔ **旧名 `Forge of Souls` / `Soulforge` 是上一个项目继承来的，已废弃，任何地方都不许再用**
（含"Soul""Forge"的组合一律禁用 —— 用户明确不喜欢）。

---

## 二、开工前必须做的三件事

1. **读 `PROJECT_CONSTITUTION.md`** —— 那是"拍过的板"。
   ⚠️ 你（或一个旧会话）八成已经对某件事下过结论了。**先查，别重新拍脑袋。**
2. **想改的东西，先去代码里核实它现在长什么样。**
   ⛔ **严禁凭记忆说"这个没做/这样接的"**。记忆会骗人，代码不会。
   取证用 `Select-String`（PowerShell）现场搜。
3. **有任务单吗？先看 `docs/任务单/`** —— 里面有这张活的「边界」（允许碰哪些文件、不许碰哪些）。
   **没有边界的活不算活**，先自己拆清楚再动手。
4. **不知道"下一步该干嘛" → 看 `00-项目导航.md` 的「🚀 干活流程（七步）」**（唯一一份，别复制到别处）。
5. **改之前先看有没有人在同时改同一个文件。**
   共享文件清单（最容易撞车）：`docs/js/core/config.js`、`docs/游戏.html`、`docs/js/core/battle.js`、
   `docs/js/pet/*.js`、`docs/js/ui/ui-common.js`、`docs/css/game.css`。

---

## 三、七条红线（踩了会出事，不是建议）

| # | 红线 | 为什么 |
|---|---|---|
| 1 | **`Supabase.savePet` 是无条件 INSERT** —— 更新宠物一律用 `updatePet(cloudId, {...})` | 用错就凭空多出一只宠，数据永久污染 |
| 2 | **先扣钱/扣材料，后发货**；反过来必出资产漏洞。顺序：容易退的先扣，难退的后扣 | 历史教训：nirvana 的不可逆操作排在最前，出过事 |
| 3 | **确认云端建档成功之前，不许删素材宠/扣素材**。删素材宠必须**先删云端成功，再删本地** | 反过来的话网络一抖，玩家的宠就没了 |
| 4 | **改了 JS 或 CSS，必须升 `docs/游戏.html` 里对应的 `?v=`** | 不升的话浏览器用缓存，改了等于没改（这是本项目最常被误判成"bug"的现象） |
| 5 | **客户端不许留"发奖"能力**。服务器是唯一的模拟器 | 客户端有兜底发奖 = 静默双倍收益，经济直接崩 |
| 6 | **给 `.tab-page` 写样式永远不要碰 `display`** | ID 选择器会碾压 `.tab-page{display:none}`，页面切换直接失效（09-12 出过） |
| 7 | **托管挂机期间，客户端不许写宠物的等级/经验**（真账只归服务器）；改写宠物的操作（进化/合成/涅槃/引导顶等级）一律走 `IdleBridge.duringPetEdit` | 本地那份是**演出预演值**（真账领先最多一个窗口），写回去就把真账改小 → 经验/等级倒退，玩家看到等级"跳回去"（09-13 出过）。守值：`vtest_idle_bridge` 的 S1~S4 |

---

## 四、干完活必做

- **跑测试**：`cd docs && npm run check`
  ✅ **2026-09-17 实测：83 通过 / 3 失败（共 86 个 vtest），约 65 秒**（`run_all.js` 已并发，默认 6 路）。
  这条命令会自动跟 `docs/tests/baseline.json` 对比，**明确告诉你有没有新搞红的**，不用自己记数字。
  - 存量红 3 个（**不是你的锅，别去修**）：`action_freeze` / `bugfix`(emoji 断言过期) / `pet_skill`
  - flaky 2 个（时红时绿）：`enemy_balance` / `botbuy` —— 报红先单独连跑 3~4 次，全绿才算真回归
  - 其它：`npm run test`（只跑，不对比）｜`npm run test:v`（看失败详情）｜`npm run baseline`（**把当前设为新基线 —— 只在确认过的健康状态用**）
  - 调并发：`cd docs/tests && node run_all.js --jobs=4`
  - ⚠️ **剩余耗时的大头是「测试在等真实时间」**（`vtest_fun.js` 直接睡 60 秒模拟挂机一分钟，
    `enemy_level` / `newbie` / `deep` 也在等战斗打完）—— **这不是卡住，别去"修"它**。
    想再往下压只能压战斗 delay 常量（会动到 `speedScale` 节奏），**属高风险改动，先问人**。
- **提交只 add 你自己的路径**，`git add -A` 出过事故。
  提交前必看 `git status --short` 有没有意外出现的 `D`（删除）。
- **改完要在浏览器里真看一眼**。测试是跑 JS 逻辑的，**测不出 CSS 不存在**。
  （血泪：商店页写了接口写了权限，但样式从来没写过。开关常年关着的功能尤其容易这样。）

---

## 五、🔴 收尾（会话结束前必做，不然下次又要从头摸一遍）

本项目的"记忆"靠**文件**，不靠对话。对话一关就没了。

| 事 | 写去哪 |
|---|---|
| 今天做了什么、踩了什么坑 | `.codebuddy/memory/YYYY-MM-DD.md`（当天日期，没有就新建） |
| **长期有效**的事实 / 教训 / 决策 | **细节按主题写进** `.codebuddy/memory/topics/<主题>.md`（已有 8 个：数值与规则 / 数值管理入口 / 模块不变量 / UI与玩法约定 / 测试与验证 / 审计与教训 / 未决与待办 / 竞品参考-口袋精灵2）—— **动到哪个模块就先读对应那份** |
| 只有「**每轮都必须知道**的红线」 | 才写 `.codebuddy/memory/MEMORY.md`（它每轮被自动注入、**有硬上限，超了后半会被截断**）。写之前先问：不记住会出事吗？每轮都要知道吗？不是 → 写 `topics/` 或当天日志。详见 `MEMORY.md` 顶部结构说明 |
| 今天拍过的板、被推翻的决定 | `PROJECT_CONSTITUTION.md`（推翻就标"已推翻（日期）"，**不删原文**） |
| 没做完的事 | 记进当天 memory + `docs/任务单/` |

**收尾时还必须向人说清楚三件事**：
1. 今天做成了什么
2. **有什么没做完**（不许藏着）
3. 下次从哪开始

> ⚠️ 用户是策划，不懂代码。汇报用**游戏语言**，别甩技术细节。

## 六、拿不准的时候

- 这事以前定过没有？→ 查 `PROJECT_CONSTITUTION.md`
- 这个系统怎么实现的？→ 查 `docs/玩法系统全览.md`
- 历史上踩过什么坑？→ 搜 `.codebuddy/memory/*.md`（每天的记录都在那儿）
- 数据流/权限对不对？→ 查 `docs/代码审计_2026-09-11.md`
- **要干一件大事（重构、搬迁、上服务端权威）？先问人，别自己拍板。**

---

## 七、写代码的第一原则

> **先问：这件事本质要解决什么问题？有没有更根本的解法？**
> 不顺手打补丁、堆抽象、加兼容层。
>
> 🔴 **禁止补丁式修复**：用 `!important` 覆盖、负 margin 硬拉、写死数值怼上去 —— 一律停手，回去找根因。
> 🔴 **不要过度堵住**：想收紧某个限制前，先把所有用到它的地方列出来逐条判断，
>   选**最小作用域**的方案。判断标准很简单：**"失败了会丢资产吗？"** 会丢 → 拦；只是"没同步上" → 不拦。

---

## 八、去哪查（权威源，别自己编）

| 想知道 | 唯一权威 |
|---|---|
| 运行代码 | `docs/` |
| 数值配置 | `docs/js/core/config.js` |
| 测试 | `docs/tests/` |
| 数据库表结构 / 迁移 | `supabase/` |
| 已拍板的决策 | `PROJECT_CONSTITUTION.md` |
| 要干的活 / 边界 | `docs/任务单/` |
| **没落地的方案 / 施工图** | `docs/待办/` |
| **干完的一次性记录**（仅供回溯） | `docs/档案/` |
| **新代码往哪放 / 目录地图** | **本页 §九**（一文件一职责 + 目录表 + 必修的三笔账） |
| AI 记忆与排障记录 | `.codebuddy/memory/` |

> 📁 **新文档放哪（2026-09-17 定的规矩，别再往 `docs/` 根堆）**：
> 根目录只留**还活着的现状文档**（要经常查的那几份：`玩法系统全览` / `资源归属矩阵现状` /
> `代码审计_2026-09-11` / `宠物总表`）。「要干、还没干」→ `docs/待办/`；「干完了」→ `docs/档案/`。
> **动手写新文档前先想清楚它属于哪一类**，否则根目录会再长回一堆。
> ⚠️ 移动 / 改名文档后，必须全仓搜一遍旧文件名再改引用（引用它的地方常常是**代码注释、宪法、导航、SQL 注释**，
> 不在文档里）。改完跑 `cd docs && npm run check` 确认没红。

详细写作/样式/云端规范在 4 个 skill 里：`fos-cloud`（数据库）、`fos-balance`（改数值）、
`fos-verify`（跑测试排障）、`forge-of-souls-dev`（总纲）。

> 🎨 **改 UI / 美化页面时，除 `forge-of-souls-dev` 外还要走设计规范**（2026-09-14 用户点名要求）：
> `ardot-design-generator` 的 `rules/style-guide.md`（视觉方向：数据面板**不要给每个数字套盒子**，
> 用分组/分隔线/字重分层；最多 1 个强调色；禁纯黑、禁霓虹外发光、禁硬编码色）
> 与 `skills/ardot-design-assistant/references/guidelines-code.md`（设计→代码：**用项目已有 token**、
> **更新现有组件而不是新建**、**不许破坏既有功能**）。

---

## 九、代码怎么摆（文件与目录规矩 · 2026-09-21 用户点名后重写）

> 用户原话：**"你写的代码很多都是一个文件管理很多事情是吧"**。
> 这条规矩 **2026-09-10 就立过**，是 AI 自己没执行 —— 它总把新功能**追加**进最大的既有文件（那样最省事）。
> 本节的用途：**动手前 30 秒对一遍**，省下事后几十个文件的搬迁。

### 9.1 铁律：一个新功能 = 一个新文件

- **新功能一律新建文件**；既有文件只准改**"它自己那件事"**。
  ✅ 允许：改命中特效的时长、修它自己的 bug。
  ⛔ 不允许：往它里面加**第二套特效 / 第二个玩法 / 另一页的内容**。
- 判据一句话：**这件事能不能用一句话说成"这个文件负责什么"？** 能 → 它该有自己的文件。
- ⛔ 高频惯犯（别再往这些文件里塞新东西）：`js/core/idle-bridge.js`、`js/ui/ui-battle.js`、`css/game.css`。
- **旧代码不回头重构**（大重构会让 100+ 守值一起动，风险远大于收益）。要清旧账就**分批**：
  一批只搬一件事，搬完立刻 `cd docs && npm run check`。

### 9.2 目录地图（新建文件前先看这张表，别放错地方）

| 目录 | 放什么 | 已有例子 |
|---|---|---|
| `js/core/` | 领域逻辑（不画界面）：数值、战斗、掉落、市场、会话 | `config.js`(数值唯一源) / `battle.js` / `idle-bridge.js` |
| `js/ui/` | 页面与组件 UI，一个文件一页/一组件；**同族 ≥4 个文件就建子目录**（见 9.6） | `ui-market.js` / `ui-console.js` / `ui-common.js` |
| `js/ui/battle/` | **战斗页一族**（已归档）：主入口 `index.js` + `act` / `loot` / `tip` / `roster` / `summary` / `stage-fx`，说明书在该目录 `README.md` | `js/ui/battle/index.js` |
| **`js/fx/`** | **纯表现"素材特效"，一套特效一个文件**（样式进 `css/fx.css` 一节、素材进 `assets/effects/<名字>/`） | `hit-fx.js`（命中脚底墨爆） |
| `js/pet/` | 宠物领域：养成 / 合成 / 进化 / 敌人数据 | `pet.js` / `pet_merge.js` / `enemy-data.js` |
| `js/equipment/` | 装备：词缀 / 打造 / 分解 | `equipment_craft.js` / `salvage.js` |
| `js/trial/` `js/tower/` | 两个玩法模块（**一文件一职责的样板**，照它们学） | `trial-engine.js` / `tower-affix.js` |
| `js/vendor/` | 第三方库，⛔ 不许改 | `supabase.min.js` |
| `css/` | `game.css` = 战斗舞台/角色/动作动画；`ui-polish.css` = 全站外壳/面板/按钮/装饰；`fx.css` = 素材特效 | — |
| `assets/effects/<名字>/` | 特效素材（含 `_源-*` 原图留档） | `hit-ink/命中墨爆.png`（9 帧 × 256） |
| `assets/ui/` | UI 图标与装饰件（`ic_*.png`；装饰件放子目录） | `panel/角-左上.png` |
| `assets/pets/` | 宠物立绘 / 头像 / 逐帧帧条 | `god/bloodmoonfox/` |
| `tests/` | 守值测试，一个契约一个文件（`vtest_*.js`） | `vtest_pet_anim.js` |

### 9.3 加文件 / 搬迁时**必付的三笔账**（漏一笔就报红）

1. **`docs/游戏.html`**：加载顺序（被依赖的先加载）+ **`?v=` 必须升**（不升 = 改了等于没改，见红线 4）。
2. **测试 harness 的加载清单**（⚠️ 有**两套机制**，别只改一套）：
   - ① **显式清单**：约 30 个 `vtest_*.js` 里写死一串 `'../js/...'`。必需模块要**逐个加**，漏一个就是 `Xxx is not defined`。
   - ② **自动清单**：`tests/vtest_files.js` 的 `readGameScripts()` **直接解析 `docs/游戏.html`** 取脚本表（`vtest_ui` 等走这条）
     ⇒ 只要把加载标签加进 `游戏.html` 就自动生效。
   - 新模块若是**可选**的 → 调用方写成 `if (window.Xxx)`（项目既有写法：`idle-bridge.js` 调 `UI.animateHit`），**两套清单都不用动**。
   - ⚠️ **加载顺序陷阱**：显式清单里新模块常被批量插在"使用它的文件"**之后**（2026-09-21 踩过，`BattleTip` 拿到 undefined）。
     所以消费者一律**用时取**：`const Tip = () => window.BattleTip;` 然后 `Tip().bind()`；
     ⛔ 不要 `const Tip = window.BattleTip;`（那样在加载顺序不利时永远是 undefined）。
   - ⚠️ 批量改清单：**别用标点猜写法，改用"下一个元素是不是 `../js/` 路径"来判定**（2026-09-21 三种变体各踩一次）：
     - 清单式有**两种排版**：`'a.js','b.js'`（逗号紧跟引号）**和** `'a.js', 'b.js'`（逗号+空格）；
     - 逐行式：`readFileSync('a.js', 'utf8')` —— 下一个字符串是 `'utf8'`，**不是路径**。
     ✅ 安全做法：用正则 `'路径'(\s*,\s*)'(?=\.\./js/)`（**下一个字符串确实是 js 路径**才插入），并保留原来的分隔风格；
     ⛔ 按标点匹配（`'路径',` 或 `','`）：会误伤逐行式（拼成 `readFileSync('a','b','utf8')` ⇒ `ERR_INVALID_ARG_VALUE`），也会漏掉"逗号+空格"排版。
     ✅ **改完必须复核两个计数相等**（本次就靠这个发现漏了 9 个）：
     `cd docs/tests` 然后 `(Select-String -Path *.js -Pattern "旧模块名" -List).Count` 与 `(… -Pattern "新模块名" …).Count` 应相等。
3. **按文件路径断言的守值**：例如 `vtest_pet_anim.js` 第 10 节原先断言 `ui-battle.js` 里的 `playFx`；
   迁移后必须把断言**改指新文件**，并顺手加一条"**不许回流**"的断言（防止下次又被塞回去）。
4. 新增**素材**文件后：`git add`（否则 `vtest_assets_tracked.js` 报红"引用的资源没入库"）。

### 9.4 样板：命中特效（2026-09-21 拆的）

```
调用时机   js/ui/ui-battle.js 的 showDamage()   → 只留一行 if (window.HitFx) HitFx.play(target)
播放器     js/fx/hit-fx.js                      → 逐帧定时器 / 时长跟出手素材对齐 / 连击清理
样式       css/fx.css 的 .hit-fx                → 位置 / 尺寸 / 层级（三个数是量出来的，注释里写了怎么量）
素材       assets/effects/hit-ink/命中墨爆.png  → 9 帧 × 256 横向帧条（RGBA）
守值       tests/vtest_pet_anim.js 第 10 节     → 驱动方式 / 触发口径 / 层级 / 素材 IHDR / 版本号 / 不许回流
```

### 9.5 已拆出来的模块（`ui-battle.js` 1115 行 → 432 行的成果，照着它们的开头注释学写法）

| 模块 | 一件事 |
|---|---|
| `js/fx/hit-fx.js` | 播命中墨爆（唯一一套素材特效的样板） |
| `js/ui/ui-stage-fx.js` | 舞台横幅 / 舞台闪光 / 屏幕脉冲 三个原语 |
| `js/ui/ui-battle-loot.js` | 掉落播报 + 掉落演出（自己挂 `UI.showLoot` / `UI.lootTierOf`） |
| `js/ui/ui-battle-tip.js` | 敌方悬浮提示（`window.BattleTip`） |
| `js/ui/ui-battle-act.js` | 出手与命中演出：节奏 / 冲刺 / 受击形变 / 飘字 / 命中特效触发 |
| `js/ui/ui-battle-roster.js` | 出战宠物竖列（头像 + 悬停详情 + 快捷入口） |
| `js/ui/ui-battle-summary.js` | 挂机结算汇总弹窗 |

**共同约定**：① 自己往 `UI` 上挂对外 API（调用方零改动）② 跨模块只走公开 API，不私接内部函数
③ 被依赖的模块一律**用时取**（`const X = () => window.X`），不假设加载顺序
④ 迁代码时**血泪注释一起搬**——注释比代码更不能丢。

### 9.6 目录分类规矩（2026-09-21 用户点名「这些文件不分好细致的类吗」后立）

> 只拆文件、不归目录 ⇒ `js/ui/` 会重新长成一坨（当天实测：**47 个文件平铺、0 个子目录**）。

- **同族 ≥4 个文件 → 建子目录**：`js/ui/<族>/`（族 = 页面/玩法名，如 `battle/`）。
- **进目录后去掉冗余前缀**：`ui-battle-act.js` → `js/ui/battle/act.js`；页面主入口叫 **`index.js`**。
- **每个子目录放一个 `README.md`**：一句话一文件 + 本目录的改法规矩 —— **人和 AI 都要能一眼读懂**。
- **单页文件不进子目录**（`ui-shop.js` / `ui-quest.js` 这种一屏一个的，留在 `js/ui/` 平铺）。
- ⚠️ **搬迁的固定代价（一次付清，别漏项）**：
  1. `git mv`（保留历史）；
  2. `docs/游戏.html`：7 处 `src` 路径 + **`?v=` 升号**；
  3. **30 个测试清单里的路径** —— 用"只替换带 `js/ui/` 前缀的完整路径"的脚本，改完**复核残留 = 0**；
  4. 🔴 **守值里转义过的正则路径**（`/js\/ui\/ui-battle\.js/` 这种）**不会被普通字符串替换命中**，必须手改；
  5. 代码注释里提到旧路径的地方（`core/battle.js` / `core/idle-bridge.js` / `tower/ui-tower-battle.js` / `trial/ui-trial-battle.js` / `fx/hit-fx.js`）；
  6. 历史文档（`docs/档案/` / `docs/任务单/`）**不要改** —— 它们记录的是当时的状态。
- ✅ 复核三件套：`残留旧路径 = 0` + 两个模块名的文件计数相等 + `npm run check` 零新红。
